---
phase: 10-lazy-audiocontext-initialization
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/guide/getting-started.md
  - docs/guide/concepts.md
  - src/index.ts
autonomous: true

must_haves:
  truths:
    - "Getting Started guide shows simplified API without initAudio() boilerplate"
    - "Getting Started guide explains initAudio() is optional with a tip callout"
    - "Core Concepts page reflects that AudioContext is created lazily"
    - "JSDoc on initAudio() says it's optional"
    - "JSDoc on factory functions no longer say 'must call initAudio first'"
  artifacts:
    - path: "docs/guide/getting-started.md"
      provides: "Simplified code examples without mandatory initAudio()"
      contains: "initAudio.*optional"
    - path: "docs/guide/concepts.md"
      provides: "Updated AudioContext lifecycle docs"
      contains: "lazily"
    - path: "src/index.ts"
      provides: "Updated JSDoc comments"
      contains: "optional"
  key_links:
    - from: "docs/guide/getting-started.md"
      to: "src/index.ts API"
      via: "code examples match actual API behavior"
      pattern: "createSound|createOscillator"
---

<objective>
Update documentation (Getting Started guide, Core Concepts, JSDoc) to reflect that initAudio() is now optional and AudioContext is created lazily on first use.

Purpose: Documentation must match the new behavior — otherwise developers still think initAudio() is required.
Output: Updated markdown guides and JSDoc comments.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lazy-audiocontext-initialization/10-01-SUMMARY.md
@docs/guide/getting-started.md
@docs/guide/concepts.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Getting Started guide and Core Concepts</name>
  <files>docs/guide/getting-started.md, docs/guide/concepts.md</files>
  <action>
1. In `docs/guide/getting-started.md`:

   a. Update the first code example ("Play a Sound") to remove `initAudio()`:
   ```typescript
   import { createSound } from 'ez-web-audio'

   document.querySelector('button')?.addEventListener('click', async () => {
     const sound = await createSound('/sounds/click.mp3')
     sound.play()
   })
   ```

   b. Replace the "Why initAudio()?" section with a new section like "AudioContext & User Interaction":
   ```markdown
   ### AudioContext & User Interaction

   Browsers require a user interaction (click, tap, keypress) before playing audio.
   This is a security feature to prevent auto-playing sounds. EZ Web Audio handles
   this automatically — the AudioContext is created lazily when you first call a
   factory function like `createSound()` or `createOscillator()`.

   ::: tip
   You don't need to call `initAudio()`. The library creates and manages the
   AudioContext automatically. Just make sure your first audio call happens inside
   a user interaction handler (click, tap, keypress).
   :::

   ::: details Advanced: Explicit initialization
   If you need explicit control over initialization timing (e.g., iOS mute switch
   workaround, pre-warming the context), you can still call `initAudio()`:

   ```typescript
   import { initAudio, createSound } from 'ez-web-audio'

   button.addEventListener('click', async () => {
     await initAudio() // Optional — for explicit control
     const sound = await createSound('/sounds/click.mp3')
     sound.play()
   })
   ```
   :::
   ```

   c. Update remaining code examples in the file to remove `initAudio()` imports and calls:
   - "Play a Music Track" example: remove `initAudio` import and call
   - "Create a Synthesizer" example: remove `initAudio` import and call
   - "Putting It Together" example: remove `initAudio` import and call

2. In `docs/guide/concepts.md`:

   a. Find the AudioContext lifecycle section and update it to explain lazy initialization:
   - Change the "This works" / "This fails" example to show the new pattern
   - Update the state table: `suspended` action should say "Handled automatically — play() calls resume(). If still suspended, a console warning appears."
   - Add a note that `initAudio()` is optional but available for explicit control

   b. If there's a "Single Context" section, add a note that the context is created lazily on first use.
  </action>
  <verify>Run `pnpm dev` and visually check the Getting Started and Core Concepts pages render correctly. Verify no broken markdown formatting.</verify>
  <done>Getting Started shows simplified examples without initAudio(). Core Concepts explains lazy initialization. initAudio() documented as optional.</done>
</task>

<task type="auto">
  <name>Task 2: Update JSDoc comments on public API functions</name>
  <files>src/index.ts</files>
  <action>
1. Update `initAudio()` JSDoc:
   - Change description to: "Optionally initialize the audio system explicitly. The library creates the AudioContext lazily on first use, so calling this function is not required. Use it when you need explicit control over initialization timing (iOS mute workaround, pre-warming the context)."
   - Update the @example to show it's optional
   - Remove "Must be called in response to a user interaction" from the main description (move to a note about when explicit control is useful)

2. Update `getAudioContext()` JSDoc:
   - Note that the AudioContext is created lazily if it doesn't exist yet

3. Update `createSound()`, `createTrack()`, `createOscillator()`, `createBeatTrack()`, `createSampler()`, `createFont()`, `createSprite()`, `createWhiteNoise()`, `createLayeredSound()` JSDoc:
   - Remove any references to "call initAudio() first"
   - If any mention initialization requirements, update to say context is created automatically

4. Update `useInteractionMethods()` JSDoc if it mentions initAudio requirement.
  </action>
  <verify>Run `pnpm typecheck` to verify no JSDoc-related issues. Run `pnpm build:lib` to ensure docs generate correctly.</verify>
  <done>All public API JSDoc reflects that initAudio() is optional and AudioContext is created lazily.</done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm build:lib` succeeds
3. Getting Started guide shows code without initAudio() boilerplate
4. Core Concepts explains lazy initialization
5. JSDoc on initAudio() says "optional"
</verification>

<success_criteria>
- No documentation tells developers they "must" call initAudio()
- Getting Started shows the simplest possible usage pattern
- initAudio() is documented as an optional power-user API
- Core Concepts explains the automatic AudioContext lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/10-lazy-audiocontext-initialization/10-02-SUMMARY.md`
</output>
