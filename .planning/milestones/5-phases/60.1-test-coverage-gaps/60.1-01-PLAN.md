---
phase: 60.1-test-coverage-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/beat-track.test.ts
  - src/lfo.test.ts
  - src/index.test.ts
autonomous: true
requirements:
  - QC-1-06
  - QC-1-15
  - QC-1-17

must_haves:
  truths:
    - "BeatTrack.setPattern() has tests covering basic, short/long arrays, booleans, chaining, truthy coercion, and empty array"
    - "LFO depth calculation tests verify actual numeric output for ratio, cents, and absolute modes"
    - "createFont(ctx, url) explicit-context overload has a passing test"
  artifacts:
    - path: "src/beat-track.test.ts"
      provides: "setPattern() test suite"
      contains: "setPattern"
    - path: "src/lfo.test.ts"
      provides: "depth numeric verification tests"
      contains: "depthGain"
    - path: "src/index.test.ts"
      provides: "createFont explicit-context test"
      contains: "createFont"
  key_links:
    - from: "src/beat-track.test.ts"
      to: "src/beat-track.ts"
      via: "setPattern() method under test"
      pattern: "setPattern"
    - from: "src/lfo.test.ts"
      to: "src/lfo.ts"
      via: "_calculateDepth() verified through GainNode spy"
      pattern: "createGain"
---

<objective>
Write HIGH and MEDIUM priority test coverage for BeatTrack.setPattern(), LFO depth numeric verification, and createFont explicit-context overload.

Purpose: Close QC-1-06, QC-1-15, QC-1-17 test gaps identified by deep review.
Output: New test cases in 3 existing test files.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60.1-test-coverage-gaps/60.1-RESEARCH.md

@src/beat-track.test.ts
@src/beat-track.ts
@src/lfo.test.ts
@src/lfo.ts
@src/index.test.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BeatTrack.setPattern() tests (QC-1-06)</name>
  <files>src/beat-track.test.ts</files>
  <action>
Add a new `describe('setPattern()')` block in `src/beat-track.test.ts` with the following tests:

1. **Basic pattern**: `setPattern([1, 0, 1, 0])` on a 4-beat track sets beats 0,2 active, beats 1,3 inactive.
2. **Short array**: `setPattern([1, 0])` on an 8-beat track -- beats 0 active, beat 1 inactive, beats 2-7 inactive (remaining default to false).
3. **Long array**: `setPattern([1,1,1,1,1,1,1,1])` on a 4-beat track -- only first 4 beats affected, extra values ignored.
4. **Boolean inputs**: `setPattern([true, false, true])` works identically to `[1, 0, 1]`.
5. **Chaining**: `setPattern([1,0,1,0])` returns `this` (the BeatTrack instance).
6. **Truthy coercion**: `setPattern([2, 0, -1])` -- non-zero values become active (`!!2 === true`, `!!-1 === true`).
7. **Empty array**: `setPattern([])` -- all beats become inactive.

Use the existing extended BeatTrack class pattern already in the test file. Create a BeatTrack with a known numBeats before each test. Use `track.beats[i].active` assertions.
  </action>
  <verify>
    <automated>pnpm test src/beat-track.test.ts</automated>
  </verify>
  <done>7 new setPattern() tests pass covering all edge cases from QC-1-06</done>
</task>

<task type="auto">
  <name>Task 2: LFO depth numeric verification (QC-1-15) and createFont ctx overload (QC-1-17)</name>
  <files>src/lfo.test.ts, src/index.test.ts</files>
  <action>
**LFO depth tests (src/lfo.test.ts):**

Add a new `describe('depth calculation numeric verification')` block with:

1. **ratio mode, gain target**: depth=0.3, target gainParam.value=1.0 -> depthGain.gain.value should be ~0.3 (0.3 * 1.0). Spy on `ctx.createGain` BEFORE calling `lfo.connect()` to capture the depthGain node. The last GainNode created is the depthGain.
2. **ratio mode, different base**: depth=0.5, target gainParam.value=0.8 -> depthGain ~0.4 (0.5 * 0.8). Use `vi.spyOn(gainNode.gain, 'value', 'get').mockReturnValue(0.8)` on the target's gain node to simulate a non-default value.
3. **cents mode**: depth=100, frequency target at 440Hz -> depthGain = 440 * (2^(100/1200) - 1) ~= 25.78. Connect to `'frequency'` param (which uses cents mode by default per LFO source).
4. **absolute mode**: depth=50 -> depthGain = 50 directly. Use `mode: 'absolute'` option.
5. **Pan with value=0 fallback**: connect to `'pan'` param, depth=0.3. When pan value is 0, ratio mode falls back to absolute, so depthGain = 0.3.

Key pattern: Spy on `ctx.createGain` before `lfo.connect()`. Capture all created GainNodes. The depthGain is the last one. Use `expect(depthGain.gain.value).toBeCloseTo(expected, 1)` (1 decimal place tolerance due to mock precision).

NOTE: If mock AudioParam doesn't persist `.value`, use `vi.spyOn(depthGainNode.gain, 'value', 'get').mockReturnValue()` to control test expectations, OR verify via `setValueAtTime` spy if `_calculateDepth` uses that method. Read `_calculateDepth()` in lfo.ts to confirm the exact mechanism.

**createFont ctx overload (src/index.test.ts):**

Follow the existing pattern at line ~1232 where `createSprite(ctx, url, manifest)` is tested. Add within the appropriate `describe` block:

1. `createFont(ctx, url)` returns a Font instance using the provided AudioContext. Mock fetch to return a valid font module response. Assert `font` is instance of `Font`.

Use the existing `vi.resetModules()` + dynamic `import('./index')` pattern already established in index.test.ts. Follow the existing `mockAudioContext` and `makeMockResponse()` patterns.
  </action>
  <verify>
    <automated>pnpm test src/lfo.test.ts && pnpm test src/index.test.ts</automated>
  </verify>
  <done>5 LFO depth numeric tests pass verifying ratio/cents/absolute/pan modes. 1 createFont(ctx) test passes verifying explicit-context overload.</done>
</task>

</tasks>

<verification>
All HIGH and MEDIUM priority test gaps closed:
- `pnpm test src/beat-track.test.ts` passes with new setPattern tests
- `pnpm test src/lfo.test.ts` passes with numeric depth verification
- `pnpm test src/index.test.ts` passes with createFont ctx test
- `pnpm test` full suite still passes (no regressions)
</verification>

<success_criteria>
- QC-1-06: 7 setPattern() tests covering all identified scenarios
- QC-1-15: 5 depth calculation tests with numeric value assertions (not just isRunning checks)
- QC-1-17: 1 createFont explicit-context test following existing pattern
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-test-coverage-gaps/60.1-01-SUMMARY.md`
</output>
