---
phase: 60.1-test-coverage-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/poly-synth.test.ts
  - src/transport.test.ts
  - src/sequence.test.ts
  - src/utils/musical-time.test.ts
  - src/grain-player.test.ts
  - src/index.test.ts
autonomous: true
requirements:
  - L4
  - L5
  - L6
  - L10
  - L11
  - L21

must_haves:
  truths:
    - "PolySynth handles rapid interleaved play/stop without orphaned voices"
    - "Transport live BPM changes affect subsequent beat timing"
    - "Sequence methods after dispose behave safely (no crash)"
    - "Musical time parser handles 0t edge case"
    - "GrainPlayer overlap boundary and jitter edge cases are verified"
    - "createSprite with Howler manifest format works through factory function"
  artifacts:
    - path: "src/poly-synth.test.ts"
      provides: "Rapid play/stop interleaving tests"
      contains: "rapid"
    - path: "src/transport.test.ts"
      provides: "Live BPM change test"
      contains: "bpm"
    - path: "src/sequence.test.ts"
      provides: "Post-dispose safety tests"
      contains: "dispose"
    - path: "src/utils/musical-time.test.ts"
      provides: "0t edge case test"
      contains: "0t"
    - path: "src/grain-player.test.ts"
      provides: "Overlap boundary and jitter tests"
      contains: "overlap"
    - path: "src/index.test.ts"
      provides: "Howler manifest factory test"
      contains: "howler"
  key_links:
    - from: "src/sequence.test.ts"
      to: "src/sequence.ts"
      via: "at/remove/clear after dispose"
      pattern: "dispose.*at|at.*dispose"
    - from: "src/transport.test.ts"
      to: "src/transport.ts"
      via: "bpm setter during playback"
      pattern: "bpm"
---

<objective>
Write LOW priority test coverage for PolySynth rapid play/stop, Transport live BPM, Sequence post-dispose safety, musical time 0t edge case, GrainPlayer overlap boundaries, and createSprite Howler manifest.

Purpose: Close L4, L5, L6, L10, L11, L21 test gaps identified by deep review.
Output: New test cases in 6 existing test files.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60.1-test-coverage-gaps/60.1-RESEARCH.md

@src/poly-synth.test.ts
@src/poly-synth.ts
@src/transport.test.ts
@src/transport.ts
@src/sequence.test.ts
@src/sequence.ts
@src/utils/musical-time.test.ts
@src/utils/musical-time.ts
@src/grain-player.test.ts
@src/grain-player.ts
@src/index.test.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PolySynth, Transport, and Sequence edge case tests (L4, L5, L6)</name>
  <files>src/poly-synth.test.ts, src/transport.test.ts, src/sequence.test.ts</files>
  <action>
**PolySynth rapid interleaving (src/poly-synth.test.ts, L4):**

Add tests within existing describe blocks:

1. **Rapid interleaved**: play(440), play(550), stop(440), play(660), stop(550) -- verify only 660 has an active voice. Use `synth.activeVoiceCount` or check the voice pool state.
2. **Burst same frequency**: call play(440) 10 times rapidly -- should result in only 1 active voice at 440Hz (same-frequency retrigger reuses the voice).
3. **Stop during voice stealing**: fill pool to maxVoices, trigger steal, immediately stop the stolen voice's handle -- should not throw and activeVoiceCount should decrease.

**Transport live BPM change (src/transport.test.ts, L5):**

Add a new `describe('live BPM change')` block. Use `vi.useFakeTimers()`:

1. Start transport at 120 BPM. Advance timers past a few beats. Change `transport.bpm = 60`. Advance timers again. Verify the interval between subsequent beat events reflects the new BPM (longer intervals at 60 BPM). The BPM setter simply sets `_bpm` -- the scheduler loop reads `_bpm` on each tick, so new BPM takes effect on next scheduled beat.
2. Position tracking: after BPM change, verify `transport.position` continues incrementing correctly (no jump or reset).

**Sequence post-dispose (src/sequence.test.ts, L6):**

Add a `describe('behavior after dispose')` block:

1. `seq.at('4n', callback)` after `seq.dispose()` -- document actual behavior. The code does NOT guard `at()` against disposed state, so it will add the event but `_scheduleEvents` short-circuits on `_disposed`. Test that calling at() does not throw (it silently adds to dead events array).
2. `seq.remove(id)` after dispose -- should not throw, returns false (event was cleared during dispose or never fires).
3. `seq.clear()` after dispose -- should not throw, empties already-dead events array.
  </action>
  <verify>
    <automated>pnpm test src/poly-synth.test.ts && pnpm test src/transport.test.ts && pnpm test src/sequence.test.ts</automated>
  </verify>
  <done>3 PolySynth rapid play/stop tests, 2 Transport live BPM tests, 3 Sequence post-dispose tests all pass</done>
</task>

<task type="auto">
  <name>Task 2: Musical time, GrainPlayer, and Howler manifest edge case tests (L10, L11, L21)</name>
  <files>src/utils/musical-time.test.ts, src/grain-player.test.ts, src/index.test.ts</files>
  <action>
**Musical time 0t (src/utils/musical-time.test.ts, L10):**

1. Test `parseMusicalTime('0t')` -- document actual behavior. 0 beats makes no musical sense. If it returns 0, assert that. If it throws, assert the error.
2. Test `isMusicalTimeNotation('0t')` -- should return true (matches regex pattern `\d+t`) but semantically 0 beats is meaningless. Document the actual return value.

**GrainPlayer edge cases (src/grain-player.test.ts, L11):**

Add within existing describe blocks:

1. **overlap === grainSize**: set overlap equal to grainSize -- verify it clamps to grainSize - 0.001. Phase 58.1 added clamping logic. Assert `grainPlayer.overlap` is strictly less than `grainPlayer.grainSize`.
2. **Boundary jitter at position=0**: set jitter=1.0, position=0.0 -- start playing, advance timers, verify no error thrown (position should stay clamped within [0, 1]).
3. **Boundary jitter at position=1**: set jitter=1.0, position=1.0 -- start playing, advance timers, verify no error (position clamped within valid range).

Use `vi.useFakeTimers()` and `vi.advanceTimersByTime()` for GrainPlayer tests that trigger the scheduling loop.

**createSprite with Howler manifest (src/index.test.ts, L21):**

Follow the existing `createSprite` test patterns in index.test.ts:

1. `createSprite('sounds.mp3', { sprite: { laser: [0, 300], explosion: [400, 1000] } })` -- Howler format uses `{ sprite: { name: [start_ms, duration_ms] } }`. Verify it returns an AudioSprite instance. Verify the sprite has the correct segment names (laser, explosion).
2. Verify Howler manifest normalization: milliseconds should be converted to seconds internally. The `normalizeManifest` function handles this.

Use the existing dynamic import + mock fetch pattern from index.test.ts.
  </action>
  <verify>
    <automated>pnpm test src/utils/musical-time.test.ts && pnpm test src/grain-player.test.ts && pnpm test src/index.test.ts</automated>
  </verify>
  <done>2 musical time 0t tests, 3 GrainPlayer edge case tests, and 2 createSprite Howler manifest tests all pass</done>
</task>

</tasks>

<verification>
All LOW priority test gaps closed:
- `pnpm test src/poly-synth.test.ts` passes with rapid play/stop tests
- `pnpm test src/transport.test.ts` passes with live BPM change tests
- `pnpm test src/sequence.test.ts` passes with post-dispose tests
- `pnpm test src/utils/musical-time.test.ts` passes with 0t tests
- `pnpm test src/grain-player.test.ts` passes with overlap/jitter edge case tests
- `pnpm test src/index.test.ts` passes with Howler manifest test
- `pnpm test` full suite still passes (no regressions)
</verification>

<success_criteria>
- L4: 3 PolySynth rapid interleaving tests
- L5: 2 Transport live BPM change tests
- L6: 3 Sequence post-dispose safety tests
- L10: 2 musical time 0t edge case tests
- L11: 3 GrainPlayer overlap boundary and jitter tests
- L21: 2 createSprite Howler manifest tests
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/60.1-test-coverage-gaps/60.1-02-SUMMARY.md`
</output>
