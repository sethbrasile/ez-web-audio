---
phase: 14-docs-examples-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/examples/index.md
  - docs/examples/basic-playback.md
  - docs/examples/synthesis.md
  - docs/examples/effects.md
  - docs/examples/drum-machine.md
  - docs/examples/xy-pad.md
  - docs/examples/audio-routing.md
  - docs/examples/timing.md
  - docs/examples/synth-keyboard.md
  - docs/examples/synth-drum-kit.md
  - docs/examples/sampled-drum-kit.md
  - docs/examples/soundfont-piano.md
  - docs/examples/drum-machine-vue.md
  - docs/examples/drum-machine-vanilla.md
autonomous: true

must_haves:
  truths:
    - "Every code snippet in example pages compiles and uses current API signatures"
    - "No outdated API usage (no removed methods, correct parameter names)"
    - "All internal links resolve to existing pages"
    - "Examples index page has accurate descriptions and correct local dev instructions"
  artifacts:
    - path: "docs/examples/index.md"
      provides: "Examples index with accurate descriptions"
    - path: "docs/examples/basic-playback.md"
      provides: "Verified basic playback example"
    - path: "docs/examples/synthesis.md"
      provides: "Verified synthesis example"
    - path: "docs/examples/effects.md"
      provides: "Verified effects example"
    - path: "docs/examples/drum-machine.md"
      provides: "Verified drum machine example"
    - path: "docs/examples/xy-pad.md"
      provides: "Verified XY pad example"
    - path: "docs/examples/audio-routing.md"
      provides: "Verified audio routing example"
    - path: "docs/examples/timing.md"
      provides: "Verified timing example"
    - path: "docs/examples/synth-keyboard.md"
      provides: "Verified synth keyboard example"
    - path: "docs/examples/synth-drum-kit.md"
      provides: "Verified synth drum kit example"
    - path: "docs/examples/sampled-drum-kit.md"
      provides: "Verified sampled drum kit example"
    - path: "docs/examples/soundfont-piano.md"
      provides: "Verified soundfont piano example"
    - path: "docs/examples/drum-machine-vue.md"
      provides: "Verified Vue drum machine example"
    - path: "docs/examples/drum-machine-vanilla.md"
      provides: "Verified vanilla drum machine example"
  key_links:
    - from: "docs/examples/index.md"
      to: "docs/examples/*.md"
      via: "navigation links"
      pattern: "/examples/"
---

<objective>
Verify and update all 14 example page code snippets for accuracy against the current API.

Purpose: DOCS-05 requires all code examples in docs to be verified against the current API. These example pages are what developers reference when building with the library.

Output: All example page code snippets verified and corrected where needed.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@docs/examples/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update example pages (basic through routing)</name>
  <files>
    docs/examples/index.md
    docs/examples/basic-playback.md
    docs/examples/synthesis.md
    docs/examples/effects.md
    docs/examples/drum-machine.md
    docs/examples/xy-pad.md
    docs/examples/audio-routing.md
    docs/examples/timing.md
  </files>
  <action>
    For each of the 8 example markdown pages:

    1. **Verify all TypeScript/Vue code snippets** against current API:
       - Method names, parameter order, return types
       - Import paths (`ez-web-audio` not internal paths)
       - Async/await usage where required (createSound, createTrack, createOscillator all return promises)
       - `getAudioContext()` is async and must be awaited
       - Effect factory functions need AudioContext as first argument

    2. **Check API Used sections** on each page:
       - Method descriptions should match actual behavior
       - No references to removed or renamed methods
       - Parameter descriptions should be accurate

    3. **Verify Next Steps links** on each page:
       - All internal links should resolve to existing pages
       - No broken links or references to removed pages

    4. **Check examples/index.md**:
       - All example pages listed in the overview
       - Descriptions match what the page actually demonstrates
       - "Running Examples Locally" section has correct commands (should use `pnpm dev` not `pnpm docs:dev`)
       - Browser compatibility table is reasonable

    5. **Check specific known issues:**
       - basic-playback.md: `seek().from()` API - verify this is the correct syntax
       - effects.md: `attachAnalyzer` vs `setAnalyzer` - verify correct method name
       - synthesis.md: `update('frequency').to(880).from('value')` - verify from() parameter
       - drum-machine.md: `playBeats(bpm, 1/16)` - verify this is correct API

    Fix all inaccuracies. Keep existing prose that is accurate. Only modify code snippets and factual claims that are wrong.
  </action>
  <verify>
    Grep for common API method names across example .md files and cross-reference against src/index.ts exports. Run `pnpm build` to verify docs build succeeds.
  </verify>
  <done>
    First 8 example pages verified against current API. No outdated method names, incorrect signatures, or broken internal links. Examples index page has accurate descriptions and correct local dev instructions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update remaining example pages (keyboards, drums, integration)</name>
  <files>
    docs/examples/synth-keyboard.md
    docs/examples/synth-drum-kit.md
    docs/examples/sampled-drum-kit.md
    docs/examples/soundfont-piano.md
    docs/examples/drum-machine-vue.md
    docs/examples/drum-machine-vanilla.md
  </files>
  <action>
    For each of the 6 remaining example markdown pages:

    1. **Verify all TypeScript/Vue code snippets** against current API:
       - Method names, parameter order, return types
       - Import paths (`ez-web-audio` not internal paths)
       - Async/await usage where required
       - `getAudioContext()` is async and must be awaited
       - Effect factory functions need AudioContext as first argument

    2. **Check API Used sections** on each page:
       - Method descriptions should match actual behavior
       - No references to removed or renamed methods
       - Parameter descriptions should be accurate

    3. **Verify Next Steps links** on each page:
       - All internal links should resolve to existing pages
       - No broken links or references to removed pages

    4. **Check specific areas:**
       - synth-keyboard.md and soundfont-piano.md: Verify MusicallyAware/SampledNote usage is accurate
       - drum-machine-vue.md and drum-machine-vanilla.md: Framework integration patterns should be current
       - sampled-drum-kit.md: Verify Sampler API usage
       - synth-drum-kit.md: Verify oscillator-based drum synthesis patterns

    Fix all inaccuracies. Keep existing prose that is accurate. Only modify code snippets and factual claims that are wrong.
  </action>
  <verify>
    Grep for API method names across these 6 example .md files and cross-reference against src/index.ts exports. Run `pnpm build` to verify docs build succeeds.
  </verify>
  <done>
    All 6 remaining example pages verified against current API. No outdated method names, incorrect signatures, or broken internal links.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. All code examples in example pages use current API method names and signatures
3. All internal links resolve to existing pages
4. Examples index has correct local dev commands
</verification>

<success_criteria>
All 14 example page code snippets verified against current API with no outdated code examples. All internal links work. Examples index accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/14-docs-examples-polish/14-05-SUMMARY.md`
</output>
