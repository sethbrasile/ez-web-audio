---
phase: 13-code-quality-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/controllers/base-param-controller.ts
  - src/index.ts
  - src/beat-track.ts
  - src/base-sound.ts
  - src/oscillator.ts
  - src/font.ts
  - src/preload.ts
  - src/musical-identity.ts
autonomous: true

must_haves:
  truths:
    - "Error messages include lists of supported values where applicable"
    - "BPM, noteType, numBeats, gain, and frequency have runtime validation"
    - "Type 'any' usages replaced with proper types in index.ts"
    - "OscillatorOpts renamed to OscillatorOptions for consistency"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/controllers/base-param-controller.ts"
      provides: "Improved error messages with supported values"
      contains: "Supported control types"
    - path: "src/beat-track.ts"
      provides: "BPM and noteType validation"
      contains: "must be greater than 0"
    - path: "src/index.ts"
      provides: "Typed createNotes and createSoundFor, renamed OscillatorOptions export"
  key_links:
    - from: "src/index.ts"
      to: "src/oscillator.ts"
      via: "OscillatorOptions type import"
      pattern: "OscillatorOptions"
    - from: "src/beat-track.ts"
      to: "validation"
      via: "runtime checks in playBeats/setTempo"
      pattern: "throw.*must be greater than 0"
---

<objective>
Improve error messages, add input validations, fix type safety issues, and rename types for consistency.

Purpose: Improve DX by making errors actionable, preventing silent failures from invalid inputs, and fixing type inconsistencies flagged by the Phase 12 audit (QUAL-02, QUAL-04).
Output: Better error messages, validated inputs, and consistent type naming across the library.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-audit/AUDIT-developer-experience.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve error messages and add input validations</name>
  <files>
    src/controllers/base-param-controller.ts
    src/beat-track.ts
    src/base-sound.ts
    src/oscillator.ts
    src/font.ts
    src/preload.ts
  </files>
  <action>
1. In `src/controllers/base-param-controller.ts`:
   - In `_update()` default case (line 98): Change `throw new Error('Control type '${type}' not supported')` to `throw new Error("Unsupported control type: '${type}'. Supported types: 'gain', 'pan', 'detune', 'frequency' (Oscillator only).")`
   - In `_update()` detune case (line 94): Change error to `"Audio source does not support detune. Only Oscillator instances support the 'detune' control type."`
   - In `update()` default case (line 119): Change to `throw new Error("Unsupported ratio type: '${method}'. Supported types: 'ratio', 'inverseRatio', 'percent'.")`
   - In `addRampValue()` default case (line 176): Change to `throw new Error("Unsupported ramp type: '${rampType}'. Supported types: 'linear', 'exponential'.")`

2. In `src/beat-track.ts`:
   - In `playBeats()`: Add validation at the top: `if (bpm <= 0) throw new Error("BPM must be greater than 0. Received: " + bpm)` and `if (noteType <= 0) throw new Error("noteType must be greater than 0. Received: " + noteType)`
   - In `playActiveBeats()`: Add same BPM and noteType validation.
   - In `setTempo()`: Add BPM validation: `if (bpm <= 0) throw new Error("BPM must be greater than 0. Received: " + bpm)`
   - In the constructor: Add numBeats validation after setting: `if (this.numBeats <= 0) throw new Error("numBeats must be greater than 0. Received: " + this.numBeats)`

3. In `src/base-sound.ts` `changeGainTo()`:
   - Add validation: `if (value < 0) throw new Error("Gain must be >= 0. Received: " + value)`. Also add a console.warn if value > 1: `if (value > 1) console.warn("ez-web-audio: Gain value " + value + " exceeds 1.0. Values above 1 amplify the signal and may cause distortion.")`

4. In `src/oscillator.ts` constructor:
   - After `this.freq = options?.frequency || 440`, add: `if (this.freq <= 0) throw new Error("Oscillator frequency must be greater than 0. Received: " + this.freq)`

5. In `src/font.ts` `play()`:
   - Change error from `throw new Error('EZ Web Audio: No note with identifier ${identifier} found.')` to include available notes: `throw new Error("EZ Web Audio: No note with identifier '" + identifier + "' found. Available notes: " + this.notes.map(n => n.identifier).slice(0, 10).join(', ') + (this.notes.length > 10 ? '... (' + this.notes.length + ' total)' : ''))`

6. In `src/preload.ts`:
   - Line 28: Change generic `throw new Error('HTTP ${response.status} loading ${url}')` to `throw new AudioLoadError('Failed to preload audio: HTTP ${response.status}. URL: ${url}', url)`. Import `AudioLoadError` from `'./errors'` (the `AudioLoadError` class already exists at `src/errors/load-error.ts` and is exported from `src/errors/index.ts`).

7. In `src/controllers/sound-controller.ts` and `src/controllers/oscillator-controller.ts`:
   - Update the `applyValues()` default cases to include supported types: `"Unsupported control type: '${item.type}'. Supported types for [Sound|Oscillator]Controller: 'gain', 'detune'[, 'frequency']."`
   - Update `applyRampValues()` default cases similarly.
  </action>
  <verify>
Run `pnpm test` — all tests pass. Run `pnpm typecheck` — no type errors. Manually verify error messages are descriptive by searching: `grep -rn "Supported types\|must be greater than 0\|must be >=" src/` shows validation in expected files.
  </verify>
  <done>
All error messages include supported values. BPM, noteType, numBeats, gain, and frequency validated at runtime. Font.play() shows available notes on error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix type safety and rename OscillatorOpts for consistency</name>
  <files>
    src/oscillator.ts
    src/index.ts
    src/musical-identity.ts
    src/interfaces/connectable.ts
  </files>
  <action>
1. In `src/oscillator.ts`:
   - Rename `OscillatorOpts` interface to `OscillatorOptions` (all 11 other option types use *Options pattern).
   - Keep `OscillatorOpts` as a deprecated type alias: `/** @deprecated Use OscillatorOptions instead */ export type OscillatorOpts = OscillatorOptions` — this prevents breaking existing consumers.
   - Rename `OscillatorOptsFilterValues` to `OscillatorFilterOptions`. Add deprecated alias: `/** @deprecated Use OscillatorFilterOptions instead */ export type OscillatorOptsFilterValues = OscillatorFilterOptions`

2. In `src/index.ts`:
   - Update imports to use new names `OscillatorOptions` and `OscillatorFilterOptions`
   - In the type re-exports section, export both old and new names (old with @deprecated)
   - Line 166: Type `createNotes(json?: any)` — change `any` to `Record<string, number>` since it's a frequency map (key=note identifier, value=frequency in Hz)
   - Line 493: Type `createSoundFor(type, props: any)` — change `props: any` to `props: AudioBuffer` since it always receives an AudioBuffer from the `load()` function

3. In `src/musical-identity.ts`:
   - Line 53: Add JSDoc to constructor explaining the `...args: any[]` pattern: `/** Constructor accepts any args to pass through to the mixin base class. The last argument is treated as note identifier options (frequency, identifier, letter, accidental, octave). The any[] type is required by TypeScript's mixin pattern — the mixin must accept all possible base class constructor signatures. */`

4. In `src/interfaces/connectable.ts`:
   - Add JSDoc to the `AudioSource` interface (in base-param-controller.ts line 39-47) explaining its purpose: `/** Duck-type interface for audio source nodes (OscillatorNode, AudioBufferSourceNode). Defines the minimum AudioParam properties needed by the controller system for parameter automation. */`

5. Make sure all deprecated type aliases are exported so existing consumers don't break.
  </action>
  <verify>
Run `pnpm test` — all tests pass. Run `pnpm typecheck` — no type errors. Run `pnpm build:lib` — build succeeds. Verify deprecated aliases exist: `grep -n "@deprecated" src/oscillator.ts` should show both deprecated aliases.
  </verify>
  <done>
OscillatorOpts renamed to OscillatorOptions with deprecated alias. OscillatorOptsFilterValues renamed to OscillatorFilterOptions with deprecated alias. No `any` types remain in createNotes or createSoundFor signatures. AudioSource and mixin constructor have JSDoc explanations.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (all 711+ tests)
- `pnpm typecheck` passes
- `pnpm build:lib` succeeds
- Error messages include supported values (grep for "Supported types")
- Input validations present for BPM, noteType, numBeats, gain, frequency
- No `any` parameter types in public factory functions
- OscillatorOptions is the primary type name with OscillatorOpts as deprecated alias
</verification>

<success_criteria>
- Every error message from controllers includes a list of supported values
- Invalid BPM (0, negative), noteType (0, negative), numBeats (0, negative), gain (negative), frequency (0, negative) all throw descriptive errors
- Type safety improved: no `any` in public-facing function signatures
- Type naming consistent: all option types use *Options suffix
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-quality-implementation/13-02-SUMMARY.md`
</output>
