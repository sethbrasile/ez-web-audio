---
phase: 15-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sound.test.ts
  - src/sampler.test.ts
  - src/layered-sound.test.ts
  - src/beat-track.test.ts
  - src/musical-identity.test.ts
  - src/utils/within-range.test.ts
autonomous: true

must_haves:
  truths:
    - "sound.test.ts playFor() test verifies stop is actually scheduled (not expect(true).toBe(true))"
    - "sound.test.ts pan tests verify actual pan value, not just toBe(true)"
    - "sound.test.ts percentGain test verifies calculated value matches expected percentage"
    - "musical-identity.test.ts has no expect(1) placeholder assertions"
    - "within-range.test.ts covers NaN, Infinity, and equal min/max edge cases"
    - "BeatTrack edge cases tested: setTempo(0), setTempo(-120), numBeats boundaries"
    - "Sound edge cases tested: play while playing, rapid play-stop cycles, boundary startOffset"
  artifacts:
    - path: "src/sound.test.ts"
      provides: "Fixed false positives and added edge case tests"
    - path: "src/beat-track.test.ts"
      provides: "Tempo and beat count edge case tests"
    - path: "src/musical-identity.test.ts"
      provides: "Cleaned up placeholder expects"
    - path: "src/utils/within-range.test.ts"
      provides: "Additional edge case tests"
  key_links:
    - from: "src/sound.test.ts"
      to: "src/sound.ts"
      via: "Tests verify actual Sound behavior, not just truthiness"
---

<objective>
Fix false positive tests, strengthen weak assertions, and add edge case coverage in existing test files.

Purpose: The Phase 12 audit identified 6 false positive tests and 13+ weak assertions that pass even when features are broken. Fixing these ensures the test suite catches real regressions.

Output: Updated test files with meaningful assertions and new edge case tests.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-audit/AUDIT-test-quality.md
@src/sound.test.ts
@src/sampler.test.ts
@src/layered-sound.test.ts
@src/beat-track.test.ts
@src/musical-identity.test.ts
@src/utils/within-range.test.ts
@src/sound.ts
@src/beat-track.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix false positives and strengthen weak assertions</name>
  <files>
    src/sound.test.ts
    src/sampler.test.ts
    src/layered-sound.test.ts
    src/beat-track.test.ts
  </files>
  <action>
Fix the 6 false positive tests identified in the audit. For each, replace the trivial assertion with one that actually verifies the feature works.

**sound.test.ts:**
1. Line ~171 `playFor()` test: Replace `expect(true).toBe(true)` with verification that `stopIn` or `stop` was spied/scheduled. Use `vi.spyOn(sound, 'stopIn')` and verify it was called with the expected duration, OR use `vi.useFakeTimers()` to advance time and verify stop fires. The `playFor` method internally calls `play()` then schedules `stop()` via setTimeout. Spy approach is simplest: verify `stopIn` is called with the duration value.
2. Lines ~319, ~344 pan tests: Replace `expect(true).toBe(true)` checks. After `sound.update('pan').to(-0.5).from('ratio')`, assert `expect(sound.pannerNode.pan.value).toBeCloseTo(-0.5)` (or check via the controller). Similarly for `changePanTo()`. Read the actual test context to find the correct assertion point.
3. `percentGain` test: Replace `typeof percent === 'number'` check with actual value check. If gain is 0.5, percentGain should be 50.

**sampler.test.ts:**
4. "handles empty sounds array" test: If it documents that play() throws on empty sounds, add `expect(() => sampler.play()).toThrow()` assertion.

**layered-sound.test.ts:**
5. Line ~66 warning test: Use `vi.spyOn(console, 'warn')` before creating the LayeredSound to verify the warning is emitted when layer count >= warnLayerCount.

**beat-track.test.ts:**
6. "playActiveBeats method" test (line ~102): Strengthen by verifying the actual side effect (e.g., that beats were played or method was called with correct arguments). Add call count assertion.
  </action>
  <verify>Run `pnpm test --run src/sound.test.ts src/sampler.test.ts src/layered-sound.test.ts src/beat-track.test.ts` — all tests pass and no assertions are trivial (grep for `expect(true)` and `expect(1)` returns no results in these files).</verify>
  <done>All 6 false positives fixed with meaningful assertions. No test uses `expect(true).toBe(true)` or checks only `toBe(true)` without verifying actual values.</done>
</task>

<task type="auto">
  <name>Task 2: Add edge cases and clean up test organization</name>
  <files>
    src/sound.test.ts
    src/beat-track.test.ts
    src/musical-identity.test.ts
    src/utils/within-range.test.ts
  </files>
  <action>
Add edge case tests and clean up test organization issues.

**sound.test.ts — Add edge case describe block:**
- Play while already playing: call `play()` twice, verify no error and second play creates new source (or handles gracefully)
- Rapid play-stop-play: call `play()`, `stop()`, `play()` synchronously, verify `isPlaying` is true after final play
- `startOffset` boundary: if sound has duration, setting startOffset to a value > duration should be tested for behavior (clamped or allowed?)

**beat-track.test.ts — Add edge case describe block:**
- `setTempo(0)`: Should throw or warn (test current behavior and document it)
- `setTempo(-120)`: Should throw or warn (test current behavior and document it)
- Very high tempo: `setTempo(999)`: verify it works without error
- `numBeats = 0`: Test setting beats to 0, verify behavior

NOTE: These tests should document current behavior. If the library doesn't validate these inputs, the tests should reflect what actually happens (and the test can use a comment like "// Current behavior: allows negative tempo. Consider adding validation."). Do NOT modify source code to add validations — just test and document.

**musical-identity.test.ts:**
- Remove all `expect(1)` placeholder assertions. These are on lines ~31, 39, 46, 54, 66, 78, 90, 100 per the audit. Each `expect(1)` should be removed (the test assertion above/below it is sufficient). If a test block ONLY has `expect(1)`, add a real assertion or remove the test if it's truly a placeholder.

**within-range.test.ts:**
- Add tests for: `withinRange(NaN, 0, 1)` — verify behavior with NaN
- `withinRange(Infinity, 0, 1)` — verify behavior with Infinity
- `withinRange(0.5, 0.5, 0.5)` — equal min/max
- `withinRange(-Infinity, -1, 1)` — negative Infinity
  </action>
  <verify>Run `pnpm test --run src/sound.test.ts src/beat-track.test.ts src/musical-identity.test.ts src/utils/within-range.test.ts` — all pass. Grep for `expect(1)` in musical-identity.test.ts returns no results.</verify>
  <done>Edge cases added for Sound and BeatTrack boundary conditions. Placeholder expects removed from musical-identity.test.ts. within-range.test.ts covers NaN/Infinity/boundary values.</done>
</task>

</tasks>

<verification>
Run full test suite: `pnpm test --run`
- All tests pass (no regressions)
- `grep -r "expect(true).toBe(true)" src/` returns no results
- `grep -r "expect(1)" src/musical-identity.test.ts` returns no results
- New edge case tests exist for BeatTrack tempo and Sound playback boundaries
</verification>

<success_criteria>
- Zero false positive tests remain (all 6 audit items fixed)
- Edge cases for BeatTrack tempo/beats and Sound playback boundaries documented via tests
- musical-identity.test.ts cleaned of placeholder expects
- within-range.test.ts expanded with boundary value tests
- All tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-coverage/15-02-SUMMARY.md`
</output>
