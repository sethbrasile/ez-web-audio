---
phase: 15-test-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sampled-note.test.ts
  - src/oscillator.test.ts
  - src/track.test.ts
  - src/sprite.test.ts
  - src/envelope.test.ts
  - src/base-sound.test.ts
autonomous: true

must_haves:
  truths:
    - "SampledNote inherits Sound playback and MusicallyAware identity correctly"
    - "Oscillator edge cases tested: 0 frequency behavior, envelope with negative times"
    - "Sprite edge cases tested: end < start, start > buffer duration"
    - "Error paths tested: load failures, missing AudioContext, invalid inputs"
    - "Track pause-when-not-playing and concurrent seek behavior documented via tests"
  artifacts:
    - path: "src/sampled-note.test.ts"
      provides: "SampledNote class tests"
    - path: "src/oscillator.test.ts"
      provides: "Oscillator edge case tests added"
    - path: "src/track.test.ts"
      provides: "Track edge case tests added"
    - path: "src/sprite.test.ts"
      provides: "Sprite boundary tests added"
    - path: "src/envelope.test.ts"
      provides: "Envelope edge case tests added"
  key_links:
    - from: "src/sampled-note.test.ts"
      to: "src/sampled-note.ts"
      via: "import SampledNote, verify mixin composition"
---

<objective>
Create SampledNote test file and add error path/edge case tests to existing test files for Oscillator, Track, Sprite, and Envelope.

Purpose: SampledNote is a core class with zero tests. Error paths across the library are largely untested per audit. Adding these tests catches regressions in critical code paths.

Output: 1 new test file (sampled-note.test.ts) and expanded edge case coverage in 5 existing test files.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-audit/AUDIT-test-quality.md
@src/sampled-note.ts
@src/musical-identity.ts
@src/sound.ts
@src/oscillator.ts
@src/track.ts
@src/sprite.ts
@src/envelope.ts
@src/base-sound.ts
@src/test/helpers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SampledNote tests and add Oscillator/Envelope edge cases</name>
  <files>
    src/sampled-note.test.ts
    src/oscillator.test.ts
    src/envelope.test.ts
  </files>
  <action>
**sampled-note.test.ts (NEW FILE):**
SampledNote extends `MusicallyAware(Sound)`. It's a thin mixin composition with no own methods, but it needs tests to verify the mixin wiring works correctly.

- Create a SampledNote instance using the same AudioContext mock pattern as sound.test.ts
- Verify it has Sound capabilities: `play()`, `stop()`, `isPlaying`, `changeGainTo()`, `changePanTo()`
- Verify it has MusicallyAware capabilities: `letter`, `accidental`, `octave`, `frequency`, `identifier`
- Set musical properties: `note.letter = 'A'; note.octave = '4'` -> `note.identifier === 'A4'` and `note.frequency === 440`
- Set identifier: `note.identifier = 'Bb3'` -> verify letter is 'B', accidental is 'b', octave is '3'
- Verify it can play (call `play()`, check `isPlaying`)
- Verify `instanceof Sound` is true

Look at how sound.test.ts creates Sound instances for the pattern (likely using a helper or factory). Use the same approach — `createSound` or direct instantiation with mocked AudioContext and AudioBuffer.

**oscillator.test.ts — Add edge case describe block:**
- Add a "edge cases" describe block (do NOT modify existing tests)
- Test oscillator with frequency: 0 — verify behavior (does it play silence? does it throw?)
- Test oscillator with very high frequency (20000 Hz) — verify no error
- Test play() then immediately play() again — verify no error, isPlaying still true
- Strengthen the "applies envelope on play" test: spy on `gainNode.gain.setValueAtTime` or `linearRampToValueAtTime` to verify envelope automation was scheduled (not just isPlaying === true)

**envelope.test.ts — Add edge case describe block:**
- Test negative attack time: `new Envelope({ attack: -0.1, ... })` — verify behavior (clamped to 0 or used as-is?)
- Test negative decay time: same pattern
- Test negative release time: same pattern
- Test zero attack + zero decay + zero release: verify no division-by-zero or scheduling errors
- Test very large values: attack: 9999 — verify no error

NOTE: Document current behavior. Do NOT modify source code. If negative values are allowed, note it in a test comment.
  </action>
  <verify>Run `pnpm test --run src/sampled-note.test.ts src/oscillator.test.ts src/envelope.test.ts` — all tests pass.</verify>
  <done>SampledNote test file created verifying mixin composition. Oscillator and Envelope edge cases documented via tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add error path and edge case tests for Track, Sprite, and BaseSound</name>
  <files>
    src/track.test.ts
    src/sprite.test.ts
    src/base-sound.test.ts
  </files>
  <action>
Add edge case and error path tests to existing files. Add new describe blocks at the end of each file — do NOT modify existing tests.

**track.test.ts — Add "edge cases" describe block:**
- Pause when not playing: call `pause()` when `isPlaying === false`, verify no error and no pause event emitted
- Resume when not paused: call `resume()` when not paused, verify no error (already tested per audit but verify)
- Concurrent seek calls: call `seek(0.5)` then immediately `seek(0.8)`, verify final position reflects last seek
- Seek to exactly duration: `seek(duration)` — verify clamped or at end

**sprite.test.ts — Add "edge cases" describe block:**
- Sprite with end < start: define sprite `{ start: 5, end: 2 }`, verify behavior (error? plays nothing? reversed?)
- Sprite with start > buffer duration: define sprite start beyond buffer length, verify behavior
- Sprite with start === end: zero-duration sprite, verify behavior
- Play sprite that doesn't exist: call `play('nonexistent')`, verify error thrown with helpful message

**base-sound.test.ts — Add "error paths" describe block:**
- Add effect to sound that has no AudioContext: verify graceful handling
- Remove effect that was never added: verify no error
- Call `play()` on stopped sound — verify it works (restart)
- Verify stop event includes correct time property (strengthen existing assertion if it only checks `expect.any(Number)`)

Read each file first to understand the test setup patterns (how AudioContext, buffers, and instances are created). Follow the same patterns for new tests.
  </action>
  <verify>Run `pnpm test --run src/track.test.ts src/sprite.test.ts src/base-sound.test.ts` — all tests pass.</verify>
  <done>Edge case and error path tests added for Track, Sprite, and BaseSound. Current behavior documented for boundary conditions.</done>
</task>

</tasks>

<verification>
Run full test suite: `pnpm test --run`
- All tests pass (no regressions)
- sampled-note.test.ts exists with mixin composition verification
- Edge case tests exist for Oscillator, Envelope, Track, Sprite, BaseSound
</verification>

<success_criteria>
- SampledNote has test coverage verifying Sound + MusicallyAware mixin works
- Oscillator 0-frequency and rapid-play edge cases documented
- Envelope negative/zero/large value behavior documented
- Track pause-when-not-playing and concurrent seek tested
- Sprite boundary conditions (end < start, out-of-range) tested
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-test-coverage/15-03-SUMMARY.md`
</output>
