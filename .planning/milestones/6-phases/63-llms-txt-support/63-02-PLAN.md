---
phase: 63-llms-txt-support
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - docs/index.md
  - docs/examples/synthesis.md
  - docs/examples/visualization.md
  - docs/examples/xy-pad.md
  - docs/examples/ambient-generator.md
  - docs/examples/synth-keyboard.md
  - docs/examples/audio-routing.md
  - docs/examples/drum-machine-vanilla.md
  - docs/examples/effects.md
  - docs/examples/sampled-drum-kit.md
  - docs/examples/soundfont-piano.md
  - docs/examples/drum-machine.md
  - docs/examples/drum-machine-vue.md
  - docs/examples/basic-playback.md
  - docs/examples/audio-sprite.md
  - docs/examples/synth-drum-kit.md
  - docs/examples/layered-sound.md
  - docs/examples/crossfade.md
  - docs/examples/timing.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Interactive Vue demo components are excluded from llms-full.txt output"
    - "Each excluded demo has a descriptive llm-only annotation in its place"
    - "Homepage has a description frontmatter field"
    - "An llm-only block in docs/index.md provides the footer message for llms-full.txt readers"
    - "Build succeeds and llms.txt + llms-full.txt are generated in dist"
    - "All prose, code examples, and API reference content remains in llms-full.txt"
  artifacts:
    - path: "docs/.vitepress/dist/llms.txt"
      provides: "Index file with links to all doc pages"
    - path: "docs/.vitepress/dist/llms-full.txt"
      provides: "Concatenated full documentation for AI assistants"
  key_links:
    - from: "docs/examples/*.md"
      to: "docs/.vitepress/dist/llms-full.txt"
      via: "vitepress-plugin-llms build processing"
      pattern: "llm-exclude|llm-only"
---

<objective>
Annotate all interactive Vue demo components with llm-exclude/llm-only tags across 16 example files (20 component instances), add homepage description and llm-only footer block, then build and verify the output.

Purpose: Ensure llms-full.txt contains descriptive annotations instead of meaningless Vue component tags, and verify the complete build pipeline works end-to-end.
Output: All markdown files annotated, successful build producing llms.txt and llms-full.txt with correct content and patched file size.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-llms-txt-support/63-CONTEXT.md
@.planning/phases/63-llms-txt-support/63-RESEARCH.md
@.planning/phases/63-llms-txt-support/63-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate all example pages with llm-exclude/llm-only tags and update homepage</name>
  <files>
    docs/index.md
    docs/examples/synthesis.md
    docs/examples/visualization.md
    docs/examples/xy-pad.md
    docs/examples/ambient-generator.md
    docs/examples/synth-keyboard.md
    docs/examples/audio-routing.md
    docs/examples/drum-machine-vanilla.md
    docs/examples/effects.md
    docs/examples/sampled-drum-kit.md
    docs/examples/soundfont-piano.md
    docs/examples/drum-machine.md
    docs/examples/drum-machine-vue.md
    docs/examples/basic-playback.md
    docs/examples/audio-sprite.md
    docs/examples/synth-drum-kit.md
    docs/examples/layered-sound.md
    docs/examples/crossfade.md
    docs/examples/timing.md
  </files>
  <action>
For each of the 20 Vue component instances below, wrap the component tag in `<llm-exclude>` and add a companion `<llm-only>` block immediately after. CRITICAL: Only wrap the component tag itself, never the `<script setup>` block (that would break the page).

Pattern to apply:
```markdown
<llm-exclude>
<ComponentName />
</llm-exclude>

<llm-only>
[Descriptive text about what the interactive demo does]
</llm-only>
```

Component instances to wrap (file -> component -> description):

1. `synthesis.md` -> `<OscillatorDemo />` -> "Interactive oscillator demo: select waveform type (sine, square, sawtooth, triangle), adjust frequency with a slider, and hear the result in real time. Includes lowpass, highpass, and bandpass filter controls."
2. `visualization.md` -> `<VisualizationDemo />` -> "Real-time audio visualization demo: waveform and frequency spectrum displays rendered on canvas using AnalyserNode with configurable FFT size."
3. `xy-pad.md` -> `<XYPad />` -> "Two-dimensional touch/mouse pad controlling oscillator frequency (X axis) and gain (Y axis) in real time. Sound plays while pointer is held down."
4. `ambient-generator.md` -> `<AmbientGenerator />` -> "Ambient soundscape generator with layered detuned oscillators, randomized panning, and fade-in/fade-out controls."
5. `synth-keyboard.md` -> `<SynthKeyboard />` -> "Playable polyphonic synthesizer keyboard with waveform selection, ADSR envelope controls, and mouse/keyboard input. Supports chords."
6. `audio-routing.md` -> `<DistortionDemo />` -> "Audio routing demo with distortion effect: toggle distortion on/off, adjust distortion amount, and hear the effect on a loaded audio sample."
7. `drum-machine-vanilla.md` -> `<DrumMachineVanilla />` -> "Vanilla TypeScript drum machine using event-based DOM updates. Step sequencer grid with kick/snare/hi-hat, BPM slider, and visual playhead sync via beat events."
8. `effects.md` -> `<FilterDemo />` -> "Interactive audio filter demo: apply lowpass, highpass, bandpass, and notch filters to a loaded audio sample. Adjust frequency cutoff and Q factor with sliders."
9. `sampled-drum-kit.md` -> `<SampledDrumKit />` -> "Clickable drum pad interface triggering sampled kick, snare, hi-hat, and percussion sounds with round-robin sample variation."
10. `soundfont-piano.md` -> `<SoundfontPiano />` -> "Multi-octave piano keyboard using base64-encoded soundfont samples. Click keys or use keyboard input for velocity-sensitive playback."
11. `drum-machine.md` -> `<DrumMachine />` -> "Step sequencer drum machine with kick, snare, and hi-hat tracks. 16-step grid with clickable cells, BPM slider, and visual playhead highlighting the current beat."
12. `drum-machine-vue.md` -> `<DrumMachineVue />` -> "Vue reactive drum machine demonstrating automatic UI sync via reactive beat properties. Includes mute/solo controls and BPM adjustment."
13. `basic-playback.md` -> `<AudioDemo />` -> "Basic audio playback demo: click to load and play a sound effect with volume and pan controls."
14. `basic-playback.md` -> `<TrackDemo />` -> "Music track demo with play/pause/stop controls, seek slider, and real-time position display showing current time and progress percentage."
15. `audio-sprite.md` -> `<AudioSpriteDemo />` -> "Audio sprite player: plays the full combined audio file, then individual named segments from a sprite manifest. Visual timeline shows colored segments with highlight on play."
16. `synth-drum-kit.md` -> `<SynthDrumKit />` -> "Synthesized drum kit generating kick, snare, and hi-hat sounds purely from oscillators. Clickable pads with no audio files required."
17. `layered-sound.md` -> `<PlayTogetherDemo />` -> "Play multiple sounds simultaneously using playTogether() for perfectly synchronized one-shot triggering."
18. `layered-sound.md` -> `<LayeredSoundDemo />` -> "LayeredSound demo with individual volume sliders for each layer and a master gain control for the combined output."
19. `crossfade.md` -> `<CrossfadeDemo />` -> "Crossfade between two audio tracks using equal-power curves. Adjustable crossfade duration with play controls for both tracks."
20. `timing.md` -> `<TimingDemo />` -> "Timing and scheduling demo: immediate playback with play(), delayed playback with playIn(), and precise scheduled playback with playAt()."

Additionally:

21. Add `description` frontmatter to `docs/index.md`:
    `description: "EZ Web Audio — a zero-dependency TypeScript library that makes the Web Audio API easy. Play sounds, synthesizers, drum machines, and audio effects with minimal code."`

22. Add an `<llm-only>` block at the bottom of `docs/index.md` (before the closing content):
    ```markdown
    <llm-only>
    AI assistants: full documentation available at /llms.txt (index) and /llms-full.txt (__LLMS_FULL_SIZE__, complete).
    </llm-only>
    ```
    This ensures llms-full.txt readers see the footer message. The __LLMS_FULL_SIZE__ placeholder will be patched by the post-build script.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && grep -c "llm-exclude" docs/examples/*.md | grep -v ":0$" | wc -l | xargs test 16 -eq && grep -c "llm-only" docs/examples/*.md | grep -v ":0$" | wc -l | xargs test 16 -eq && grep -q "llm-only" docs/index.md && grep -q "description:" docs/index.md</automated>
  </verify>
  <done>All 20 Vue component instances across 16 example files are wrapped in llm-exclude tags with companion llm-only descriptions. Homepage has description frontmatter and an llm-only footer block.</done>
</task>

<task type="auto">
  <name>Task 2: Build and verify llms.txt output</name>
  <files></files>
  <action>
1. Run the full build: `pnpm build`
   This runs: build:lib -> docs:build (typedoc -> vitepress build -> patch-llms-size.mjs)

2. Verify output files exist:
   - `docs/.vitepress/dist/llms.txt` exists and contains page links
   - `docs/.vitepress/dist/llms-full.txt` exists and contains concatenated content

3. Verify llms.txt content:
   - Contains section headers and description text from frontmatter
   - URLs include the base path `/ez-web-audio/`

4. Verify llms-full.txt content:
   - Contains guide content (search for "Getting Started" or "Core Concepts")
   - Contains API reference content (search for "createSound" or "createOscillator")
   - Does NOT contain raw Vue component tags like `<DrumMachine />` or `<OscillatorDemo />`
   - DOES contain the llm-only descriptions (e.g., "Step sequencer drum machine")
   - The `__LLMS_FULL_SIZE__` placeholder has been replaced with actual KB value

5. Verify HTML output:
   - Pick any HTML file in dist, confirm it contains the footer text with actual KB size (not __LLMS_FULL_SIZE__)

6. If any verification fails, diagnose and fix the issue.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && test -f docs/.vitepress/dist/llms.txt && test -f docs/.vitepress/dist/llms-full.txt && ! grep -q "__LLMS_FULL_SIZE__" docs/.vitepress/dist/llms-full.txt && grep -q "KB" docs/.vitepress/dist/llms-full.txt && ! grep -q "<DrumMachine />" docs/.vitepress/dist/llms-full.txt && ! grep -q "<OscillatorDemo />" docs/.vitepress/dist/llms-full.txt</automated>
  </verify>
  <done>Build succeeds end-to-end. llms.txt contains page index with descriptions. llms-full.txt contains all guide + API content, llm-only descriptions instead of Vue tags, and patched file size. No __LLMS_FULL_SIZE__ placeholders remain in dist output.</done>
</task>

</tasks>

<verification>
- All 16 example files contain llm-exclude and llm-only tags
- docs/index.md has description frontmatter and llm-only footer block
- `pnpm build` completes successfully
- llms.txt and llms-full.txt exist in dist
- No raw Vue component tags in llms-full.txt
- File size placeholder patched in all dist files
</verification>

<success_criteria>
- 20 Vue component instances wrapped with llm-exclude + llm-only across 16 files
- Homepage has description and llm-only block
- Full build produces valid llms.txt and llms-full.txt
- llms-full.txt contains descriptive annotations, not raw Vue tags
- File size is patched (no __LLMS_FULL_SIZE__ remaining)
- All prose, code examples, and API reference content present in llms-full.txt
</success_criteria>

<output>
After completion, create `.planning/phases/63-llms-txt-support/63-02-SUMMARY.md`
</output>
