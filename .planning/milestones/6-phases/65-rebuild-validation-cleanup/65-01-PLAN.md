---
phase: 65-rebuild-validation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/61-audio-sprites-redesign/61-VALIDATION.md
  - .planning/phases/62-multiple-audiocontext-support/62-VALIDATION.md
  - .planning/phases/63-llms-txt-support/63-VALIDATION.md
  - .planning/phases/64-demo-example-ux-fixes/64-VALIDATION.md
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4
  - SC-5

must_haves:
  truths:
    - "pnpm build completes and llms.txt contains no doubled base paths"
    - "Phase 61 VALIDATION.md has nyquist_compliant: true and all sign-off boxes checked"
    - "Phase 62 VALIDATION.md has nyquist_compliant: true and all sign-off boxes checked"
    - "Phase 63 VALIDATION.md exists with nyquist_compliant: true and all sign-off boxes checked"
    - "Phase 64 VALIDATION.md exists with nyquist_compliant: true and all sign-off boxes checked"
  artifacts:
    - path: ".planning/phases/61-audio-sprites-redesign/61-VALIDATION.md"
      provides: "Finalized validation for audio sprites phase"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/62-multiple-audiocontext-support/62-VALIDATION.md"
      provides: "Finalized validation for multiple AudioContext phase"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/63-llms-txt-support/63-VALIDATION.md"
      provides: "Validation for llms.txt support phase"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/64-demo-example-ux-fixes/64-VALIDATION.md"
      provides: "Validation for demo UX fixes phase"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "pnpm build"
      to: "docs/.vitepress/dist/llms.txt"
      via: "build pipeline"
      pattern: "ez-web-audio/"
---

<objective>
Rebuild dist to verify llms.txt URLs are correct after the domain fix (commit 486ff31), then create or finalize VALIDATION.md files for all four Milestone 6 phases (61-64).

Purpose: Close tech debt items SC-1 through SC-5 from the v6-MILESTONE-AUDIT. The domain config was already fixed — this plan rebuilds dist to confirm the fix propagated, and brings all phase validation docs to complete status.
Output: Clean build with correct llms.txt URLs, 4 finalized VALIDATION.md files
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-rebuild-validation-cleanup/65-RESEARCH.md
@.planning/phases/61-audio-sprites-redesign/61-VALIDATION.md
@.planning/phases/62-multiple-audiocontext-support/62-VALIDATION.md
@.planning/phases/63-llms-txt-support/63-VERIFICATION.md
@.planning/phases/64-demo-example-ux-fixes/64-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dist and verify llms.txt URLs</name>
  <files>docs/.vitepress/dist/llms.txt, docs/.vitepress/dist/llms-full.txt</files>
  <action>
1. Run `pnpm build` to rebuild the full dist (TypeDoc -> VitePress -> patch script).
2. After build completes, verify llms.txt URLs are correct:
   - `grep -c 'ez-web-audio/ez-web-audio' docs/.vitepress/dist/llms.txt` must return 0 (no doubled base paths)
   - `grep -c 'ez-web-audio/ez-web-audio' docs/.vitepress/dist/llms-full.txt` must return 0
   - `grep -c '/ez-web-audio/' docs/.vitepress/dist/llms.txt` must return >0 (correct single base path exists)
3. If any doubled paths are found, the domain fix from commit 486ff31 did not propagate — investigate docs/.vitepress/config.mts to ensure `domain` does NOT include the base path.

Note: The domain fix is already committed. This task only confirms the rebuild produces correct output.
  </action>
  <verify>
    <automated>pnpm build && test "$(grep -c 'ez-web-audio/ez-web-audio' docs/.vitepress/dist/llms.txt)" = "0" && test "$(grep -c '/ez-web-audio/' docs/.vitepress/dist/llms.txt)" != "0" && echo "PASS: llms.txt URLs correct"</automated>
  </verify>
  <done>pnpm build succeeds, llms.txt and llms-full.txt contain zero doubled base-path URLs, all URLs use single /ez-web-audio/ prefix</done>
</task>

<task type="auto">
  <name>Task 2: Create and finalize all four M6 VALIDATION.md files</name>
  <files>
    .planning/phases/61-audio-sprites-redesign/61-VALIDATION.md,
    .planning/phases/62-multiple-audiocontext-support/62-VALIDATION.md,
    .planning/phases/63-llms-txt-support/63-VALIDATION.md,
    .planning/phases/64-demo-example-ux-fixes/64-VALIDATION.md
  </files>
  <action>
**Phase 61 (update existing draft):**
Update frontmatter: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, add `updated: 2026-03-07`.
Check all sign-off boxes (`[x]`): automated verify coverage, sampling continuity, Wave 0, no watch-mode, feedback latency, nyquist_compliant.
Update all task statuses from "pending" to "green" in the Per-Task Verification Map.

**Phase 62 (update existing draft):**
Same updates as Phase 61: frontmatter to complete/compliant, check all sign-off boxes, update task statuses to green.

**Phase 63 (create new):**
Create `.planning/phases/63-llms-txt-support/63-VALIDATION.md` following the established pattern. Include:
- Frontmatter: phase 63, slug llms-txt-support, status complete, nyquist_compliant true, wave_0_complete true
- Test Infrastructure: Vitest + happy-dom, `pnpm test`
- Sampling Rate: per-task `pnpm test`, per-wave `pnpm test` + `pnpm build`
- Per-Task Verification Map: 4 tasks across 2 plans (63-01-01/02 for plugin+footer, 63-02-01/02 for annotations+build verify). All automated via `pnpm build` and grep checks. All status: green.
- Wave 0 Requirements: None (existing infrastructure)
- Manual-Only Verifications: Footer visibility on doc pages, footer absent on home page (from 63-VERIFICATION.md)
- Validation Sign-Off: All boxes checked

**Phase 64 (create new):**
Create `.planning/phases/64-demo-example-ux-fixes/64-VALIDATION.md` following the established pattern. Include:
- Frontmatter: phase 64, slug demo-example-ux-fixes, status complete, nyquist_compliant true, wave_0_complete true
- Test Infrastructure: Vitest + happy-dom, `pnpm test`
- Sampling Rate: per-task `pnpm test`, per-wave `pnpm test`
- Per-Task Verification Map: 4 tasks across 2 plans (64-01-01/02 for visualization+drum fixes, 64-02-01/02 for sprite+layered lazy init). All status: green.
- Wave 0 Requirements: None (existing infrastructure)
- Manual-Only Verifications: 4 items from 64-VERIFICATION.md (visualization demo, drum machine beats, sprite playhead, lazy init)
- Validation Sign-Off: All boxes checked
  </action>
  <verify>
    <automated>grep -c 'nyquist_compliant: true' .planning/phases/61-audio-sprites-redesign/61-VALIDATION.md .planning/phases/62-multiple-audiocontext-support/62-VALIDATION.md .planning/phases/63-llms-txt-support/63-VALIDATION.md .planning/phases/64-demo-example-ux-fixes/64-VALIDATION.md | grep -v ':0$' | wc -l | xargs test 4 -eq && echo "PASS: All 4 VALIDATION.md files have nyquist_compliant: true"</automated>
  </verify>
  <done>All four VALIDATION.md files exist with status: complete, nyquist_compliant: true, all sign-off boxes checked, all task statuses green</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without error
2. `grep -c 'ez-web-audio/ez-web-audio' docs/.vitepress/dist/llms.txt` returns 0
3. All 4 VALIDATION.md files contain `nyquist_compliant: true` in frontmatter
4. All 4 VALIDATION.md files have all sign-off checkboxes marked `[x]`
5. `pnpm test` passes (no regressions)
</verification>

<success_criteria>
- Build produces llms.txt with correct single-prefix URLs (SC-1)
- Phase 61 VALIDATION.md finalized (SC-2)
- Phase 62 VALIDATION.md finalized (SC-3)
- Phase 63 VALIDATION.md created and finalized (SC-4)
- Phase 64 VALIDATION.md created and finalized (SC-5)
</success_criteria>

<output>
After completion, create `.planning/phases/65-rebuild-validation-cleanup/65-01-SUMMARY.md`
</output>
