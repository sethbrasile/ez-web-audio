---
phase: 35-documentation-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/concepts.md
  - docs/guide/parameter-control.md
  - docs/guide/utilities.md
  - docs/.vitepress/config.mts
  - docs/examples/synthesis.md
  - docs/examples/audio-routing.md
autonomous: true
requirements:
  - DOC2-05
  - DOC2-06
  - DOC2-07

must_haves:
  truths:
    - "concepts.md is under 250 lines and covers only sound types, AudioContext lifecycle, audio routing, and events"
    - "parameter-control.md exists with all parameter control content (immediate updates, scheduled updates, extending ControlType) under 250 lines"
    - "utilities.md exists with all utility content (collection control, synchronized playback, batch loading, crossfade, preloading, cache, debug, interaction helpers) under 250 lines"
    - "synthesis.md uses update('frequency').to().as('ratio') instead of changeFrequencyTo()"
    - "audio-routing.md uses 1-arg wrapEffect(node) form in all code examples"
  artifacts:
    - path: "docs/guide/parameter-control.md"
      provides: "Parameter control guide page"
    - path: "docs/guide/utilities.md"
      provides: "Utilities guide page"
    - path: "docs/guide/concepts.md"
      provides: "Trimmed core concepts page"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "docs/guide/parameter-control.md"
      via: "sidebar link"
      pattern: "parameter-control"
    - from: "docs/.vitepress/config.mts"
      to: "docs/guide/utilities.md"
      via: "sidebar link"
      pattern: "utilities"
---

<objective>
Split the oversized concepts.md (693 lines) into three focused guide pages and fix incorrect API examples in synthesis.md and audio-routing.md.

Purpose: Improve guide navigability and ensure all code examples use the current API correctly.
Output: Three guide pages each under ~250 lines, corrected synthesis.md and audio-routing.md examples.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/guide/concepts.md
@docs/.vitepress/config.mts
@docs/examples/synthesis.md
@docs/examples/audio-routing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split concepts.md into three focused guide pages</name>
  <files>
    docs/guide/concepts.md
    docs/guide/parameter-control.md
    docs/guide/utilities.md
    docs/.vitepress/config.mts
  </files>
  <action>
Split `docs/guide/concepts.md` (693 lines) into three focused pages, each under ~250 lines.

**Page 1: concepts.md (keep, trim)** — Keep these sections:
- The Class Hierarchy
- Sound: One-Shot Playback (+ How Sound Works)
- Track: Music Playback (+ Track Position Tracking)
- Oscillator: Sound Synthesis (+ Waveform Types, ADSR Envelope, Envelope Class)
- AudioContext Lifecycle (Lazy Initialization, Single Context, Context States)
- Audio Routing (signal chain, Effect Chain, Batch Effect Addition, Effect Bypass, Generic Effect Wrapping)
- Events (event table)
- Other Sound Types (Sampler, BeatTrack, LayeredSound, AudioSprite, White Noise)
- Update title/description frontmatter to reflect narrower scope
- Update "Next Steps" links to include new guide pages

**Page 2: parameter-control.md (new)** — Extract these sections:
- Parameter Control (new heading)
- Immediate Updates (update().to().as())
- Scheduled Updates (onPlaySet, onPlayRamp)
- Common Patterns (fade in, fade out, pitch bend)
- Extending ControlType (module augmentation)
- Add frontmatter with title "Parameter Control - Gain, Pan, Frequency Automation" and SEO description
- Add "Next Steps" linking to concepts.md and examples

**Page 3: utilities.md (new)** — Extract these sections:
- Utility Functions (new heading)
- Collection Control (pauseAll, playAll, stopAll)
- Synchronized Playback (playTogether)
- Batch Loading (createSounds)
- Crossfade (equal-power crossfade)
- Preloading Audio (preload, isPreloaded)
- Cache Management (clearPreloadCache)
- Debug Mode (setDebugMode, setDebugHandler)
- Interaction Helpers (useInteractionMethods, preventEventDefaults)
- Add frontmatter with title "Utilities - Batch Loading, Crossfade, Debug, Preloading" and SEO description
- Add "Next Steps" linking to concepts.md and examples

**Update VitePress sidebar** in `docs/.vitepress/config.mts`:
In the `/guide/` sidebar, add entries after "Core Concepts":
```
{ text: 'Parameter Control', link: '/guide/parameter-control' },
{ text: 'Utilities', link: '/guide/utilities' },
```
  </action>
  <verify>
Verify each file exists and check line counts:
```bash
wc -l docs/guide/concepts.md docs/guide/parameter-control.md docs/guide/utilities.md
```
All three should be under 250 lines. Verify sidebar config includes new entries:
```bash
grep -c 'parameter-control\|utilities' docs/.vitepress/config.mts
```
Should return 2+ matches.
  </verify>
  <done>
concepts.md, parameter-control.md, and utilities.md each exist under ~250 lines. VitePress sidebar includes links to all three guide pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix incorrect API examples in synthesis.md and audio-routing.md</name>
  <files>
    docs/examples/synthesis.md
    docs/examples/audio-routing.md
  </files>
  <action>
**Fix synthesis.md (DOC2-06):**
In the "Changing Frequency During Playback" section (around line 246), replace:
```typescript
synth.changeFrequencyTo(523.25) // C5
```
with:
```typescript
synth.update('frequency').to(523.25).as('ratio') // C5
```
`changeFrequencyTo()` is a valid method but `update('frequency')` is the recommended fluent API pattern. Keep the `update('frequency').to(880).as('ratio')` line above it — just fix the second example.

**Fix audio-routing.md (DOC2-07):**
Replace ALL 2-arg `wrapEffect(ctx, node)` calls with 1-arg `wrapEffect(node)` form throughout the file. There are 4 occurrences:

1. Line 56: `const effect = wrapEffect(ctx, distortionNode)` → `const effect = wrapEffect(distortionNode)`
2. Line 127: `const reverb = wrapEffect(ctx, convolver)` → `const reverb = wrapEffect(convolver)`
3. Line 145: `const delayEffect = wrapEffect(ctx, delay)` → `const delayEffect = wrapEffect(delay)`
4. Line 163: `const chorusEffect = wrapEffect(ctx, chorus)` → `const chorusEffect = wrapEffect(chorus)`

Also update the "API Used" section at the bottom:
- Change `wrapEffect(context, node)` to `wrapEffect(node)` in the API summary

Remove the now-unnecessary `const ctx = await getAudioContext()` line and its `getAudioContext` import in the basic example (lines 42-48), since wrapEffect no longer needs the context. Keep `getAudioContext` in examples that still use `ctx.createWaveShaper()` etc. — just remove it from imports where it's only used for wrapEffect.

Actually, `ctx` is still needed for `ctx.createWaveShaper()`, `ctx.createConvolver()`, `ctx.createDelay()`, `ctx.createGain()`. So keep the ctx variable — just change wrapEffect calls to 1-arg form.
  </action>
  <verify>
```bash
grep 'changeFrequencyTo' docs/examples/synthesis.md
```
Should return 0 matches.
```bash
grep 'wrapEffect(ctx' docs/examples/audio-routing.md
```
Should return 0 matches.
```bash
grep 'wrapEffect(' docs/examples/audio-routing.md
```
Should return 4+ matches (all using 1-arg form).
  </verify>
  <done>
synthesis.md uses `update('frequency').to().as('ratio')` instead of `changeFrequencyTo()`. audio-routing.md uses 1-arg `wrapEffect(node)` form in all code examples.
  </done>
</task>

</tasks>

<verification>
1. `wc -l docs/guide/concepts.md` — under 250 lines
2. `wc -l docs/guide/parameter-control.md` — under 250 lines
3. `wc -l docs/guide/utilities.md` — under 250 lines
4. `grep 'changeFrequencyTo' docs/examples/synthesis.md` — 0 matches
5. `grep 'wrapEffect(ctx' docs/examples/audio-routing.md` — 0 matches
6. `grep 'parameter-control' docs/.vitepress/config.mts` — at least 1 match
7. `pnpm lint` — passes (no broken markdown or imports)
</verification>

<success_criteria>
- concepts.md split into three pages each under ~250 lines
- All three guide pages appear in VitePress sidebar
- synthesis.md uses correct `update('frequency')` API
- audio-routing.md uses recommended 1-arg `wrapEffect()` form
</success_criteria>

<output>
After completion, create `.planning/phases/35-documentation-expansion/35-01-SUMMARY.md`
</output>
