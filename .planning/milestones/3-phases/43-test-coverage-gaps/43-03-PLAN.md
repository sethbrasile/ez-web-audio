---
phase: 43-test-coverage-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.test.ts
autonomous: true
requirements:
  - TCOV-02
  - TCOV-07
  - TCOV-08
  - TCOV-09
  - TCOV-10

must_haves:
  truths:
    - "preventEventDefaults registers listeners and cleanup removes them"
    - "useInteractionMethods binds play/stop to touch/mouse events and cleanup removes them"
    - "createNotes() returns Note array from frequency map"
    - "_disposeUnmute() cleans up iOS mute workaround listeners"
    - "createAnalyzer() context-free overload creates Analyzer without explicit AudioContext"
    - "createLayeredSound() creates LayeredSound from Sound/Oscillator array"
  artifacts:
    - path: "src/index.test.ts"
      provides: "Tests for DOM helpers, factory functions, and _disposeUnmute"
      contains: "preventEventDefaults"
  key_links:
    - from: "src/index.test.ts"
      to: "src/index.ts"
      via: "import { preventEventDefaults, useInteractionMethods, createNotes, _disposeUnmute, createAnalyzer, createLayeredSound }"
      pattern: "preventEventDefaults|useInteractionMethods|createNotes"
---

<objective>
Add test coverage for DOM helper functions, factory functions, and _disposeUnmute.

Purpose: Close test gaps for preventEventDefaults/useInteractionMethods (M14), createNotes (L15), _disposeUnmute (L16), context-free createAnalyzer (L17), and createLayeredSound (L18).
Output: Enhanced tests in src/index.test.ts
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@src/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add preventEventDefaults and useInteractionMethods DOM tests</name>
  <files>src/index.test.ts</files>
  <action>
Add new describe blocks to `src/index.test.ts`. The test environment is happy-dom which provides DOM simulation.

**`describe('preventEventDefaults')`**:

Import `preventEventDefaults` from the module.

1. `it('returns a cleanup function')` — create a `document.createElement('div')`, call `preventEventDefaults(div)`, verify return type is function
2. `it('prevents default on registered events')` — create element, call `preventEventDefaults(element)`. Create a MouseEvent('mousedown', { cancelable: true }), dispatch it on element, verify `event.defaultPrevented === true`. Test at least mousedown and touchstart.
3. `it('cleanup removes all event listeners')` — call `preventEventDefaults(element)`, call the returned cleanup function, dispatch mousedown event, verify `event.defaultPrevented === false` (default behavior not prevented after cleanup)
4. `it('covers all expected event types')` — verify the function handles the full list: touchstart, touchend, touchcancel, touchmove, mousedown, mouseup, click, contextmenu, dragstart, dragend, dragenter, dragover, drag, dragleave, drop. Test by dispatching each and checking defaultPrevented. (Alternatively, spy on addEventListener to verify all 15 events registered.)

**`describe('useInteractionMethods')`**:

Import `useInteractionMethods` from the module.

1. `it('returns a cleanup function')` — create element, create mock player `{ play: vi.fn(), stop: vi.fn() }`, call `await useInteractionMethods(element, player)`, verify returns function
2. `it('calls play on mousedown')` — dispatch mousedown event on element, verify `player.play` was called (note: play internally calls initAudio, so mock AudioContext must be stubbed)
3. `it('calls stop on mouseup')` — dispatch mouseup event, verify `player.stop` was called
4. `it('calls stop on mouseleave')` — dispatch mouseleave event, verify `player.stop` was called
5. `it('cleanup removes all listeners')` — call cleanup function, dispatch mousedown, verify `player.play` call count did not increase

Note: `useInteractionMethods` calls `initAudio()` internally, so ensure MockAudioContext is stubbed globally (already done in the test file's beforeEach). The player mock should have play/stop as vi.fn() returning undefined (not async — the function calls player.play() without await).
  </action>
  <verify>
    <automated>pnpm test src/index.test.ts</automated>
  </verify>
  <done>preventEventDefaults tested: registers listeners, prevents defaults, cleanup removes them. useInteractionMethods tested: binds play/stop to mouse/touch events, cleanup works.</done>
</task>

<task type="auto">
  <name>Task 2: Add createNotes, _disposeUnmute, createAnalyzer context-free, and createLayeredSound tests</name>
  <files>src/index.test.ts</files>
  <action>
Add new describe blocks to `src/index.test.ts`:

**`describe('createNotes')`**:

Import `createNotes` and `Note` from the module (Note may need import from `'./note'`).

1. `it('returns an array of Note objects from default frequency map')` — call `createNotes()`, verify returns array, length > 0, each entry has `frequency` property
2. `it('returns notes with correct frequencies from frequency map')` — verify the returned array contains a note with frequency 440 (A4)
3. `it('accepts custom JSON frequency map')` — call `createNotes({ 'A4': 440, 'B4': 493.88 })`, verify returns exactly 2 notes with those frequencies
4. `it('returns empty array for empty JSON')` — call `createNotes({})`, verify returns empty array

**`describe('_disposeUnmute')`**:

Import `_disposeUnmute` from the module.

1. `it('does not throw when called without prior initAudio')` — call `_disposeUnmute()` with no prior init, verify no error
2. `it('does not throw when called multiple times')` — call twice, verify no error
3. `it('cleans up after initAudio has been called')` — call `initAudio()` first (with mocked AudioContext), then `_disposeUnmute()`, verify no error. This documents the cleanup path even though we cannot inspect the internal `_unmuteDispose` variable.

**`describe('createAnalyzer context-free overload')`**:

1. `it('creates Analyzer without AudioContext parameter')` — call `await createAnalyzer({ fftSize: 1024 })`, verify returns Analyzer instance (has `getFrequencyData` method or similar)
2. `it('creates Analyzer with no arguments')` — call `await createAnalyzer()`, verify returns Analyzer instance
3. `it('creates Analyzer with explicit AudioContext')` — call `await createAnalyzer(audioContext, { fftSize: 2048 })` to verify the overload still works

**`describe('createLayeredSound')`**:

1. `it('creates LayeredSound from array of Sound instances')` — create mock Sound instances (with valid AudioBuffer), call `await createLayeredSound([sound1, sound2])`, verify returns LayeredSound instance (has `play` and `stop` methods)
2. `it('accepts optional name option')` — call `await createLayeredSound([sound], { name: 'test-layer' })`, verify the instance is created
3. `it('works with empty layers array')` — call `await createLayeredSound([])`, verify creates without error (LayeredSound may accept empty arrays)

For Sound mocks in createLayeredSound tests, reuse the existing fetch mock + `load()` pattern from the test file (mock fetch to return ArrayBuffer, create Sounds via the factory).
  </action>
  <verify>
    <automated>pnpm test src/index.test.ts</automated>
  </verify>
  <done>createNotes returns Note array with correct frequencies. _disposeUnmute doesn't throw. createAnalyzer context-free overload creates Analyzer. createLayeredSound creates from Sound array.</done>
</task>

</tasks>

<verification>
```bash
pnpm test src/index.test.ts
```
All tests pass. New test count increased by ~15-20 tests.
</verification>

<success_criteria>
- preventEventDefaults tested with DOM simulation (register + cleanup)
- useInteractionMethods tested with play/stop binding (register + cleanup)
- createNotes returns Notes from default and custom frequency maps
- _disposeUnmute safely callable with and without prior initAudio
- createAnalyzer context-free overload tested
- createLayeredSound factory tested
</success_criteria>

<output>
After completion, create `.planning/phases/43-test-coverage-gaps/43-03-SUMMARY.md`
</output>
