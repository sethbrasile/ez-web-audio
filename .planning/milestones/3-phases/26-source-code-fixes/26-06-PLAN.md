---
phase: 26-source-code-fixes
plan: 06
type: execute
wave: 2
depends_on: ["01", "05"]
files_modified:
  - src/utils/decode-base64.ts
  - src/preload.ts
  - src/index.ts
  - src/utils/init-audio.ts
  - src/play-together.ts
  - src/font.ts
  - src/sprite.ts
  - src/utils/prop-access.ts
  - src/gain-effect.ts
autonomous: true
requirements:
  - SC-16
  - SC-17
  - SC-18

must_haves:
  truths:
    - "mungeSoundFont validates input and throws descriptive errors on malformed data"
    - "Response cache clone pattern is safe against double-consumption"
    - "All low-priority source issues (L-1 through L-9) are resolved"
  artifacts:
    - path: "src/utils/decode-base64.ts"
      provides: "Input validation for mungeSoundFont"
      contains: "MIDI.Soundfont"
    - path: "src/preload.ts"
      provides: "Safe response cache pattern"
      pattern: "response\\.clone\\(\\)"
  key_links:
    - from: "src/preload.ts"
      to: "src/index.ts"
      via: "consistent response cache clone pattern"
      pattern: "responseCache"
---

<objective>
Fix utility and polish issues: mungeSoundFont input validation, response cache safety, and all low-priority items (L-1 through L-9).

Purpose: mungeSoundFont fails with cryptic errors on malformed input. Response cache pattern is fragile. Various low-priority code quality issues need cleanup.
Output: Validated soundfont parsing, safe caching, cleaned-up utilities.

NOTE: This plan depends on 26-01 (modifies src/index.ts) and 26-05 (modifies src/sprite.ts). Read those files fresh before modifying — do not assume their prior state.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-source-code-fixes/26-RESEARCH.md
@.planning/phases/26-source-code-fixes/26-01-SUMMARY.md
@.planning/phases/26-source-code-fixes/26-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mungeSoundFont validation and response cache pattern</name>
  <files>src/utils/decode-base64.ts, src/preload.ts, src/index.ts</files>
  <action>
**M-8 — mungeSoundFont validation:**
In `src/utils/decode-base64.ts`, add input validation to `mungeSoundFont()`:
1. Check input is a non-empty string
2. Check for `MIDI.Soundfont.` marker
3. Check for `=` after the marker
4. Validate `end > begin` before slicing
5. Wrap `JSON.parse` in try/catch with descriptive error

Follow the exact implementation from the research RESEARCH.md section on M-8.

**M-13 — Response cache clone pattern:**
IMPORTANT: Read src/index.ts and src/preload.ts fresh — plan 26-01 has already modified src/index.ts to add type exports.

In `src/preload.ts`, where `responseCache.set(url, response)` is called, change to:
```typescript
responseCache.set(url, response.clone())
```
And use the original response for the immediate operation.

In `src/index.ts`, find the load function where `responseCache.set(src, response)` is called. Apply the same pattern:
```typescript
responseCache.set(src, response.clone())  // store clone for future use
buffer = await audioContext.decodeAudioData(await response.arrayBuffer())  // consume original
```

Add a comment explaining why: "Store a clone so the cached response body is always unconsumed. Response.body can only be read once."
  </action>
  <verify>`pnpm test` passes. Search decode-base64.ts for "MIDI.Soundfont" validation. Search preload.ts and index.ts for `response.clone()` in cache set calls.</verify>
  <done>mungeSoundFont throws descriptive errors on invalid input. Response cache consistently stores clones.</done>
</task>

<task type="auto">
  <name>Task 2: Fix low-priority issues L-1, L-5, L-6, L-7, L-8, L-9</name>
  <files>src/utils/init-audio.ts, src/play-together.ts, src/font.ts, src/sprite.ts, src/utils/prop-access.ts, src/gain-effect.ts</files>
  <action>
Read each file before modifying. Apply fixes only where the issue is confirmed.

IMPORTANT: Read src/sprite.ts fresh — plan 26-05 has already added `activeSources` tracking and `stop()` to this file.

**L-1 — preventEventDefaults/useInteractionMethods leak listeners:**
In `src/utils/init-audio.ts` (or wherever these functions are defined), make them return a cleanup function that removes the added event listeners. Find the functions, read how they attach listeners, and return `() => { removeEventListener(...) }`.

**L-5 — playTogether duck-typing:**
In `src/play-together.ts`, replace `(p as any).audioContext` with a proper type guard or interface check. Read the file to see how audioContext is accessed. If the function accepts `Playable[]`, add a type assertion with a comment, or create a minimal interface `{ audioContext: AudioContext }` and intersect it with the parameter type.

**L-6 — GainEffect equal-power crossfade:**
In `src/gain-effect.ts`, replace the linear crossfade interpolation with equal-power crossfade using cosine/sine curves. The formula:
```typescript
// Equal-power crossfade (constant power, no volume dip at midpoint)
const fadeOutGain = Math.cos(position * Math.PI / 2)
const fadeInGain = Math.sin(position * Math.PI / 2)
```
Where `position` is normalized 0-1 through the crossfade duration. Read the file to find where linear interpolation is used and replace with the equal-power formula. This is a simple formula swap — no restructuring needed.

**L-7 — Font.getNote O(n) search:**
In `src/font.ts`, read how `getNote()` works. If it uses `Array.find()`, add a `Map<string, SampledNote>` built during construction for O(1) lookup. Only do this if the array is iterated on each getNote call. If the array is small (12-88 notes), this is a minor optimization — add the Map if straightforward, otherwise add a comment noting the linear search is acceptable for small collections.

**L-8 — AudioSprite boundary validation:**
In `src/sprite.ts` (already modified by plan 26-05 — read fresh), add validation in `play()` that `sprite.start >= 0` and `sprite.start + sprite.duration <= buffer.duration`. Throw a descriptive error if out of bounds.

**L-9 — prop-access.ts dead code:**
In `src/utils/prop-access.ts`, delete all commented-out code blocks. Read the file, identify commented-out sections, and remove them entirely.

**Skip L-2** (handled in plan 01), **L-3** (handled in plan 02), **L-4** (handled in plan 03).
  </action>
  <verify>`pnpm test` passes. `pnpm typecheck` passes. `pnpm lint` passes.</verify>
  <done>All low-priority issues L-1 through L-9 resolved. No `as any` in playTogether. No commented-out code in prop-access.ts. Cleanup functions returned from listener setup. Equal-power crossfade in GainEffect. Sprite boundaries validated.</done>
</task>

</tasks>

<verification>
- `pnpm test` passes with no regressions
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm build:lib` succeeds
</verification>

<success_criteria>
mungeSoundFont validates input. Response cache is safe. All low-priority items L-1 through L-9 addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/26-source-code-fixes/26-06-SUMMARY.md`
</output>
