---
phase: 30-test-coverage-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/beat.test.ts
  - src/sound.test.ts
  - src/controllers/oscillator-controller.test.ts
autonomous: true
requirements: [SC-03, SC-05, SC-07]

must_haves:
  truths:
    - "beat.ts playIfActive() and playInIfActive() are tested — the primary BeatTrack scheduler methods"
    - "BaseSound timing methods playFor(), playInAndStopAfter(), stopIn(), stopAt() are tested"
    - "OscillatorController frequency ramp scheduling is tested"
  artifacts:
    - path: "src/beat.test.ts"
      provides: "Expanded tests for playIfActive and playInIfActive"
      min_lines: 100
    - path: "src/sound.test.ts"
      provides: "Expanded tests for timing methods"
      min_lines: 200
    - path: "src/controllers/oscillator-controller.test.ts"
      provides: "Expanded tests for frequency ramp scheduling"
      min_lines: 120
  key_links:
    - from: "src/beat.test.ts"
      to: "src/beat.ts"
      via: "import Beat"
      pattern: "playIfActive|playInIfActive"
    - from: "src/controllers/oscillator-controller.test.ts"
      to: "src/controllers/oscillator-controller.ts"
      via: "onPlayRamp frequency scheduling"
      pattern: "onPlayRamp.*frequency"
---

<objective>
Expand existing test files with missing method coverage: beat.ts scheduler methods, BaseSound timing methods, and OscillatorController frequency ramp scheduling.

Purpose: Closes review findings T-3, T-5, T-7 — expanding partial test coverage on critical modules.
Output: Three existing test files expanded with targeted new test cases.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/beat.ts
@src/beat.test.ts
@src/base-sound.ts
@src/sound.test.ts
@src/controllers/oscillator-controller.ts
@src/controllers/oscillator-controller.test.ts
@src/test/helpers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand beat.test.ts with playIfActive and playInIfActive tests</name>
  <files>src/beat.test.ts</files>
  <action>
Add new test cases to `src/beat.test.ts` for the two untested methods: `playIfActive()` and `playInIfActive()`.

Follow the existing pattern: use `MockParentClass`, `createBeat()`, `mockSetTimeout` from `./test/helpers`, and `settle()` for async flag checks.

New test cases for `playIfActive()`:
1. **When active=true, calls parent play and sets isPlaying=true** — Set `beat.active = true`, call `playIfActive()`. Verify `parent.playCalled === true` and `beat.isPlaying === true`.
2. **When active=false, does NOT call parent play** — Leave `beat.active = false`, call `playIfActive()`. Verify `parent.playCalled === false` and `beat.isPlaying === false`.
3. **Always sets currentTimeIsPlaying regardless of active** — With `active=false`, call `playIfActive()`. Verify `beat.currentTimeIsPlaying === true`.
4. **isPlaying resets after duration** — Set `active=true`, `duration=1`, call `playIfActive()`. Use `settle()` to verify `isPlaying` resets to false.
5. **currentTimeIsPlaying resets after duration** — Call `playIfActive()`. Use `settle()` to verify `currentTimeIsPlaying` resets to false.

New test cases for `playInIfActive()`:
1. **When active=true, calls parentPlayIn with offset** — Set `active=true`, call `playInIfActive(0.5)`. Verify `parent.playInCalled === true` and `parent.playInValue === 0.5`.
2. **When active=false, does NOT call parentPlayIn** — Leave `active=false`, call `playInIfActive(0.5)`. Verify `parent.playInCalled === false`.
3. **Always sets currentTimeIsPlaying after offset** — With `active=false`, call `playInIfActive(0)`. Use `settle()` to verify `currentTimeIsPlaying` becomes true.
4. **When active, both isPlaying and currentTimeIsPlaying set after offset** — Set `active=true`, call `playInIfActive(0)`. Use `settle()` to verify both flags become true.

All tests using flag resets must pass `setTimeout: mockSetTimeout` in BeatOptions so timing is controllable.
  </action>
  <verify>`pnpm test src/beat.test.ts` — all tests pass (existing + new)</verify>
  <done>beat.test.ts covers playIfActive() and playInIfActive() — active/inactive branching, flag setting, flag reset after duration, currentTimeIsPlaying always-set behavior</done>
</task>

<task type="auto">
  <name>Task 2: Expand sound.test.ts timing methods and oscillator-controller.test.ts frequency ramps</name>
  <files>src/sound.test.ts, src/controllers/oscillator-controller.test.ts</files>
  <action>
**sound.test.ts — BaseSound timing methods:**

Add a new `describe` block for timing method coverage. Follow existing Sound test patterns (create mock AudioContext, create Sound with AudioBuffer, use `settle()` for async).

Check what's already tested by reading the existing `playFor` and `playInAndStopAfter` describe blocks. Add these if missing:

1. **stopIn(seconds) schedules stop in the future** — Play a sound, call `stopIn(0.5)`. Verify the sound eventually stops (isPlaying becomes false via settle).
2. **stopAt(time) stops at specific AudioContext time** — Play a sound, call `stopAt(audioContext.currentTime + 0.001)`. Verify stop occurs.
3. **stopIn(0) stops immediately** — Play, `stopIn(0)`. Verify immediate stop.
4. **stopAt with time in the past or current stops immediately** — Play, `stopAt(audioContext.currentTime)`. Verify immediate stop.
5. **playFor(duration) plays and then stops** — Already partially tested. Verify the `isPlaying` flag lifecycle: true after play, false after duration.
6. **playInAndStopAfter combines playIn and stopIn** — Verify it calls playAt with future time and schedules stop.

**oscillator-controller.test.ts — frequency ramp scheduling:**

Add a new `describe('frequency ramp scheduling')` block after the existing test blocks.

Test cases:
1. **onPlayRamp('frequency', 'linear') schedules linear ramp** — Call `controller.onPlayRamp('frequency', 'linear').from(220).to(440).in(0.5)`. Then call `controller.setValuesAtTimes()`. Verify `oscillatorNode.frequency.setValueAtTime` was called with 220, and `oscillatorNode.frequency.linearRampToValueAtTime` was called with 440.
2. **onPlayRamp('frequency', 'exponential') schedules exponential ramp** — Same but with exponential. Verify `exponentialRampToValueAtTime` called.
3. **onPlaySet('frequency') schedules value at time** — Call `controller.onPlaySet('frequency').to(880).at(0)`. Call `setValuesAtTimes()`. Verify `oscillatorNode.frequency.setValueAtTime` called with 880.
4. **onPlayRamp('detune') schedules detune ramp** — Verify detune ramp works through scheduling.
5. **onPlayRamp('pan') schedules pan ramp** — Verify pan ramp scheduling on pannerNode.
6. **Scheduled values are cleared after setValuesAtTimes** — Schedule a ramp, call `setValuesAtTimes()` twice. Verify the ramp only applied once (second call is a no-op for that ramp).

Use `vi.spyOn` on `oscillatorNode.frequency`, `oscillatorNode.detune`, `pannerNode.pan` AudioParam methods to verify scheduling calls.
  </action>
  <verify>`pnpm test src/sound.test.ts src/controllers/oscillator-controller.test.ts` — all tests pass</verify>
  <done>Sound timing methods (stopIn, stopAt, playFor lifecycle, playInAndStopAfter) tested. OscillatorController frequency/detune/pan ramp scheduling tested with verification of AudioParam method calls and schedule clearing.</done>
</task>

</tasks>

<verification>
```bash
pnpm test src/beat.test.ts src/sound.test.ts src/controllers/oscillator-controller.test.ts
```
All expanded test files pass. No regressions.
</verification>

<success_criteria>
- beat.test.ts has playIfActive and playInIfActive tests covering active/inactive branching and flag lifecycle
- sound.test.ts has stopIn, stopAt, playFor lifecycle, and playInAndStopAfter tests
- oscillator-controller.test.ts has frequency ramp scheduling tests for linear, exponential, and value-at-time
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-coverage-expansion/30-02-SUMMARY.md`
</output>
