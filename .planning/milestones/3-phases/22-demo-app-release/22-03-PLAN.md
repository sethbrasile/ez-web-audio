---
phase: 22-demo-app-release
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - .github/workflows/publish.yml
  - package.json
autonomous: false
requirements:
  - DOC-02
  - DOC-04

must_haves:
  truths:
    - "publish.yml runs typecheck, unit tests, build, and E2E tests before npm publish"
    - "package.json version is 1.0.0"
    - "pnpm build succeeds (lib + docs site + typedoc)"
    - "All tests pass (unit + E2E)"
    - "Demo site builds and deploys successfully"
  artifacts:
    - path: ".github/workflows/publish.yml"
      provides: "CI pipeline with full test + build + E2E gates"
      contains: "test:e2e"
    - path: "package.json"
      provides: "Package metadata at version 1.0.0"
      contains: "1.0.0"
  key_links:
    - from: ".github/workflows/publish.yml"
      to: "package.json"
      via: "npm publish reads version"
      pattern: "npm publish"
---

<objective>
Finalize CI pipeline, bump version to 1.0.0, run full verification, and build the demo site.

Purpose: Ensure npm 1.0.0 can be published with full confidence — all tests pass, the build is clean, the demo site works, and CI gates prevent broken releases.
Output: Updated publish.yml with E2E gates, version bumped to 1.0.0, verified build.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/publish.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI pipeline and bump version</name>
  <files>
    .github/workflows/publish.yml
    package.json
  </files>
  <action>
    1. **Update `.github/workflows/publish.yml`** to add comprehensive pre-publish gates:
       - Add `pnpm typecheck` step after install (before tests)
       - Add `pnpm build` step (full build: lib + typedoc + docs) after tests
       - Add Playwright install step: `npx playwright install --with-deps chromium`
       - Add E2E test step: `pnpm test:e2e` after build (E2E tests need the built site)
       - Keep existing `npm publish --provenance --access public` at the end
       - The final step order should be: install deps → typecheck → unit tests → build lib → build full (docs) → install playwright → E2E tests → publish

    2. **Bump version in `package.json`** from `"0.1.0"` to `"1.0.0"`.

    3. Run full local verification:
       - `pnpm typecheck` — must pass
       - `pnpm test run` — all 937+ tests must pass
       - `pnpm build` — lib + docs must build successfully
       - `pnpm test:e2e` — all E2E tests must pass
  </action>
  <verify>
    `pnpm typecheck` passes.
    `pnpm test run` passes (937+ tests).
    `pnpm build` succeeds.
    `pnpm test:e2e` passes (20 E2E tests).
    `grep '"version": "1.0.0"' package.json` matches.
    publish.yml contains typecheck, test, build, e2e, and publish steps in order.
  </verify>
  <done>CI pipeline has full gates (typecheck + tests + build + E2E). package.json is version 1.0.0. All local verification passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final verification before release</name>
  <files>package.json</files>
  <action>
    Present the complete 1.0.0 release for human verification. All automated work is done:
    - All Vue demo components updated to final API (.as(), context-free factories, addEffects)
    - TypeDoc regenerated with protected members visible
    - Comprehensive CHANGELOG.md with breaking changes and migration guide
    - CI pipeline with full pre-publish gates
    - Version bumped to 1.0.0
    - Full test suite passing (unit + E2E)
    - Demo site built successfully
  </action>
  <verify>
    1. Run `pnpm dev` and browse the demo site — check interactive examples work (synth drum kit, filter demo, track demo)
    2. Check API reference section — protected members should appear
    3. Review CHANGELOG.md for completeness
    4. Review publish.yml CI gates
    5. When ready to publish: `git tag v1.0.0 && git push origin v1.0.0` to trigger GitHub Actions
  </verify>
  <done>User has verified the release and confirmed readiness. npm 1.0.0 can be published via git tag.</done>
  <resume-signal>Type "approved" to confirm release readiness, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm test run` — 937+ tests pass
3. `pnpm build` — succeeds (lib + docs)
4. `pnpm test:e2e` — 20 E2E tests pass
5. `grep '"version": "1.0.0"' package.json` matches
6. publish.yml has typecheck → test → build → e2e → publish pipeline
</verification>

<success_criteria>
npm 1.0.0 is ready for publish. CI pipeline gates all critical checks. Version is bumped. Full test suite passes. Demo site builds. User has verified and can trigger publish via git tag.
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-app-release/22-03-SUMMARY.md`
</output>
