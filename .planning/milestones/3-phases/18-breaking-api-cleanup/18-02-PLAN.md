---
phase: 18-breaking-api-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/base-sound.ts
  - src/base-sound.test.ts
  - src/sound.ts
  - src/sound.test.ts
  - src/track.ts
  - src/track.test.ts
  - src/oscillator.ts
  - src/interfaces/connectable.ts
  - src/interfaces/playable.ts
  - src/debug/index.ts
  - src/index.ts
  - src/utils/crossfade.ts
  - src/utils/crossfade.test.ts
  - src/effects/gain-effect.ts
  - src/sampled-note.test.ts
  - src/debug/debug.test.ts
  - src/app/pages/audio-routing.ts
  - src/app/pages/distortable-play-button.ts
autonomous: true
requirements: [API-03, API-04, API-05, API-06, API-07]

must_haves:
  truths:
    - "`gainNode` and `pannerNode` are protected on BaseSound — user code cannot access them"
    - "`effectChainInput` is protected on BaseSound — user code cannot access them"
    - "`startOffset` is protected on BaseSound but accessible on Track if needed"
    - "The entire connections API is removed: addConnection, removeConnection, getConnection, getNodeFrom, connections array"
    - "OscillatorOpts and OscillatorOptsFilterValues type aliases do not exist — only OscillatorOptions and OscillatorFilterOptions"
    - "The commented-out stopAfter is removed from Playable interface"
    - "All tests pass and the library builds"
  artifacts:
    - path: "src/base-sound.ts"
      provides: "Protected gainNode, pannerNode, effectChainInput, startOffset; no connections methods"
      contains: "protected gainNode"
    - path: "src/interfaces/playable.ts"
      provides: "Clean Playable interface with no stopAfter reference"
    - path: "src/index.ts"
      provides: "No OscillatorOpts or OscillatorOptsFilterValues exports"
  key_links:
    - from: "src/utils/crossfade.ts"
      to: "src/base-sound.ts"
      via: "crossfade needs gainNode access — must use public getter or accept GainNode param"
      pattern: "gainNode"
    - from: "src/track.ts"
      to: "src/base-sound.ts"
      via: "Track extends BaseSound — may need to re-expose startOffset as public"
      pattern: "startOffset"
---

<objective>
Enforce encapsulation by making internal properties protected, remove all deprecated APIs (connections, type aliases, stopAfter), and clean up dead code.

Purpose: Ensure the 1.0 public API surface is minimal and correct. Internal audio nodes should not be part of the public contract. Deprecated patterns must be fully removed with no trace.
Output: Clean BaseSound with protected internals, no connections API, no deprecated type aliases, no dead code.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/base-sound.ts
@src/interfaces/connectable.ts
@src/interfaces/playable.ts
@src/utils/crossfade.ts
@src/track.ts
@src/index.ts
@src/oscillator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make gainNode, pannerNode, effectChainInput, startOffset protected and resolve access issues</name>
  <files>
    src/base-sound.ts
    src/base-sound.test.ts
    src/sound.ts
    src/sound.test.ts
    src/track.ts
    src/track.test.ts
    src/oscillator.ts
    src/utils/crossfade.ts
    src/utils/crossfade.test.ts
    src/effects/gain-effect.ts
    src/sampled-note.test.ts
    src/debug/debug.test.ts
  </files>
  <action>
    **Step 1: Assess current access patterns.** Before changing visibility, identify all external access:

    - `gainNode` is accessed externally by `crossfade.ts` (needs `.gainNode` to schedule gain ramps). Solution: Add a public readonly `getGainNode()` method or accept the GainNode as a parameter to crossfade. Per CONTEXT.md, Claude decides the approach. Recommended: crossfade() already takes BaseSound args — add a `getGainNode(): GainNode` public accessor method on BaseSound so crossfade can use it. This keeps gainNode itself protected while providing controlled access.
    - `pannerNode` — check if anything external accesses it. If only internal (wireEffectChain, constructor), just make it protected.
    - `effectChainInput` — already protected in current code. Verify and confirm.
    - `startOffset` — used by Track for seek/position. Since Track extends BaseSound, `protected` gives Track access. Check if Track needs to expose it publicly — the Track has `position` and `time` getters that expose the current position. Per CONTEXT.md, Claude decides whether Track re-exposes startOffset. Recommended: Track should NOT re-expose it — `position` and `seek()` are the public API for Track positioning.

    **Step 2: Apply visibility changes in `base-sound.ts`:**
    - `gainNode`: Change from `public` to `protected`. Add `public getGainNode(): GainNode { return this.gainNode }` accessor.
    - `pannerNode`: Already protected. Confirm no change needed.
    - `effectChainInput`: Already protected. Confirm no change needed.
    - `startOffset`: Change from `public` to `protected`.

    **Step 3: Update crossfade.ts** to use `getGainNode()` instead of `.gainNode` direct access.

    **Step 4: Update test files** that directly access these properties. Tests that access `.gainNode` or `.startOffset` on BaseSound instances need to either:
    - Use the new `getGainNode()` accessor
    - Cast to `any` for test-only access to protected members (standard test pattern for this codebase)
    - Use a TestableBaseSound subclass that exposes them

    Run `pnpm typecheck` after each major change to catch compilation errors.
  </action>
  <verify>
    1. `pnpm typecheck` passes — confirms no external code illegally accesses protected members
    2. `pnpm test` — all tests pass
    3. `grep -rn 'public gainNode' src/base-sound.ts` returns zero results
    4. `grep -rn 'public startOffset' src/base-sound.ts` returns zero results
  </verify>
  <done>
    `gainNode`, `pannerNode`, `effectChainInput`, and `startOffset` are all protected on BaseSound. `getGainNode()` provides controlled access where needed. Track accesses `startOffset` via inheritance. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove connections API, deprecated type aliases, and stopAfter dead code</name>
  <files>
    src/base-sound.ts
    src/base-sound.test.ts
    src/sound.ts
    src/sound.test.ts
    src/interfaces/connectable.ts
    src/interfaces/playable.ts
    src/debug/index.ts
    src/oscillator.ts
    src/index.ts
    src/app/pages/audio-routing.ts
    src/app/pages/distortable-play-button.ts
  </files>
  <action>
    **Part A: Remove the entire connections API from BaseSound.**

    In `src/base-sound.ts`:
    1. Remove the `connections` property declaration and initialization
    2. Remove the `addConnection(connection: Connection)` method entirely
    3. Remove the `removeConnection(name: string)` method entirely
    4. Remove the `getConnection(name: string)` method entirely
    5. Remove the `getNodeFrom(connectionName: string)` method entirely
    6. Remove any `wireConnections()` method if it exists
    7. Remove the `Connection` type import if no longer used
    8. Remove JSDoc references to the connections array in the class description
    9. Clean up the audio chain comments that reference connections

    In `src/interfaces/connectable.ts`:
    - Remove the `connections` property from the Connectable interface
    - If the entire Connectable interface is now empty or only has non-connections members, assess whether to keep it. If it still has gain/pan/audioSourceNode members, keep it. Remove the `@deprecated` tag about connections-based routing being superseded.

    In `src/debug/index.ts`:
    - Remove any debug logging that references `addConnection`/`removeConnection` events

    In `src/index.ts`:
    - Remove `Connection` type export if it exists
    - Remove any re-exports of connection-related types

    **Part B: Remove deprecated type aliases.**

    In `src/oscillator.ts`:
    - Delete: `export type OscillatorOpts = OscillatorOptions`
    - Delete: `export type OscillatorOptsFilterValues = OscillatorFilterOptions`

    In `src/index.ts`:
    - Remove `OscillatorOpts` and `OscillatorOptsFilterValues` from exports

    **Part C: Remove stopAfter from Playable interface.**

    In `src/interfaces/playable.ts`:
    - Remove the `playInAndStopAfter` line (it's commented out or part of the interface). Check carefully — if `playInAndStopAfter` is an actual implemented method, do NOT remove the implementation, only the `stopAfter` dead code. The requirement says "Commented-out `stopAfter` removed from Playable interface."
    - Actually check: the grep showed `playInAndStopAfter` which IS implemented (playFor covers the use case according to CONTEXT). If it's a real method, check the CONTEXT.md more carefully. The requirement API-07 says: "Commented-out `stopAfter` removed from Playable interface." So find the commented-out `stopAfter` line and remove ONLY that.

    **Part D: Update demo pages** that use the connections API:
    - `src/app/pages/audio-routing.ts` — replace any `addConnection`/`connections` usage with the `addEffect()` API
    - `src/app/pages/distortable-play-button.ts` — same

    **Part E: Update tests.**
    - Remove tests for `addConnection`, `removeConnection`, `getConnection`, `getNodeFrom` in `base-sound.test.ts` and `sound.test.ts`
    - Ensure no test references the removed type aliases

    Run `pnpm typecheck` to find remaining references.
  </action>
  <verify>
    1. `pnpm typecheck` passes
    2. `pnpm test` — all tests pass
    3. `grep -rn 'addConnection\|removeConnection\|getConnection\|getNodeFrom' src/` returns zero results
    4. `grep -rn 'OscillatorOpts[^O]' src/` returns zero results (no old alias references)
    5. `grep -rn 'stopAfter' src/interfaces/playable.ts` returns zero results
    6. `pnpm build:lib` builds successfully
  </verify>
  <done>
    The connections API (addConnection, removeConnection, getConnection, getNodeFrom, connections array) is completely removed. OscillatorOpts and OscillatorOptsFilterValues type aliases are deleted. The commented-out stopAfter is removed from Playable. All tests pass, library builds.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` — zero errors
2. `pnpm test` — all tests pass
3. `pnpm build:lib` — successful build
4. Verify protected: `grep -n 'public gainNode\|public pannerNode\|public effectChainInput\|public startOffset' src/base-sound.ts` returns zero
5. Verify connections gone: `grep -rn 'addConnection\|removeConnection\|getConnection\|getNodeFrom' src/*.ts src/**/*.ts` returns zero
6. Verify aliases gone: `grep -rn 'OscillatorOpts[^O]' src/` returns zero
7. Verify stopAfter gone: `grep -rn 'stopAfter' src/interfaces/playable.ts` returns zero
</verification>

<success_criteria>
- gainNode, pannerNode, effectChainInput, startOffset are all protected on BaseSound
- getGainNode() accessor provides controlled access for crossfade utility
- Track inherits startOffset access via protected; position/seek are the public API
- Entire connections API removed with zero trace
- OscillatorOpts and OscillatorOptsFilterValues aliases deleted
- stopAfter dead code removed from Playable interface
- All 913+ tests pass, library builds, demo app compiles
</success_criteria>

<output>
After completion, create `.planning/phases/18-breaking-api-cleanup/18-02-SUMMARY.md`
</output>
