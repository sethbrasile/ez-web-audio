---
phase: 21-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/base-sound.test.ts
  - src/base-sound-events.test.ts
  - src/base-sound-effects.test.ts
  - src/base-sound-debug.test.ts
  - src/base-sound-analyzer.test.ts
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "base-sound.test.ts no longer contains event, effect, debug, or analyzer tests — it is deleted or empty"
    - "Each split file is independently runnable via pnpm test src/base-sound-{concern}.test.ts"
    - "Total test count is unchanged after the split (no tests lost or duplicated)"
  artifacts:
    - path: "src/base-sound-events.test.ts"
      provides: "Event system tests (on, once, off, payloads)"
    - path: "src/base-sound-effects.test.ts"
      provides: "Effect system tests (addEffect, removeEffect, getEffects, bypass, persistence, defensive guards)"
    - path: "src/base-sound-debug.test.ts"
      provides: "Debug mode tests (global, per-sound, custom handler, event logging)"
    - path: "src/base-sound-analyzer.test.ts"
      provides: "Analyzer tests (setAnalyzer, getAnalyzer, playback, error paths)"
  key_links:
    - from: "src/base-sound-effects.test.ts"
      to: "src/base-sound.ts"
      via: "imports Sound, Oscillator, Effect, createMockEffect helper"
      pattern: "import.*Sound.*from"
---

<objective>
Split base-sound.test.ts (818 lines, 4 concerns) into focused test files by concern: events, effects, debug, and analyzer.

Purpose: Improve test maintainability — each file covers a single concern and is independently navigable.
Output: 4 focused test files replacing the monolithic base-sound.test.ts.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/base-sound.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract event and debug tests into separate files</name>
  <files>
    src/base-sound.test.ts
    src/base-sound-events.test.ts
    src/base-sound-debug.test.ts
  </files>
  <action>
Create `src/base-sound-events.test.ts` containing:
- The shared imports and factory functions (`createMockContext`, `createSound`)
- The entire `describe('event System', ...)` block (lines 19-138 of base-sound.test.ts)

Create `src/base-sound-debug.test.ts` containing:
- The shared imports and factory functions
- Import `DebugMessage`, `setDebugHandler`, `setDebugMode` from `./debug`
- The entire `describe('debug Mode Integration', ...)` block (lines 140-269)

Remove both blocks from base-sound.test.ts.

Each file must have its own complete imports and factory functions — no shared setup file needed, just duplicate the 2-line helpers (`createMockContext`, `createSound`).
  </action>
  <verify>
Run: `pnpm test src/base-sound-events.test.ts` — all pass.
Run: `pnpm test src/base-sound-debug.test.ts` — all pass.
  </verify>
  <done>Event tests (4 describe blocks) live in base-sound-events.test.ts. Debug tests (4 describe blocks) live in base-sound-debug.test.ts. Both run independently.</done>
</task>

<task type="auto">
  <name>Task 2: Extract effects and analyzer tests, delete original</name>
  <files>
    src/base-sound.test.ts
    src/base-sound-effects.test.ts
    src/base-sound-analyzer.test.ts
  </files>
  <action>
Create `src/base-sound-effects.test.ts` containing:
- Shared imports and factory functions
- The `createMockEffect` helper function (lines 272-280)
- The entire `describe('effect System Integration', ...)` block (lines 282-548)
- The entire `describe('defensive Guards', ...)` block (lines 551-619) — these are all effect/parameter related guards

Create `src/base-sound-analyzer.test.ts` containing:
- Shared imports and factory functions
- The `createMockEffect` helper (needed by error paths section)
- The `createMockAnalyzer` helper
- The entire `describe('analyzer Integration', ...)` block (lines 621-818)

Delete `src/base-sound.test.ts` — all content has been extracted.

Run `pnpm test` to verify the full suite count is unchanged from 901.
  </action>
  <verify>
Run: `pnpm test src/base-sound-effects.test.ts` — all pass.
Run: `pnpm test src/base-sound-analyzer.test.ts` — all pass.
Run: `pnpm test` — total test count matches pre-split count (901 tests).
Confirm `src/base-sound.test.ts` no longer exists.
  </verify>
  <done>Effects tests and analyzer tests each in their own file. Original base-sound.test.ts deleted. Full suite passes with same test count.</done>
</task>

</tasks>

<verification>
- `ls src/base-sound*.test.ts` shows exactly 4 files: events, effects, debug, analyzer
- `pnpm test` runs all tests with the same count as before the split
- No file exceeds ~400 lines
</verification>

<success_criteria>
base-sound.test.ts is replaced by 4 focused test files, all tests pass, test count unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/21-test-coverage/21-01-SUMMARY.md`
</output>
