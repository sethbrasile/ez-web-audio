---
phase: 29-demo-component-fixes
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - docs/.vitepress/theme/components/AmbientGenerator.vue
  - docs/.vitepress/theme/components/VisualizationDemo.vue
  - docs/.vitepress/theme/components/DrumMachineVue.vue
  - docs/.vitepress/theme/components/SynthKeyboard.vue
  - docs/.vitepress/theme/components/XYPad.vue
  - docs/.vitepress/theme/components/SampledDrumKit.vue
  - docs/.vitepress/theme/components/FilterDemo.vue
  - docs/.vitepress/theme/components/SoundfontPiano.vue
autonomous: true
requirements: [SC-06, SC-07, SC-08, SC-09]

must_haves:
  truths:
    - "All interactive sliders have aria-labels"
    - "Volume warnings use accessible text, not emoji-only"
    - "XYPad height calculation uses logicalHeight, not clientWidth fallback"
    - "Demo components use proper TypeScript types instead of any"
  artifacts:
    - path: "docs/.vitepress/theme/components/XYPad.vue"
      provides: "Fixed height fallback using clientHeight"
    - path: "docs/.vitepress/theme/components/SynthKeyboard.vue"
      provides: "Accessible volume warning text"
  key_links:
    - from: "XYPad.vue drawGrid()"
      to: "canvas.dataset.logicalHeight"
      via: "height calculation fallback"
      pattern: "clientHeight"
---

<objective>
Add accessibility improvements and TypeScript type safety across all demo components.

Purpose: Screen readers need proper labels for interactive elements, volume warnings should use text not emoji, and TypeScript `any` types hide potential bugs. These are cross-cutting concerns applied after functional fixes.
Output: All demo components with proper aria-labels, accessible warnings, correct XYPad sizing, and typed library instances.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REVIEW-FINDINGS.md
@.planning/phases/29-demo-component-fixes/29-01-SUMMARY.md
@.planning/phases/29-demo-component-fixes/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aria-labels, fix volume warnings, and fix XYPad height</name>
  <files>
    docs/.vitepress/theme/components/AmbientGenerator.vue
    docs/.vitepress/theme/components/VisualizationDemo.vue
    docs/.vitepress/theme/components/XYPad.vue
    docs/.vitepress/theme/components/SynthKeyboard.vue
  </files>
  <action>
**Aria-labels (M-17 / SC-06):** Add `aria-label` attributes to sliders and play buttons that are missing them. Check each component:

1. **AmbientGenerator.vue**: The master volume slider (line 202-208) has no aria-label. Add `aria-label="Master volume"`. The drone frequency slider (line 222-230) needs `aria-label="Drone frequency"`. The texture filter cutoff slider (line 244-252) needs `aria-label="Texture filter cutoff"`. The shimmer frequency slider (line 266-274) needs `aria-label="Shimmer frequency"`. The play button needs `aria-label` — add `:aria-label="isPlaying ? 'Stop ambient playback' : 'Start ambient playback'"`.

2. **VisualizationDemo.vue**: The play button (line 277-280) needs `:aria-label="isPlaying ? 'Stop visualization' : 'Start visualization'"`. The frequency slider (line 293-300) needs `aria-label="Oscillator frequency"`. Note: FFT size select and waveform select already have labels via the wrapping `<label>` element.

3. **DrumMachineVue.vue**: The play button already has aria-label (line 187). The BPM slider already has aria-label (line 194). The mute/solo buttons (lines 214-227) have `title` but should also have `aria-label`. Add `:aria-label="'Mute ' + track.name + ' track'"` to mute buttons and `:aria-label="'Solo ' + track.name + ' track'"` to solo buttons.

**Volume warnings (M-18 / SC-07):** Replace emoji-only warnings with accessible text.

4. **SynthKeyboard.vue** line 119: Change `<strong>⚠️ Volume Warning:</strong>` to `<strong>Volume Warning:</strong>` — remove the emoji. The warning is already conveyed by the text.

5. **XYPad.vue** line 283: Change `<strong>⚠️ Volume Warning:</strong>` to `<strong>Volume Warning:</strong>` — same fix.

**XYPad height fallback (L-10 / SC-08):**

6. **XYPad.vue**: In `drawGrid()` (line 46) and `updateFromPosition()` (line 129), the height fallback uses `canvas.value.clientWidth` instead of `canvas.value.clientHeight`. Fix both:
   - Line 46: `const height = Number(canvas.value.dataset.logicalHeight) || canvas.value.clientHeight` (was `clientWidth`)
   - Line 129: `const height = Number(canvas.value.dataset.logicalHeight) || canvas.value.clientHeight` (was `clientWidth`)

Note: Since the canvas is square (aspect-ratio: 1), clientWidth === clientHeight in practice, but clientHeight is semantically correct.
  </action>
  <verify>
Run `pnpm lint` and `pnpm typecheck`. Grep all component files for `aria-label` to confirm presence on interactive elements. Grep XYPad.vue for `clientWidth` in the height fallback — should find zero (only in width calculation). Grep SynthKeyboard.vue and XYPad.vue for the emoji character — should find zero occurrences.
  </verify>
  <done>
All interactive sliders and buttons have aria-labels. Volume warnings use text "Volume Warning:" without emoji. XYPad height fallback uses clientHeight.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace any types with proper library types across demo components</name>
  <files>
    docs/.vitepress/theme/components/SampledDrumKit.vue
    docs/.vitepress/theme/components/DrumMachineVue.vue
    docs/.vitepress/theme/components/FilterDemo.vue
    docs/.vitepress/theme/components/AmbientGenerator.vue
    docs/.vitepress/theme/components/SoundfontPiano.vue
    docs/.vitepress/theme/components/XYPad.vue
    docs/.vitepress/theme/components/VisualizationDemo.vue
    docs/.vitepress/theme/components/SynthKeyboard.vue
  </files>
  <action>
**Replace `any` types (L-11 / SC-09):** The library exports concrete types from `ez-web-audio`. Replace `any` with proper types using `import type` where the type is only used for annotations.

For each component, add a type-only import at the top and replace `any` declarations:

1. **SampledDrumKit.vue**: `let kickSampler: any` etc.
   - Add: `import type { Sampler } from 'ez-web-audio'`
   - Change: `let kickSampler: Sampler | null = null` (same for snare, hihat)
   - Remove `let lib: any = null` — use inline `await import('ez-web-audio')` result directly, or type it as `typeof import('ez-web-audio')`.
   - Simplest: keep `lib` but type it: `let lib: typeof import('ez-web-audio') | null = null` — but this is async. Instead, since `lib` is only used for its factory functions, keep it as `any` OR restructure to avoid it. The pragmatic approach: use `let lib: any = null` for the dynamic import module reference (there's no clean type for `await import()` results stored in a variable), but type the Sampler variables.

2. **DrumMachineVue.vue**: `beatTrack: null as any`
   - Add: `import type { BeatTrack } from 'ez-web-audio'`
   - Change: `beatTrack: null as BeatTrack | null`
   - The `track` parameter in `toggleMute` and `toggleSolo` — these receive items from `tracks.value` array. Type the tracks ref properly or keep the parameter as the inferred type. Since the track object is a custom shape `{ name, beats, beatTrack, muted, activeStates }`, leave function parameters using the inferred type.

3. **FilterDemo.vue**: `let lib: any`, `let source: any`, `let filter: any`
   - Add: `import type { Sound, FilterEffect } from 'ez-web-audio'`
   - The source can be either an Oscillator or Sound (white noise). Use: `import type { Sound, Oscillator, FilterEffect } from 'ez-web-audio'`
   - Change: `let source: Sound | Oscillator | null = null`, `let filter: FilterEffect | null = null`
   - Keep `let lib: any = null` for the dynamic import module.

4. **AmbientGenerator.vue**: `let droneOscillator: any`, `let textureNoise: any`, `let textureFilter: any`, `let shimmerOscillator: any`
   - Add: `import type { Oscillator, Sound, FilterEffect } from 'ez-web-audio'`
   - Change: `let droneOscillator: Oscillator | null = null`, `let textureNoise: Sound | null = null`, `let textureFilter: FilterEffect | null = null`, `let shimmerOscillator: Oscillator | null = null`

5. **SoundfontPiano.vue**: `let font: any`, `let lib: any`
   - Add: `import type { Font } from 'ez-web-audio'`
   - Change: `let font: Font | null = null`
   - Keep `let lib: any = null` for dynamic import.

6. **XYPad.vue**: `let oscillator: any`, `let lib: any`
   - Add: `import type { Oscillator } from 'ez-web-audio'`
   - Change: `let oscillator: Oscillator | null = null`
   - Keep `let lib: any = null`.

7. **VisualizationDemo.vue**: `let oscillator: any`, `let analyzer: any`
   - Add: `import type { Oscillator, Analyzer } from 'ez-web-audio'`
   - Change: `let oscillator: Oscillator | null = null`, `let analyzer: Analyzer | null = null`

8. **SynthKeyboard.vue**: `oscillators = new Map<string, any>()`
   - Add: `import type { Oscillator } from 'ez-web-audio'`
   - Change: `const oscillators = new Map<string, Oscillator>()`

**Important:** Use `import type` (not `import`) since these are only used for type annotations, not runtime values. This avoids SSR issues with dynamic imports — the type import is erased at compile time.

After adding types, run typecheck to verify. If any type errors surface (e.g., methods like `changeGainTo` not matching), check if the method exists on the typed class and adjust. The types should match since all these components were previously using the library API correctly.
  </action>
  <verify>
Run `pnpm typecheck` — must pass with zero errors. Run `pnpm lint` — must pass. Grep across all demo components for `: any` — the only remaining `any` should be `let lib: any` in components using dynamic imports (acceptable since there's no clean type for `await import()` module refs). Count: should be at most one `any` per component (for `lib`).
  </verify>
  <done>
All demo components use proper TypeScript types (Sampler, Oscillator, Sound, FilterEffect, Font, Analyzer, BeatTrack) for library instances. Only `lib` variables for dynamic import modules remain as `any`.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` passes
2. `pnpm lint` passes
3. `grep -r 'aria-label' docs/.vitepress/theme/components/` shows labels on all interactive elements
4. `grep -r '⚠️' docs/.vitepress/theme/components/` returns zero results
5. `grep -c 'clientWidth' docs/.vitepress/theme/components/XYPad.vue` — only in width calculations, not height
6. `grep -rn ': any' docs/.vitepress/theme/components/` — minimal (only dynamic import module refs)
</verification>

<success_criteria>
- All sliders and play buttons have aria-labels (SC-06)
- Volume warnings use accessible text without emoji (SC-07)
- XYPad height fallback uses clientHeight (SC-08)
- Library instances typed with concrete types, not any (SC-09)
</success_criteria>

<output>
After completion, create `.planning/phases/29-demo-component-fixes/29-03-SUMMARY.md`
</output>
