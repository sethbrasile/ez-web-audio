# Plan 23-01: Export audioContextAwareTimeout & Fix Critical API Bugs

**Phase Goal**: All demo components call the library's actual API correctly with no runtime crashes. The audioContext-aware timeout utility is exported as a public API for consumers.

**Depends on**: Nothing (first plan in phase)

## Tasks

### Task 1: Export audioContextAwareTimeout as a public API

**File**: `src/index.ts`

**action**:
- Add import: `import audioContextAwareTimeout from './utils/timeout'`
- Add `audioContextAwareTimeout` to the export block (alphabetical, with a `// Timing utilities` comment)
- Add JSDoc to the function in `src/utils/timeout.ts` explaining:
  - What it does: returns `setTimeout` and `clearTimeout` functions that use `audioContext.currentTime` + `requestAnimationFrame` instead of native timers, avoiding browser throttling drift
  - When to use it: any UI timing that needs to stay in sync with audio playback (visual beat indicators, countdowns, scheduled UI updates)
  - Example showing `getAudioContext()` → `audioContextAwareTimeout(ctx)` → `setTimeout(fn, ms)`
  - Note about the graceful fallback to native setTimeout when audioContext is unavailable

**File**: `src/utils/timeout.ts`

**action**:
- Add JSDoc block above the `export default function audioContextAwareTimeout` declaration
- Remove the commented-out `audioContextAwareInterval` stub at the bottom (lines 69-74) — dead code

**verify**: `pnpm typecheck` passes; `import { audioContextAwareTimeout } from 'ez-web-audio'` resolves
**done**: Utility is part of the public API with documentation

### Task 2: Fix createAnalyzer missing audioContext in VisualizationDemo

**File**: `docs/.vitepress/theme/components/VisualizationDemo.vue`

**action**:
- Line 68: Import `getAudioContext` alongside `createOscillator` and `createAnalyzer`
- After creating the oscillator, get the audioContext: `const ctx = await lib.getAudioContext()`
- Line 78: Change `createAnalyzer({ fftSize: fftSize.value })` to `createAnalyzer(ctx, { fftSize: fftSize.value })`
- Same fix needed in `updateFFTSize()` — the stop/restart path at line 228 will re-call `startVisualization()` which will now be correct

**verify**: Run `pnpm typecheck` — the call should have been a type error (was hidden by `any` typing)
**done**: `createAnalyzer` receives AudioContext as first arg

### Task 3: Fix .as('number') → .as('ratio') in AmbientGenerator and VisualizationDemo

**File**: `docs/.vitepress/theme/components/AmbientGenerator.vue`

**action**:
- Line 165: Change `.as('number')` to `.as('ratio')`
- Line 179: Change `.as('number')` to `.as('ratio')`

**File**: `docs/.vitepress/theme/components/VisualizationDemo.vue`

**action**:
- Line 225: Change `.as('number')` to `.as('ratio')`

**verify**: `pnpm typecheck` passes; grep confirms zero remaining `.as('number')` in docs components
**done**: All `update().to().as()` calls use valid RatioType values

### Task 4: Fix uppercase Q → lowercase q in AmbientGenerator

**File**: `docs/.vitepress/theme/components/AmbientGenerator.vue`

**action**:
- Line 68: Change `Q: 1.0` to `q: 1.0`

**verify**: `pnpm typecheck` passes
**done**: FilterEffectOptions property name matches the interface

### Task 5: Fix wrapEffect to context-free API in DistortionDemo

**File**: `docs/.vitepress/theme/components/DistortionDemo.vue`

**action**:
- Line 86: Change `lib.wrapEffect(ctx, distNode)` to `lib.wrapEffect(distNode)`
- Remove the `const ctx = await lib.getAudioContext()` call on line 72 if it's only used for wrapEffect
- Check if `ctx` is still needed elsewhere in the function (line 82 creates `ctx.createWaveShaper()`) — if so, keep the `getAudioContext()` call
- Line 104: Same check in `updateDistortionCurve()` — `ctx` is used for `makeDistortionCurve` but `makeDistortionCurve` doesn't use ctx. Remove unused `getAudioContext()` there too

**verify**: `pnpm typecheck` passes; grep confirms no remaining `wrapEffect(ctx,` pattern in docs
**done**: DistortionDemo uses context-free `wrapEffect()` consistent with other demos

### Checkpoint

**action**: Run `pnpm typecheck && pnpm test` to verify no regressions
**verify**: All 913+ unit tests pass, typecheck clean
**done**: All critical API bugs fixed, audioContextAwareTimeout exported, tests green
