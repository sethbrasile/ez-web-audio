# Plan 23-02: Fix Design Issues in Demo Components

**Phase Goal**: Demo components follow web audio best practices — no audio gaps, correct event handling, accurate timing

**Depends on**: 23-01 (audioContextAwareTimeout is now exported)

## Tasks

### Task 1: Fix OscillatorDemo stop/recreate on slider changes

**File**: `docs/.vitepress/theme/components/OscillatorDemo.vue`

**action**:
- Remove the combined `watch([frequency, gain, waveType], ...)` watcher (lines 65-70)
- Add separate watchers:
  - `watch(frequency, (v) => { if (playing.value && oscillator) oscillator.update('frequency').to(v).as('ratio') })`
  - `watch(gain, (v) => { if (playing.value && oscillator) oscillator.changeGainTo(v) })`
  - `watch(waveType, async () => { if (playing.value) { stop(); await play() } })` — only waveType requires stop/recreate (Web Audio API constraint)

**verify**: Start the demo, drag frequency and gain sliders — audio should update smoothly without gaps or clicks. Changing waveType will still briefly restart (acceptable).
**done**: Frequency and gain update in real-time; only waveType triggers stop/recreate

### Task 2: Fix XYPad mouseup on document instead of canvas

**File**: `docs/.vitepress/theme/components/XYPad.vue`

**action**:
- Remove `@mouseup="handleMouseUp"` and `@mouseleave="handleMouseLeave"` from the canvas template (lines 288-289)
- In `onMounted()`, add: `document.addEventListener('mouseup', handleMouseUp)`
- In `onUnmounted()`, add corresponding `removeEventListener` call
- Keep `handleMouseMove` on canvas (gated by `isPlaying.value` check — fine)
- Remove the `handleMouseLeave` function entirely — `mouseup` on document is sufficient

**verify**: Start oscillator by clicking canvas, drag mouse outside canvas bounds, release — oscillator should stop immediately
**done**: mouseup events caught regardless of where release happens

### Task 3: Replace window.setTimeout with audioContextAwareTimeout in TimingDemo

**File**: `docs/.vitepress/theme/components/TimingDemo.vue`

**action**:
The `playSequence()` function (lines 65-104) uses `window.setTimeout` at 500ms and 1000ms for visual sync. Now that `audioContextAwareTimeout` is exported (Task 1 of 23-01), use it directly — this also serves as a documentation example of the utility's real-world use.

- After getting the audioContext via `getAudioContext()`, create the timer:
  ```
  const { setTimeout: acSetTimeout } = lib.audioContextAwareTimeout(ctx)
  ```
- Replace the three `window.setTimeout` calls with `acSetTimeout` calls (same signature, same delay values)
- The `timeouts[]` tracking array continues to work since `acSetTimeout` returns numeric IDs
- For cleanup in `onUnmounted`, use the matching `clearTimeout` from the same `audioContextAwareTimeout` instance
- Also replace `window.setTimeout` in `playChord()` (line 125) with the same utility for consistency
- Add a brief inline comment explaining why audioContext-aware timing is used here:
  `// Use audioContext-aware setTimeout to keep visual sync with audio clock`

**verify**: Play the sequence — timeline markers should light up in sync with audio, not drifting even under CPU load
**done**: TimingDemo uses the library's own timing utility, serving as a usage example for consumers

### Task 4: Fix SynthKeyboard noteOff truncating ADSR release

**File**: `docs/.vitepress/theme/components/SynthKeyboard.vue`

**action**:
- In `handleNoteOff()` (lines 84-97), instead of immediately calling `oscillator.stop()`:
  1. Get the release time from the envelope: `const releaseTime = envelope.value.release`
  2. Call `oscillator.stop()` — if the library's envelope handles the release phase on stop, this is correct. Need to verify: does the Oscillator's stop() method respect the ADSR release?
  3. If stop() immediately kills the sound (bypassing release), we need to schedule the stop after the release duration using the newly exported `audioContextAwareTimeout`. Get the audioContext via `getAudioContext()`, then use `acSetTimeout(() => osc.stop(), releaseTime * 1000)`.
  4. Remove the oscillator from the map after the release completes, not immediately

**Pre-check**: Read `src/oscillator.ts` stop() method to understand if ADSR release is respected. If the library already handles this, no code change needed — just verify.

**verify**: Select "Pad" preset (release: 1.0s), play and release a key — the note should fade out over ~1 second, not cut off instantly
**done**: ADSR release phase audibly completes before oscillator cleanup

### Checkpoint

**action**: Run `pnpm typecheck && pnpm test`
**verify**: All tests pass, typecheck clean
**done**: All design issues resolved
