---
phase: 36-documentation-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - docs/.vitepress/config.mts
autonomous: true
requirements:
  - SYNC-03

must_haves:
  truths:
    - "TypeDoc API reference is regenerated and reflects Phase 32-33 source changes"
    - "Navigation sidebar and cross-links resolve correctly"
    - "All public exports have corresponding TypeDoc entries"
  artifacts:
    - path: "docs/api/"
      provides: "Regenerated TypeDoc API reference"
  key_links:
    - from: "docs/api/"
      to: "src/"
      via: "TypeDoc generation from source JSDoc"
      pattern: "typedoc"
---

<objective>
Regenerate the TypeDoc API reference to reflect Phase 32-33 source code changes and verify all navigation links resolve.

Purpose: Ensure the API reference accurately documents the current public API including EnvelopeOptions short names, new convenience methods (fadeIn/fadeOut/dispose/loop), note-based oscillator, context-free createAnalyzer, and setPattern.
Output: Regenerated TypeDoc output in docs/api/, verified navigation.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-documentation-sync/36-01-SUMMARY.md

@typedoc.json
@tsconfig.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate TypeDoc API reference and verify output</name>
  <files>docs/api/</files>
  <action>
Run the TypeDoc generation command to rebuild the API reference from current source:

```bash
pnpm build:lib && pnpm exec typedoc
```

If the project uses a different command for TypeDoc generation, check `package.json` scripts. The typical pattern is `typedoc` or a build script that includes it.

After generation, verify the output includes Phase 32-33 changes:

1. **EnvelopeOptions short names**: Check that the generated docs for `EnvelopeOptions` show `attack`, `decay`, `sustain`, `release` (not the old verbose names)
2. **Envelope.triggerRelease()**: Verify the method appears (not the old `release()`)
3. **Phase 33 methods**: Verify `fadeIn`, `fadeOut`, `dispose`, `loop` appear in BaseSound/Sound class docs
4. **createAnalyzer overloads**: Verify both signatures appear
5. **createOscillator note option**: Verify `note` appears in OscillatorOptions
6. **BeatTrack.setPattern**: Verify method appears
7. **Playable interface**: Verify `playAt` and `stopAt` show `Promise<void>` return types

Verify no build errors or warnings from TypeDoc.
  </action>
  <verify>
`pnpm exec typedoc` completes without errors. Grep the generated docs for `fadeIn`, `triggerRelease`, `setPattern`, and `dispose` to confirm they appear. Check that `attackTime` does NOT appear in generated docs (old name).
  </verify>
  <done>
TypeDoc API reference regenerated from current source. All Phase 32-33 API changes reflected: EnvelopeOptions short names, triggerRelease(), fadeIn/fadeOut, dispose, loop, note-based oscillator, context-free createAnalyzer, setPattern, async Playable return types. No stale references to removed APIs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final cross-reference verification of all docs</name>
  <files>docs/guide/concepts.md, docs/guide/parameter-control.md, docs/guide/utilities.md, docs/guide/getting-started.md, docs/examples/synthesis.md</files>
  <action>
Perform a final verification sweep across all documentation:

1. **Stale API grep**: Search all docs/ markdown files for these removed/renamed patterns that should NOT exist:
   - `attackTime` / `decayTime` / `sustainLevel` / `releaseTime` (old EnvelopeOptions names)
   - `\.from\('ratio'\)` / `.from('percent')` / `.from('seconds')` (old fluent API — should be `.as()`)
   - `ifActivePlayIn` (old method name — should be `playInIfActive`)
   - `addConnection` / `removeConnection` / `getNodeFrom` (removed connections API)
   - `OscillatorOpts` (removed type alias)
   - `changeFrequencyTo` (removed convenience method)
   - `wrapEffect(ctx` (old 2-arg form)

2. **Link verification**: Run `pnpm build` (which includes docs build) and check for broken links or build warnings.

3. **Sidebar verification**: Confirm all sidebar entries in `docs/.vitepress/config.mts` point to files that exist. Verify no 404s in the built output.

If any stale references are found, fix them in the affected files. If build errors occur, fix them.
  </action>
  <verify>
`pnpm build` completes without errors. Grep for stale API patterns across docs/ returns 0 matches. All sidebar links resolve to existing files.
  </verify>
  <done>
Zero stale API references remain in documentation. Full build (lib + docs + typedoc) succeeds. All navigation links resolve. Documentation is fully synchronized with current source code.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes successfully (lib + typedoc + docs)
2. No stale API references in docs/ (attackTime, .from('ratio'), ifActivePlayIn, addConnection, OscillatorOpts, changeFrequencyTo, wrapEffect(ctx)
3. TypeDoc output includes fadeIn, fadeOut, dispose, loop, setPattern, triggerRelease, note option
4. No broken navigation links in built docs site
</verification>

<success_criteria>
- TypeDoc API reference fully regenerated reflecting Phases 32-33
- Zero stale API references in documentation
- Full build passes without errors
- All navigation links resolve
</success_criteria>

<output>
After completion, create `.planning/phases/36-documentation-sync/36-02-SUMMARY.md`
</output>
