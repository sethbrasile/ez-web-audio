---
phase: 36-documentation-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/concepts.md
  - docs/guide/parameter-control.md
  - docs/guide/utilities.md
  - docs/guide/getting-started.md
  - CHANGELOG.md
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-04

must_haves:
  truths:
    - "Guide pages document fadeIn/fadeOut convenience methods"
    - "Guide pages document loop property on Sound/Track"
    - "Guide pages document dispose() cleanup method"
    - "Guide pages document note-based oscillator creation"
    - "Guide pages document context-free createAnalyzer overload"
    - "Guide pages document BeatTrack.setPattern()"
    - "CHANGELOG has Phase 32-35 entries covering EnvelopeOptions rename, AudioSprite loop fix, stale timeout fix, and all Phase 33 convenience APIs"
  artifacts:
    - path: "docs/guide/concepts.md"
      provides: "Note-based oscillator example, loop mention"
      contains: "note:"
    - path: "docs/guide/parameter-control.md"
      provides: "fadeIn/fadeOut convenience section"
      contains: "fadeIn"
    - path: "docs/guide/utilities.md"
      provides: "dispose() documentation"
      contains: "dispose"
    - path: "CHANGELOG.md"
      provides: "Phase 32-35 changelog entries"
      contains: "EnvelopeOptions"
  key_links:
    - from: "docs/guide/concepts.md"
      to: "src/oscillator.ts"
      via: "createOscillator({ note: 'A4' }) example"
      pattern: "note.*A4"
    - from: "docs/guide/parameter-control.md"
      to: "src/base-sound.ts"
      via: "fadeIn/fadeOut documentation"
      pattern: "fadeIn|fadeOut"
---

<objective>
Update all guide pages to document Phase 32-33 API changes and update CHANGELOG.md with Phase 32-35 entries.

Purpose: Ensure every public API added or changed in Phases 32-35 is accurately documented in narrative guides and tracked in the changelog.
Output: Updated guide pages with Phase 33 convenience API docs; CHANGELOG.md with Phase 32-35 entries.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-critical-fixes-api-contracts/32-01-SUMMARY.md
@.planning/phases/32-critical-fixes-api-contracts/32-02-SUMMARY.md
@.planning/phases/32-critical-fixes-api-contracts/32-03-SUMMARY.md
@.planning/phases/33-dx-convenience-apis/33-01-SUMMARY.md
@.planning/phases/33-dx-convenience-apis/33-02-SUMMARY.md

@docs/guide/concepts.md
@docs/guide/parameter-control.md
@docs/guide/utilities.md
@docs/guide/getting-started.md
@CHANGELOG.md
@src/base-sound.ts
@src/sound.ts
@src/oscillator.ts
@src/beat-track.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 33 convenience APIs to guide pages</name>
  <files>docs/guide/concepts.md, docs/guide/parameter-control.md, docs/guide/utilities.md, docs/guide/getting-started.md</files>
  <action>
Update four guide pages to document the Phase 33 convenience APIs. Read each file first, then add targeted sections:

**docs/guide/concepts.md** (Oscillator section, ~line 70):
- Update the `createOscillator` example to show `note: 'A4'` option alongside `frequency: 440`:
  ```typescript
  const synth = await createOscillator({
    note: 'A4', // or use frequency: 440
    type: 'sine',
    envelope: { attack: 0.01, decay: 0.1, sustain: 0.7, release: 0.3 }
  })
  ```
- Add a brief note after the example: "The `note` option looks up frequency from the built-in frequency map. When both `note` and `frequency` are provided, `note` takes precedence."

**docs/guide/concepts.md** (Sound section, after the play examples ~line 35):
- Add a `loop` example:
  ```typescript
  click.loop = true // Enables native looping (no gap between repeats)
  click.play()
  click.stop() // Must stop manually — looped sounds don't end
  ```

**docs/guide/concepts.md** (BeatTrack section, ~line 203):
- Add `setPattern()` example after the manual beat assignment:
  ```typescript
  kick.setPattern([1, 0, 1, 0, 1, 0, 1, 0]) // Set all beats at once
  ```

**docs/guide/parameter-control.md** (after Common Patterns section, before Extending ControlType):
- Add a "## Convenience Methods" section with fadeIn/fadeOut:
  ```markdown
  ## Convenience Methods

  For common fade operations, use the built-in convenience methods instead of manual scheduling:

  ### Fade In

  ```typescript
  sound.fadeIn(0.5) // Play with gain ramping from 0 to current gain over 0.5s
  ```

  ### Fade Out

  ```typescript
  await sound.fadeOut(1.0) // Ramp gain to 0 over 1s, then stop. Returns a Promise.
  ```

  These methods handle the scheduling internally — no need to call `onPlaySet()` or manage timeouts yourself.
  ```

**docs/guide/utilities.md** (after existing sections, before any "Next Steps"):
- Add a "## Resource Cleanup" section documenting dispose():
  ```markdown
  ## Resource Cleanup

  When you're done with a sound, call `dispose()` to disconnect all audio nodes and free resources:

  ```typescript
  sound.dispose()
  // sound.play() will now throw — the instance is no longer usable
  ```

  This is especially important in single-page applications where audio instances are created and destroyed as users navigate between views.
  ```

- Add a "## Analyzer Without AudioContext" subsection in the appropriate location:
  ```markdown
  ### Context-Free Analyzer

  `createAnalyzer()` works without passing an AudioContext — it resolves the shared context automatically:

  ```typescript
  const analyzer = createAnalyzer({ fftSize: 2048 })
  ```
  ```

Keep each page under 250 lines. Match existing formatting style (section headers, code fences, tip blocks). Do NOT add emojis.
  </action>
  <verify>
Run `pnpm lint` on modified files. Grep for `fadeIn` in parameter-control.md (must find). Grep for `note:` in concepts.md (must find). Grep for `dispose` in utilities.md (must find). Grep for `setPattern` in concepts.md (must find). Check each file is under 250 lines with `wc -l`.
  </verify>
  <done>
All four guide pages document the Phase 33 convenience APIs: fadeIn/fadeOut in parameter-control.md, loop/note/setPattern in concepts.md, dispose/context-free analyzer in utilities.md. No stale API references remain. All files under 250 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md with Phase 32-35 entries</name>
  <files>CHANGELOG.md</files>
  <action>
Read CHANGELOG.md. Add a new section **above** the existing `[1.0.0]` entry for unreleased changes. Use this structure:

```markdown
## [Unreleased]

### Breaking Changes

- **`EnvelopeOptions` property names shortened** — `attackTime` renamed to `attack`, `decayTime` to `decay`, `sustainLevel` to `sustain`, `releaseTime` to `release`. The `Envelope.release()` method renamed to `Envelope.triggerRelease()` to avoid collision with the new `release` property.

### Added

- **`fadeIn(duration)`** — Play with gain ramping from 0 to current gain over specified duration
- **`fadeOut(duration)`** — Ramp gain to 0 and stop; returns a Promise that resolves when complete
- **`loop` property** on Sound and Track — enables native `AudioBufferSourceNode.loop` without timing gaps
- **`dispose()`** on BaseSound — disconnects all audio nodes, removes event listeners, marks instance as disposed (idempotent)
- **`createOscillator({ note: 'A4' })`** — accepts note name from built-in frequency map
- **`createAnalyzer()` overload** — works without AudioContext parameter (resolves shared context internally)
- **`BeatTrack.setPattern([1,0,1,0])`** — set beat active states from a numeric/boolean array
- AudioSprite interactive example page
- LayeredSound interactive example page
- Crossfade interactive example page
- React integration example with hooks patterns
- `docs/guide/parameter-control.md` and `docs/guide/utilities.md` split from oversized `concepts.md`

### Fixed

- **AudioSprite `loop: true`** now actually loops — duration arg no longer passed to `source.start()` when looping
- **Stale setTimeout** can no longer corrupt `_isPlaying` across rapid play/stop cycles — timeout IDs tracked and cancelled
- **`Playable` interface** return types match async implementations (`Promise<void>`)
- **`Sound` constructor** `opts` parameter typed as `BaseSoundOptions` (was `any`)
- **`createFont()`** validates `response.ok` and wraps fetch in try/catch with descriptive errors
- **`CLAUDE.md`** uses `.as('ratio')` (was `.from('ratio')`)
- **Oscillator `setup()`** documents GainNode replacement risk in JSDoc
- `synthesis.md` uses `update('frequency')` instead of removed `changeFrequencyTo()`
- `audio-routing.md` uses 1-arg `wrapEffect(node)` instead of 2-arg form

### Changed

- Test count: 1038+ unit tests (up from 937)
```

Preserve the existing `[1.0.0]` and `[0.1.0]` sections unchanged below.
  </action>
  <verify>
Grep for `EnvelopeOptions` in CHANGELOG.md (must find). Grep for `fadeIn` in CHANGELOG.md (must find). Grep for `AudioSprite.*loop` in CHANGELOG.md (must find). Verify `[1.0.0]` section still present.
  </verify>
  <done>
CHANGELOG.md has an [Unreleased] section documenting all Phase 32-35 changes: EnvelopeOptions rename, convenience APIs (fadeIn/fadeOut, loop, dispose, note oscillator, context-free analyzer, setPattern), bug fixes (AudioSprite loop, stale setTimeout, Playable types, Sound opts, createFont errors), and documentation improvements. Existing changelog entries preserved.
  </done>
</task>

</tasks>

<verification>
1. `pnpm lint` passes on all modified guide pages
2. `grep -c fadeIn docs/guide/parameter-control.md` returns >= 1
3. `grep -c "note:" docs/guide/concepts.md` returns >= 1
4. `grep -c dispose docs/guide/utilities.md` returns >= 1
5. `grep -c setPattern docs/guide/concepts.md` returns >= 1
6. `grep -c EnvelopeOptions CHANGELOG.md` returns >= 1
7. All guide pages under 250 lines
8. No references to removed/stale APIs in modified files
</verification>

<success_criteria>
- Guide pages document all Phase 33 convenience APIs
- CHANGELOG.md has complete Phase 32-35 entries
- No stale API references in modified docs
- Lint passes on all changes
</success_criteria>

<output>
After completion, create `.planning/phases/36-documentation-sync/36-01-SUMMARY.md`
</output>
