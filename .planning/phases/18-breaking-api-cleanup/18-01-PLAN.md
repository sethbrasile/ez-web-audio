---
phase: 18-breaking-api-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/controllers/base-param-controller.ts
  - src/controllers/base-param-controller.test.ts
  - src/controllers/sound-controller.test.ts
  - src/controllers/oscillator-controller.test.ts
  - src/base-sound.ts
  - src/track.ts
  - src/track.test.ts
  - src/sound.test.ts
  - src/oscillator.test.ts
  - src/oscillator.ts
  - src/font.test.ts
  - src/layered-sound.test.ts
  - src/index.ts
  - src/beat.ts
  - src/beat-track.ts
  - src/beat-track.test.ts
  - src/app/pages/audio-files/mp3-player.ts
  - src/app/pages/audio-routing.ts
  - src/app/pages/distortable-play-button.ts
  - src/app/pages/synthesis/drum-kit.ts
  - src/app/pages/synthesis/hihat-button.ts
  - src/app/pages/synthesis/snare-button.ts
  - src/app/pages/synthesis/kick-button.ts
  - src/app/pages/synthesis/bass-drop-button.ts
  - src/app/components/mp3-player/setup.ts
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - "`.as()` is the method name on `update().to()` chains; `.from()` no longer exists on that chain"
    - "`.as()` is the method name on `seek()` chains; `seek().from()` no longer exists"
    - "`playInIfActive()` is the method name on Beat; `ifActivePlayIn()` no longer exists"
    - "All tests pass with the renamed methods"
    - "All demo app pages use the new method names"
  artifacts:
    - path: "src/controllers/base-param-controller.ts"
      provides: "Fluent API with .as() instead of .from() on update().to() chain"
      contains: ".as("
    - path: "src/track.ts"
      provides: "seek() returning { as: (type) => void } instead of { from: }"
      contains: "as:"
    - path: "src/beat.ts"
      provides: "playInIfActive() method replacing ifActivePlayIn()"
      contains: "playInIfActive"
  key_links:
    - from: "src/base-sound.ts"
      to: "src/controllers/base-param-controller.ts"
      via: "update() delegates to controller.update().to().as()"
      pattern: "\\.as\\("
    - from: "src/beat-track.ts"
      to: "src/beat.ts"
      via: "callPlayMethodOnBeats calls playInIfActive"
      pattern: "playInIfActive"
---

<objective>
Rename fluent API `.from()` to `.as()` on `update().to()` and `seek()` chains, and rename `ifActivePlayIn()` to `playInIfActive()` on Beat/BeatTrack.

Purpose: Clean up confusing method names before 1.0 locks the API. `.as()` reads naturally: `update('gain').to(0.5).as('ratio')`. `playInIfActive()` matches `playIfActive()` naming convention.
Output: All source, test, and demo files use the new method names with zero references to old names.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/controllers/base-param-controller.ts
@src/track.ts
@src/beat.ts
@src/beat-track.ts
@src/base-sound.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename .from() to .as() on update().to() and seek() chains</name>
  <files>
    src/controllers/base-param-controller.ts
    src/controllers/base-param-controller.test.ts
    src/controllers/sound-controller.test.ts
    src/controllers/oscillator-controller.test.ts
    src/base-sound.ts
    src/track.ts
    src/track.test.ts
    src/sound.test.ts
    src/oscillator.test.ts
    src/oscillator.ts
    src/font.test.ts
    src/layered-sound.test.ts
    src/index.ts
    src/app/pages/audio-files/mp3-player.ts
    src/app/pages/audio-routing.ts
    src/app/pages/distortable-play-button.ts
    src/app/pages/synthesis/drum-kit.ts
    src/app/pages/synthesis/hihat-button.ts
    src/app/pages/synthesis/snare-button.ts
    src/app/pages/synthesis/kick-button.ts
    src/app/pages/synthesis/bass-drop-button.ts
    src/app/components/mp3-player/setup.ts
  </files>
  <action>
    Rename `.from()` to `.as()` ONLY on these two fluent chains:

    1. **`update().to().from(unit)`** in `base-param-controller.ts`:
       - Change the return type from `{ from: (method: RatioType) => void }` to `{ as: (method: RatioType) => void }`
       - Rename the method in the implementation object from `from:` to `as:`
       - Remove the comment about ".from() method name in the fluent API is a known DX concern — deferred to v2"
       - Update the `update()` method on BaseSound (base-sound.ts) return type similarly

    2. **`seek().from(type)`** in `track.ts`:
       - Change the return type from `{ from: (type: SeekType) => void }` to `{ as: (type: SeekType) => void }`
       - Rename the method in the returned object from `from:` to `as:`

    **DO NOT** rename `onPlayRamp().from()` — that `.from()` means "starting from value X" which is semantically correct.

    Then update ALL call sites across the codebase:
    - `grep -r '\.from(' src/` and change `.from('ratio')`, `.from('percent')`, `.from('inverseRatio')`, `.from('seconds')` to `.as('ratio')`, `.as('percent')`, `.as('inverseRatio')`, `.as('seconds')` — but ONLY where it's on an `update().to()` or `seek()` chain, NOT on `onPlayRamp().from()` chains.
    - Update all test files that test these fluent chains
    - Update all demo app pages in `src/app/`
    - Update JSDoc examples in base-sound.ts and track.ts that show `.from()` usage on these chains

    Run `pnpm typecheck` to catch any missed call sites via TypeScript errors.
  </action>
  <verify>
    1. `pnpm typecheck` passes with zero errors
    2. `pnpm test` — all tests pass
    3. `grep -r '\.from(' src/controllers/base-param-controller.ts` shows only the `onPlayRamp` usage
    4. `grep -r 'seek.*\.from(' src/` returns zero results
    5. `grep -r "\.to(.*).from(" src/` returns zero results (no update().to().from() left)
  </verify>
  <done>
    `.as()` is the method on `update().to()` and `seek()` chains. `.from()` only exists on `onPlayRamp()` where it means "from value". All tests pass. All demo pages updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename ifActivePlayIn() to playInIfActive() on Beat and BeatTrack</name>
  <files>
    src/beat.ts
    src/beat-track.ts
    src/beat-track.test.ts
  </files>
  <action>
    1. In `src/beat.ts`: Rename the public method `ifActivePlayIn(offset)` to `playInIfActive(offset)`. Update its JSDoc.

    2. In `src/beat-track.ts`:
       - Rename all references from `ifActivePlayIn` to `playInIfActive` in the `callPlayMethodOnBeats` method type union: change `'ifActivePlayIn' | 'playIn'` to `'playInIfActive' | 'playIn'`
       - Update the call site `beat.ifActivePlayIn(offset)` to `beat.playInIfActive(offset)`
       - Update the comment referencing `ifActivePlayIn`

    3. In `src/beat-track.test.ts`:
       - Update the test helper class method signature from `'ifActivePlayIn' | 'playIn'` to `'playInIfActive' | 'playIn'`
       - Update any test assertions or calls referencing the old name

    4. Run typecheck to catch any remaining references.
  </action>
  <verify>
    1. `pnpm typecheck` passes
    2. `pnpm test src/beat-track.test.ts` passes
    3. `grep -r 'ifActivePlayIn' src/` returns zero results
  </verify>
  <done>
    `playInIfActive()` is the method name on Beat. `ifActivePlayIn()` no longer exists anywhere in the codebase. All beat-track tests pass.
  </done>
</task>

</tasks>

<verification>
1. `pnpm typecheck` — zero errors (catches any missed rename)
2. `pnpm test` — all tests pass
3. `grep -r 'ifActivePlayIn' src/` — zero results
4. `grep -rE '\.to\([^)]+\)\.from\(' src/` — zero results (no update().to().from() left)
5. `grep -r 'seek.*\.from(' src/` — zero results
6. `pnpm build:lib` — library builds successfully
</verification>

<success_criteria>
- `.as()` works on all `update().to()` and `seek()` chains
- `onPlayRamp().from()` is untouched (different semantic)
- `playInIfActive()` is the only method name; `ifActivePlayIn()` is gone
- All 913+ unit tests pass
- Library builds cleanly
- Demo app pages compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-breaking-api-cleanup/18-01-SUMMARY.md`
</output>
