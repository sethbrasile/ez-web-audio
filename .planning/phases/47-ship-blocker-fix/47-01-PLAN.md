---
phase: 47-ship-blocker-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/timeout.ts]
autonomous: true
requirements: [SHIP-01]

must_haves:
  truths:
    - "dist/index.d.ts does not contain any reference to standardized-audio-context-mock"
    - "pnpm build:lib succeeds without errors"
    - "pnpm test passes — no regressions from the type change"
    - "ContextLike type uses only built-in Web Audio API types (AudioContext, BaseAudioContext)"
  artifacts:
    - path: "src/utils/timeout.ts"
      provides: "audioContextAwareTimeout with production-safe ContextLike type"
      contains: "type ContextLike = AudioContext | BaseAudioContext"
    - path: "dist/index.d.ts"
      provides: "Published type declarations free of test-only imports"
  key_links:
    - from: "src/utils/timeout.ts"
      to: "dist/index.d.ts"
      via: "vite build with rollupTypes"
      pattern: "no.*standardized-audio-context-mock"
---

<objective>
Remove the test-only `standardized-audio-context-mock` import from `src/utils/timeout.ts` so that published type declarations do not reference a devDependency.

Purpose: This is the only ship-blocking bug. Every TypeScript consumer with `skipLibCheck: false` (the default) gets a compile error because `standardized-audio-context-mock` is not in their `node_modules`. The mock type is structurally compatible with `AudioContext | BaseAudioContext` at runtime, so the import is unnecessary.

Output: `timeout.ts` uses only built-in Web Audio API types; `dist/index.d.ts` is clean of test-only imports.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/timeout.ts

<interfaces>
From src/utils/timeout.ts (current — to be modified):
```typescript
import type { AudioContext as AudioContextMock } from 'standardized-audio-context-mock'

type ContextLike = AudioContext | BaseAudioContext | AudioContextMock

export default function audioContextAwareTimeout(audioContext: ContextLike): {
  setTimeout: (fn: () => void, delayMillis: number) => number
  clearTimeout: (id: number) => void
}
```

The `AudioContextMock` from `standardized-audio-context-mock` is structurally compatible with the native `AudioContext` type — it has `.currentTime` and all the same properties. Removing it from the union does not change runtime behavior since the mock satisfies `AudioContext | BaseAudioContext` structurally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove mock type import and fix ContextLike</name>
  <files>src/utils/timeout.ts</files>
  <action>
  In `src/utils/timeout.ts`:

  1. **Delete line 1** — the `import type { AudioContext as AudioContextMock } from 'standardized-audio-context-mock'` import.

  2. **Change the `ContextLike` type** on what is currently line 17 from:
     ```typescript
     type ContextLike = AudioContext | BaseAudioContext | AudioContextMock
     ```
     to:
     ```typescript
     type ContextLike = AudioContext | BaseAudioContext
     ```

  That is the entire code change. Do NOT modify any other lines. The function signature, WeakMap usage, scheduler logic, and fallback behavior all remain identical.

  **Why this is safe:** The `standardized-audio-context-mock` `AudioContext` class structurally satisfies `AudioContext` (it implements the same interface). Test files that pass a mock `AudioContext` to `audioContextAwareTimeout()` will continue to compile because the mock is structurally assignable to the native `AudioContext` type.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm test -- --run src/utils/timeout.test.ts</automated>
  </verify>
  <done>The `standardized-audio-context-mock` import is removed from `timeout.ts` and `ContextLike` uses only `AudioContext | BaseAudioContext`.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild and verify clean declarations</name>
  <files>dist/index.d.ts</files>
  <action>
  1. Run `pnpm build:lib` to rebuild the library (includes `tsc --noEmit` typecheck + vite build with rollupTypes).

  2. Verify the built declarations are clean:
     - `grep -r "standardized-audio-context-mock" dist/` must return **no matches**.
     - `dist/index.d.ts` must exist and not contain any import from a devDependency.

  3. Run the full test suite with `pnpm test --run` to confirm no regressions.

  If `pnpm build:lib` fails with a type error, it means something still references the mock type — investigate and fix in `timeout.ts`.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm build:lib && ! grep -r "standardized-audio-context-mock" dist/ && pnpm test --run</automated>
  </verify>
  <done>`pnpm build:lib` succeeds, `dist/` contains no reference to `standardized-audio-context-mock`, and all tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -r "standardized-audio-context-mock" dist/` returns no output (exit code 1)
2. `pnpm build:lib` exits 0
3. `pnpm test --run` exits 0 with all tests passing
4. `grep "ContextLike" src/utils/timeout.ts` shows `AudioContext | BaseAudioContext` only
</verification>

<success_criteria>
- Published type declarations (`dist/index.d.ts`) do not import `standardized-audio-context-mock`
- `timeout.ts` compiles using only production-safe Web Audio API types
- All existing tests pass without modification
- A TypeScript consumer with `moduleResolution: "nodenext"` would not see test-mock types
</success_criteria>

<output>
After completion, create `.planning/phases/47-ship-blocker-fix/47-01-SUMMARY.md`
</output>
