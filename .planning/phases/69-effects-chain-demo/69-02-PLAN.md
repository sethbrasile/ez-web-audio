---
phase: 69-effects-chain-demo
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - e2e/demos.spec.ts
  - e2e/interactions.spec.ts
autonomous: false
requirements: [FX-01, FX-02, FX-03, FX-04, FX-05]

must_haves:
  truths:
    - "effects-chain page loads without JavaScript errors"
    - "Effect bypass toggle buttons are present and clickable"
    - "Parameter sliders exist for each effect"
    - "Move up/down buttons exist for effect reordering"
    - "Source switch buttons exist and are clickable"
    - "Signal flow diagram renders with Source and Output nodes"
    - "Human confirms: no audio clicks on bypass, sonic difference on reorder, smooth slider response"
  artifacts:
    - path: "e2e/demos.spec.ts"
      provides: "Page load smoke test for effects-chain"
      contains: "examples/effects-chain"
    - path: "e2e/interactions.spec.ts"
      provides: "Interaction tests: bypass, sliders, move buttons, source switch, signal flow"
  key_links:
    - from: "e2e/interactions.spec.ts"
      to: "EffectsChainDemo.vue DOM elements"
      via: "aria-label selectors (resilient pattern from Phase 68)"
      pattern: "aria-label"
---

<objective>
Add E2E tests for the Effects Chain demo page and perform human verification of audio quality.

Purpose: Automated tests confirm DOM correctness; human checkpoint confirms audio behavior (click-free bypass, audible reorder difference, smooth parameter response).

Output: New test blocks in demos.spec.ts and interactions.spec.ts, all green. Human approval of audio quality.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-effects-chain-demo/69-01-SUMMARY.md
@.planning/phases/69-effects-chain-demo/69-VALIDATION.md

Reference E2E structure (replicate this pattern):
- e2e/demos.spec.ts lines 1-49 — page load array pattern
- e2e/interactions.spec.ts lines 307-421 — PolySynth interaction tests (aria-label selectors, describe blocks)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E tests for Effects Chain demo</name>
  <files>e2e/demos.spec.ts, e2e/interactions.spec.ts</files>
  <action>
**e2e/demos.spec.ts:**
Add `'examples/effects-chain'` to the `demoPages` array. No other changes needed.

**e2e/interactions.spec.ts:**
Add a new `test.describe('EffectsChain page interactions', ...)` block after the existing PolySynth block. Include these tests:

**Test 1: Play button is present and clickable**
```typescript
test('Play button is present and clickable', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('[aria-label="Play"]', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  expect(await page.locator('[aria-label="Play"]').isVisible()).toBe(true)
  await page.locator('[aria-label="Play"]').click()
  expect(errors, 'effects-chain play should have no page errors').toHaveLength(0)
})
```

**Test 2: Effect cards and bypass buttons are present**
```typescript
test('Effect cards and bypass toggle buttons are present', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('.effect-card', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  // Four effect cards should exist
  expect(await page.locator('.effect-card').count()).toBe(4)

  // Each effect card has a bypass toggle with aria-pressed
  const bypassBtns = page.locator('.bypass-btn')
  expect(await bypassBtns.count()).toBe(4)

  // Initial state: all effects active (aria-pressed="true")
  expect(await bypassBtns.nth(0).getAttribute('aria-pressed')).toBe('true')

  // Click bypass on first effect — aria-pressed should toggle to false
  await bypassBtns.nth(0).click()
  expect(await bypassBtns.nth(0).getAttribute('aria-pressed')).toBe('false')
  // The effect card should gain 'bypassed' class
  expect(await page.locator('.effect-card.bypassed').count()).toBe(1)

  expect(errors, 'effects-chain bypass should have no page errors').toHaveLength(0)
})
```

**Test 3: Parameter sliders are present**
```typescript
test('Parameter sliders are present for each effect', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('.effect-card', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  // At minimum 11 sliders: 3 delay + 3 reverb + 2 compressor + 3 eq
  const sliders = page.locator('.effect-card input[type="range"]')
  expect(await sliders.count()).toBeGreaterThanOrEqual(11)

  expect(errors, 'effects-chain sliders should have no page errors').toHaveLength(0)
})
```

**Test 4: Move up/down buttons exist**
```typescript
test('Move up and move down buttons exist for reordering', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('.effect-card', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  // Each card should have move up and move down buttons (by aria-label)
  // First card: move-up disabled, move-down enabled
  const firstCard = page.locator('.effect-card').nth(0)
  const moveUpFirst = firstCard.locator('[aria-label*="up"]')
  const moveDownFirst = firstCard.locator('[aria-label*="down"]')
  expect(await moveUpFirst.isDisabled()).toBe(true)
  expect(await moveDownFirst.isDisabled()).toBe(false)

  // Last card: move-down disabled, move-up enabled
  const lastCard = page.locator('.effect-card').nth(3)
  const moveUpLast = lastCard.locator('[aria-label*="up"]')
  const moveDownLast = lastCard.locator('[aria-label*="down"]')
  expect(await moveUpLast.isDisabled()).toBe(false)
  expect(await moveDownLast.isDisabled()).toBe(true)

  expect(errors, 'effects-chain move buttons should have no page errors').toHaveLength(0)
})
```

**Test 5: Source switch buttons exist**
```typescript
test('Source switch buttons are present and clickable', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('[aria-label="Source: oscillator"]', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  expect(await page.locator('[aria-label="Source: oscillator"]').isVisible()).toBe(true)
  expect(await page.locator('[aria-label="Source: file"]').isVisible()).toBe(true)

  // Clicking source buttons should not throw
  await page.locator('[aria-label="Source: file"]').click()
  expect(errors, 'effects-chain source switch should have no page errors').toHaveLength(0)
})
```

**Test 6: Signal flow diagram renders**
```typescript
test('Signal flow diagram renders with Source and Output nodes', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', err => errors.push(err.message))

  await page.goto('examples/effects-chain')
  await page.waitForSelector('.signal-flow', { timeout: 10000 })
  await page.waitForLoadState('networkidle')

  // Signal flow container exists
  expect(await page.locator('.signal-flow').isVisible()).toBe(true)

  // Source and Output nodes exist
  expect(await page.locator('.flow-node.source').isVisible()).toBe(true)
  expect(await page.locator('.flow-node.output').isVisible()).toBe(true)

  // All 4 effects are active by default so 4 effect nodes should appear
  expect(await page.locator('.flow-node.effect').count()).toBe(4)

  // Bypass an effect and verify diagram updates (one fewer node)
  await page.locator('.bypass-btn').nth(0).click()
  await page.waitForFunction(
    () => document.querySelectorAll('.flow-node.effect').length === 3,
    { timeout: 3000 },
  )
  expect(await page.locator('.flow-node.effect').count()).toBe(3)

  expect(errors, 'effects-chain signal flow should have no page errors').toHaveLength(0)
})
```
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/demos.spec.ts e2e/interactions.spec.ts</automated>
  </verify>
  <done>All new E2E tests pass. effects-chain page load smoke test green. All 6 interaction tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of audio quality and visual correctness</name>
  <what-built>
Full Effects Chain demo page at /examples/effects-chain with:
- Bypass toggles for all four effects (delay, reverb, compressor, EQ)
- Parameter sliders for each effect (time/feedback/mix, decay/damping/mix, threshold/ratio, low/mid/high dB)
- Move up/down buttons for effect reordering
- Oscillator / Audio File source toggle
- Live signal flow diagram showing active effects
  </what-built>
  <how-to-verify>
1. Run `pnpm dev` and open http://localhost:5173/ez-web-audio/examples/effects-chain
2. Verify the page renders fully without a Load button — all controls visible immediately
3. Click Play — oscillator should start playing
4. Toggle each effect bypass on/off — listen carefully for AUDIO CLICKS (there should be none; equal-power crossfade)
5. Adjust delay time slider while playing — confirm smooth delay change (no glitches)
6. Adjust EQ low slider to +12 dB, then -12 dB — confirm audible bass boost/cut
7. Adjust reverb mix — confirm reverb blend changes
8. Click "Move Down" on EQ (moves it below Compressor) — watch signal flow diagram update. Play and listen for sonic difference compared to default order
9. Click "Audio File" source button — confirm music starts playing with effects applied, sliders retain their values
10. Verify signal flow diagram shows: when all effects enabled, all four nodes appear; when an effect is bypassed, it disappears from the diagram
  </how-to-verify>
  <action>Run pnpm dev, open http://localhost:5173/ez-web-audio/examples/effects-chain, and follow the how-to-verify steps above to confirm audio quality and visual behavior.</action>
  <files>none</files>
  <verify>
    <automated>MISSING — human ear required for audio artifact detection</automated>
  </verify>
  <done>Human confirms: no audio clicks on bypass, audible sonic difference on effect reorder, smooth parameter slider response, signal flow diagram updates correctly</done>
  <resume-signal>Type "approved" if audio and visuals work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full Playwright suite passes: `pnpm exec playwright test`
All 6 new Effects Chain interaction tests green.
Human checkpoint approved.
</verification>

<success_criteria>
- `pnpm exec playwright test` — entire suite green including new Effects Chain tests
- No audio clicks on bypass toggle (equal-power crossfade working)
- Signal flow diagram correctly reflects active effects and updates on bypass/reorder
- Source switch preserves effect parameter settings
- Human approves audio quality and visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/69-effects-chain-demo/69-02-SUMMARY.md`
</output>
