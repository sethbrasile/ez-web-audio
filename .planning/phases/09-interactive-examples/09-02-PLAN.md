---
phase: 09-interactive-examples
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/theme/components/PianoKeyboard.vue
  - docs/.vitepress/theme/components/SynthKeyboard.vue
  - docs/examples/synth-keyboard.md
autonomous: true

must_haves:
  truths:
    - "User sees a visual piano keyboard (C4-C5) with white and black keys"
    - "User can click/touch keys to play synthesized notes"
    - "User can use computer keyboard (AWSEDFTGYHUJK) to play notes"
    - "User can select waveform type and adjust ADSR envelope"
    - "Multiple keys can be held simultaneously (polyphonic)"
    - "Current note name displays when playing"
  artifacts:
    - path: "docs/.vitepress/theme/components/PianoKeyboard.vue"
      provides: "Reusable piano keyboard UI with mouse/touch/keyboard input"
      min_lines: 80
    - path: "docs/.vitepress/theme/components/SynthKeyboard.vue"
      provides: "Polyphonic synthesizer using PianoKeyboard"
      min_lines: 100
    - path: "docs/examples/synth-keyboard.md"
      provides: "Synth keyboard example page"
      contains: "SynthKeyboard"
  key_links:
    - from: "docs/.vitepress/theme/components/SynthKeyboard.vue"
      to: "PianoKeyboard.vue"
      via: "Vue component import"
      pattern: "import PianoKeyboard"
    - from: "docs/.vitepress/theme/components/SynthKeyboard.vue"
      to: "ez-web-audio"
      via: "dynamic import for createOscillator"
      pattern: "await import.*ez-web-audio"
---

<objective>
Build the shared PianoKeyboard component and the polyphonic SynthKeyboard example.

Purpose: PianoKeyboard is a reusable foundation shared by SynthKeyboard and SoundfontPiano. SynthKeyboard is priority 2 example -- an interactive polyphonic synthesizer with waveform and ADSR control.
Output: PianoKeyboard.vue, SynthKeyboard.vue, synth-keyboard.md
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/interactive-examples-plan.md
@.planning/phases/09-interactive-examples/09-RESEARCH.md
@docs/.vitepress/theme/components/OscillatorDemo.vue
@docs/.vitepress/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PianoKeyboard.vue shared component</name>
  <files>docs/.vitepress/theme/components/PianoKeyboard.vue</files>
  <action>
    Create a reusable piano keyboard Vue component at docs/.vitepress/theme/components/PianoKeyboard.vue.

    Props:
    - `startNote?: string` (default 'C4')
    - `endNote?: string` (default 'C5')
    - `activeKeys?: Set<string>` (currently pressed keys for visual styling)

    Events:
    - `@noteOn(note: string)` — emitted when key pressed (mouse down, touch start, or keyboard key down)
    - `@noteOff(note: string)` — emitted when key released (mouse up, touch end, keyboard key up)

    Visual layout:
    - White keys: C, D, E, F, G, A, B, C5 — ~40px wide, ~150px tall
    - Black keys: Db, Eb, Gb, Ab, Bb — ~28px wide, ~95px tall, positioned between white keys using absolute positioning
    - Active key state: slightly darker background + subtle translateY(2px) for press effect
    - Use VitePress CSS variables: `var(--vp-c-bg)` for white keys, `var(--vp-c-text-1)` for black keys, `var(--vp-c-brand)` for active state

    Computer keyboard mapping (handle in this component):
    - A=C4, W=Db4, S=D4, E=Eb4, D=E4, F=F4, T=Gb4, G=G4, Y=Ab4, H=A4, U=Bb4, J=B4, K=C5
    - Register keydown/keyup listeners on window in onMounted, remove in onUnmounted
    - Track pressed keys to prevent key repeat (keydown fires repeatedly when held)
    - Only register keyboard events if component is visible (use a flag or check document focus)

    Mouse/touch handling:
    - mousedown on key -> emit noteOn
    - mouseup / mouseleave on key -> emit noteOff
    - touchstart on key -> emit noteOn (prevent default to avoid mouse event)
    - touchend -> emit noteOff
    - Support dragging across keys (mouseenter while mouse is down -> noteOn previous noteOff)

    The component does NOT import ez-web-audio — it is purely a UI component. Audio logic lives in parent components (SynthKeyboard, SoundfontPiano).

    Use `<script setup lang="ts">` with Vue 3 Composition API.
  </action>
  <verify>Component file exists and has proper Vue SFC structure with template, script setup, and scoped styles. No import of ez-web-audio.</verify>
  <done>PianoKeyboard.vue renders a 1-octave keyboard with mouse, touch, and computer keyboard input, emitting noteOn/noteOff events</done>
</task>

<task type="auto">
  <name>Task 2: Create SynthKeyboard component and example page</name>
  <files>
    docs/.vitepress/theme/components/SynthKeyboard.vue
    docs/examples/synth-keyboard.md
  </files>
  <action>
    **SynthKeyboard.vue** at docs/.vitepress/theme/components/SynthKeyboard.vue:

    Import PianoKeyboard.vue locally (not global registration yet — that happens in Plan 07).

    State:
    - `waveType: ref<OscillatorType>('triangle')` — waveform selector (sine, square, sawtooth, triangle)
    - `envelope: ref({ attack: 0.01, decay: 0.3, sustain: 0.4, release: 0.5 })` — ADSR values
    - `masterGain: ref(0.3)` — volume slider
    - `activeNotes: ref(new Set<string>())` — currently playing notes for visual feedback
    - `currentNote: ref('')` — last played note name for display
    - `initialized: ref(false)` — tracks whether initAudio() has been called
    - `loading: ref(false)` — loading state
    - `error: ref('')` — error display

    Oscillator management:
    - Use a `Map<string, any>` to track active oscillators by note name
    - On noteOn: create oscillator with `createOscillator({ frequency: frequencyMap[note], type: waveType.value, envelope: envelope.value })`, set gain to masterGain.value, call play()
    - On noteOff: find oscillator in map, call stop(), remove from map
    - CRITICAL: Use `await import('ez-web-audio')` for SSR safety
    - Call `initAudio()` on first noteOn if not initialized

    ADSR presets (buttons above sliders):
    - Piano: { attack: 0.005, decay: 0.4, sustain: 0.2, release: 0.8 }
    - Pad: { attack: 0.5, decay: 0.3, sustain: 0.8, release: 1.0 }
    - Pluck: { attack: 0.001, decay: 0.2, sustain: 0.0, release: 0.1 }
    - Lead: { attack: 0.05, decay: 0.1, sustain: 0.7, release: 0.2 }

    Layout:
    - Controls row at top: waveform dropdown, ADSR preset buttons, volume slider
    - ADSR sliders row: attack (0-2s), decay (0-2s), sustain (0-1), release (0-3s) — labeled with current values
    - Current note display (large text, brand color)
    - PianoKeyboard component below
    - Keyboard hint text: "Use keys A-K to play (W,E,T,Y,U for sharps)"

    Cleanup: onUnmounted() — stop all active oscillators, clear the map.

    Use component wrapper div with class matching existing demo style: border, border-radius, padding, bg-soft.

    **synth-keyboard.md** at docs/examples/synth-keyboard.md:

    Frontmatter: `title: Synth Keyboard`

    Content structure (component-first per Phase 7 decision):
    1. Title + brief intro (polyphonic synthesizer with ADSR envelopes)
    2. Browser requirement warning
    3. `<SynthKeyboard />` component (interactive demo at top)
    4. "How It Works" section explaining oscillator creation per note
    5. Code snippet showing createOscillator with envelope
    6. ADSR envelope explanation (can reference or link to synthesis.md)
    7. "API Used" section listing: createOscillator, frequencyMap, envelope options
    8. Next steps links to other examples

    Import SynthKeyboard in the markdown using VitePress script setup block:
    ```
    <script setup>
    import SynthKeyboard from '../.vitepress/theme/components/SynthKeyboard.vue'
    </script>
    ```
  </action>
  <verify>
    Run `pnpm docs:build 2>&1 | tail -10` — should build without errors for synth-keyboard page.
    Check that SynthKeyboard.vue imports PianoKeyboard.vue.
    Check that synth-keyboard.md includes the SynthKeyboard component.
  </verify>
  <done>Synth keyboard page renders with interactive polyphonic piano, waveform selector, ADSR controls with presets, and volume slider</done>
</task>

</tasks>

<verification>
- PianoKeyboard.vue has no ez-web-audio imports (pure UI)
- SynthKeyboard.vue uses dynamic import for ez-web-audio
- SynthKeyboard.vue has onUnmounted cleanup
- synth-keyboard.md has component-first layout
- VitePress builds without errors
</verification>

<success_criteria>
Users can play a polyphonic synth keyboard via mouse, touch, or computer keyboard, with selectable waveforms, ADSR presets, and real-time note display.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-examples/09-02-SUMMARY.md`
</output>
