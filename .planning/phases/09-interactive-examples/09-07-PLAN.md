---
phase: 09-interactive-examples
plan: 07
type: execute
wave: 4
depends_on: ["09-02", "09-03", "09-04", "09-05", "09-06"]
files_modified:
  - docs/.vitepress/theme/components/FilterDemo.vue
  - docs/examples/effects.md
  - docs/.vitepress/theme/index.ts
autonomous: false

must_haves:
  truths:
    - "User can hear real-time filter effect on a playing oscillator"
    - "User can select filter type, adjust frequency, Q, and gain"
    - "User can toggle filter bypass while sound plays"
    - "All 10 new components are globally registered and available in markdown"
    - "VitePress docs build succeeds with all example pages"
    - "All example pages are navigable from sidebar"
  artifacts:
    - path: "docs/.vitepress/theme/components/FilterDemo.vue"
      provides: "Real-time filter control demo"
      min_lines: 80
    - path: "docs/examples/effects.md"
      provides: "Updated effects page with FilterDemo component"
      contains: "FilterDemo"
    - path: "docs/.vitepress/theme/index.ts"
      provides: "Global component registration for all demo components"
      contains: "DrumMachine"
  key_links:
    - from: "docs/.vitepress/theme/index.ts"
      to: "docs/.vitepress/theme/components/*.vue"
      via: "app.component() registration"
      pattern: "app.component"
    - from: "docs/.vitepress/theme/components/FilterDemo.vue"
      to: "ez-web-audio"
      via: "dynamic import for createFilterEffect"
      pattern: "createFilterEffect"
---

<objective>
Build the FilterDemo (effects page upgrade), register all new components globally, and verify the complete docs build.

Purpose: FilterDemo completes the interactive effects page. Global registration ensures all components work when referenced directly in markdown. This final plan ties everything together.
Output: FilterDemo.vue, updated effects.md, updated index.ts, verified build
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/interactive-examples-plan.md
@docs/.vitepress/theme/index.ts
@docs/examples/effects.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterDemo component and update effects page</name>
  <files>
    docs/.vitepress/theme/components/FilterDemo.vue
    docs/examples/effects.md
  </files>
  <action>
    **FilterDemo.vue** at docs/.vitepress/theme/components/FilterDemo.vue:

    State:
    - `initialized: ref(false)`
    - `playing: ref(false)`
    - `loading: ref(false)`
    - `error: ref('')`
    - `filterType: ref('lowpass')` — dropdown: lowpass, highpass, bandpass, notch, lowshelf, highshelf, peaking, allpass
    - `frequency: ref(1000)` — slider 20-20000 Hz (use logarithmic mapping for slider)
    - `q: ref(1)` — slider 0.1-20
    - `filterGain: ref(0)` — slider -24 to +24 dB (only for shelf/peaking types)
    - `bypassed: ref(false)` — bypass toggle
    - `sourceType: ref<'oscillator' | 'noise'>('oscillator')` — what to filter
    - Let: `lib: any`, `source: any`, `filter: any`

    Logarithmic frequency slider:
    - Slider range 0-100 (linear)
    - Map to frequency: `20 * Math.pow(1000, sliderValue / 100)` gives 20-20000 Hz
    - Display actual Hz value next to slider

    Play/Stop:
    - Play: create source (sawtooth oscillator at 200Hz — rich harmonics for filter demo, or white noise), create filter, add filter to source, play
    - Stop: stop source

    Filter parameter updates (while playing):
    - Watch filterType: if changed, remove old filter, create new one, add to source. Call `source.rewireEffects()` if needed.
    - Watch frequency: update `filter.frequency = value`
    - Watch q: update `filter.q = value`
    - Watch filterGain: update `filter.gain = value` (only applies to shelf/peaking)
    - Watch bypassed: update `filter.bypass = value`, call `source.rewireEffects()`

    Show/hide gain slider: only visible when filterType is 'lowshelf', 'highshelf', or 'peaking'.

    Template:
    ```
    Source: [Oscillator / White Noise toggle]  [Play/Stop]

    Filter Type: [dropdown]
    Frequency: [====slider====] 1000 Hz
    Resonance (Q): [====slider====] 1.0
    Gain: [====slider====] 0 dB  (only for shelf/peaking)

    [x] Bypass filter
    ```

    Cleanup: standard.

    **Update effects.md:**
    Add FilterDemo component at the TOP of the page (after title and intro, before the existing content). Follow component-first pattern:
    1. Keep existing title and first paragraph
    2. Add browser requirement warning if not already present
    3. Add interactive demo section:
       ```markdown
       ## Try It: Real-Time Filter Control

       Experiment with different filter types and parameters. Try switching between the oscillator
       and white noise sources to hear how filters affect different signals.

       <script setup>
       import FilterDemo from '../.vitepress/theme/components/FilterDemo.vue'
       </script>

       <FilterDemo />
       ```
    4. Keep ALL existing content below the demo (filter types table, code examples, GainEffect, effect chains, bypass/mix, wrapEffect, analyzer, DJ EQ example)
    5. Update "Next Steps" to include new example pages (Audio Routing, Synth Drum Kit)
  </action>
  <verify>
    Check FilterDemo.vue has all filter types, logarithmic frequency slider, real-time parameter updates.
    Check effects.md has FilterDemo at top, preserves existing content below.
  </verify>
  <done>Effects page has interactive FilterDemo at top with all filter types, real-time control, and bypass toggle</done>
</task>

<task type="auto">
  <name>Task 2: Register all new components globally in theme index</name>
  <files>docs/.vitepress/theme/index.ts</files>
  <action>
    Update docs/.vitepress/theme/index.ts to import and register all new components globally.

    Add imports for all new components:
    ```typescript
    import PianoKeyboard from './components/PianoKeyboard.vue'
    import SynthKeyboard from './components/SynthKeyboard.vue'
    import DrumMachine from './components/DrumMachine.vue'
    import XYPad from './components/XYPad.vue'
    import SynthDrumKit from './components/SynthDrumKit.vue'
    import SampledDrumKit from './components/SampledDrumKit.vue'
    import TimingDemo from './components/TimingDemo.vue'
    import DistortionDemo from './components/DistortionDemo.vue'
    import SoundfontPiano from './components/SoundfontPiano.vue'
    import FilterDemo from './components/FilterDemo.vue'
    ```

    Add app.component() registrations for each in the enhanceApp function.

    Keep existing registrations (AudioDemo, OscillatorDemo, TrackDemo).

    This allows using components directly in markdown without script setup blocks (e.g., `<DrumMachine />`). The script setup approach in markdown pages also works and is preferred for explicit imports, but global registration is a fallback.
  </action>
  <verify>
    Read index.ts and confirm all 13 components (3 existing + 10 new) are imported and registered.
    Run `pnpm docs:build 2>&1 | tail -20` — should build successfully.
  </verify>
  <done>All demo components globally registered in VitePress theme</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all interactive examples work end-to-end</name>
  <action>All 9 new interactive examples have been built and registered. Human verification needed to confirm audio playback, visual feedback, and responsiveness work correctly across all demos.</action>
  <what-built>Complete set of 9 interactive examples added to the docs site: Drum Machine, Synth Keyboard, XY Pad, Synth Drum Kit, Sampled Drum Kit, Timing Basics, Audio Routing, Soundfont Piano, and Filter Demo upgrade. Plus sidebar navigation, audio assets, and examples overview.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` in the project root
    2. Visit http://localhost:5173/ez-web-audio/examples/
    3. Verify sidebar shows all 5 category groups with correct links
    4. Test each example page:
       - **Drum Machine**: Click cells to make a pattern, press Play, hear the beat loop, adjust BPM
       - **Synth Keyboard**: Click piano keys or press A-K keys, try different waveforms and ADSR presets
       - **XY Pad**: Click and drag on the canvas, hear frequency change on X and volume on Y
       - **Synth Drum Kit**: Tap each pad (Kick, Snare, Hi-Hat), try the Bass Drop, try Snare Breakdown buttons
       - **Sampled Drum Kit**: Tap pads, watch the sample counter cycle 1/3 -> 2/3 -> 3/3
       - **Soundfont Piano**: Wait for soundfont to load, then play keys (should sound like real piano)
       - **Timing Basics**: Try all 4 sections (Play Now, Play In 1 Second with countdown, Play 3 Notes with timeline, Play Chord)
       - **Audio Routing**: Play oscillator, toggle distortion on/off, adjust amount and mix sliders
       - **Effects**: Try the new FilterDemo at top of page, change filter types, adjust frequency/Q
    5. Check mobile responsiveness by resizing browser to narrow width
    6. Verify existing pages still work: Basic Playback, Synthesis, Effects (scroll past new demo)
  </how-to-verify>
  <verify>Human confirms all demos produce audio and respond to interaction</verify>
  <done>All interactive examples verified working by human testing</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `pnpm docs:build` completes without errors
- All 10 example pages accessible via sidebar
- All interactive components load and produce sound on interaction
- No console errors on any example page
- Existing examples (Basic Playback, Synthesis, Effects) still function
</verification>

<success_criteria>
All 9 new interactive examples work in the browser. Users can experience drum machines, synthesizers, effects, timing, and soundfonts interactively. Documentation build succeeds. Human verification confirms all demos work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-interactive-examples/09-07-SUMMARY.md`
</output>
