---
phase: 14-docs-examples-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/examples/ambient-generator.md
  - docs/.vitepress/theme/components/AmbientGenerator.vue
  - docs/examples/visualization.md
  - docs/.vitepress/theme/components/VisualizationDemo.vue
  - docs/.vitepress/config.mts
autonomous: true

must_haves:
  truths:
    - "At least 2 new creative demo pages exist showcasing advanced library capabilities"
    - "New demos highlight features not well-covered by existing examples (visualization, layered sounds, crossfade, white noise)"
    - "New demo pages integrate into sidebar navigation and examples index"
  artifacts:
    - path: "docs/examples/ambient-generator.md"
      provides: "Creative ambient sound generator example page"
    - path: "docs/.vitepress/theme/components/AmbientGenerator.vue"
      provides: "Interactive ambient generator component"
    - path: "docs/examples/visualization.md"
      provides: "Audio visualization example page"
    - path: "docs/.vitepress/theme/components/VisualizationDemo.vue"
      provides: "Interactive visualization component"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "docs/examples/ambient-generator.md"
      via: "sidebar configuration"
      pattern: "ambient-generator"
    - from: "docs/.vitepress/config.mts"
      to: "docs/examples/visualization.md"
      via: "sidebar configuration"
      pattern: "visualization"
---

<objective>
Create 2 new creative demo pages that showcase advanced library capabilities not well-covered by existing examples.

Purpose: DOCS-02 requires new creative demo pages showcasing advanced capabilities. Existing demos cover basic playback, synthesis, drum machines, and effects well, but lack examples showing visualization (Analyzer API), layered/ambient sounds, crossfade, and white noise.

Output: 2 new example pages with interactive Vue components, integrated into sidebar navigation.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@docs/.vitepress/config.mts
@docs/examples/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Ambient Sound Generator demo</name>
  <files>
    docs/examples/ambient-generator.md
    docs/.vitepress/theme/components/AmbientGenerator.vue
  </files>
  <action>
    Create an "Ambient Sound Generator" demo that showcases multiple advanced features in one creative application. This demo should feel inspiring - showing users they can build something cool with the library.

    **Vue component (AmbientGenerator.vue):**
    Build an interactive ambient sound generator with these layers:
    1. **Drone layer** - Low-frequency oscillator (sine wave, 60-120 Hz) with slow gain modulation. Use `createOscillator` with a pad-like envelope (slow attack, high sustain, long release).
    2. **Texture layer** - White noise filtered through a lowpass filter. Use `createWhiteNoise()` + `createFilterEffect()`. User can adjust filter frequency to change the texture from rumble to hiss.
    3. **Shimmer layer** - Higher-frequency oscillator (triangle wave, 400-800 Hz) with very low gain. Creates ethereal overtones.

    **Controls:**
    - Master volume slider
    - Per-layer on/off toggle buttons
    - Drone frequency slider (60-120 Hz range)
    - Texture filter cutoff slider (200-4000 Hz)
    - Shimmer frequency slider (400-800 Hz)
    - Start/Stop button

    **Features demonstrated:**
    - `createOscillator()` with envelopes
    - `createWhiteNoise()` for noise synthesis
    - `createFilterEffect()` for tone shaping
    - `changeGainTo()` for mixing
    - `update().to().from()` for real-time parameter changes
    - Multiple sounds playing simultaneously

    **Code patterns:**
    - Use dynamic import for ez-web-audio
    - Proper onUnmounted cleanup
    - Loading state while initializing
    - VitePress-compatible styling (use CSS variables)
    - Responsive layout

    **Markdown page (ambient-generator.md):**
    - Title and brief intro explaining the concept
    - The interactive component
    - "How It Works" section explaining each layer
    - Code example showing how to create an ambient generator from scratch
    - "API Used" section listing the relevant functions
    - "Next Steps" links to related examples
  </action>
  <verify>
    Run `pnpm build` to verify the component compiles. Check that the markdown page renders correctly.
  </verify>
  <done>
    Ambient Generator demo page exists with interactive Vue component showing layered synthesis, white noise filtering, and real-time parameter control. Component follows established patterns (dynamic imports, cleanup, loading states).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Audio Visualization demo and integrate new pages</name>
  <files>
    docs/examples/visualization.md
    docs/.vitepress/theme/components/VisualizationDemo.vue
    docs/.vitepress/config.mts
    docs/examples/index.md
  </files>
  <action>
    **Vue component (VisualizationDemo.vue):**
    Create an audio visualization demo that showcases the Analyzer API. This is a feature that existing demos don't demonstrate at all.

    Build a component with:
    1. **Audio source** - An oscillator with selectable waveform (sine, square, sawtooth, triangle) and adjustable frequency. Keep it simple - the visualization is the star.
    2. **Frequency spectrum visualization** - Canvas-based bar chart showing frequency data from `getFrequencyData()`. Use requestAnimationFrame for smooth animation.
    3. **Waveform visualization** - Canvas showing the time-domain waveform from `getTimeDomainData()`. Display as a line graph.
    4. **Controls:**
       - Play/Stop button
       - Waveform type selector (sine, square, sawtooth, triangle)
       - Frequency slider (100-2000 Hz)
       - FFT size selector (256, 512, 1024, 2048) to show different resolutions

    **Features demonstrated:**
    - `createAnalyzer()` factory function
    - `setAnalyzer()` / `getAnalyzer()` on BaseSound
    - `getFrequencyData()` and `getTimeDomainData()` for visualization data
    - Canvas rendering with requestAnimationFrame
    - Real-time parameter modulation while visualizing

    **Code patterns:**
    - Dynamic import for ez-web-audio
    - Proper cleanup (cancel RAF, stop audio, disconnect nodes)
    - Canvas responsive to container width
    - VitePress-compatible colors using CSS variables
    - Loading state

    **Markdown page (visualization.md):**
    - Title and intro explaining audio visualization
    - The interactive component
    - "How It Works" section explaining Analyzer, FFT, frequency vs time domain
    - Code example showing how to set up visualization from scratch
    - "API Used" section
    - "Next Steps" links

    **Integration:**
    1. Update `docs/.vitepress/config.mts` sidebar to add both new pages:
       - Add "Creative" section after "Effects & Routing" with:
         - "Ambient Generator" -> /examples/ambient-generator
         - "Visualization" -> /examples/visualization
    2. Update `docs/examples/index.md`:
       - Add a "Creative" section with entries for both new demos
       - Follow the existing pattern (title, description, "You'll learn" list)
  </action>
  <verify>
    Run `pnpm build` to verify everything compiles. Check that both new pages appear in sidebar. Check that the examples index page lists both new demos.
  </verify>
  <done>
    Audio Visualization demo page exists with interactive canvas-based frequency and waveform display. Both new pages integrated into sidebar navigation and examples index. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with all new pages
2. Both new demo pages accessible from sidebar navigation
3. Both new demos listed in examples index page
4. New Vue components have proper cleanup (onUnmounted)
5. New demos use dynamic imports for ez-web-audio
</verification>

<success_criteria>
2 new creative demo pages exist (Ambient Generator and Visualization) showcasing advanced features not covered by existing demos. Both integrated into sidebar and examples index. All build successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/14-docs-examples-polish/14-03-SUMMARY.md`
</output>
