---
phase: 14-docs-examples-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/getting-started.md
  - docs/guide/concepts.md
  - docs/index.md
autonomous: true

must_haves:
  truths:
    - "Every code example in guides compiles and uses current API signatures"
    - "Getting Started guide gets users to working audio in under 5 minutes"
    - "Core Concepts guide covers all major classes, patterns, and AudioContext lifecycle"
    - "No outdated API usage (no removed methods, correct parameter names)"
  artifacts:
    - path: "docs/guide/getting-started.md"
      provides: "Polished getting started guide with verified code examples"
    - path: "docs/guide/concepts.md"
      provides: "Complete core concepts documentation with verified code examples"
    - path: "docs/index.md"
      provides: "Homepage with accurate feature descriptions and test count"
  key_links:
    - from: "docs/guide/getting-started.md"
      to: "docs/guide/concepts.md"
      via: "Next Steps link"
      pattern: "/guide/concepts"
    - from: "docs/guide/concepts.md"
      to: "docs/examples/"
      via: "Next Steps link"
      pattern: "/examples/"
---

<objective>
Review and update guide pages (Getting Started, Core Concepts) and homepage for accuracy, completeness, and clarity against the current API.

Purpose: DOCS-03 and DOCS-04 require the Getting Started guide to provide a clear quick-win experience and Core Concepts to be complete. Homepage should accurately represent the library.

Output: Updated guide pages and homepage with verified, accurate code snippets.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.ts
@docs/guide/getting-started.md
@docs/guide/concepts.md
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and update Getting Started guide, Core Concepts, and homepage</name>
  <files>
    docs/guide/getting-started.md
    docs/guide/concepts.md
    docs/index.md
  </files>
  <action>
    **Getting Started guide (DOCS-03):**
    1. Read src/index.ts exports to get the current API surface
    2. Verify every code example in getting-started.md compiles with the current API:
       - Check `createSound`, `createTrack`, `createOscillator` signatures
       - Check `changeGainTo`, `changePanTo` method names
       - Check `onPlaySet`, `onPlayRamp` fluent API syntax
       - Check `createFilterEffect` signature (needs AudioContext as first arg)
       - Check `preload`, `isPreloaded` signatures
       - Check `getAudioContext` is async (returns Promise)
    3. Verify the guide covers the lazy AudioContext pattern (Phase 10 change):
       - No mention of requiring initAudio() in main flow
       - initAudio() documented as optional/advanced
    4. Review flow and clarity:
       - First code example should be minimal (3-4 lines max to first sound)
       - Sound vs Track comparison table should be accurate
       - Effects example should be self-contained and correct
    5. Update the homepage (docs/index.md):
       - Verify test count matches reality (currently says 711, should be 714+)
       - Feature descriptions should be accurate
       - Tagline and hero text should be compelling

    **Core Concepts guide (DOCS-04):**
    1. Verify class hierarchy matches current codebase (BaseSound tree)
    2. Verify all code examples use current API:
       - `createSound`, `createTrack`, `createOscillator` usage
       - `seek().from()` fluent syntax
       - Event subscription `.on()` API
       - `createSampler`, `createBeatTrack`, `createLayeredSound`, `createSprite` usage
    3. Check for missing concepts that should be documented:
       - Effects system (addEffect/removeEffect/getEffects) - currently covered
       - Debug mode (setDebugMode/setDebugHandler) - currently NOT in concepts guide, add brief section
       - Crossfade utility - currently NOT in concepts guide, add brief section
       - Collection utilities (stopAll/pauseAll/playAll) - currently NOT in concepts guide, add brief section
    4. Verify AudioContext lifecycle section matches Phase 10 lazy initialization behavior
    5. Verify "Parameter Control" section covers update(), onPlaySet(), onPlayRamp() accurately
    6. Add any missing "Other Sound Types" that are now in the API:
       - LayeredSound is listed but verify code example accuracy
       - AudioSprite is listed but verify code example accuracy
       - Check if createWhiteNoise should be mentioned

    Fix all issues found. Do not rewrite sections that are already accurate - only fix inaccuracies and add missing content.
  </action>
  <verify>
    Read through all code examples in getting-started.md and concepts.md. Cross-reference each method call against src/index.ts exports. Verify no method name or signature mismatches.
    Run `pnpm build` to verify docs build.
  </verify>
  <done>
    Getting Started guide has verified code examples, clear quick-win flow (first sound in 3-4 lines), and covers lazy AudioContext. Core Concepts guide is complete with all major features documented and all code examples verified against current API. Homepage test count is accurate.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. All code examples in guides use current API method names and signatures
3. All internal links resolve to existing pages
4. Getting Started first example is 3-4 lines to first sound
5. Core Concepts covers all major features
</verification>

<success_criteria>
Guide pages and homepage verified against current API with no outdated code examples. Getting Started provides clear quick-win. Core Concepts is complete. All internal links work.
</success_criteria>

<output>
After completion, create `.planning/phases/14-docs-examples-polish/14-04-SUMMARY.md`
</output>
