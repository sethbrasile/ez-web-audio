---
phase: 10-lazy-audiocontext-initialization
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/examples/basic-playback.md
  - docs/examples/synthesis.md
  - docs/examples/timing.md
  - docs/examples/xy-pad.md
  - docs/examples/drum-machine.md
autonomous: true

must_haves:
  truths:
    - "Example markdown code snippets show simplified API without initAudio()"
    - "All example pages build correctly in VitePress"
  artifacts:
    - path: "docs/examples/basic-playback.md"
      provides: "Code snippets without initAudio"
    - path: "docs/examples/drum-machine.md"
      provides: "Code snippets without initAudio"
  key_links:
    - from: "docs/examples/*.md"
      to: "ez-web-audio API"
      via: "code snippets showing simplified usage"
      pattern: "createSound|createOscillator|createBeatTrack"
---

<objective>
Update example code snippets in markdown pages to remove initAudio() and show the simplified lazy API pattern.

Purpose: Example pages should match the new API behavior where initAudio() is not required.
Output: Updated markdown example pages without initAudio() in code snippets.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lazy-audiocontext-initialization/10-01-SUMMARY.md
@docs/examples/basic-playback.md
@docs/examples/drum-machine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update example code snippets in markdown pages</name>
  <files>
    docs/examples/basic-playback.md
    docs/examples/synthesis.md
    docs/examples/timing.md
    docs/examples/xy-pad.md
    docs/examples/drum-machine.md
  </files>
  <action>
For each example markdown page that contains code snippets with `initAudio`:

1. Read the file and find all code blocks containing `initAudio`
2. Remove `initAudio` from import statements in code snippets
3. Remove `await initAudio()` calls from code snippets
4. Ensure code snippets still show the user interaction pattern (button click handler) since that's still required by browsers

Only update the inline code snippets in the markdown — NOT the Vue component references (those are handled in plan 10-03).

If a code snippet has `initAudio` as part of an explanation paragraph (not a code block), update the text to reflect it's optional.
  </action>
  <verify>Run `pnpm build` to verify all markdown pages build correctly. Grep for `initAudio` in example markdown files to confirm code snippets updated.</verify>
  <done>Example code snippets show simplified API without initAudio(). Markdown pages build correctly.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds (includes docs build)
2. Code snippets in example markdown pages don't show initAudio as required
3. All example pages still show user interaction handlers (click events)
</verification>

<success_criteria>
- All 5 example markdown pages show simplified code snippets
- VitePress docs build succeeds
- Example pages demonstrate the new "just works" pattern
</success_criteria>

<output>
After completion, create `.planning/phases/10-lazy-audiocontext-initialization/10-04-SUMMARY.md`
</output>
