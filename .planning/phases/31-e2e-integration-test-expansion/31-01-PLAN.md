---
phase: 31-e2e-integration-test-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/demos.spec.ts
  - e2e/navigation.spec.ts
  - e2e/interactions.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "At least 5 demo pages have E2E tests that click buttons or toggle beats"
    - "E2E tests verify DOM state changes after interaction (not just error absence)"
    - "All waitForTimeout(3000) calls replaced with condition-based waits"
    - "At least one Playwright test runs with a mobile viewport configuration"
  artifacts:
    - path: "e2e/interactions.spec.ts"
      provides: "Interaction E2E tests with DOM change verification"
      min_lines: 80
    - path: "e2e/demos.spec.ts"
      provides: "Updated: waitForTimeout replaced with waitForSelector"
      contains: "waitForSelector"
    - path: "e2e/navigation.spec.ts"
      provides: "Updated: waitForTimeout replaced with condition-based waits"
      contains: "waitForSelector"
  key_links:
    - from: "e2e/interactions.spec.ts"
      to: "/examples/drum-machine"
      via: "page.click('.play-btn')"
      pattern: "play-btn"
    - from: "e2e/interactions.spec.ts"
      to: "/examples/basic-playback"
      via: "page.click('.play-btn')"
      pattern: "play-btn"
---

<objective>
Replace fragile `waitForTimeout` calls with condition-based waits in existing E2E specs and create a new `e2e/interactions.spec.ts` that tests actual user interactions across 5+ demo pages, verifying DOM changes after those interactions. Add a mobile viewport test.

Purpose: Review findings H-10 (no interaction tests), H-11 (waitForTimeout fragile), M-26 (no DOM verification), L-17 (no mobile viewport) — all closed by this plan.
Output: Three E2E spec files with condition-based waits, interaction tests, DOM verification, and mobile viewport coverage.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@e2e/demos.spec.ts
@e2e/navigation.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace waitForTimeout with condition-based waits in existing specs</name>
  <files>e2e/demos.spec.ts, e2e/navigation.spec.ts</files>
  <action>
    Replace all `waitForTimeout` calls in both existing E2E spec files with condition-based alternatives that are more reliable.

    In `e2e/demos.spec.ts`:
    - Replace `await page.waitForTimeout(3000)` with `await page.waitForSelector('.VPContent', { timeout: 10000 })` followed by `await page.waitForLoadState('networkidle')`. The `.VPContent` selector is the VitePress main content container that is present after Vue hydration.
    - Keep the `waitForLoadState('domcontentloaded')` call before the new wait.

    In `e2e/navigation.spec.ts`:
    - Replace `await page.waitForTimeout(3000)` in the loop with `await page.waitForSelector('.VPContent', { timeout: 10000 })` followed by `await page.waitForLoadState('networkidle')`.
    - Replace `await page.waitForTimeout(2000)` in the title test with `await page.waitForSelector('h1', { timeout: 10000 })`.
    - Keep existing `waitForLoadState('domcontentloaded')` calls.

    Rationale: VitePress renders `.VPContent` once the SPA has hydrated. `networkidle` ensures all dynamic imports (lazy audio library) have settled. This is more reliable than arbitrary timeouts and matches VitePress SPA behavior (Phase 16 decision: E2E tests focus on error-detection and VitePress SPA timing).
  </action>
  <verify>
    `grep -n "waitForTimeout" /Users/seth/Documents/GitHub/ez-audio/e2e/demos.spec.ts /Users/seth/Documents/GitHub/ez-audio/e2e/navigation.spec.ts`
    Should return zero results.
  </verify>
  <done>No `waitForTimeout` calls remain in demos.spec.ts or navigation.spec.ts. Both files use `waitForSelector('.VPContent')` and `waitForLoadState('networkidle')` instead.</done>
</task>

<task type="auto">
  <name>Task 2: Create interaction E2E tests with DOM verification and mobile viewport</name>
  <files>e2e/interactions.spec.ts</files>
  <action>
    Create `e2e/interactions.spec.ts` with tests that click buttons and verify DOM changes. Use condition-based waits only — no `waitForTimeout`.

    Structure the file with these test suites:

    **Suite 1: Basic Playback page interactions**
    - Test: "Play Sound button changes label to Loading... then back"
      - `page.goto('/examples/basic-playback')`
      - `await page.waitForSelector('.play-btn', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Click `.play-btn` — note: audio won't actually play in headless, but the button click triggers the async handler
      - Verify button is present and clickable (not disabled permanently after click)
      - `expect(await page.locator('.play-btn').isVisible()).toBe(true)`

    **Suite 2: Effects page (FilterDemo) interactions**
    - Test: "Filter type select renders — selecting highpass updates the control"
      - Navigate to `/examples/effects`
      - `await page.waitForSelector('.filter-demo, select, [type="range"]', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Locate the filter type select (use `page.locator('select').first()`)
      - Verify it is visible
      - Select 'highpass' value: `await page.locator('select').first().selectOption('highpass')`
      - Verify the select now has value 'highpass': `expect(await page.locator('select').first().inputValue()).toBe('highpass')`

    **Suite 3: Drum Machine page — beat toggle interactions**
    - Test: "Beat cell toggles aria-pressed on click"
      - Navigate to `/examples/drum-machine`
      - `await page.waitForSelector('.beat-cell', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Get the first beat cell: `const beatCell = page.locator('.beat-cell').first()`
      - Read initial `aria-pressed` value
      - Click the beat cell
      - Verify `aria-pressed` has toggled (opposite of initial value)
      - This confirms DOM change after interaction, not just error absence

    - Test: "Play button text changes to Stop after click"
      - Navigate to `/examples/drum-machine`
      - `await page.waitForSelector('.play-btn', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Get initial text of `.play-btn` (should be 'Play')
      - Click `.play-btn`
      - `await page.waitForFunction(() => document.querySelector('.play-btn')?.textContent?.trim() === 'Stop', { timeout: 10000 })`
      - Verify button text is now 'Stop'
      - Click again to stop: click `.play-btn`
      - `await page.waitForFunction(() => document.querySelector('.play-btn')?.textContent?.trim() === 'Play', { timeout: 10000 })`

    **Suite 4: Synthesis page (OscillatorDemo) interactions**
    - Test: "Volume slider input changes value"
      - Navigate to `/examples/synthesis`
      - `await page.waitForSelector('input[type="range"]', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Locate first range input
      - Get initial value
      - Fill with new value: `await page.locator('input[type="range"]').first().fill('0.5')`
      - Verify value changed

    **Suite 5: Drum Machine Vue page — beat toggle**
    - Test: "Beat cell aria-pressed toggles on click"
      - Navigate to `/examples/drum-machine-vue`
      - `await page.waitForSelector('.beat-cell', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Same beat toggle pattern as drum-machine test above

    **Suite 6: Mobile viewport test**
    - Add a `test.describe('Mobile viewport', () => { ... })` block using `test.use({ viewport: { width: 390, height: 844 } })` (iPhone 14 dimensions)
    - Test: "Drum machine loads and beat cells are visible on mobile"
      - Navigate to `/examples/drum-machine`
      - `await page.waitForSelector('.beat-cell', { timeout: 10000 })`
      - `await page.waitForLoadState('networkidle')`
      - Verify at least one `.beat-cell` is visible: `expect(await page.locator('.beat-cell').first().isVisible()).toBe(true)`
      - Verify no JS errors during mobile render

    Import `expect` and `test` from `@playwright/test`. Collect errors via `page.on('pageerror', ...)` in each test. Assert `errors` length is 0 alongside the DOM assertions.

    Use condition-based waits exclusively — zero `waitForTimeout` calls in this file.
  </action>
  <verify>
    `grep -n "waitForTimeout" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` — should return zero results.
    `grep -c "test(" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` — should return 7 or more.
    `grep -n "viewport" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` — should show mobile viewport config.
  </verify>
  <done>
    - `e2e/interactions.spec.ts` exists with 7+ tests across 6 suites
    - 5+ distinct demo pages are tested with interaction (click/select/fill)
    - Each test verifies a DOM change (aria-pressed toggle, button text, select value)
    - Mobile viewport suite uses `{ width: 390, height: 844 }`
    - Zero `waitForTimeout` calls in the file
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "waitForTimeout" /Users/seth/Documents/GitHub/ez-audio/e2e/` returns zero results
2. `grep -c "test(" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` shows 7+
3. `grep -n "viewport" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` shows mobile config
4. `grep -n "aria-pressed\|inputValue\|textContent\|isVisible" /Users/seth/Documents/GitHub/ez-audio/e2e/interactions.spec.ts` shows DOM verification calls
5. File passes TypeScript check: `pnpm typecheck` (Playwright types inferred, no explicit type errors expected)
</verification>

<success_criteria>
- All `waitForTimeout` calls gone from all 3 E2E files
- At least 5 demo pages covered with interaction tests in interactions.spec.ts
- Every test in interactions.spec.ts asserts a DOM change (not just error absence)
- Mobile viewport test block exists with `{ width: 390, height: 844 }`
- Zero `waitForTimeout` calls in any e2e spec file
</success_criteria>

<output>
After completion, create `.planning/phases/31-e2e-integration-test-expansion/31-01-SUMMARY.md`
</output>
