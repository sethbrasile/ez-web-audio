---
phase: 02-adsr-envelopes
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/envelope.ts
  - src/envelope.test.ts
autonomous: true

must_haves:
  truths:
    - "Rapid retriggering does not cause audible clicks"
    - "Envelope picks up from current value when retriggered during active envelope"
    - "Envelope works with existing onPlaySet/onPlayRamp API"
    - "cancelAndHoldAtTime used when available, fallback for other browsers"
  artifacts:
    - path: "src/envelope.ts"
      provides: "Envelope with retriggering support"
      contains: "cancelAndHoldAtTime|estimateCurrentValue"
    - path: "src/envelope.test.ts"
      provides: "Retriggering tests"
      min_lines: 120
  key_links:
    - from: "src/envelope.ts"
      to: "AudioParam"
      via: "retriggering methods"
      pattern: "cancelAndHoldAtTime|cancelScheduledValues"
---

<objective>
Implement clickless retriggering for the Envelope class using TDD.

Purpose: When a user rapidly plays notes (fast arpeggios, drum rolls), the envelope must pick up from its current value rather than jumping to zero, preventing audible clicks and pops. This is critical for professional-quality synthesis.

Output: Envelope class that handles retriggering smoothly with browser feature detection and fallback.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adsr-envelopes/02-RESEARCH.md
@.planning/phases/02-adsr-envelopes/02-01-SUMMARY.md

Key research insights:
- cancelAndHoldAtTime: Chrome/Edge only, holds value at cancellation point
- Fallback: cancelScheduledValues + manual estimation + setValueAtTime
- Manual estimation uses linear interpolation based on envelope phase
- Time tracking needed: attackStartTime, attackStartValue, isActive flag
</context>

<feature>
  <name>Envelope retriggering without clicks</name>
  <files>src/envelope.ts, src/envelope.test.ts</files>
  <behavior>
    Extend Envelope class with retriggering support:

    State tracking (private):
    - isActive: boolean = false
    - attackStartTime: number = 0
    - attackStartValue: number = 0
    - hasCancelAndHold: boolean (feature detection)

    Modified applyTo(gainParam: AudioParam, startTime: number): void
    - If isActive (retriggering):
      - Use cancelAndHoldAtTime if available
      - Otherwise: cancelScheduledValues + estimateCurrentValue + setValueAtTime
      - Attack ramps from current value (not zero) to peak
    - If not active (first trigger):
      - Start from zero as before
    - Update state: isActive = true, attackStartTime, attackStartValue

    New method estimateCurrentValue(startTime: number): number
    - If in attack phase: linear interpolate between attackStartValue and 1
    - If in decay phase: linear interpolate between 1 and sustainLevel
    - If in sustain phase: return sustainLevel
    - Handles edge cases (time before attack start, etc.)

    Modified release(): void
    - Set isActive = false after release

    Test cases (TDD):
    - First trigger starts from zero
    - Retrigger during attack picks up current value (not zero)
    - Retrigger during decay picks up current value
    - Retrigger during sustain picks up sustain level
    - Release sets isActive to false
    - Feature detection for cancelAndHoldAtTime
    - Fallback path (manual calculation) works correctly
    - estimateCurrentValue returns correct values for each phase
  </behavior>
  <implementation>
    1. RED: Write failing tests for retriggering scenarios
       - Test isActive state management
       - Test estimateCurrentValue for each phase
       - Test that retrigger during active envelope doesn't start at zero
       - Test cancelAndHoldAtTime feature detection

    2. GREEN: Implement retriggering logic
       - Add state tracking properties
       - Add feature detection in constructor
       - Modify applyTo to handle retriggering
       - Implement estimateCurrentValue with phase detection
       - Update release to reset isActive

    3. REFACTOR: Clean up estimation logic
       - Extract phase detection helper
       - Ensure consistent time calculations
  </implementation>
</feature>

<verification>
```bash
# Run all envelope tests
pnpm test src/envelope.test.ts

# Type check
pnpm typecheck

# Verify retriggering implementation
grep -q "isActive" src/envelope.ts && echo "State tracking added"
grep -q "estimateCurrentValue" src/envelope.ts && echo "Estimation method added"
grep -q "cancelAndHoldAtTime\|cancelScheduledValues" src/envelope.ts && echo "Cancellation implemented"
```
</verification>

<success_criteria>
1. All envelope tests pass including new retriggering tests
2. TypeScript compiles without errors
3. estimateCurrentValue correctly calculates value for attack/decay/sustain phases
4. Feature detection for cancelAndHoldAtTime implemented
5. Fallback path uses cancelScheduledValues + setValueAtTime
6. isActive state properly managed across trigger/release cycles
</success_criteria>

<output>
After completion, create `.planning/phases/02-adsr-envelopes/02-03-SUMMARY.md`
</output>
