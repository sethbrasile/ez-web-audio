---
phase: 39-documentation-code-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/utilities.md
  - docs/examples/visualization.md
  - docs/examples/react-integration.md
  - src/index.ts
  - README.md
  - docs/guide/concepts.md
autonomous: true
requirements: [HI2, HI3, M9, M11]

must_haves:
  truths:
    - "Every createAnalyzer() call in docs and JSDoc uses await"
    - "React oscillator example uses type property, not waveType"
    - "README Node.js version matches package.json engines field (>=18)"
    - "Noise generation docs accurately describe Sound instance (not auto-looped)"
  artifacts:
    - path: "docs/guide/utilities.md"
      provides: "Corrected createAnalyzer with await"
      contains: "await createAnalyzer"
    - path: "docs/examples/visualization.md"
      provides: "Corrected createAnalyzer with await"
      contains: "await createAnalyzer"
    - path: "docs/examples/react-integration.md"
      provides: "Corrected oscillator option name"
      contains: "type: 'sine'"
    - path: "src/index.ts"
      provides: "Corrected JSDoc createAnalyzer example"
      contains: "await createAnalyzer"
    - path: "README.md"
      provides: "Correct Node.js version"
      contains: "Node.js 18+"
    - path: "docs/guide/concepts.md"
      provides: "Accurate noise description"
      contains: "1-second"
  key_links:
    - from: "docs/guide/utilities.md"
      to: "src/index.ts"
      via: "createAnalyzer async signature"
      pattern: "await createAnalyzer"
---

<objective>
Fix broken code examples and inaccurate claims in docs, JSDoc, and README (non-landing-page files).

Purpose: Users copying code examples from these pages would get runtime errors or use nonexistent APIs. Fix before anyone encounters them.
Output: Corrected documentation across 6 files.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/guide/utilities.md
@docs/examples/visualization.md
@docs/examples/react-integration.md
@src/index.ts
@README.md
@docs/guide/concepts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix createAnalyzer missing await in docs and JSDoc</name>
  <files>docs/guide/utilities.md, docs/examples/visualization.md, src/index.ts</files>
  <action>
HI2 fix: `createAnalyzer()` is async (returns `Promise<Analyzer>`) but shown without `await` in multiple locations.

1. **docs/guide/utilities.md line 234** — Change:
   ```
   const analyzer = createAnalyzer({ fftSize: 2048 })
   ```
   to:
   ```
   const analyzer = await createAnalyzer({ fftSize: 2048 })
   ```

2. **docs/examples/visualization.md line 57** — Change:
   ```
   const analyzer = createAnalyzer(audioContext, { fftSize: 1024 })
   ```
   to:
   ```
   const analyzer = await createAnalyzer(audioContext, { fftSize: 1024 })
   ```

3. **docs/examples/visualization.md line 148** — Change:
   ```
   const analyzer = createAnalyzer(audioContext, { fftSize: 2048 })
   ```
   to:
   ```
   const analyzer = await createAnalyzer(audioContext, { fftSize: 2048 })
   ```

4. **src/index.ts JSDoc example** (around line 498) — Change the context-free example:
   ```
   * const analyzer = createAnalyzer({ fftSize: 1024 })
   ```
   to:
   ```
   * const analyzer = await createAnalyzer({ fftSize: 1024 })
   ```
   And the explicit-context example:
   ```
   * const analyzer2 = createAnalyzer(ctx, { fftSize: 2048 })
   ```
   to:
   ```
   * const analyzer2 = await createAnalyzer(ctx, { fftSize: 2048 })
   ```
  </action>
  <verify>
    <automated>grep -n "createAnalyzer" docs/guide/utilities.md docs/examples/visualization.md src/index.ts | grep -v "await\|import\|export\|function\|async\|overload\|Creates\|Create an\|API\|connect\|//\|setAnalyzer\|\*\s*-" | head -20</automated>
    <manual>Every createAnalyzer() usage in code examples should have await prefix</manual>
  </verify>
  <done>All createAnalyzer() calls in docs and JSDoc examples use await, matching the async function signature</done>
</task>

<task type="auto">
  <name>Task 2: Fix React waveType, README Node.js version, and noise docs</name>
  <files>docs/examples/react-integration.md, README.md, docs/guide/concepts.md</files>
  <action>
Three independent fixes:

1. **HI3 — docs/examples/react-integration.md line 130**: Change:
   ```
   oscRef.current = await createOscillator({ frequency: 440, waveType: 'sine' })
   ```
   to:
   ```
   oscRef.current = await createOscillator({ frequency: 440, type: 'sine' })
   ```
   The correct property name is `type`, not `waveType`. The `OscillatorOptions` interface uses `type`.

2. **M9 — README.md line 85**: Change:
   ```
   This package is ESM-only. It requires Node.js 16+
   ```
   to:
   ```
   This package is ESM-only. It requires Node.js 18+
   ```
   Must match `package.json` engines field which says `>=18`.

3. **M11 — docs/guide/concepts.md line 247**: Change:
   ```
   All noise types return a looped `Sound` instance.
   ```
   to:
   ```
   All noise types return a 1-second `Sound` instance. Set `.loop = true` for continuous playback.
   ```
   The `createNoise()` function does NOT set `loop = true` automatically. The example above (lines 243-244) already shows manually setting `pink.loop = true`, which is correct — update the prose to match.
  </action>
  <verify>
    <automated>grep -n "waveType" docs/examples/react-integration.md; grep -n "Node.js 16" README.md; grep -n "looped \`Sound\`" docs/guide/concepts.md</automated>
    <manual>All three greps should return no matches (the old incorrect text is gone)</manual>
  </verify>
  <done>React example uses `type: 'sine'`, README says Node.js 18+, noise docs say "1-second Sound instance" with manual loop instruction</done>
</task>

</tasks>

<verification>
1. `grep -rn "waveType" docs/` returns no matches
2. `grep -rn "Node.js 16" README.md` returns no matches
3. `grep -c "await createAnalyzer" docs/guide/utilities.md docs/examples/visualization.md src/index.ts` shows counts > 0 for each file
4. `grep "looped \`Sound\`" docs/guide/concepts.md` returns no matches
</verification>

<success_criteria>
- All createAnalyzer() examples use await
- React oscillator uses `type`, not `waveType`
- README Node.js version is 18+
- Noise docs accurately describe non-looped Sound instance
</success_criteria>

<output>
After completion, create `.planning/phases/39-documentation-code-correctness/39-01-SUMMARY.md`
</output>
