---
phase: 54.1-effects-and-lfo-deep-review-fixes
plan: 04
type: execute
wave: 3
depends_on:
  - 54.1-01
  - 54.1-02
  - 54.1-03
files_modified:
  - src/lfo.test.ts
  - src/effects/base-effect.test.ts
  - src/effects/reverb-effect.test.ts
autonomous: true
requirements:
  - L8

must_haves:
  truths:
    - "disconnect() with no args removes all LFO connections"
    - "getParam() delegates to getAudioParam() on all effects"
    - "Convolution reverb from URL path works (mock fetch)"
    - "Zero/NaN/negative edge cases tested on all effect setters"
  artifacts:
    - path: "src/lfo.test.ts"
      provides: "disconnect-all test, edge case tests"
    - path: "src/effects/base-effect.test.ts"
      provides: "getParam() delegation test"
    - path: "src/effects/reverb-effect.test.ts"
      provides: "Convolution URL path test"
  key_links: []
---

<objective>
Close remaining test coverage gaps (Group 5 from deep review).

Purpose: The deep review identified untested code paths that could mask regressions. This plan adds targeted tests for disconnect-all, getParam delegation, convolution URL path, and edge cases.

Output: Comprehensive test coverage for all deep review fixes and previously untested paths.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.1-effects-and-lfo-deep-review-fixes/54.1-RESEARCH.md

@src/lfo.ts
@src/lfo.test.ts
@src/effects/base-effect.ts
@src/effects/base-effect.test.ts
@src/effects/reverb-effect.ts
@src/effects/reverb-effect.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test coverage for disconnect-all, getParam delegation, and convolution URL</name>
  <files>src/lfo.test.ts, src/effects/base-effect.test.ts, src/effects/reverb-effect.test.ts</files>
  <action>
1. **src/lfo.test.ts** — Add test for `disconnect()` with no arguments:
   ```typescript
   it('disconnect() with no args removes all connections', () => {
     // Connect LFO to multiple targets/params
     // Call lfo.disconnect() with no args
     // Verify all connections are removed (lfo has 0 connections)
     // Verify depthGain nodes are disconnected
   })
   ```

2. **src/effects/base-effect.test.ts** — Add test for `getParam()` delegation:
   ```typescript
   it('getParam() delegates to getAudioParam()', () => {
     // Create a concrete effect (e.g., DelayEffect)
     // Call getParam('time') — should return the delay time AudioParam
     // Call getParam('nonexistent') — should return null
   })
   ```

3. **src/effects/reverb-effect.test.ts** — Add test for convolution reverb from URL:
   ```typescript
   it('creates convolution reverb from URL string', async () => {
     // Mock global fetch to return a valid ArrayBuffer (impulse response)
     // Create reverb with string URL: new ReverbEffect(audioContext, '/impulse.wav')
     //   or however the convolution constructor path works
     // Verify the convolver node has a buffer set
   })
   ```
   NOTE: Read the ReverbEffect constructor to understand how URL-based convolution is created. If it uses `fetch()`, mock `globalThis.fetch`. If it uses a different pattern, adapt the test accordingly.

4. **Edge case tests across effect test files** — Add tests for:
   - Zero values on setters (where relevant and not already covered by Plan 03)
   - Boundary values (e.g., compressor ratio=1, ratio=20, attack=0, attack=1)
   - Confirm these tests pass with the clamping added in Plan 03

5. Run the full test suite to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm test</automated>
  </verify>
  <done>All L8 test gaps closed. disconnect-all, getParam delegation, convolution URL, and edge cases all have test coverage. Full suite passes.</done>
</task>

</tasks>

<verification>
1. `pnpm test` — full suite passes with new tests
2. New test count: at least 6 new tests added across the 3 test files
</verification>

<success_criteria>
- disconnect() with no args has dedicated test
- getParam() delegation has dedicated test
- Convolution reverb from URL path has dedicated test (with mock fetch)
- Edge cases (zero, boundary, NaN) tested on all effect setters
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/54.1-effects-and-lfo-deep-review-fixes/54.1-04-SUMMARY.md`
</output>
