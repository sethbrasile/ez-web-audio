---
phase: 17-dependency-security-upgrades
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, pnpm-lock.yaml, eslint.config.js]
autonomous: true
requirements: [SEC-04]

must_haves:
  truths:
    - "ESLint runs without errors on the codebase"
    - "No new lint violations are introduced by the upgrade"
  artifacts:
    - path: "package.json"
      provides: "Updated eslint and @antfu/eslint-config versions"
      contains: "eslint.*10"
    - path: "eslint.config.js"
      provides: "ESLint flat config compatible with v10 and @antfu/eslint-config v7"
  key_links:
    - from: "eslint.config.js"
      to: "@antfu/eslint-config"
      via: "default import"
      pattern: "import antfu from"
---

<objective>
Upgrade eslint from 9.x to 10.x and @antfu/eslint-config from 2.x to 7.x.

Purpose: Keep the linting stack current. @antfu/eslint-config 7.x may require eslint 10.x as a peer dependency and may change rule defaults.
Output: Working lint setup with eslint 10 and @antfu/eslint-config 7
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ESLint and @antfu/eslint-config</name>
  <files>package.json, pnpm-lock.yaml, eslint.config.js</files>
  <action>
Step 1 — Upgrade both packages:
```bash
pnpm add -D eslint@latest @antfu/eslint-config@latest
```

Step 2 — Check eslint.config.js for API changes:
The current config is minimal (`import antfu from '@antfu/eslint-config'` with `antfu({ ignores, type })`). The @antfu/eslint-config v7 API should be backwards-compatible with this simple usage. If the import or API changes, check the @antfu/eslint-config changelog.

Step 3 — Run lint to check for issues:
```bash
pnpm lint 2>&1 | tail -50
```

Step 4 — If there are NEW lint violations (not pre-existing):
- If the violations are auto-fixable: run `pnpm lint:fix`
- If violations are from new rules that don't fit the project (unlikely with `type: 'lib'`): add rule overrides in eslint.config.js:
  ```js
  export default antfu({
    ignores: ['src/app/public'],
    type: 'lib',
    rules: {
      // Add any rule overrides needed here
    },
  })
  ```
- If the new version changes the config API: adapt eslint.config.js accordingly (check @antfu/eslint-config README for v7 migration)

Step 5 — Verify lint passes cleanly:
```bash
pnpm lint
```
  </action>
  <verify>
```bash
pnpm lint   # Exits 0 with no errors
```
  </verify>
  <done>
- eslint upgraded to 10.x
- @antfu/eslint-config upgraded to 7.x
- `pnpm lint` passes without errors
- eslint.config.js updated if API changes required it
  </done>
</task>

</tasks>

<verification>
1. `pnpm lint` exits with code 0
2. package.json shows eslint 10.x and @antfu/eslint-config 7.x
</verification>

<success_criteria>
- ESLint 10 + @antfu/eslint-config 7 installed and working
- All source files pass lint
</success_criteria>

<output>
After completion, create `.planning/phases/17-dependency-security-upgrades/17-02-SUMMARY.md`
</output>
