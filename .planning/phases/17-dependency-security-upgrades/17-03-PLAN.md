---
phase: 17-dependency-security-upgrades
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified: [package.json, pnpm-lock.yaml, tsconfig.json]
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "TypeScript 5.9 compiles the entire codebase without errors"
    - "pnpm audit reports zero vulnerabilities"
    - "All tests pass, build succeeds, and lint passes after all upgrades"
  artifacts:
    - path: "package.json"
      provides: "TypeScript 5.9 version"
      contains: "typescript.*5.9"
  key_links:
    - from: "tsconfig.json"
      to: "typescript"
      via: "compiler options"
      pattern: "compilerOptions"
---

<objective>
Upgrade TypeScript from 5.6 to 5.9, then run full verification of the entire upgraded stack.

Purpose: Complete the dependency security upgrades by bringing TypeScript current. TS 5.9 may introduce new strictness checks or deprecate options. This plan depends on plans 01 and 02 so it can run a final comprehensive verification across the entire stack.
Output: Fully upgraded, vulnerability-free dependency stack with all tests, build, and lint passing
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@tsconfig.json
@tsconfig.test.json
@tsconfig.typedoc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade TypeScript to 5.9</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json</files>
  <action>
Step 1 — Upgrade TypeScript:
```bash
pnpm add -D typescript@latest
```

Step 2 — Run type checking:
```bash
pnpm typecheck
```

If type errors appear:
- TS 5.9 may flag new issues with strict mode. Fix type errors in source files.
- Check if any tsconfig.json options are deprecated in 5.9. If so, update or remove them.
- Common TS 5.7+ changes: `isolatedDeclarations` is available but not required, `erasableSyntaxOnly` is new but opt-in.

Step 3 — Run full verification suite:
```bash
pnpm test -- --run   # All unit tests
pnpm build:lib        # Library build
pnpm lint             # Linting
pnpm audit            # Security audit
```

All four must pass. If any fail, triage and fix.

Step 4 — If `pnpm audit` still shows any vulnerabilities after all plan 01/02/03 upgrades, document which ones remain and whether they are in direct deps or deep transitive deps that cannot be controlled.
  </action>
  <verify>
Run the complete verification:
```bash
pnpm typecheck        # Zero type errors
pnpm test -- --run    # All 893+ tests pass
pnpm build:lib         # Build succeeds
pnpm lint              # Zero lint errors
pnpm audit             # Zero vulnerabilities (or only unfixable transitive)
```
  </verify>
  <done>
- TypeScript upgraded to 5.9.x
- `pnpm typecheck` passes with zero errors
- All unit tests pass
- Library builds successfully
- Lint passes
- `pnpm audit` reports zero vulnerabilities (phase success criterion #1)
  </done>
</task>

</tasks>

<verification>
1. `pnpm audit` reports zero critical/moderate vulnerabilities
2. `pnpm test -- --run` passes all tests
3. `pnpm build:lib` succeeds
4. `pnpm typecheck` reports zero errors
5. `pnpm lint` reports zero errors
</verification>

<success_criteria>
- The project runs on a fully up-to-date, vulnerability-free dependency stack with all tests passing (phase goal)
- TypeScript 5.9 installed and compiling cleanly
- Complete stack verification passes: tests + build + lint + audit
</success_criteria>

<output>
After completion, create `.planning/phases/17-dependency-security-upgrades/17-03-SUMMARY.md`
</output>
