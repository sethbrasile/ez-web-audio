# 78-01: Release 0.2.0 + Announce Prep

> **For agentic workers:** Execute per `.planning/M8-PLAYBOOK.md`. **Blocked on:** HUMAN GATE 3 — full UAT passed (Seth, all demos + groovebox + both binding packages sanity). Contains HUMAN GATE 4 — nothing publishes without it.

**Goal:** ez-web-audio 0.2.0 + @ez-web-audio/vue 0.2.0 + @ez-web-audio/react 0.2.0 live on npm; README/CHANGELOG/social assets current; announcement drafts ready for Seth to post.

## Global Constraints

- NEVER publish, tag, or push without explicit human approval (gate 4). Tag push IS the publish trigger.
- Version stays 0.2.0 everywhere. No 1.0 language anywhere ("early but tested" honesty).
- Announcement angles (spec): Howler manifest migration wedge, llms.txt AI-discoverability, interactive demos + groovebox.

---

### Task 1: CHANGELOG

**Files:**
- Modify/Create: `CHANGELOG.md`

- [ ] **Step 1:** Reconstruct user-facing changes since npm 0.1.0 from `.planning/MILESTONES.md` + `milestones/*-MILESTONE-AUDIT.md` + git log. Sections for 0.2.0: **Added** (effects suite, LFO, Transport/Sequencer, PolySynth, GrainPlayer, audio sprites redesign + Howler manifests, multiple AudioContext support, llms.txt, `@ez-web-audio/vue`, `@ez-web-audio/react`, 16+ new demos incl. groovebox), **Changed** (breaking API cleanup from M3 Phase 18 — list each break), **Fixed** (headline items only). Keep-a-Changelog format.
- [ ] **Step 2:** Commit — `docs: CHANGELOG for 0.2.0`.

### Task 2: README + docs final pass

**Files:**
- Modify: `README.md`, `packages/vue/README.md`, `packages/react/README.md`, `docs/index.md`

- [ ] **Step 1:** Root README: feature list current (all M5 features + bindings), Quick Start verified against actual API (run the snippets), bindings section linking sub-package READMEs, badges intact, a "Migrating from Howler?" subsection (manifest auto-detection example), and an "AI-friendly docs" line (llms.txt URL).
- [ ] **Step 2:** Verify homepage messaging matches (groovebox hero + CTAs from 76-02, no stale version claims).
- [ ] **Step 3:** Commit — `docs: README and homepage release pass`.

### Task 3: Demo GIFs + og-image

**Files:**
- Create: `docs/public/social/` assets; Modify: README (embed groovebox GIF), og-image reference in `docs/.vitepress/config.mts` if replaced.

- [ ] **Step 1:** Playwright script (scratch, this phase dir): record groovebox playing (preset switch + playhead) via Playwright video; convert to GIF with system ffmpeg (`ffmpeg -i in.webm -vf "fps=12,scale=720:-1" out.gif`). If ffmpeg absent → `M8-QUESTIONS.md`, skip GIF, keep PNG screenshots.
- [ ] **Step 2:** Screenshots (light+dark) of groovebox + 2 best demos for social/og use. Update og-image only if the groovebox shot is clearly better than current.
- [ ] **Step 3:** Commit — `docs: social assets for 0.2.0 announcement`.

### Task 4: Announcement drafts

**Files:**
- Create: `.planning/phases/78-release-0.2.0-announce/ANNOUNCEMENTS.md`

- [ ] **Step 1:** Drafts (Seth posts; write in his register — technical, plain, no hype):
  - **Show HN:** title ≤80 chars (e.g. `Show HN: EZ Web Audio – a zero-dependency TypeScript wrapper for the Web Audio API`), body: what/why, groovebox link first, honest 0.x framing, Howler-migration + llms.txt angles, tech notes (1,900+ tests, tree-shakeable ESM, Vue/React bindings).
  - **Reddit:** r/webdev + r/javascript variants (subreddit-appropriate tone, demo link first).
  - **Web Audio community** (Slack/Discord): short, peer-technical.
  - Each: link to groovebox demo, docs, GitHub; no more than one emoji total; URLs on own lines.
- [ ] **Step 2:** Commit drafts.

### Task 5: HUMAN GATE 4 — publish

- [ ] **Step 1: Preflight (orchestrator):** `pnpm typecheck && pnpm lint && pnpm test --run && pnpm build && pnpm test:e2e`; `pnpm -r publish --dry-run --access public --no-git-checks` lists exactly 3 packages at 0.2.0; `git status` clean.
- [ ] **Step 2: STOP. Report to Seth:** preflight results + reminders: (a) create npm org `ez-web-audio` + grant publish rights / verify `NPM_TOKEN` secret covers scoped packages, (b) approve push of all M8 commits, (c) approve tag.
- [ ] **Step 3: On explicit approval only:** `git push`, `git tag v0.2.0 && git push origin v0.2.0`. Watch the publish workflow (`gh run watch`). Verify: `npm view ez-web-audio version` → `0.2.0`; `npm view @ez-web-audio/vue version`; `npm view @ez-web-audio/react version`.
- [ ] **Step 4: Post-publish checks:** docs site deployed (GitHub Pages workflow), StackBlitz embed now resolves (77-01 Task 4 note), install smoke test in a temp dir: `npm i ez-web-audio@0.2.0 @ez-web-audio/react@0.2.0` + import check.
- [ ] **Step 5:** Seth posts announcements (his action; drafts ready).

### Exit

- [ ] `78-01-SUMMARY.md`. Run `/gsd-complete-milestone` for M8 (archives phases, REQUIREMENTS, updates MILESTONES.md). Update STATE.md to post-release position.
