---
phase: 35-documentation-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - docs/examples/audio-sprite.md
  - docs/examples/layered-sound.md
  - docs/examples/crossfade.md
  - docs/.vitepress/theme/components/AudioSpriteDemo.vue
  - docs/.vitepress/theme/components/LayeredSoundDemo.vue
  - docs/.vitepress/theme/components/CrossfadeDemo.vue
  - docs/.vitepress/theme/index.ts
  - docs/.vitepress/config.mts
  - docs/examples/index.md
autonomous: true
requirements:
  - DOC2-01
  - DOC2-02
  - DOC2-03
  - DOC2-08

must_haves:
  truths:
    - "AudioSprite example page exists with interactive Vue demo showing sprite loading, named segment playback"
    - "LayeredSound example page exists with interactive Vue demo showing synchronized multi-layer playback"
    - "Crossfade example page exists with interactive Vue demo showing smooth transition between two tracks"
    - "All three new example pages have <script setup> imports for their Vue components"
    - "All three new pages appear in VitePress sidebar and examples index"
  artifacts:
    - path: "docs/examples/audio-sprite.md"
      provides: "AudioSprite interactive example page"
    - path: "docs/examples/layered-sound.md"
      provides: "LayeredSound interactive example page"
    - path: "docs/examples/crossfade.md"
      provides: "Crossfade interactive example page"
    - path: "docs/.vitepress/theme/components/AudioSpriteDemo.vue"
      provides: "AudioSprite Vue demo component"
    - path: "docs/.vitepress/theme/components/LayeredSoundDemo.vue"
      provides: "LayeredSound Vue demo component"
    - path: "docs/.vitepress/theme/components/CrossfadeDemo.vue"
      provides: "Crossfade Vue demo component"
  key_links:
    - from: "docs/examples/audio-sprite.md"
      to: "docs/.vitepress/theme/components/AudioSpriteDemo.vue"
      via: "script setup import"
      pattern: "import AudioSpriteDemo"
    - from: "docs/.vitepress/config.mts"
      to: "docs/examples/audio-sprite.md"
      via: "sidebar link"
      pattern: "audio-sprite"
---

<objective>
Create three new interactive example pages — AudioSprite, LayeredSound, and Crossfade — each with a Vue demo component demonstrating the feature.

Purpose: Fill gaps where significant library features have no interactive examples.
Output: Three new example pages with working Vue demo components, all linked from sidebar and examples index.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-documentation-expansion/35-01-SUMMARY.md
@docs/.vitepress/theme/index.ts
@docs/.vitepress/config.mts
@docs/examples/index.md
@docs/guide/concepts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AudioSprite, LayeredSound, and Crossfade Vue demo components</name>
  <files>
    docs/.vitepress/theme/components/AudioSpriteDemo.vue
    docs/.vitepress/theme/components/LayeredSoundDemo.vue
    docs/.vitepress/theme/components/CrossfadeDemo.vue
    docs/.vitepress/theme/index.ts
  </files>
  <action>
Create three Vue 3 demo components following existing demo patterns (see AudioDemo.vue, FilterDemo.vue for reference).

**AudioSpriteDemo.vue:**
- Init button that creates a sprite using `createSprite()` with the existing `/ez-web-audio/audio/` assets
- Since we may not have a dedicated sprite audio file, use a creative approach: create a sprite from one of the existing longer audio files (e.g., a drum loop) and define named segments within it
- Alternatively, demonstrate the concept by loading an audio file and defining regions manually
- Show buttons for each named sprite segment (e.g., "Click", "Hover", "Success")
- Each button calls `sprite.play('name')` on click
- Show a "Loop" toggle for one of the sprites demonstrating `sprite.play('name', { loop: true })` and `sprite.stop('name')`
- Use `<script setup lang="ts">` with `ref` for loading state
- Style with scoped CSS matching other demo components' visual style (simple buttons, clean layout)
- If no suitable long audio file exists in the assets, create the sprite from the existing kick/snare/hihat samples by combining them conceptually, or use `createSprite` with a URL to one of the existing mp3 files with artificial sprite regions based on its duration

**LayeredSoundDemo.vue:**
- Init button that loads 2-3 sounds using `createSound()`
- A "Play All" button that calls `createLayeredSound()` or `playTogether()` to play them synchronized
- Individual play/stop buttons for each layer
- Gain slider for each layer
- Show which layers are currently playing
- Use existing audio assets from `/ez-web-audio/audio/`

**CrossfadeDemo.vue:**
- Init button that loads two tracks using `createTrack()`
- "Play Track A" and "Play Track B" buttons
- A "Crossfade A→B" button that calls `crossfade(trackA, trackB, duration)`
- A duration slider (1-5 seconds)
- Show which track is currently playing
- Display track position/progress during crossfade
- Use existing audio assets

**Register all three components** in `docs/.vitepress/theme/index.ts`:
- Add imports for AudioSpriteDemo, LayeredSoundDemo, CrossfadeDemo
- Add `app.component()` registrations

Note: Check existing audio assets available at `docs/public/audio/` or similar paths. Reference other demo components for the correct audio file paths. If specific audio files are not available, the demos should degrade gracefully with a "Load audio" interaction pattern.
  </action>
  <verify>
```bash
ls docs/.vitepress/theme/components/AudioSpriteDemo.vue docs/.vitepress/theme/components/LayeredSoundDemo.vue docs/.vitepress/theme/components/CrossfadeDemo.vue
```
All three files exist.
```bash
grep -c 'AudioSpriteDemo\|LayeredSoundDemo\|CrossfadeDemo' docs/.vitepress/theme/index.ts
```
Should return 6+ (3 imports + 3 registrations).
  </verify>
  <done>
Three Vue demo components created and registered in the VitePress theme. Each follows existing demo patterns with init-on-interaction, loading states, and interactive controls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create example pages and update sidebar + index</name>
  <files>
    docs/examples/audio-sprite.md
    docs/examples/layered-sound.md
    docs/examples/crossfade.md
    docs/.vitepress/config.mts
    docs/examples/index.md
  </files>
  <action>
**Create docs/examples/audio-sprite.md:**
- Frontmatter: title "AudioSprite - Named Sound Regions from a Single File", SEO description
- `<script setup>` import for AudioSpriteDemo component
- Brief intro explaining what AudioSprite is (pack multiple sounds into one file, reduce HTTP requests)
- `<AudioSpriteDemo />` component embed
- Code section showing `createSprite()` usage with spritemap definition
- Code section showing `sprite.play('name')`, loop, and `sprite.stop('name')`
- "You'll learn" list: sprite loading, named segment playback, looping sprites, reducing HTTP requests
- "Next Steps" links

**Create docs/examples/layered-sound.md:**
- Frontmatter: title "LayeredSound - Synchronized Multi-Layer Playback", SEO description
- `<script setup>` import for LayeredSoundDemo component
- Brief intro explaining LayeredSound (play multiple sounds in perfect sync, control as a group)
- `<LayeredSoundDemo />` component embed
- Code section showing `createLayeredSound()` usage
- Code showing `layer.play()`, `layer.setGain()`, individual layer control
- "You'll learn" list
- "Next Steps" links

**Create docs/examples/crossfade.md:**
- Frontmatter: title "Crossfade - Smooth Transitions Between Tracks", SEO description
- `<script setup>` import for CrossfadeDemo component
- Brief intro explaining equal-power crossfade
- `<CrossfadeDemo />` component embed
- Code section showing `crossfade(trackA, trackB, duration)` usage
- Explain equal-power curve and why it sounds better than linear
- "You'll learn" list
- "Next Steps" links

**Update docs/.vitepress/config.mts sidebar:**
Add new entries to the `/examples/` sidebar. Add a new section or integrate into existing sections:
- Add "Audio Sprite" under a new "Composition" or "Advanced" section, or under existing "Sampling" section
- Add "LayeredSound" nearby
- Add "Crossfade" nearby
Good grouping: Add a new section called "Composition" with AudioSprite, LayeredSound, Crossfade.

**Update docs/examples/index.md:**
Add entries for the three new example pages. Place them in appropriate sections:
- AudioSprite under "Sampling" or a new "Composition" section
- LayeredSound under a new "Composition" section
- Crossfade under a new "Composition" section

Follow the existing format: heading link, description paragraph, "You'll learn" bullet list.
  </action>
  <verify>
```bash
ls docs/examples/audio-sprite.md docs/examples/layered-sound.md docs/examples/crossfade.md
```
All three files exist.
```bash
grep -c 'audio-sprite\|layered-sound\|crossfade' docs/.vitepress/config.mts
```
Should return 3+ (sidebar links).
```bash
grep -c 'audio-sprite\|layered-sound\|crossfade' docs/examples/index.md
```
Should return 3+ (index links).
```bash
grep 'script setup' docs/examples/audio-sprite.md docs/examples/layered-sound.md docs/examples/crossfade.md
```
All three have `<script setup>` blocks.
  </verify>
  <done>
Three example pages exist with `<script setup>` imports, appear in VitePress sidebar and examples index page. Each page has intro text, interactive demo embed, code examples, and "You'll learn" section.
  </done>
</task>

</tasks>

<verification>
1. All 3 Vue components exist in `docs/.vitepress/theme/components/`
2. All 3 components registered in `docs/.vitepress/theme/index.ts`
3. All 3 example pages exist in `docs/examples/`
4. All 3 example pages have `<script setup>` imports
5. VitePress sidebar includes links to all 3 new pages
6. Examples index page includes entries for all 3 new pages
7. `pnpm lint` passes
</verification>

<success_criteria>
- AudioSprite, LayeredSound, and Crossfade each have interactive example pages with Vue demo components
- All pages use `<script setup>` imports for their components
- All pages are linked from sidebar and examples index
- Code examples use current 1.0 API correctly
</success_criteria>

<output>
After completion, create `.planning/phases/35-documentation-expansion/35-02-SUMMARY.md`
</output>
