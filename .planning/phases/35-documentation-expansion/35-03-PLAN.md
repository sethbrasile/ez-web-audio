---
phase: 35-documentation-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - 35-02
files_modified:
  - docs/examples/react-integration.md
  - docs/.vitepress/config.mts
  - docs/examples/index.md
autonomous: true
requirements:
  - DOC2-04
  - DOC2-09
  - DOC2-08

must_haves:
  truths:
    - "React integration example page exists with hooks pattern (useRef, useEffect, useState)"
    - "Examples index page lists Vue Reactive Pattern, Vanilla TS Events, and React Integration under Integration Patterns"
    - "React example page has proper <script setup> import or is pure markdown (no Vue component needed)"
  artifacts:
    - path: "docs/examples/react-integration.md"
      provides: "React integration example with hooks pattern"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "docs/examples/react-integration.md"
      via: "sidebar link"
      pattern: "react-integration"
    - from: "docs/examples/index.md"
      to: "docs/examples/react-integration.md"
      via: "markdown link"
      pattern: "react-integration"
---

<objective>
Create a React integration example page showing how to use ez-web-audio with React hooks, and update the examples index page to list all integration patterns.

Purpose: Show React developers how to integrate the library using idiomatic React patterns, and make integration patterns discoverable.
Output: React integration example page, updated examples index with integration patterns section.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-documentation-expansion/35-02-SUMMARY.md
@docs/examples/index.md
@docs/.vitepress/config.mts
@docs/examples/drum-machine-vue.md
@docs/examples/drum-machine-vanilla.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React integration example page</name>
  <files>
    docs/examples/react-integration.md
  </files>
  <action>
Create `docs/examples/react-integration.md` — a code-only example page (no interactive Vue demo needed since it shows React patterns).

**Frontmatter:** title "React Integration - Hooks Pattern with useRef and useEffect", SEO description about using ez-web-audio with React.

**Content structure:**

1. **Introduction** — Brief explanation that ez-web-audio is framework-agnostic and works with any framework. React integration uses standard hooks patterns.

2. **Basic Sound Playback with React** — Show a simple component:
```tsx
import { useRef, useState } from 'react'
import { createSound } from 'ez-web-audio'
import type { Sound } from 'ez-web-audio'

function SoundButton() {
  const soundRef = useRef<Sound | null>(null)
  const [isLoaded, setIsLoaded] = useState(false)

  async function init() {
    soundRef.current = await createSound('/audio/click.mp3')
    setIsLoaded(true)
  }

  return (
    <div>
      {!isLoaded ? (
        <button onClick={init}>Load Sound</button>
      ) : (
        <button onClick={() => soundRef.current?.play()}>Play</button>
      )}
    </div>
  )
}
```

3. **Track with Progress** — Show a component with position tracking using `useEffect` + `requestAnimationFrame`:
```tsx
function TrackPlayer() {
  const trackRef = useRef<Track | null>(null)
  const [position, setPosition] = useState('0:00')
  const [isPlaying, setIsPlaying] = useState(false)
  const rafRef = useRef<number>(0)

  // Poll position during playback
  useEffect(() => {
    if (!isPlaying || !trackRef.current) return
    function tick() {
      if (trackRef.current) {
        setPosition(trackRef.current.position.string)
      }
      rafRef.current = requestAnimationFrame(tick)
    }
    rafRef.current = requestAnimationFrame(tick)
    return () => cancelAnimationFrame(rafRef.current)
  }, [isPlaying])
  // ... init, play, pause, stop handlers
}
```

4. **Oscillator with Cleanup** — Show `useEffect` cleanup pattern:
```tsx
function Synth() {
  const oscRef = useRef<Oscillator | null>(null)

  useEffect(() => {
    return () => {
      // Cleanup on unmount
      oscRef.current?.stop()
      oscRef.current?.dispose()
    }
  }, [])
  // ... init and play/stop handlers
}
```

5. **Custom Hook Pattern** — Show a reusable `useSound` hook:
```tsx
function useSound(url: string) {
  const soundRef = useRef<Sound | null>(null)
  const [isLoaded, setIsLoaded] = useState(false)

  const load = useCallback(async () => {
    soundRef.current = await createSound(url)
    setIsLoaded(true)
  }, [url])

  const play = useCallback(() => soundRef.current?.play(), [])
  const stop = useCallback(() => soundRef.current?.stop(), [])

  useEffect(() => {
    return () => { soundRef.current?.dispose() }
  }, [])

  return { load, play, stop, isLoaded }
}
```

6. **Key Principles** — Bullet list:
   - Use `useRef` to hold audio instances (not `useState` — audio objects are mutable, not serializable)
   - Initialize audio in event handlers (user interaction required for AudioContext)
   - Clean up with `dispose()` in `useEffect` cleanup functions
   - Poll position with `requestAnimationFrame` rather than `setInterval`
   - Use `useCallback` for stable handler references

7. **Next Steps** links

No `<script setup>` needed — this is pure markdown with code blocks (no interactive demo). DOC2-08 is satisfied because the page doesn't embed a Vue component.
  </action>
  <verify>
```bash
ls docs/examples/react-integration.md
```
File exists.
```bash
grep -c 'useRef\|useEffect\|useState' docs/examples/react-integration.md
```
Should return 5+ matches showing React hooks patterns.
  </verify>
  <done>
React integration example page exists with hooks pattern showing useRef for audio instances, useEffect for cleanup, useState for UI state, and a reusable useSound custom hook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update examples index and sidebar with integration patterns and new pages</name>
  <files>
    docs/examples/index.md
    docs/.vitepress/config.mts
  </files>
  <action>
**Update docs/examples/index.md:**

Add the React Integration entry to the existing "Integration Patterns" section (which is implicitly there via drum-machine-vue and drum-machine-vanilla, but may not be an explicit section in index.md).

Check if there's already an "Integration Patterns" section. If not, add one. The section should list:

```markdown
## Integration Patterns

### [Vue Reactive Pattern](/examples/drum-machine-vue)
Build a drum machine using Vue 3's reactive system for automatic UI updates when beat states change.

**You'll learn:**
- Using `wrapWith: reactive` for Vue reactivity
- Computed properties for beat state
- Watch-based tempo control

### [Vanilla TypeScript Events](/examples/drum-machine-vanilla)
Build a drum machine with vanilla TypeScript using DOM events and manual state management.

**You'll learn:**
- Event-based audio state management
- Manual DOM updates on audio events
- Framework-free audio integration

### [React Integration](/examples/react-integration)
Integrate ez-web-audio into React applications using hooks patterns with useRef, useEffect, and custom hooks.

**You'll learn:**
- Using useRef for audio instance management
- useEffect cleanup for audio disposal
- Custom hooks for reusable audio logic
- Position tracking with requestAnimationFrame
```

Also ensure the three new example pages from Plan 02 (AudioSprite, LayeredSound, Crossfade) are in index.md. Add a "Composition" section if Plan 02 created one, or verify they were already added.

**Update docs/.vitepress/config.mts sidebar:**

Add the React Integration page to the "Integration Patterns" sidebar section:
```
{ text: 'React Integration', link: '/examples/react-integration' },
```
  </action>
  <verify>
```bash
grep -c 'react-integration' docs/examples/index.md
```
Should return 1+ match.
```bash
grep -c 'react-integration' docs/.vitepress/config.mts
```
Should return 1+ match.
```bash
grep -c 'Integration Patterns' docs/examples/index.md
```
Should return 1+ match.
Check that Vue, Vanilla TS, and React integration patterns all appear:
```bash
grep -c 'drum-machine-vue\|drum-machine-vanilla\|react-integration' docs/examples/index.md
```
Should return 3.
  </verify>
  <done>
Examples index page lists all three integration patterns (Vue, Vanilla TS, React) with descriptions. React Integration appears in VitePress sidebar under Integration Patterns.
  </done>
</task>

</tasks>

<verification>
1. `docs/examples/react-integration.md` exists with React hooks patterns
2. `grep 'react-integration' docs/.vitepress/config.mts` returns at least 1 match
3. `grep 'Integration Patterns' docs/examples/index.md` returns at least 1 match
4. All three integration patterns (Vue, Vanilla TS, React) appear in both sidebar and index
5. `pnpm lint` passes
</verification>

<success_criteria>
- React integration example page exists with useRef, useEffect, useState, useCallback patterns
- Examples index page lists all integration patterns including React
- VitePress sidebar includes React Integration link
</success_criteria>

<output>
After completion, create `.planning/phases/35-documentation-expansion/35-03-SUMMARY.md`
</output>
