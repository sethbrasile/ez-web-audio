# Plan 23-03: Fix Minor Issues and Final Validation

**Phase Goal**: All demo components are polished, consistent, and render correctly on HiDPI displays

**Depends on**: 23-02

## Tasks

### Task 1: Fix canvas DPI handling in XYPad

**File**: `docs/.vitepress/theme/components/XYPad.vue`

**action**:
- In `onMounted()` (lines 257-267), after computing `size`:
  - Set `canvas.width = size * (window.devicePixelRatio || 1)`
  - Set `canvas.height = size * (window.devicePixelRatio || 1)`
  - Scale the canvas context: `ctx.scale(window.devicePixelRatio || 1, window.devicePixelRatio || 1)`
  - The CSS already constrains display size via `.canvas-container` max-width and `canvas { width: 100%; height: 100% }`
- In `updateFromPosition()` and `handleMouseDown()` / touch handlers, the `x` and `y` coordinates from mouse/touch events are relative to the CSS display size, which is correct — no changes needed there since the context is scaled
- In `drawGrid()`, all drawing coordinates are in CSS-pixel space (pre-scale), which is correct after the context scale

**verify**: Open the XY pad on a Retina display (or Chrome DevTools device emulation with DPR=2) — lines and text should be crisp, not blurry
**done**: XYPad renders at full device pixel resolution

### Task 2: Fix canvas DPI handling in VisualizationDemo

**File**: `docs/.vitepress/theme/components/VisualizationDemo.vue`

**action**:
- In `setupCanvases()` (lines 29-45):
  - For each canvas, multiply width and height by `window.devicePixelRatio || 1`
  - Scale each canvas context accordingly
  - The CSS already sets `.viz-canvas { width: 100%; height: 200px }` which constrains display size
- In `drawFrequencySpectrum()` and `drawWaveform()`, drawing uses `canvas.width` and `canvas.height` for calculations, which will now be the scaled dimensions — this is correct since the context is scaled
  - Actually, since the drawing functions read `canvas.width/height` directly for bar widths and slice widths, we need to use logical (CSS) dimensions for the drawing math, not the physical pixel dimensions
  - Store logical width/height as data attributes or compute from `canvas.width / dpr`

**verify**: Frequency bars and waveform render crisply on HiDPI displays; bar proportions are unchanged from current behavior
**done**: Visualization canvases render at full device pixel resolution

### Task 3: Fix AmbientGenerator filter property access

**File**: `docs/.vitepress/theme/components/AmbientGenerator.vue`

**action**:
- Line 172: Change `textureFilter.frequency.value = textureFilterCutoff.value` to `textureFilter.frequency = textureFilterCutoff.value`
  - This uses the FilterEffect's public setter instead of reaching into the BiquadFilterNode's AudioParam directly

**verify**: Start ambient generator, toggle texture on, drag filter cutoff slider — filter should respond in real time
**done**: Uses public FilterEffect API consistently

### Task 4: Fix DistortionDemo internal node access

**File**: `docs/.vitepress/theme/components/DistortionDemo.vue`

**action**:
- Line 109: `effect.input.curve` assumes internal structure of EffectWrapper
- Check if EffectWrapper exposes the underlying node via a public property (e.g., `effect.node` or similar)
- If yes, use the public accessor
- If no, the current approach is the only option — add a code comment explaining why internal access is necessary here and that it's specific to WaveShaper curve updates (no public API exists for this use case)

**verify**: Start oscillator, add distortion, drag the amount slider — distortion curve should update in real time
**done**: DistortionDemo uses the cleanest available API for curve updates

### Task 5: Final Validation

**action**:
1. Run `pnpm typecheck` — zero errors
2. Run `pnpm test` — all unit tests pass
3. Run `pnpm build` — library and docs build successfully
4. Grep for remaining issues:
   - `grep -r "\.as('number')" docs/` — should return 0 results
   - `grep -r "wrapEffect(ctx" docs/` — should return 0 results
   - `grep -r "Q:" docs/.vitepress/theme/components/` — should return 0 results (case-sensitive)
   - `grep -r "createAnalyzer(" docs/.vitepress/theme/components/` — all calls should have audioContext as first arg
5. Run `pnpm dev` and manually verify each demo page loads without console errors (spot-check)

**verify**: All automated checks pass, no console errors on demo pages
**done**: Phase 23 complete — all demo example bugs fixed and validated
