---
phase: 23-demo-example-bugfixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/theme/components/XYPad.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "XYPad grid lines, axis labels, and crosshair render within the visible canvas area on HiDPI displays (DPR=2)"
    - "XYPad frequency/gain mapping responds proportionally to mouse position across the full canvas width and height"
  artifacts:
    - path: "docs/.vitepress/theme/components/XYPad.vue"
      provides: "HiDPI-correct XY pad with logical dimension reads"
      contains: "dataset.logicalWidth"
  key_links:
    - from: "XYPad.vue onMounted"
      to: "XYPad.vue drawGrid + updateFromPosition"
      via: "canvas.dataset.logicalWidth/logicalHeight"
      pattern: "dataset\\.logical(Width|Height)"
---

<objective>
Fix XYPad HiDPI canvas rendering bug where drawGrid() and updateFromPosition() read physical pixel dimensions instead of logical CSS dimensions.

Purpose: Close the single remaining gap from Phase 23 verification (success criterion #10). After ctx.scale(dpr, dpr), the drawing coordinate space is in CSS pixels, but canvas.width returns physical pixels, causing grid lines and interaction mapping to be off by a factor of DPR on Retina displays.

Output: XYPad.vue with correct logical dimension reads matching the pattern already used by VisualizationDemo.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/.vitepress/theme/components/XYPad.vue
@docs/.vitepress/theme/components/VisualizationDemo.vue
@.planning/phases/23-demo-example-bugfixes/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix XYPad logical dimension reads for HiDPI correctness</name>
  <files>docs/.vitepress/theme/components/XYPad.vue</files>
  <action>
    In `onMounted`, after setting `canvas.value.width = size * dpr` and `canvas.value.height = size * dpr`, add:
    ```
    canvas.value.dataset.logicalWidth = String(size)
    canvas.value.dataset.logicalHeight = String(size)
    ```

    In `drawGrid()` (lines 45-46), replace:
    ```
    const width = canvas.value.width
    const height = canvas.value.height
    ```
    with:
    ```
    const width = Number(canvas.value.dataset.logicalWidth) || canvas.value.clientWidth
    const height = Number(canvas.value.dataset.logicalHeight) || canvas.value.clientWidth
    ```

    In `updateFromPosition()` (lines 128-129), replace the same pattern:
    ```
    const width = canvas.value.width
    const height = canvas.value.height
    ```
    with:
    ```
    const width = Number(canvas.value.dataset.logicalWidth) || canvas.value.clientWidth
    const height = Number(canvas.value.dataset.logicalHeight) || canvas.value.clientWidth
    ```

    This matches the exact pattern VisualizationDemo.vue uses (lines 40-41, 155-156, 186-187, 225-226). The fallback to clientWidth ensures the canvas still works if dataset attributes are somehow missing.

    Do NOT change any other code in XYPad.vue. The canvas backing buffer setup (size * dpr) and ctx.scale(dpr, dpr) are already correct.
  </action>
  <verify>
    1. `grep -n 'dataset.logicalWidth\|dataset.logicalHeight' docs/.vitepress/theme/components/XYPad.vue` shows 4 matches: 2 writes in onMounted, 1 read each in drawGrid and updateFromPosition.
    2. `grep -n 'canvas.value.width' docs/.vitepress/theme/components/XYPad.vue` shows only the 2 lines in onMounted that SET the backing buffer (size * dpr). Zero reads of canvas.value.width in drawGrid or updateFromPosition.
    3. `pnpm build` succeeds without errors.
  </verify>
  <done>drawGrid() and updateFromPosition() use logical CSS dimensions via dataset attributes. On a DPR=2 display, a 400px canvas stores logicalWidth=400, and drawing coordinates stay in the 0-400 range matching the scaled context, while canvas.width=800 is only used for the backing buffer.</done>
</task>

</tasks>

<verification>
- `grep -c 'canvas.value.width' docs/.vitepress/theme/components/XYPad.vue` returns exactly 2 (only the onMounted backing buffer writes)
- `grep -c 'dataset.logicalWidth' docs/.vitepress/theme/components/XYPad.vue` returns at least 2 (write + read)
- `pnpm build` passes
- The pattern matches VisualizationDemo.vue's approach exactly
</verification>

<success_criteria>
- XYPad canvas drawing functions use logical dimensions (via dataset attributes), not physical pixel dimensions
- Grid lines, axis labels, crosshair, and frequency/gain ratio calculations all operate in CSS pixel coordinate space (0 to size) after ctx.scale(dpr, dpr)
- No regressions: canvas still renders on standard (DPR=1) displays since the fallback matches existing behavior
</success_criteria>

<output>
After completion, create `.planning/phases/23-demo-example-bugfixes/23-04-SUMMARY.md`
</output>
