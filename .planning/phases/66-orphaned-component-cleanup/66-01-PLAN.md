---
phase: 66-orphaned-component-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/66-orphaned-component-cleanup/66-VALIDATION.md
autonomous: true
requirements:
  - SC-1
  - SC-2

must_haves:
  truths:
    - "PlayTogetherDemo.vue has at least one docs page that imports and renders it"
    - "Every Vue component in docs/.vitepress/theme/components/ is referenced by at least one docs page, theme file, or sibling component"
  artifacts:
    - path: ".planning/phases/66-orphaned-component-cleanup/66-VALIDATION.md"
      provides: "Audit results confirming zero orphaned components"
      contains: "Approval: complete"
  key_links: []
---

<objective>
Verify that PlayTogetherDemo.vue is not orphaned and that no Vue components in the docs site lack references. Close the false-positive audit finding from v6-MILESTONE-AUDIT.md.

Purpose: Close the final M6 tech debt item (item 8 from the milestone audit). Research already confirmed PlayTogetherDemo.vue is imported by layered-sound.md -- this plan runs the automated verification and finalizes the phase.
Output: Updated VALIDATION.md with sign-off confirming zero orphaned components.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-orphaned-component-cleanup/66-RESEARCH.md
@.planning/phases/66-orphaned-component-cleanup/66-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated orphan scan and verify PlayTogetherDemo usage</name>
  <files>.planning/phases/66-orphaned-component-cleanup/66-VALIDATION.md</files>
  <action>
Run two verification checks:

1. Confirm PlayTogetherDemo.vue is referenced by a docs page:
   `grep -rl "PlayTogetherDemo" docs/examples/`
   Expected result: `docs/examples/layered-sound.md`

2. Run a full orphan scan across all 22 Vue components in `docs/.vitepress/theme/components/`. For each `.vue` file, grep for its basename (without extension) in all `docs/` files (*.md, *.vue, *.ts, *.mts), excluding the component's own file. Any component with zero external references is orphaned.
   Expected result: zero orphaned components.

3. Update 66-VALIDATION.md:
   - Set task 66-01-01 status to green
   - Set task 66-01-02 status to green
   - Set "Approval: complete" at the bottom
   - Add a brief summary of the scan results (all 22 components have references, PlayTogetherDemo imported by layered-sound.md)

No source code changes. No component deletions. The audit finding was a false positive.
  </action>
  <verify>
    <automated>grep -rl "PlayTogetherDemo" docs/examples/ | grep -q "layered-sound.md" && echo "SC-1 PASS" || echo "SC-1 FAIL"; ORPHANS=0; for comp in $(ls docs/.vitepress/theme/components/*.vue | xargs -I{} basename {} .vue); do REFS=$(grep -rl "$comp" docs/ --include="*.md" --include="*.vue" --include="*.ts" --include="*.mts" 2>/dev/null | grep -v "components/$comp.vue" | head -1); if [ -z "$REFS" ]; then echo "ORPHANED: $comp"; ORPHANS=$((ORPHANS+1)); fi; done; if [ "$ORPHANS" -eq 0 ]; then echo "SC-2 PASS: 0 orphaned components"; else echo "SC-2 FAIL: $ORPHANS orphaned"; fi</automated>
  </verify>
  <done>Both SC-1 and SC-2 pass. VALIDATION.md updated with green status and approval.</done>
</task>

</tasks>

<verification>
- `grep -rl "PlayTogetherDemo" docs/examples/` returns `docs/examples/layered-sound.md`
- Full orphan scan of all 22 components finds zero orphans
- VALIDATION.md shows approval: complete
</verification>

<success_criteria>
1. PlayTogetherDemo.vue confirmed as actively used (imported by layered-sound.md)
2. Zero orphaned Vue components in the docs site
3. VALIDATION.md finalized with sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/66-orphaned-component-cleanup/66-01-SUMMARY.md`
</output>
