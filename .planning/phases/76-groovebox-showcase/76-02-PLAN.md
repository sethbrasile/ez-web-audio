# 76-02: Groovebox UI, Homepage Hero, Share, E2E

> **For agentic workers:** Execute per `.planning/M8-PLAYBOOK.md`. **Blocked on:** 76-01 complete AND design handoff packet returned (`.planning/DESIGN-HANDOFF/` — the groovebox section). If the packet lacks a groovebox design, STOP and report (human gate 1 incomplete).

**Goal:** The flagship demo: full groovebox page at `/examples/groovebox`, compact hero variant on the docs homepage, URL sharing, and its own E2E suite.

**Architecture:** `GrooveboxDemo.vue` (full) + `GrooveboxHero.vue` (compact) both wrap `useGroovebox` + the state module; UI per the claude-design packet using the Phase 74 kit components. State lives in a `ref<GrooveboxState>`; the engine watches it (76-01 contract).

## Global Constraints

- 74-01 Hard Constraints apply (VitePress vars, 320px, AA, ≥44px, no load button, no new runtime deps).
- Packet design wins on visuals; 76-01 engine contract wins on behavior. Conflicts → closest compliant version + `M8-QUESTIONS.md`.
- Hero variant must keep: play/stop, preset selector, at least the drum-lane grid. Everything else per packet.

---

### Task 1: GrooveboxDemo.vue (full component)

**Files:**
- Create: `docs/.vitepress/theme/components/GrooveboxDemo.vue`
- Create: `docs/examples/groovebox.md` (page: title, intro prose, `<llm-exclude>` component embed, `<llm-only>` text fallback, "How It Works" section with a real code example building a mini version via the public API, cross-links to transport/sequencer + effects pages)
- Modify: `docs/.vitepress/config.mts` (sidebar entry under a "Showcase" group)

- [ ] **Step 1: Implement per packet:** step grid (32 steps, bar/beat groupings visually marked, click-to-toggle on drum lanes, per-step note picker or packet-specified control for bass lane), transport row (play/stop single-button pattern — see STATE.md M16 decision, BPM slider, swing slider), lane headers with M/S (≥44px), preset selector, FX controls (delay time/feedback/send, reverb send), share button. Playhead: column-spanning indicator driven by `currentStep` (deep-review M19 lesson).
- [ ] **Step 2: Wire state → engine:** edits mutate the `ref<GrooveboxState>`; engine live-applies (76-01). First interaction calls `start()` per the no-load-button rule; grid renders and is editable BEFORE audio init (stub-beats pattern from DrumMachine — edits before init must apply once audio starts; this was deep-review H2's lesson).
- [ ] **Step 3: Verify:** `pnpm typecheck && pnpm lint`, `pnpm dev` screenshots light+dark saved to phase dir, existing E2E green.
- [ ] **Step 4: Commit** — `feat(groovebox): full demo component and page`.

### Task 2: URL sharing

**Files:**
- Modify: `GrooveboxDemo.vue`

- [ ] **Step 1:** On mount: `stateFromLocation(location.hash)` → if present, replace state (before audio init; engine applies on start). Share button: `history.replaceState` the current `stateToHash(state)` then `navigator.clipboard.writeText(location.href)` with a transient "Link copied" confirmation (packet styling). Do NOT live-sync every edit to the hash (history spam) — hash updates only on Share.
- [ ] **Step 2: Verify manually** (`pnpm dev`): edit → share → open copied URL in new tab → identical pattern. Commit — `feat(groovebox): shareable URL state`.

### Task 3: GrooveboxHero.vue + homepage embed

**Files:**
- Create: `docs/.vitepress/theme/components/GrooveboxHero.vue`
- Modify: `docs/index.md` (hero embed), `docs/.vitepress/theme/index.ts` (register if needed)

- [ ] **Step 1:** Compact variant per packet: play/stop, preset selector, drum grid; shares state module + engine composable with the full demo (import, don't duplicate). "Open full groovebox →" link to `/examples/groovebox` carrying current state via `stateToHash`.
- [ ] **Step 2:** Homepage must not regress SEO/messaging (Phase 16/44 work): hero sits below the existing tagline/CTAs; VitePress home layout slot per packet guidance.
- [ ] **Step 3:** Lighthouse sanity: homepage still loads no audio assets before interaction (network tab check via Playwright: zero `.mp3`/soundfont requests pre-click).
- [ ] **Step 4: Commit** — `feat(groovebox): homepage hero variant`.

### Task 4: E2E suite

**Files:**
- Create: `e2e/groovebox.spec.ts`

- [ ] **Step 1: Specs:** page renders grid + transport with no load button; play → `currentStep` playhead advances (poll DOM class) and `__EZ_DEMO_PEAK__() > 0.05`; step toggle flips cell state; mute silences (peak drops with only one lane active); preset switch changes grid; share roundtrip — set a distinctive pattern, click Share, `page.evaluate(() => location.href)`, open new page at that URL, assert grid matches; hero on homepage: renders, play works, full-page link preserves state. Relative paths (baseURL has `/ez-web-audio/` prefix).
- [ ] **Step 2:** `pnpm test:e2e e2e/groovebox.spec.ts` green, then full suite. Commit — `test(groovebox): E2E suite`.

### Exit

- [ ] Playbook exit gate. `76-02-SUMMARY.md`. STATE/ROADMAP. Screenshots (light+dark, full+hero) in phase dir for UAT.
