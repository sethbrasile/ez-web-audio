---
phase: 07-documentation-demo-site
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - docs/guide/getting-started.md
  - docs/guide/concepts.md
  - docs/examples/index.md
autonomous: true

must_haves:
  truths:
    - "User can follow getting-started guide from install to playing first sound"
    - "User understands when to use Sound vs Track vs Oscillator"
    - "Examples page provides overview of available demos"
  artifacts:
    - path: "docs/guide/getting-started.md"
      provides: "Step-by-step tutorial from install to first sound"
      min_lines: 100
      contains: "pnpm add ez-web-audio"
    - path: "docs/guide/concepts.md"
      provides: "Explanation of Sound, Track, Oscillator differences"
      min_lines: 80
    - path: "docs/examples/index.md"
      provides: "Examples overview page"
      min_lines: 30
  key_links:
    - from: "docs/guide/getting-started.md"
      to: "/guide/concepts"
      via: "markdown link"
      pattern: "\\[.*\\]\\(/guide/concepts\\)"
---

<objective>
Create the Getting Started guide and Core Concepts documentation.

Purpose: The Getting Started guide is the first thing users read. It must be complete, accurate, and get users to a working example quickly. Core Concepts explains the mental model (Sound vs Track vs Oscillator).

Output: Two complete guide pages and an examples overview page.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-documentation-demo-site/07-RESEARCH.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Getting Started guide</name>
  <files>docs/guide/getting-started.md</files>
  <action>
Create comprehensive getting-started guide at docs/guide/getting-started.md:

```markdown
---
title: Getting Started
---

# Getting Started

Get up and running with EZ Web Audio in under 5 minutes.

## Installation

::: code-group
\`\`\`bash [pnpm]
pnpm add ez-web-audio
\`\`\`

\`\`\`bash [npm]
npm install ez-web-audio
\`\`\`

\`\`\`bash [yarn]
yarn add ez-web-audio
\`\`\`
:::

## Your First Sound

Play a sound file with just a few lines of code:

\`\`\`typescript
import { initAudio, createSound } from 'ez-web-audio'

// Must be called in response to user interaction (browser requirement)
document.querySelector('button')?.addEventListener('click', async () => {
  await initAudio()
  const sound = await createSound('/sounds/click.mp3')
  sound.play()
})
\`\`\`

### Why initAudio()?

Browsers require a user interaction (click, tap, keypress) before playing audio.
This is a security feature to prevent auto-playing sounds. `initAudio()` creates
and unlocks the AudioContext. Call it once, inside a click handler.

::: tip
You only need to call `initAudio()` once. After that, you can create and play
sounds anywhere in your code.
:::

## Playing Music Tracks

For longer audio files with pause/resume:

\`\`\`typescript
import { initAudio, createTrack } from 'ez-web-audio'

button.addEventListener('click', async () => {
  await initAudio()

  const track = await createTrack('/music/song.mp3')
  track.play()

  // Control playback
  pauseBtn.onclick = () => track.pause()
  resumeBtn.onclick = () => track.resume()
  seekBtn.onclick = () => track.seek(30) // Jump to 30 seconds
})
\`\`\`

### Sound vs Track

| Feature | Sound | Track |
|---------|-------|-------|
| Use case | Short sounds (clicks, effects) | Music, long audio |
| Overlap | Multiple plays overlap | One play at a time |
| Pause/Resume | No | Yes |
| Seek | No | Yes |
| Position tracking | No | Yes |

## Generating Sounds

Create sounds from scratch with oscillators:

\`\`\`typescript
import { initAudio, createOscillator } from 'ez-web-audio'

button.addEventListener('click', async () => {
  await initAudio()

  const synth = await createOscillator({
    frequency: 440, // A4 note
    type: 'sine'    // sine, square, sawtooth, or triangle
  })

  synth.play()

  // Stop after 1 second
  setTimeout(() => synth.stop(), 1000)
})
\`\`\`

## Controlling Volume and Pan

All sounds support gain (volume) and pan (stereo position):

\`\`\`typescript
const sound = await createSound('/sounds/effect.mp3')

// Set volume to 50%
sound.changeGainTo(0.5)

// Pan to the left
sound.changePanTo(-1) // -1 = left, 0 = center, 1 = right

sound.play()
\`\`\`

## Next Steps

- [Core Concepts](/guide/concepts) - Understand the library architecture
- [Interactive Examples](/examples/) - Try features in your browser
- [API Reference](/api/) - Complete documentation
```

Ensure the guide:
- Starts with installation
- Shows simplest possible example first
- Explains initAudio() requirement upfront
- Progresses from simple to complex
- Links to next steps
  </action>
  <verify>Run VitePress dev server and verify the Getting Started page renders correctly with all code blocks.</verify>
  <done>Getting Started guide exists and provides complete tutorial from install to first sound.</done>
</task>

<task type="auto">
  <name>Task 2: Create Core Concepts guide</name>
  <files>docs/guide/concepts.md</files>
  <action>
Create core concepts documentation at docs/guide/concepts.md:

```markdown
---
title: Core Concepts
---

# Core Concepts

Understanding when to use each class and how they work together.

## The Class Hierarchy

```
BaseSound (abstract)
├── Sound        - One-shot audio playback from file
│   └── Track    - Music with position tracking
├── Oscillator   - Synthesized sound generation
└── SampledNote  - Musical notes with pitch identity
```

## Sound: One-Shot Playback

**Use Sound for:** Short audio files that may play multiple times simultaneously.

\`\`\`typescript
const click = await createSound('/sounds/click.mp3')

// Each play() creates a new AudioBufferSourceNode
click.play() // First instance plays
click.play() // Second instance overlaps
click.play() // Third instance overlaps
\`\`\`

Sounds are perfect for:
- UI feedback (clicks, hovers)
- Game sound effects
- Drum samples
- Any audio under ~10 seconds

## Track: Music Playback

**Use Track for:** Longer audio files where you need playback control.

\`\`\`typescript
const song = await createTrack('/music/background.mp3')

song.play()

// Track knows its position
console.log(song.position.string) // "1:23"
console.log(song.percentPlayed)   // 0.35 (35% complete)

// Control playback
song.pause()
song.resume()
song.seek(60) // Jump to 1 minute
\`\`\`

Track limitations:
- Only one playback at a time (calling play() while playing restarts)
- Slightly more memory overhead than Sound

## Oscillator: Sound Synthesis

**Use Oscillator for:** Generating sounds from scratch without audio files.

\`\`\`typescript
const synth = await createOscillator({
  frequency: 440,     // Hz (A4 note)
  type: 'sine',       // sine, square, sawtooth, triangle
  envelope: {         // ADSR envelope (optional)
    attack: 0.01,
    decay: 0.1,
    sustain: 0.7,
    release: 0.3
  }
})

synth.play()
// ... later
synth.stop() // Triggers release phase
\`\`\`

Oscillators are perfect for:
- Musical instruments
- Sound design
- Procedural audio
- Anything without pre-recorded files

## AudioContext Lifecycle

### Initialization

The Web Audio API requires a user gesture before playing audio:

\`\`\`typescript
// This works
button.onclick = async () => {
  await initAudio() // Creates AudioContext inside click handler
  const sound = await createSound('/audio/click.mp3')
  sound.play()
}

// This fails silently or throws
await initAudio() // No user gesture!
\`\`\`

### Single Context

EZ Web Audio uses a single shared AudioContext. All sounds route through it:

\`\`\`typescript
const sound1 = await createSound('/a.mp3')
const sound2 = await createSound('/b.mp3')
const osc = await createOscillator({ frequency: 440 })

// All three use the same AudioContext
// This is efficient and prevents resource exhaustion
\`\`\`

## Audio Routing

Each sound follows this signal path:

```
Source → [Effects] → Gain → Panner → Destination
```

\`\`\`typescript
const sound = await createSound('/audio/guitar.mp3')

// Add effects
const reverb = await loadReverbImpulse()
sound.addEffect(wrapEffect(reverb))

// Control gain and pan
sound.changeGainTo(0.8)
sound.changePanTo(-0.5)

sound.play()
// Audio flows: source → reverb → gain (0.8) → pan (left) → speakers
\`\`\`

## Parameter Control

### Immediate Updates

Change parameters right now:

\`\`\`typescript
sound.update('gain').to(0.5).from('ratio')
sound.update('pan').to(-1)
\`\`\`

### Scheduled Updates

Schedule parameter changes relative to play time:

\`\`\`typescript
// Fade in over 1 second
sound.onPlaySet('gain').to(0).endingAt(1, 'exponential')

// Ramp frequency from 200 to 800 over 0.5 seconds
oscillator.onPlayRamp('frequency').from(200).to(800).in(0.5)
\`\`\`

## Events

All playable sounds emit events:

\`\`\`typescript
const sound = await createSound('/audio/effect.mp3')

sound.on('play', (e) => console.log('Started at', e.detail.time))
sound.on('stop', () => console.log('Stopped'))
sound.on('end', () => console.log('Finished naturally'))

sound.play()
\`\`\`

Track has additional events:

\`\`\`typescript
track.on('pause', (e) => console.log('Paused at', e.detail.position))
track.on('resume', (e) => console.log('Resumed from', e.detail.position))
track.on('seek', (e) => console.log('Seeked to', e.detail.position))
\`\`\`

## Next Steps

- [Interactive Examples](/examples/) - See concepts in action
- [API Reference](/api/) - Detailed method documentation
```
  </action>
  <verify>Run VitePress dev server and verify the Core Concepts page renders with proper code blocks and diagram.</verify>
  <done>Core Concepts guide explains Sound vs Track vs Oscillator with practical examples.</done>
</task>

<task type="auto">
  <name>Task 3: Create Examples overview page</name>
  <files>docs/examples/index.md</files>
  <action>
Create examples overview page at docs/examples/index.md:

```markdown
---
title: Interactive Examples
---

# Interactive Examples

Try EZ Web Audio features directly in your browser.

::: warning Browser Requirement
All examples require clicking a button to start. This is a browser security
requirement - audio cannot auto-play without user interaction.
:::

## Basic Examples

### [Basic Playback](/examples/basic-playback)
Play sounds and music tracks with volume and pan control.

### [Synthesis](/examples/synthesis)
Generate sounds with oscillators, including ADSR envelopes.

### [Effects](/examples/effects)
Add effects like filters and gain to your audio.

## Advanced Examples

### Drum Machine
Build rhythmic patterns with BeatTrack and timed playback.

### Layered Sounds
Combine multiple sounds for complex audio experiences.

### Audio Sprites
Efficiently play multiple sounds from a single audio file.

## Running Examples Locally

Clone the repository and run the development server:

\`\`\`bash
git clone https://github.com/sethbrasile/ez-web-audio.git
cd ez-web-audio
pnpm install
pnpm docs:dev
\`\`\`

Then visit http://localhost:5173/ez-web-audio/examples/
```
  </action>
  <verify>Run VitePress dev server and verify the Examples index page renders with links to sub-pages.</verify>
  <done>Examples overview page exists with links to all interactive examples.</done>
</task>

</tasks>

<verification>
1. VitePress dev server shows Getting Started at /guide/getting-started
2. Getting Started has installation, first example, and next steps
3. Core Concepts explains Sound vs Track vs Oscillator with examples
4. Examples index page lists available demos
5. All internal links work (no 404s when clicking)
6. Code blocks render with syntax highlighting
</verification>

<success_criteria>
- Getting Started guide takes user from install to working sound in 5 minutes
- Core Concepts explains mental model for choosing Sound/Track/Oscillator
- Examples overview provides navigation to interactive demos
- All pages link to each other appropriately
- Code examples are complete and copy-pasteable
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-demo-site/07-03-SUMMARY.md`
</output>
