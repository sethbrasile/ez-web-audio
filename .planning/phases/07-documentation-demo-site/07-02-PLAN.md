---
phase: 07-documentation-demo-site
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/sound.ts
  - src/track.ts
  - src/oscillator.ts
  - src/base-sound.ts
autonomous: true

must_haves:
  truths:
    - "All public factory functions have JSDoc with @example tags"
    - "All public classes have class-level JSDoc describing purpose"
    - "No YUIDoc-style syntax remains ({{#crossLink}} etc.)"
  artifacts:
    - path: "src/index.ts"
      provides: "Factory functions with complete JSDoc"
      contains: "@example"
    - path: "src/sound.ts"
      provides: "Sound class with modern JSDoc"
      contains: "@example"
    - path: "src/track.ts"
      provides: "Track class with modern JSDoc"
      contains: "@example"
  key_links:
    - from: "TypeDoc"
      to: "src/index.ts"
      via: "entryPoints config"
      pattern: "@param.*@returns.*@example"
---

<objective>
Modernize JSDoc documentation across core source files to produce high-quality TypeDoc output.

Purpose: TypeDoc generates API reference from JSDoc comments. Current codebase has old YUIDoc-style comments that TypeDoc doesn't parse. Modern JSDoc with @example tags creates useful API docs.

Output: All public APIs have complete, TypeDoc-compatible JSDoc documentation with working examples.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-documentation-demo-site/07-RESEARCH.md
@src/index.ts
@src/sound.ts
@src/track.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document factory functions in index.ts</name>
  <files>src/index.ts</files>
  <action>
Add complete JSDoc to all public factory functions. Replace any YUIDoc syntax ({{#crossLink}}, {{#crossLinkModule}}) with standard JSDoc.

Functions to document:
1. initAudio() - Already has basic docs, add @example
2. getAudioContext() - Add JSDoc
3. createSound() - Add full JSDoc with @example
4. createTrack() - Add full JSDoc with @example
5. createBeatTrack() - Add full JSDoc with @example
6. createSampler() - Add full JSDoc with @example
7. createOscillator() - Add full JSDoc with @example
8. createLayeredSound() - Already has good docs, verify @example works
9. createFont() - Add full JSDoc with @example
10. createSprite() - Already has good docs
11. createWhiteNoise() - Add full JSDoc with @example
12. preload() - Add JSDoc with @example
13. crossfade() - Add JSDoc with @example
14. stopAll/pauseAll/playAll - Add JSDoc with @example

JSDoc template for each function:
```typescript
/**
 * Brief description of what this does.
 *
 * Longer description explaining behavior, edge cases, or important notes.
 *
 * @param paramName - Description of parameter
 * @returns Description of return value
 * @throws {ErrorType} When this error occurs
 *
 * @example
 * ```typescript
 * import { functionName } from 'ez-web-audio'
 *
 * // Working code example
 * const result = await functionName('arg')
 * result.play()
 * ```
 */
```

Remove the private functions' YUIDoc comments (createSoundFor, load) - they have @private already, just modernize the syntax.
  </action>
  <verify>Run `pnpm typedoc` and check docs/api/functions/ has detailed pages with examples for each function.</verify>
  <done>All public factory functions have modern JSDoc with @example tags that render correctly in TypeDoc output.</done>
</task>

<task type="auto">
  <name>Task 2: Document Sound and Track classes</name>
  <files>src/sound.ts, src/track.ts</files>
  <action>
Update JSDoc in Sound and Track classes:

**src/sound.ts:**
1. Replace YUIDoc class comment with modern JSDoc:
```typescript
/**
 * One-shot audio playback from an AudioBuffer.
 *
 * Sound is the core class for playing audio files. Each `.play()` call creates
 * a new AudioBufferSourceNode, allowing simultaneous overlapping playback.
 * Use Track instead if you need pause/resume/seek functionality.
 *
 * @example
 * ```typescript
 * import { createSound } from 'ez-web-audio'
 *
 * const sound = await createSound('click.mp3')
 * sound.play()
 *
 * // Adjust volume before playing
 * sound.changeGainTo(0.5)
 * sound.play()
 * ```
 */
```

2. Document public methods: play(), stop(), duration getter
3. Document update(), onPlaySet(), onPlayRamp() from BaseSound

**src/track.ts:**
1. Replace the existing YUIDoc-style class comment with:
```typescript
/**
 * Music track with position tracking, pause/resume, and seeking.
 *
 * Track extends Sound with playback position awareness. Use Track for longer
 * audio files where users need to pause, resume, or seek to specific positions.
 * Each Track can only play once at a time (unlike Sound which allows overlap).
 *
 * @example
 * ```typescript
 * import { createTrack } from 'ez-web-audio'
 *
 * const track = await createTrack('song.mp3')
 * track.play()
 *
 * // Pause and resume
 * track.pause()
 * track.resume()
 *
 * // Seek to 30 seconds
 * track.seek(30)
 *
 * // Get current position
 * console.log(track.position) // { raw: 30.5, string: '0:30', pojo: { minutes: 0, seconds: 30 } }
 * ```
 */
```

2. Document: play(), pause(), resume(), seek(), stop(), position, duration, percentPlayed, isPlaying
  </action>
  <verify>Run `pnpm typedoc` and check docs/api/classes/Sound.md and Track.md have complete documentation with examples.</verify>
  <done>Sound and Track classes have modern JSDoc that produces useful TypeDoc pages.</done>
</task>

<task type="auto">
  <name>Task 3: Document Oscillator and BaseSound</name>
  <files>src/oscillator.ts, src/base-sound.ts</files>
  <action>
**src/oscillator.ts:**
1. Add class-level JSDoc:
```typescript
/**
 * Synthesizer that generates audio from oscillator waveforms.
 *
 * Oscillator creates sound from scratch using sine, square, sawtooth, or triangle
 * waves. Supports ADSR envelopes for professional-quality synthesis, filters for
 * tone shaping, and all the gain/pan controls from BaseSound.
 *
 * @example
 * ```typescript
 * import { createOscillator } from 'ez-web-audio'
 *
 * // Simple sine wave at 440Hz (A4)
 * const synth = await createOscillator({ frequency: 440, type: 'sine' })
 * synth.play()
 *
 * // With ADSR envelope
 * const piano = await createOscillator({
 *   frequency: 440,
 *   type: 'triangle',
 *   envelope: { attack: 0.01, decay: 0.1, sustain: 0.7, release: 0.3 }
 * })
 * piano.play()
 * setTimeout(() => piano.stop(), 500) // Release after 500ms
 * ```
 */
```

2. Document public methods and the OscillatorOpts interface

**src/base-sound.ts:**
1. Add class-level JSDoc for BaseSound (abstract class)
2. Document the fluent API methods that all sounds inherit:
   - update(param) - immediate value change
   - onPlaySet(param) - schedule value at play time
   - onPlayRamp(param) - schedule ramp at play time
   - addEffect() / removeEffect() / clearEffects()
   - changeGainTo() / changePanTo()

Example for update():
```typescript
/**
 * Update an audio parameter immediately.
 *
 * Returns a fluent builder for setting the parameter value. Use `.to(value)`
 * to set the value, optionally with `.from(unit)` for unit conversion.
 *
 * @param param - The parameter to update ('gain', 'pan', 'frequency', 'detune')
 * @returns Fluent builder for setting the value
 *
 * @example
 * ```typescript
 * // Set gain to 50%
 * sound.update('gain').to(0.5).from('ratio')
 *
 * // Set pan to left
 * sound.update('pan').to(-1)
 * ```
 */
```
  </action>
  <verify>Run `pnpm typedoc` and check docs/api/classes/Oscillator.md and BaseSound.md have complete documentation.</verify>
  <done>Oscillator and BaseSound classes have modern JSDoc documenting the fluent API patterns.</done>
</task>

</tasks>

<verification>
1. `pnpm typedoc` completes without warnings about missing documentation
2. docs/api/classes/ has detailed pages for Sound, Track, Oscillator, BaseSound
3. docs/api/functions/ has detailed pages for all factory functions
4. All @example blocks contain valid TypeScript that would work if copy-pasted
5. No YUIDoc syntax remains (search for "{{#" returns no results in src/)
</verification>

<success_criteria>
- All public factory functions have complete JSDoc with @example tags
- Sound, Track, Oscillator, BaseSound classes have class-level and method-level JSDoc
- TypeDoc generates comprehensive API pages without "no description" placeholders
- Examples show real-world usage patterns users can copy
- YUIDoc syntax completely removed from codebase
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation-demo-site/07-02-SUMMARY.md`
</output>
