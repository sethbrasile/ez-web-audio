---
phase: 26-source-code-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
  - src/interfaces/connectable.ts
  - src/interfaces/playable.ts
autonomous: true
requirements:
  - SC-01
  - SC-03
  - SC-18

must_haves:
  truths:
    - "BeatTrackOptions, SamplerOptions, TimeObject, RatioType, SeekType are importable from 'ez-web-audio'"
    - "Connectable interface update() signature matches BaseSound.update() implementation"
    - "Playable interface play() and stop() return Promise<void> matching BaseSound"
  artifacts:
    - path: "src/index.ts"
      provides: "Missing type exports"
      contains: "BeatTrackOptions"
    - path: "src/interfaces/connectable.ts"
      provides: "Corrected update() signature"
      pattern: "update.*ControlType\\)"
    - path: "src/interfaces/playable.ts"
      provides: "Async return types"
      contains: "Promise<void>"
  key_links:
    - from: "src/index.ts"
      to: "src/beat-track.ts"
      via: "type re-export"
      pattern: "BeatTrackOptions"
---

<objective>
Fix API contract issues: add 5 missing type exports to src/index.ts, correct the Connectable interface update() signature, and update Playable interface to return Promise<void>.

Purpose: Consumers cannot import BeatTrackOptions, SamplerOptions, TimeObject, RatioType, or SeekType. The Connectable interface has an extra parameter on update() that doesn't match BaseSound. Playable declares void returns but implementations return Promise<void>.
Output: Three fixed interface/export files.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-source-code-fixes/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing type exports to src/index.ts</name>
  <files>src/index.ts</files>
  <action>
Add the following 5 types to the `export type { ... }` block at the bottom of `src/index.ts`:

1. `BeatTrackOptions` — imported from `./beat-track`
2. `SamplerOptions` — imported from `./sampler` (already imported at top of file for BeatTrack usage)
3. `TimeObject` — imported from `@utils/create-time-object`
4. `RatioType` — imported from `@controllers/base-param-controller`
5. `SeekType` — imported from `@controllers/base-param-controller`

Add them to the existing alphabetically-sorted `export type` block. `ControlType` and `ControlTypeMap` are already exported from the controller, so `RatioType` and `SeekType` should be added nearby.
  </action>
  <verify>`pnpm typecheck` passes. Grep `src/index.ts` for all 5 type names in export block.</verify>
  <done>All 5 types are importable from the library's public API.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Connectable and Playable interfaces</name>
  <files>src/interfaces/connectable.ts, src/interfaces/playable.ts</files>
  <action>
**Connectable (C-5):** In `src/interfaces/connectable.ts`, find the `update` method signature. It currently has an extra `value: number` parameter:
```
update: (type: ControlType, value: number) => { to: ... }
```
Remove the extra `value: number` parameter so it matches `BaseSound.update()`:
```
update: (type: ControlType) => { to: (value: number) => { as: (method: RatioType) => void } }
```

**Playable (L-2):** In `src/interfaces/playable.ts`, update method return types:
- `play()` should return `Promise<void>` (not `void`)
- `stop()` should return `Promise<void>` (not `void`)

This matches the actual `BaseSound` implementations which are `async`.
  </action>
  <verify>`pnpm typecheck` passes. The interface signatures match the implementations.</verify>
  <done>Connectable.update() takes only `(type: ControlType)`. Playable.play() and .stop() return Promise<void>.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors
- `pnpm test` passes (no regressions)
- `pnpm build:lib` succeeds
</verification>

<success_criteria>
All 5 missing types are exported. Connectable.update() signature matches BaseSound.update(). Playable interface reflects async return types.
</success_criteria>

<output>
After completion, create `.planning/phases/26-source-code-fixes/26-01-SUMMARY.md`
</output>
