---
phase: 67-lfo-modulation-demo
plan: 02
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - e2e/demos.spec.ts
  - e2e/interactions.spec.ts
autonomous: false
requirements: [LFO-01, LFO-02, LFO-03, LFO-04, LFO-05]

must_haves:
  truths:
    - "LFO modulation page loads without JavaScript errors in headless browser"
    - "Play button click initializes audio and changes button text to Stop"
    - "Tab switching changes active tab styling without page errors"
    - "Canvas element is present and visible on the page"
    - "Rate and depth sliders are interactive after playback starts"
  artifacts:
    - path: "e2e/demos.spec.ts"
      provides: "LFO page added to smoke test list"
      contains: "lfo-modulation"
    - path: "e2e/interactions.spec.ts"
      provides: "LFO-specific interaction tests"
      contains: "LFO"
  key_links:
    - from: "e2e/demos.spec.ts"
      to: "/examples/lfo-modulation"
      via: "page URL in demoPages array"
      pattern: "lfo-modulation"
    - from: "e2e/interactions.spec.ts"
      to: "/examples/lfo-modulation"
      via: "page.goto in test"
      pattern: "lfo-modulation"
---

<objective>
Add E2E tests for the LFO modulation demo page -- smoke test for page load and interaction tests for play/stop, tab switching, slider controls, and canvas presence. Then human-verify the complete demo.

Purpose: Ensure the LFO demo page renders without errors and that interactive elements respond to user actions.
Output: Updated demos.spec.ts and interactions.spec.ts with LFO coverage
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67-lfo-modulation-demo/67-01-SUMMARY.md

@e2e/demos.spec.ts
@e2e/interactions.spec.ts

<interfaces>
<!-- LFODemo.vue DOM structure (from Plan 01) -->
<!-- Key selectors for E2E tests: -->
<!-- .play-button — Play/Stop toggle button -->
<!-- .tab-bar button — Tab buttons (Tremolo, Vibrato, Filter Sweep) -->
<!-- .tab-bar button.active — Currently active tab -->
<!-- canvas — LFO waveform visualization canvas -->
<!-- input[type="range"] — Rate and Depth sliders -->
<!-- .waveform-toggle button — Waveform type buttons (sine, square, etc.) -->
<!-- .preset-button — Preset buttons -->
<!-- .lfo-demo — Root container class -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E smoke and interaction tests for LFO demo</name>
  <files>e2e/demos.spec.ts, e2e/interactions.spec.ts</files>
  <action>
**Update `e2e/demos.spec.ts`:**

Add `'/examples/lfo-modulation'` to the `demoPages` array. This ensures the page loads without JavaScript errors.

**Update `e2e/interactions.spec.ts`:**

Add a new `test.describe('LFO Modulation page interactions')` block with these tests:

1. **Play/Stop button toggles text:**
   - Navigate to `/examples/lfo-modulation`
   - Wait for `.play-button` selector
   - Verify initial text contains "Play"
   - Click `.play-button`
   - Wait for button text to change to "Stop" (use `waitForFunction` like drum machine test)
   - Click again, wait for text to revert to "Play"
   - Verify no page errors

2. **Tab switching updates active tab:**
   - Navigate to `/examples/lfo-modulation`
   - Wait for tab buttons to appear
   - Click the second tab button (Vibrato)
   - Verify second tab has `.active` class or equivalent active styling
   - Click third tab (Filter Sweep)
   - Verify third tab is now active
   - Verify no page errors

3. **Canvas element is present:**
   - Navigate to `/examples/lfo-modulation`
   - Wait for page load
   - Verify `canvas` element exists and is visible
   - Verify no page errors

4. **Sliders are interactive after play:**
   - Navigate to `/examples/lfo-modulation`
   - Wait for page load
   - Click Play button
   - Wait for button to show "Stop"
   - Verify `input[type="range"]` elements exist (at least 2: rate and depth)
   - Verify no page errors

All tests follow the established patterns: capture `pageerror` events, use `waitForSelector` and `waitForFunction` (never `waitForTimeout`), verify no uncaught errors at the end.

Use the CSS class selectors that match LFODemo.vue's template structure. If the component uses different class names, adjust selectors to match. The key selectors are: `.play-button` for play/stop, tab buttons inside a tab bar container, `canvas` for visualization, `input[type="range"]` for sliders.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm exec playwright test e2e/demos.spec.ts e2e/interactions.spec.ts</automated>
  </verify>
  <done>demos.spec.ts includes LFO modulation page in smoke tests. interactions.spec.ts has LFO-specific tests for play/stop toggle, tab switching, canvas presence, and slider interactivity. All E2E tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete LFO modulation demo</name>
  <files>docs/.vitepress/theme/components/LFODemo.vue</files>
  <action>
Human verifies the complete LFO modulation demo works correctly -- audio output, visual canvas animation, control responsiveness, and navigation integration.

  <what-built>Complete LFO modulation demo with tabbed tremolo/vibrato/filter sweep, scrolling waveform canvas, rate/depth/waveform controls, preset buttons, and E2E test coverage</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and visit http://localhost:5173/examples/lfo-modulation
    2. Verify page renders immediately with tabs, canvas, sliders, presets visible (no Load button)
    3. Click "Play" -- hear a sawtooth tone with tremolo (gain wobble). Canvas shows scrolling sine wave.
    4. Click "Vibrato" tab -- sound changes to pitch wobble (different from tremolo). No audio interruption.
    5. Click "Filter Sweep" tab -- sound changes to wah/sweep effect. No audio interruption.
    6. Drag Rate slider -- modulation speed changes in real-time
    7. Drag Depth slider -- modulation intensity changes in real-time
    8. Click waveform toggle buttons (square, sawtooth, triangle) -- waveform shape changes, canvas updates
    9. Click a preset button -- parameters snap to curated values and appropriate tab activates
    10. Click "Stop" -- audio stops, canvas stops
    11. Click "Play" again -- audio restarts correctly
    12. Check sidebar navigation -- "Modulation" section with "LFO Modulation" link
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm exec playwright test</automated>
  </verify>
  <done>Human confirms tremolo, vibrato, and filter sweep sound distinctly different; canvas animates smoothly; controls update audio in real-time; presets work; no audio interruption on tab switch.</done>
</task>

</tasks>

<verification>
1. `pnpm exec playwright test` -- all E2E tests pass including new LFO tests
2. LFO modulation page loads without JS errors in Playwright
3. Play/stop, tab switching, canvas, sliders all verified via E2E
4. Human verification confirms audio and visual quality
</verification>

<success_criteria>
- LFO page in demos.spec.ts smoke test -- passes
- 4 LFO interaction tests in interactions.spec.ts -- all pass
- Human confirms tremolo, vibrato, and filter sweep sound distinctly different
- Human confirms canvas animation is smooth and responsive
- Human confirms controls update audio in real-time
</success_criteria>

<output>
After completion, create `.planning/phases/67-lfo-modulation-demo/67-02-SUMMARY.md`
</output>
