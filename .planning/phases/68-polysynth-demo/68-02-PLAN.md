---
phase: 68-polysynth-demo
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - e2e/interactions.spec.ts
  - e2e/demos.spec.ts
autonomous: false
requirements: [POLY-01, POLY-02, POLY-03, POLY-04]

must_haves:
  truths:
    - "E2E tests confirm piano keyboard renders and keys are clickable without JS errors"
    - "E2E tests confirm voice count display is visible on page"
    - "E2E tests confirm steal strategy dropdown is interactive"
    - "E2E tests confirm ADSR sliders are present and adjustable"
    - "Human confirms polyphonic audio works (chords, steal behavior, ADSR presets)"
  artifacts:
    - path: "e2e/interactions.spec.ts"
      provides: "PolySynth interaction test describe block"
      contains: "PolySynth"
    - path: "e2e/demos.spec.ts"
      provides: "polysynth page in smoke test array"
      contains: "polysynth"
  key_links:
    - from: "e2e/interactions.spec.ts"
      to: "examples/polysynth"
      via: "page.goto navigation"
      pattern: "goto.*polysynth"
---

<objective>
Add E2E tests for the PolySynth demo page and get human verification of audio behavior.

Purpose: Ensure the demo page loads without errors, all interactive controls work, and polyphonic audio behavior is correct.
Output: Passing E2E tests in both demos.spec.ts and interactions.spec.ts, plus human sign-off on audio.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-polysynth-demo/68-CONTEXT.md
@.planning/phases/68-polysynth-demo/68-01-SUMMARY.md

<interfaces>
<!-- E2E test patterns from existing tests -->

From e2e/demos.spec.ts (smoke test array pattern):
```typescript
const demoPages = [
  'examples/basic-playback',
  // ... add 'examples/polysynth' to this array
]
```

From e2e/interactions.spec.ts (LFO test pattern -- follow this structure):
```typescript
test.describe('LFO Modulation page interactions', () => {
  test('Play/Stop button toggles text', async ({ page }) => {
    const errors: string[] = []
    page.on('pageerror', err => errors.push(err.message))
    await page.goto('examples/lfo-modulation')
    await page.waitForSelector('.play-button', { timeout: 10000 })
    await page.waitForLoadState('networkidle')
    // ... assertions
    expect(errors).toHaveLength(0)
  })
})
```

NOTE: Use relative paths for page.goto (not absolute) since baseURL includes /ez-web-audio/ prefix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PolySynth E2E tests</name>
  <files>e2e/interactions.spec.ts, e2e/demos.spec.ts</files>
  <action>
**In `e2e/demos.spec.ts`:** Add `'examples/polysynth'` to the `demoPages` array (line ~29, after `'examples/lfo-modulation'`).

**In `e2e/interactions.spec.ts`:** Add a new `test.describe('PolySynth page interactions', () => { ... })` block at the end of the file. Include these tests:

1. **"Piano keyboard renders and keys are clickable"** (covers POLY-01):
   - Navigate to `examples/polysynth`
   - Wait for PianoKeyboard to render (wait for a piano key element -- look for the CSS class used in PianoKeyboard.vue for white/black keys)
   - Click a piano key
   - Verify no JS errors via pageerror listener
   - Verify the key element exists and is visible

2. **"Voice count display is visible"** (covers POLY-02):
   - Navigate to `examples/polysynth`
   - Wait for page load
   - Verify voice count text is visible (look for text matching pattern like "Voices:" or the voice counter element)
   - Verify fill bar element exists

3. **"Steal strategy dropdown changes value"** (covers POLY-03):
   - Navigate to `examples/polysynth`
   - Find the strategy dropdown/select element
   - Verify it has the expected options (LRU, Oldest Active, Quietest)
   - Change the selection to a different strategy
   - Verify the selection changed

4. **"ADSR sliders are present and interactive"** (covers POLY-04):
   - Navigate to `examples/polysynth`
   - Verify 4 ADSR slider inputs exist (attack, decay, sustain, release)
   - Verify preset buttons exist (Piano, Pad, Pluck, Lead)
   - Click a preset button (e.g., "Pad")
   - Verify at least one slider value changed after preset click

**Test conventions:**
- Use `page.on('pageerror', ...)` error collection in every test
- Use relative URLs: `page.goto('examples/polysynth')`
- Use `page.waitForLoadState('networkidle')` after navigation
- Use condition-based waits, never `waitForTimeout`
- Assert `expect(errors).toHaveLength(0)` at end of each test

Use CSS class names and data attributes that match what PolySynthDemo.vue actually uses. The executor should read the built component from Plan 01's summary to get exact selectors. Use resilient selectors (text content, role, visible labels) when possible.
  </action>
  <verify>
    <automated>cd /Users/seth/Documents/GitHub/ez-audio && pnpm exec playwright test e2e/interactions.spec.ts --grep "PolySynth" 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 PolySynth interaction tests pass. Polysynth page appears in demos.spec.ts smoke test array and passes. No test regressions in existing tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify PolySynth audio and interaction behavior</name>
  <files>docs/.vitepress/theme/components/PolySynthDemo.vue</files>
  <action>
Human verifies the complete PolySynth demo works correctly -- polyphonic audio, voice stealing, ADSR envelopes, and all interactive controls.

  <what-built>Complete PolySynth demo page with piano keyboard (C4-C5) playing polyphonic notes via createPolySynth, voice count badge with fill bar, steal strategy dropdown (LRU, Oldest Active, Quietest), max voices slider (1-8, default 4), voice stolen notification flash, ADSR sliders with presets (Piano, Pad, Pluck, Lead), and waveform type selector (sine, triangle, sawtooth, square)</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and visit http://localhost:5173/ez-web-audio/examples/polysynth
    2. Click piano keys -- confirm you hear sound on first click (lazy init)
    3. Hold multiple keys (or use computer keys A-J) -- confirm you hear a chord (polyphony)
    4. Check voice counter updates as you play/release notes
    5. Set max voices to 2, then play 3+ keys -- confirm voice stealing occurs and notification flashes
    6. Try each steal strategy (LRU, Oldest Active, Quietest) with 2 max voices -- confirm different stealing behavior
    7. Set max voices to 1 (monophonic) -- confirm every new note immediately steals
    8. Click preset buttons (Piano, Pad, Pluck, Lead) -- confirm distinctly different envelope shapes
    9. Adjust ADSR sliders manually -- confirm new notes use updated envelope
    10. Switch waveform types -- confirm tonal difference on new notes
    11. Check sidebar navigation -- "PolySynth" link under Synthesis section
  </how-to-verify>
  </action>
  <verify>Human approves polyphonic audio, voice management, and ADSR behavior</verify>
  <done>Human confirms all 4 POLY requirements work correctly: polyphonic keyboard, voice count display, steal strategy switching, and ADSR envelope control</done>
</task>

</tasks>

<verification>
1. `pnpm exec playwright test e2e/interactions.spec.ts --grep "PolySynth"` -- all 4 tests pass
2. `pnpm exec playwright test e2e/demos.spec.ts` -- polysynth included in smoke tests, all pass
3. `pnpm exec playwright test` -- full suite green, no regressions
4. Human confirms audio behavior (chords, stealing, ADSR, waveforms)
</verification>

<success_criteria>
- 4 E2E interaction tests pass for PolySynth page
- Polysynth page passes smoke test in demos.spec.ts
- Full Playwright suite green (no regressions)
- Human approves polyphonic audio, voice stealing, and ADSR behavior
</success_criteria>

<output>
After completion, create `.planning/phases/68-polysynth-demo/68-02-SUMMARY.md`
</output>
