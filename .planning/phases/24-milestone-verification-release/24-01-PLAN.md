---
phase: 24-milestone-verification-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md
  - .planning/phases/19-dx-improvements/19-VERIFICATION.md
  - src/index.ts
  - CHANGELOG.md
autonomous: false

requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - DOC-01
  - DX-01
  - DX-02
  - DX-03
  - DX-04
  - DX-05
  - DX-06
  - DX-07
  - DX-08
  - DEF-05
  - DOC-03

must_haves:
  truths:
    - "18-VERIFICATION.md exists and confirms all 8 Phase 18 requirements satisfied via code inspection"
    - "19-VERIFICATION.md exists and confirms all 10 Phase 19 requirements satisfied via code inspection"
    - "createWhiteNoise() JSDoc example uses context-free API (createFilterEffect('lowpass', {...}))"
    - "CHANGELOG.md 1.0.0 entry includes audioContextAwareTimeout as a new export"
    - "npm ez-web-audio@1.0.0 is published (triggered by git tag v1.0.0)"
  artifacts:
    - path: ".planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md"
      provides: "Formal verification of Phase 18 API cleanup requirements"
      contains: "API-01, API-02, API-03, API-04, API-05, API-06, API-07, DOC-01"
    - path: ".planning/phases/19-dx-improvements/19-VERIFICATION.md"
      provides: "Formal verification of Phase 19 DX improvements requirements"
      contains: "DX-01, DX-02, DX-03, DX-04, DX-05, DX-06, DX-07, DX-08, DEF-05, DOC-03"
    - path: "src/index.ts"
      provides: "Fixed createWhiteNoise JSDoc example"
      contains: "createFilterEffect('lowpass'"
    - path: "CHANGELOG.md"
      provides: "Complete 1.0.0 changelog"
      contains: "audioContextAwareTimeout"
  key_links:
    - from: "18-VERIFICATION.md"
      to: "src/base-sound.ts, src/controllers/base-param-controller.ts, src/track.ts, src/beat.ts"
      via: "code inspection grep patterns"
      pattern: "\\.as\\(|playInIfActive|protected.*gainNode"
    - from: "19-VERIFICATION.md"
      to: "src/base-sound.ts, src/effects/filter-effect.ts, src/index.ts"
      via: "code inspection grep patterns"
      pattern: "bypass|createFilterEffect|addEffects|playTogether|createSounds"
    - from: "CHANGELOG.md"
      to: "src/index.ts"
      via: "audioContextAwareTimeout export documented in changelog"
      pattern: "audioContextAwareTimeout"
---

<objective>
Formally verify Phase 18 and 19 by creating VERIFICATION.md documents based on code inspection, fix two low-severity documentation gaps (stale JSDoc, missing CHANGELOG entry), then trigger the npm 1.0.0 publish via git tag.

Purpose: Close the final blockers from the v1.0 milestone audit. Phases 18 and 19 completed their implementation but never got formal verification documents. All code is correct — this plan creates the audit trail, fixes minor doc inaccuracies, and ships 1.0.0.
Output: 18-VERIFICATION.md, 19-VERIFICATION.md, patched src/index.ts JSDoc, updated CHANGELOG.md, npm 1.0.0 published.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/18-breaking-api-cleanup/18-01-SUMMARY.md
@.planning/phases/18-breaking-api-cleanup/18-02-SUMMARY.md
@.planning/phases/18-breaking-api-cleanup/18-03-SUMMARY.md
@.planning/phases/19-dx-improvements/19-01-SUMMARY.md
@.planning/phases/19-dx-improvements/19-02-SUMMARY.md
@.planning/phases/19-dx-improvements/19-03-SUMMARY.md
@.planning/phases/23-demo-example-bugfixes/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 18-VERIFICATION.md and 19-VERIFICATION.md</name>
  <files>
    .planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md
    .planning/phases/19-dx-improvements/19-VERIFICATION.md
  </files>
  <action>
Create two VERIFICATION.md files using code inspection to confirm all requirements. Follow the exact format of 23-VERIFICATION.md.

**For 18-VERIFICATION.md** (at `.planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md`):

Phase 18 goal: The public API is clean, consistent, and correctly encapsulated — all breaking changes applied before 1.0 locks the API.

Verify each requirement via grep/read before writing the file:

- **API-01** (`.as()` replaces `.from()` on fluent chains): grep for `.from(` in src/ — must find zero results on `update().to().from(` or `seek().from(` patterns. Confirm only `onPlayRamp().from()` remains (different semantic — "from value X").
- **API-02** (`playInIfActive` replaces `ifActivePlayIn`): grep for `ifActivePlayIn` in src/ — zero results. `playInIfActive` exists in src/beat.ts and src/beat-track.ts.
- **API-03** (`gainNode`/`pannerNode`/`effectChainInput` protected): read src/base-sound.ts — all three are declared `protected`.
- **API-04** (`startOffset` protected): read src/base-sound.ts — `protected startOffset` declaration exists.
- **API-05** (connections API removed): grep for `addConnection|removeConnection|getConnection|getNodeFrom` in src/ — zero results.
- **API-06** (deprecated type aliases removed): grep for `OscillatorOpts[^i]|OscillatorOptsFilterValues` in src/ — zero results.
- **API-07** (commented-out `stopAfter` removed): grep for `stopAfter` in src/ — zero results.
- **DOC-01** (JSDoc updated for renamed methods): read src/base-sound.ts, src/controllers/base-param-controller.ts, src/track.ts — `.as()` appears in all JSDoc @example blocks; no `.from()` in JSDoc examples for update/seek chains.

Write the file with `status: passed`, `score: 8/8`. Include evidence column with specific file:line references from grep/read results. Format frontmatter as:

```yaml
---
phase: 18-breaking-api-cleanup
verified: 2026-02-19T[current-time]Z
status: passed
score: 8/8 must-haves verified
---
```

**For 19-VERIFICATION.md** (at `.planning/phases/19-dx-improvements/19-VERIFICATION.md`):

Phase 19 goal: Developers can accomplish common audio tasks with less boilerplate, and the documentation reflects all new capabilities.

Verify each requirement via grep/read:

- **DX-01** (effect bypass auto-rewires): read src/base-sound.ts — `Object.defineProperty` intercept on bypass setter; `rewireEffects` called in the intercept.
- **DX-02** (context-free effect factories): read src/effects/filter-effect.ts, src/effects/gain-effect.ts — function overloads with no-AudioContext path using `getOrCreateAudioContext`.
- **DX-03** (`addEffects([])` batch): grep for `addEffects` in src/base-sound.ts — method exists; grep in src/index.ts — exported.
- **DX-04** (`playTogether([])`): grep for `playTogether` in src/index.ts — function exported.
- **DX-05** (`createSounds([])` + progress): grep for `createSounds` in src/index.ts — exported function with progress callback parameter.
- **DX-06** (`getFilters()` on Oscillator): grep for `getFilters` in src/oscillator.ts — method returns readonly filter array.
- **DX-07** (`getSounds()` on Sampler): grep for `getSounds` in src/sampler.ts — method returns readonly sounds array.
- **DX-08** (extensible `ControlType` via `ControlTypeMap`): grep for `ControlTypeMap` in src/ — interface defined for module augmentation.
- **DEF-05** (`unlockAudioContext` documented): grep for `unlockAudioContext` in src/audio-context.ts and src/index.ts — exported with JSDoc.
- **DOC-03** (guide pages updated): confirm docs/guide/getting-started.md and docs/guide/core-concepts.md contain `.as()` and new convenience API examples.

Write the file with `status: passed`, `score: 10/10`. Include evidence column with specific file:line references. Format frontmatter as:

```yaml
---
phase: 19-dx-improvements
verified: 2026-02-19T[current-time]Z
status: passed
score: 10/10 must-haves verified
---
```
  </action>
  <verify>
Run: `ls /Users/seth/Documents/GitHub/ez-audio/.planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md /Users/seth/Documents/GitHub/ez-audio/.planning/phases/19-dx-improvements/19-VERIFICATION.md`
Both files must exist.

Run: `grep -c "VERIFIED" /Users/seth/Documents/GitHub/ez-audio/.planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md`
Must return 8 (one per requirement row).

Run: `grep -c "VERIFIED" /Users/seth/Documents/GitHub/ez-audio/.planning/phases/19-dx-improvements/19-VERIFICATION.md`
Must return 10 (one per requirement row).
  </verify>
  <done>
18-VERIFICATION.md exists with status: passed and 8 VERIFIED rows.
19-VERIFICATION.md exists with status: passed and 10 VERIFIED rows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix createWhiteNoise JSDoc and add audioContextAwareTimeout to CHANGELOG</name>
  <files>
    src/index.ts
    CHANGELOG.md
  </files>
  <action>
Two targeted fixes:

**Fix 1 — src/index.ts JSDoc example in createWhiteNoise:**

The `createWhiteNoise` JSDoc @example block uses the old context-explicit factory call:
```typescript
const lowpass = createFilterEffect(await getAudioContext(), 'lowpass', {
  frequency: 400
})
```

Replace with the context-free 1.0 API:
```typescript
const lowpass = createFilterEffect('lowpass', {
  frequency: 400
})
```

Do NOT change any runtime code — only the @example comment block. The `await getAudioContext()` call inside the JSDoc example string should be removed entirely (not just the argument — remove the `await getAudioContext(), ` portion from that line).

**Fix 2 — CHANGELOG.md:**

In the `## [1.0.0] - 2026-02-17` section, under `### Added`, add `audioContextAwareTimeout` to the list of new exports. Insert after the `getSounds()` on Sampler bullet and before any `### Fixed` section. Add:

```
- **`audioContextAwareTimeout` utility** -- schedules callbacks synchronized to AudioContext time, enabling audio-accurate UI timers (e.g., beat-synchronized visual updates in drum machine UIs)
```

Do not modify any other CHANGELOG content.
  </action>
  <verify>
Run: `grep "createFilterEffect('lowpass'" /Users/seth/Documents/GitHub/ez-audio/src/index.ts`
Must find the fixed example (no `await getAudioContext()` argument).

Run: `grep "audioContextAwareTimeout" /Users/seth/Documents/GitHub/ez-audio/CHANGELOG.md`
Must return at least one match in the 1.0.0 section.

Run: `cd /Users/seth/Documents/GitHub/ez-audio && pnpm typecheck`
Must pass — JSDoc-only change should not affect types.
  </verify>
  <done>
createWhiteNoise @example uses context-free `createFilterEffect('lowpass', {...})`.
CHANGELOG.md 1.0.0 ### Added section mentions audioContextAwareTimeout.
pnpm typecheck passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all fixes and trigger npm 1.0.0 publish</name>
  <action>Human verification and npm publish trigger — see how-to-verify below.</action>
  <what-built>
Created 18-VERIFICATION.md and 19-VERIFICATION.md formally closing the Phase 18 and 19 verification gaps. Fixed createWhiteNoise JSDoc example to use context-free API. Added audioContextAwareTimeout to CHANGELOG.md 1.0.0 entry.

The v1.0 milestone audit blockers are resolved. To complete the milestone and publish npm 1.0.0, trigger the CI publish pipeline by creating and pushing the v1.0.0 git tag.
  </what-built>
  <how-to-verify>
1. Review the verification docs (optional):
   - `.planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md`
   - `.planning/phases/19-dx-improvements/19-VERIFICATION.md`

2. Check the JSDoc fix: `grep -A3 "Filter white noise" src/index.ts` — should show `createFilterEffect('lowpass', {` with no `await getAudioContext()`.

3. Check CHANGELOG: `grep "audioContextAwareTimeout" CHANGELOG.md` — should match in the 1.0.0 ### Added section.

4. **To publish npm 1.0.0**, run:
   ```bash
   git tag v1.0.0 && git push origin v1.0.0
   ```
   This triggers `.github/workflows/publish.yml` which runs tests, builds, and publishes `ez-web-audio@1.0.0` to npm.

5. After a few minutes, verify: `npm view ez-web-audio version` should return `1.0.0`.
  </how-to-verify>
  <resume-signal>Type "published" after npm 1.0.0 is live, or "skip-publish" to complete the phase without publishing.</resume-signal>
</task>

</tasks>

<verification>
All three success criteria groups must be satisfied:

1. Verification docs:
   - `ls .planning/phases/18-breaking-api-cleanup/18-VERIFICATION.md` — exists
   - `ls .planning/phases/19-dx-improvements/19-VERIFICATION.md` — exists
   - Both have `status: passed` in frontmatter

2. Documentation fixes:
   - `grep "createFilterEffect('lowpass'" src/index.ts` — matches
   - `grep "audioContextAwareTimeout" CHANGELOG.md` — matches in 1.0.0 section

3. Publish (human-verified):
   - `npm view ez-web-audio version` returns `1.0.0`
</verification>

<success_criteria>
1. 18-VERIFICATION.md exists with 8/8 requirements VERIFIED
2. 19-VERIFICATION.md exists with 10/10 requirements VERIFIED
3. createWhiteNoise @example uses context-free createFilterEffect (no await getAudioContext() argument)
4. CHANGELOG.md 1.0.0 entry lists audioContextAwareTimeout
5. npm ez-web-audio@1.0.0 is published (human gate)
</success_criteria>

<output>
After completion, create `.planning/phases/24-milestone-verification-release/24-01-SUMMARY.md`
</output>
