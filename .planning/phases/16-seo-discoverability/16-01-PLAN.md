---
phase: 16-seo-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/config.mts
  - docs/index.md
autonomous: true

must_haves:
  truths:
    - "Docs site has proper meta title and description tags for search engines"
    - "OpenGraph and Twitter Card tags render rich previews when shared on social media"
    - "Homepage hero, tagline, and feature cards clearly communicate what ez-web-audio does and why to use it"
    - "Structured data (JSON-LD) identifies the site as a SoftwareSourceCode project for search engines"
  artifacts:
    - path: "docs/.vitepress/config.mts"
      provides: "VitePress head config with meta, OG, Twitter, and JSON-LD tags"
      contains: "head:"
    - path: "docs/index.md"
      provides: "Homepage with compelling hero messaging and keyword-rich feature cards"
      contains: "hero:"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "all docs pages"
      via: "VitePress head injection"
      pattern: "head:.*\\[.*meta"
---

<objective>
Add comprehensive SEO metadata (meta tags, OpenGraph, Twitter Cards, JSON-LD structured data) to the VitePress docs site configuration, and rewrite homepage messaging for clear value communication and keyword optimization.

Purpose: Make the docs site discoverable by audio developers searching for web audio libraries, and ensure rich previews when links are shared on social media or developer communities.
Output: Updated VitePress config with SEO head tags and an improved homepage with compelling, keyword-rich messaging.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/.vitepress/config.mts
@docs/index.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO meta tags, OpenGraph, Twitter Cards, and JSON-LD to VitePress config</name>
  <files>docs/.vitepress/config.mts</files>
  <action>
Add a `head` array to the VitePress `defineConfig` with the following tags:

1. **Basic meta tags:**
   - `<meta name="keywords" content="web audio api, javascript audio library, typescript audio, synthesizer, oscillator, drum machine, audio effects, sound playback, browser audio, audio visualization">`
   - `<meta name="author" content="Seth Brasile">`
   - `<meta name="robots" content="index, follow">`

2. **OpenGraph tags:**
   - `og:type` = `website`
   - `og:title` = `EZ Web Audio - Simple Web Audio API for JavaScript & TypeScript`
   - `og:description` = `Zero-dependency TypeScript library that makes the Web Audio API easy. Play sounds, create synthesizers, build drum machines, and add audio effects with minimal code.`
   - `og:url` = `https://sethbrasile.github.io/ez-web-audio/`
   - `og:site_name` = `EZ Web Audio`

3. **Twitter Card tags:**
   - `twitter:card` = `summary`
   - `twitter:title` = `EZ Web Audio - Simple Web Audio API for JavaScript & TypeScript`
   - `twitter:description` = `Zero-dependency TypeScript library that makes the Web Audio API easy. Play sounds, create synthesizers, build drum machines, and add audio effects with minimal code.`

4. **JSON-LD structured data** (as a `<script type="application/ld+json">` tag):
   ```json
   {
     "@context": "https://schema.org",
     "@type": "SoftwareSourceCode",
     "name": "ez-web-audio",
     "description": "Zero-dependency TypeScript library that wraps the Web Audio API with a simpler, more intuitive interface.",
     "url": "https://github.com/sethbrasile/ez-web-audio",
     "codeRepository": "https://github.com/sethbrasile/ez-web-audio",
     "programmingLanguage": ["TypeScript", "JavaScript"],
     "runtimePlatform": "Browser",
     "license": "https://opensource.org/licenses/MIT",
     "author": {
       "@type": "Person",
       "name": "Seth Brasile",
       "url": "https://github.com/sethbrasile"
     }
   }
   ```

Also update the top-level `description` field in defineConfig to be more keyword-rich:
`"A zero-dependency TypeScript library that makes the Web Audio API easy — play sounds, synthesizers, drum machines, and audio effects with minimal code."`

Use VitePress `head` config format: `['tag', { attr: 'value' }, 'content']`. For the JSON-LD script tag, use: `['script', { type: 'application/ld+json' }, JSON.stringify(structuredData)]`.
  </action>
  <verify>Run `pnpm build` to confirm VitePress builds successfully with the new head config. Inspect the built HTML (`docs/.vitepress/dist/index.html`) to verify meta tags, OG tags, and JSON-LD script are present.</verify>
  <done>VitePress config has comprehensive SEO head tags (meta, OG, Twitter, JSON-LD) and builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite homepage hero messaging and feature cards for value communication and keywords</name>
  <files>docs/index.md</files>
  <action>
Rewrite the homepage frontmatter (`docs/index.md`) to better communicate value and include relevant keywords:

1. **Hero section:**
   - `name`: Keep "EZ Web Audio"
   - `text`: Change to something more descriptive like "The Simple Web Audio API for JavaScript & TypeScript" (includes key search terms)
   - `tagline`: Change to something that communicates the value proposition clearly, e.g. "Play sounds, create synthesizers, build drum machines, and visualize audio — all with zero dependencies and full TypeScript support."

2. **Actions:**
   - Keep "Get Started" (brand theme)
   - Change "View on GitHub" to "Try the Examples" linking to `/examples/` (alt theme) — this is a better CTA for new visitors discovering via search; GitHub link is already in the nav social links
   - Add a third action: "API Reference" linking to `/api/` (alt theme)

3. **Features section** — rewrite for clarity and keywords:
   - **"Play Sounds in 3 Lines"** — details: "Load and play audio files, create synthesizers, or build drum machines. No Web Audio boilerplate needed." (shows simplicity + keywords)
   - **"TypeScript-First"** — details: "Complete type safety with IntelliSense support. Every class, method, and option is fully typed — no @types packages needed." (emphasizes DX)
   - **"Zero Dependencies"** — details: "Pure Web Audio API wrapper with nothing extra. Tree-shakeable ESM build keeps your bundle small." (technical credibility)
   - **"Rich Feature Set"** — details: "ADSR envelopes, effects chain, audio sprites, beat sequencer, crossfade, visualization, and more — everything you need for browser audio." (keyword-rich feature list)

4. **Add content below the frontmatter** — a brief "What is EZ Web Audio?" section with a quick code example showing how simple it is to play a sound (3-4 lines). This gives search engines indexable text content beyond the frontmatter-driven layout. Example:

```markdown
## What is EZ Web Audio?

EZ Web Audio is a TypeScript library that wraps the Web Audio API with a simpler interface. Instead of dealing with AudioContext, AudioBufferSourceNode, and GainNode directly, you write clean, readable code:

\`\`\`typescript
import { createSound } from 'ez-web-audio'

const sound = await createSound('/audio/click.mp3')
sound.play()
\`\`\`

From simple sound effects to full synthesizers with ADSR envelopes, EZ Web Audio handles the complexity so you can focus on creating.
```
  </action>
  <verify>Run `pnpm build` to confirm VitePress builds successfully. View the built homepage HTML to verify the new content is present and well-structured.</verify>
  <done>Homepage clearly communicates what ez-web-audio is, why to use it, and includes keyword-rich content for search engine discoverability. CTAs guide visitors to examples and getting started guide.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. Built `docs/.vitepress/dist/index.html` contains:
   - `<meta name="keywords"` tag with audio-related terms
   - `<meta property="og:title"` tag
   - `<meta name="twitter:card"` tag
   - `<script type="application/ld+json">` with SoftwareSourceCode schema
3. Homepage hero text includes "Web Audio API" and "TypeScript" keywords
4. Homepage has indexable text content below the hero section
5. CTAs include "Get Started", "Try the Examples", and "API Reference"
</verification>

<success_criteria>
- VitePress site builds and deploys with comprehensive SEO metadata
- Homepage immediately communicates what ez-web-audio does (audio library), who it's for (JS/TS developers), and why to use it (simple, zero deps, full-featured)
- Social media link previews show rich title and description via OpenGraph tags
- Search engines can index keyword-rich content on the homepage
</success_criteria>

<output>
After completion, create `.planning/phases/16-seo-discoverability/16-01-SUMMARY.md`
</output>
