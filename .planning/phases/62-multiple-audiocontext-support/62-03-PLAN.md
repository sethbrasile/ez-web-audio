---
phase: 62-multiple-audiocontext-support
plan: 03
type: execute
wave: 2
depends_on: [62-01, 62-02]
files_modified:
  - src/index.test.ts
  - src/utils/play-together.test.ts
  - docs/guide/multiple-contexts.md
  - docs/.vitepress/config.mts
autonomous: true
requirements: [SC-05, SC-06]

must_haves:
  truths:
    - "Tests verify explicit context path for factory functions"
    - "Tests verify default path still works (no regression)"
    - "Guide page documents why, when, browser limits, and shared-context constraint"
    - "Guide page is linked from sidebar"
  artifacts:
    - path: "src/index.test.ts"
      provides: "Tests for BaseAudioContext overloads on factory functions"
      contains: "BaseAudioContext"
    - path: "src/utils/play-together.test.ts"
      provides: "Tests for playTogether BaseAudioContext overload"
      contains: "BaseAudioContext"
    - path: "docs/guide/multiple-contexts.md"
      provides: "Advanced usage guide for multiple AudioContexts"
      contains: "multiple"
    - path: "docs/.vitepress/config.mts"
      provides: "Sidebar entry for guide page"
      contains: "multiple-contexts"
  key_links:
    - from: "docs/.vitepress/config.mts"
      to: "docs/guide/multiple-contexts.md"
      via: "sidebar link"
      pattern: "multiple-contexts"
---

<objective>
Write tests for all overloaded factory function paths (explicit context vs default) and create the advanced usage guide page documenting multiple AudioContext patterns.

Purpose: Verify the overloads work correctly and document the feature for power users.
Output: Comprehensive tests and a guide page linked from the sidebar.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-multiple-audiocontext-support/62-CONTEXT.md
@.planning/phases/62-multiple-audiocontext-support/62-RESEARCH.md
@.planning/phases/62-multiple-audiocontext-support/62-01-SUMMARY.md
@.planning/phases/62-multiple-audiocontext-support/62-02-SUMMARY.md

@src/index.ts
@src/index.test.ts
@src/utils/play-together.test.ts
@docs/.vitepress/config.mts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests for BaseAudioContext overloads</name>
  <files>src/index.test.ts, src/utils/play-together.test.ts</files>
  <action>
Add a new describe block to `src/index.test.ts` for explicit AudioContext overloads. CRITICAL: Must stub `BaseAudioContext` as a global in addition to `AudioContext` for `instanceof BaseAudioContext` to work in the happy-dom test environment.

**Test setup pattern:**
```typescript
import { AudioContext as Mock } from 'standardized-audio-context-mock'

// In beforeEach:
const MockCtx = Mock as unknown as typeof AudioContext
vi.stubGlobal('AudioContext', MockCtx)
vi.stubGlobal('BaseAudioContext', MockCtx) // Required for instanceof check
const audioContext = new Mock() as unknown as AudioContext
```

**Tests to write in `src/index.test.ts`:**

1. `describe('factory functions with explicit AudioContext')` containing:
   - `createOscillator(ctx, options)` returns Oscillator using provided context
   - `createPolySynth(ctx, options)` returns PolySynth using provided context
   - `createTransport(ctx, options)` returns Transport using provided context
   - `createLayeredSound(ctx, layers)` returns LayeredSound using provided context
   - `createGrainPlayer(ctx, buffer, options)` returns GrainPlayer using provided context
   - `createAnalyzer(ctx, options)` returns Analyzer using provided context (update existing test if it uses AudioContext to use BaseAudioContext)
   - `createWhiteNoise(ctx)` returns Sound using provided context
   - `createNoise(ctx, 'white')` returns Sound using provided context
   - `createNoise(ctx, 'pink')` returns Sound using provided context
   - `createNoise(ctx, 'brown')` returns Sound using provided context

2. For audio-loading factories (createSound, createTrack, createSounds, createTracks, createBeatTrack, createSampler, createFont, createSprite): These require network mocking (fetch). Add tests that verify the overload signature compiles correctly. If existing tests already mock fetch, add explicit-context variants. Otherwise, test at minimum that the function exists with the correct overload arity.

3. Each test should verify:
   - The function resolves successfully with explicit context
   - The returned object is the correct type

**Tests to add in `src/utils/play-together.test.ts`:**
- `playTogether(ctx, playables)` uses the provided context's currentTime
- `playTogether(playables)` still works (default path, no regression)

**IMPORTANT:** Also add `vi.stubGlobal('BaseAudioContext', ...)` to any existing test `beforeEach` blocks that set up `AudioContext` globally and test the effect factories' overload paths. Check the existing effect test files — if they already test the `createDelay(ctx, opts)` pattern, add the `BaseAudioContext` stub so `instanceof BaseAudioContext` works.
  </action>
  <verify>
    <automated>pnpm test src/index.test.ts src/utils/play-together.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Tests exist for explicit-context paths of factory functions. Tests exist for default paths (no regressions). All new and existing tests pass. BaseAudioContext is properly stubbed in test environments.</done>
</task>

<task type="auto">
  <name>Task 2: Create multiple AudioContexts guide page and sidebar entry</name>
  <files>docs/guide/multiple-contexts.md, docs/.vitepress/config.mts</files>
  <action>
Create `docs/guide/multiple-contexts.md` with the following structure. Code examples only, no interactive demo (per user decision).

```markdown
# Multiple AudioContexts

By default, EZ Web Audio uses a single shared AudioContext — this is the right choice
for most applications. But advanced use cases sometimes need multiple contexts.

## When to Use Multiple Contexts

- **Separate output devices** — Route monitoring to headphones while main mix goes to speakers via `sinkId`
- **Context isolation** — Close one context without orphaning sounds in another (e.g., independent widgets)
- **Multiple widgets on one page** — Each widget manages its own audio lifecycle
- **Different sample rates** — 44.1kHz for playback, 48kHz for processing

## Usage

Every factory function accepts an optional `AudioContext` as its first parameter:

\`\`\`typescript
import { createSound, createOscillator, createDelay } from 'ez-web-audio'

// Default — uses the shared singleton (recommended for most apps)
const sound = await createSound('click.mp3')
const synth = await createOscillator({ frequency: 440 })

// Explicit — uses your own AudioContext
const ctx = new AudioContext({ sampleRate: 48000 })
const sound2 = await createSound(ctx, 'click.mp3')
const synth2 = await createOscillator(ctx, { frequency: 440 })
const delay = createDelay(ctx, { time: 0.3, feedback: 0.4 })
\`\`\`

## Important Constraints

### Sounds and effects must share a context

All audio nodes in a connected graph must belong to the same AudioContext.
A sound created with context A cannot use an effect created with context B:

\`\`\`typescript
const ctxA = new AudioContext()
const ctxB = new AudioContext()

const sound = await createSound(ctxA, 'click.mp3')
const delay = createDelay(ctxB, { time: 0.3 })

// This will throw — nodes from different contexts can't connect
sound.addEffect(delay) // Error!
\`\`\`

### Browser limits

Chrome limits active AudioContexts to approximately 6. Exceeding this may cause
context creation to silently fail or existing contexts to be suspended. Keep context
count low and close contexts you no longer need.

### When NOT to use multiple contexts

Most applications should use the default singleton. Multiple contexts add complexity
and consume browser resources. Use them only when you have a specific need listed above.

## Full Example: Monitoring Output

\`\`\`typescript
import { createSound, createTrack, createDelay } from 'ez-web-audio'

// Main mix — default speakers
const mainCtx = new AudioContext()
const music = await createTrack(mainCtx, 'song.mp3')
const delay = createDelay(mainCtx, { time: 0.3, wet: 0.3 })
music.addEffect(delay)

// Monitor — headphones (if available)
const monitorCtx = new AudioContext({ sinkId: 'headphone-device-id' })
const click = await createSound(monitorCtx, 'metronome.mp3')

// Each context is independent
music.play()     // Goes to speakers
click.play()     // Goes to headphones

// Clean up one without affecting the other
monitorCtx.close()  // click stops, music continues
\`\`\`
```

Adjust prose and examples for clarity. Follow the existing guide page conventions (check `docs/guide/getting-started.md` for tone/style reference).

**Sidebar entry:** In `docs/.vitepress/config.mts`, find the Guide sidebar section and add an entry for the new page. Place it after the existing guide pages, as a separate "Advanced" subsection or at the end of the Guide items list:

```typescript
{ text: 'Multiple AudioContexts', link: '/guide/multiple-contexts' }
```

Look at the existing sidebar structure and place it logically — likely under an "Advanced" group or at the end of the Guide section.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Guide page exists at `docs/guide/multiple-contexts.md` with all 4 use cases, browser limitations, shared-context constraint, "when NOT to use" section, and full example. Sidebar links to the page. Build succeeds.</done>
</task>

</tasks>

<verification>
Full test suite and build:
```bash
pnpm test && pnpm typecheck && pnpm build
```
All tests pass, types check, docs build with new page.
</verification>

<success_criteria>
- Tests cover explicit-context and default paths for factory functions
- BaseAudioContext is properly stubbed in test environment
- Guide page documents all 4 use cases per CONTEXT.md
- Guide page includes browser limitations and shared-context constraint
- Guide page includes "when NOT to use" section
- Sidebar links to the new guide page
- Full build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/62-multiple-audiocontext-support/62-03-SUMMARY.md`
</output>
