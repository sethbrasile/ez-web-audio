---
phase: 38-final-documentation-sync
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: [SYNC2-03, SYNC2-04]

must_haves:
  truths:
    - "CHANGELOG.md [Unreleased] section includes all Phase 37 additions"
    - "CHANGELOG.md [Unreleased] section has complete Phase 32-38 record"
    - "pnpm lint passes with zero errors"
    - "pnpm typecheck passes with zero errors"
    - "pnpm test passes with all tests green"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Complete Phase 32-38 change record"
      contains: "createNoise"
  key_links:
    - from: "CHANGELOG.md"
      to: "src/index.ts"
      via: "all new exports listed in changelog"
      pattern: "createNoise|createTracks|AudioInput|volume|TypedEventEmitter"
---

<objective>
Update CHANGELOG.md with Phase 37 additions and run full verification suite (lint + typecheck + tests) to confirm project health.

Purpose: SYNC2-03 requires full CI-equivalent checks pass, SYNC2-04 requires complete changelog record.
Output: Updated CHANGELOG.md and verified passing CI checks.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-final-documentation-sync/38-01-SUMMARY.md
@.planning/phases/37-nice-to-have-dx-features/37-01-SUMMARY.md
@.planning/phases/37-nice-to-have-dx-features/37-02-SUMMARY.md
@.planning/phases/37-nice-to-have-dx-features/37-03-SUMMARY.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG with Phase 37 additions and verify completeness</name>
  <files>CHANGELOG.md</files>
  <action>
Add Phase 37 features to the `[Unreleased]` section of CHANGELOG.md under the **Added** subsection:

- **`createSound()` / `createTrack()` accept `ArrayBuffer`, `Blob`, or `File`** — not just URL strings; use `AudioInput` type
- **`createNoise('white' | 'pink' | 'brown')`** — unified noise generation factory (pink uses Voss-McCartney algorithm, brown uses random walk)
- **`volume` getter/setter** on BaseSound — alias for `changeGainTo()` with same validation
- **`createTracks(urls[], onProgress?)`** — batch Track loader matching `createSounds()` pattern
- **Typed event sources** — event detail `source` field typed as `AudioEventSource` (was `unknown`)
- **Narrowed `ControlType` per class** — `Sound.update()` accepts `SoundControlType` (`'gain' | 'pan' | 'detune'`); `Oscillator.update()` accepts full `ControlType` (including `'frequency'`)
- **Event map types exported** — `SoundEventMap`, `BeatTrackEventMap`, `LayeredSoundEventMap`, `AudioEventSource`, `SoundEventType`, and all detail types
- **`TypedEventEmitter<TMap>`** — shared base class for typed event systems, exported for advanced consumers
- **`onPlaySet()` / `onPlayRamp()` consume-once behavior** prominently documented in JSDoc

Also add under **Changed**:
- `BaseSound` and `LayeredSound` now extend `TypedEventEmitter` (DRY refactor, ~120 lines removed)

Then review the entire [Unreleased] section to verify it covers ALL changes from Phases 32-38:
- Phase 32: EnvelopeOptions short names, LICENSE, Playable interface, Sound opts type, createFont errors, CLAUDE.md fix, AudioSprite loop fix, stale setTimeout fix, Oscillator GainNode warning
- Phase 33: fadeIn/fadeOut, loop, dispose, createAnalyzer overload, note-based oscillator, setPattern
- Phase 34: Test count increase (already noted)
- Phase 35: Example pages, concepts.md split, synthesis.md fix, audio-routing.md fix
- Phase 36: Documentation sync (no user-facing changes besides docs)
- Phase 37: All items listed above
- Phase 38: Documentation sync (no user-facing changes besides docs)

If any Phase 32-36 items are missing from [Unreleased], add them. Documentation-only changes (Phases 35, 36, 38) generally don't need changelog entries unless they add new example pages.
  </action>
  <verify>
Grep CHANGELOG.md for key Phase 37 terms: `grep -n "createNoise\|AudioInput\|createTracks\|TypedEventEmitter\|volume\|SoundControlType\|AudioEventSource" CHANGELOG.md`. All should have at least one hit.
  </verify>
  <done>
CHANGELOG.md [Unreleased] section has a complete record of all Phases 32-38 user-facing changes, including all Phase 37 additions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full verification suite</name>
  <files></files>
  <action>
Run the complete CI-equivalent verification:

1. `pnpm lint` — must pass with zero errors
2. `pnpm typecheck` — must pass with zero errors
3. `pnpm test` — all tests must pass

If any fail:
- Lint errors in docs/ markdown code blocks: fix the code block syntax
- Lint errors in modified guide pages: fix import ordering or formatting
- Typecheck errors: investigate and fix (likely from doc code blocks or recent changes)
- Test failures: investigate — Phase 37 changes should not have broken tests

Do NOT fix pre-existing failures unrelated to Phase 38 changes. Only fix issues introduced by this phase's documentation changes.
  </action>
  <verify>
All three commands exit with code 0:
- `pnpm lint` → 0 errors
- `pnpm typecheck` → 0 errors
- `pnpm test` → all tests pass
  </verify>
  <done>
Full lint + typecheck + test suite passes with zero errors, confirming project health after all Phase 32-38 changes.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "createNoise" CHANGELOG.md` returns at least 1
2. `grep -c "createTracks" CHANGELOG.md` returns at least 1
3. `grep -c "TypedEventEmitter" CHANGELOG.md` returns at least 1
4. `pnpm lint` exits 0
5. `pnpm typecheck` exits 0
6. `pnpm test` exits 0
</verification>

<success_criteria>
CHANGELOG.md contains a complete record of all Phases 32-38 changes. Full lint, typecheck, and test suite all pass with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/38-final-documentation-sync/38-02-SUMMARY.md`
</output>
