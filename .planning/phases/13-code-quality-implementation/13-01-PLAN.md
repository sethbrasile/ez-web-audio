---
phase: 13-code-quality-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/base-sound.ts
  - src/controllers/base-param-controller.ts
  - src/controllers/sound-controller.ts
  - src/controllers/oscillator-controller.ts
  - src/effects/filter-effect.ts
  - src/effects/effect-wrapper.ts
  - src/index.ts
  - src/interfaces/playable.ts
autonomous: true

must_haves:
  truths:
    - "Duplicated equal-power crossfade logic exists in one shared location"
    - "Duplicated applyRampValues logic is reduced between controllers"
    - "Safe disconnect pattern replaces repeated try/catch blocks in wireEffectChain"
    - "Dead code (commented-out lines) removed from codebase"
    - "All existing tests continue to pass after refactoring"
  artifacts:
    - path: "src/utils/equal-power-crossfade.ts"
      provides: "Shared equal-power crossfade calculation"
      exports: ["applyEqualPowerCrossfade"]
    - path: "src/base-sound.ts"
      provides: "Refactored wireEffectChain with safeDisconnect helper"
    - path: "src/index.ts"
      provides: "Clean code without commented-out lines or dead code"
  key_links:
    - from: "src/effects/filter-effect.ts"
      to: "src/utils/equal-power-crossfade.ts"
      via: "import"
      pattern: "import.*equal-power-crossfade"
    - from: "src/effects/effect-wrapper.ts"
      to: "src/utils/equal-power-crossfade.ts"
      via: "import"
      pattern: "import.*equal-power-crossfade"
---

<objective>
Extract shared helpers, remove code duplication, and eliminate dead code across the library source.

Purpose: Reduce LOC, improve maintainability, and eliminate copy-paste patterns flagged by the Phase 12 audit (QUAL-02).
Output: Cleaner library source with shared utilities and no dead code.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-audit/AUDIT-code-quality.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared helpers and remove duplication</name>
  <files>
    src/utils/equal-power-crossfade.ts
    src/effects/filter-effect.ts
    src/effects/effect-wrapper.ts
    src/base-sound.ts
  </files>
  <action>
1. Create `src/utils/equal-power-crossfade.ts` with a shared function:
   ```typescript
   export function applyEqualPowerCrossfade(
     dryGain: GainNode, wetGain: GainNode, mix: number, bypass: boolean
   ): void
   ```
   The logic: if bypass, set dry=1 wet=0. Otherwise compute `angle = mix * 0.5 * Math.PI`, dry=cos(angle), wet=sin(angle). This is currently duplicated identically in FilterEffect.applyMix() and EffectWrapper.applyMix().

2. Update `src/effects/filter-effect.ts`: Replace the private `applyMix()` body with a call to `applyEqualPowerCrossfade(this.dryGain, this.wetGain, this._mix, this._bypass)`. Keep the method as a thin wrapper.

3. Update `src/effects/effect-wrapper.ts`: Same change — replace `applyMix()` body with the shared function call.

4. In `src/base-sound.ts`, extract a `safeDisconnect(node: AudioNode): void` private helper method that wraps the try/catch pattern. Replace the 4 identical try/catch blocks in `wireEffectChain()` (lines ~228-267) and the effect loop with calls to `safeDisconnect()`. This should reduce wireEffectChain from ~73 lines to ~40 lines.

5. Do NOT change any public API signatures. All changes are internal refactoring only.
  </action>
  <verify>
Run `pnpm test` — all 711+ tests must pass. Run `pnpm typecheck` — no type errors. Run `pnpm build:lib` — build succeeds.
  </verify>
  <done>
Equal-power crossfade exists in one shared utility. wireEffectChain uses safeDisconnect helper. No duplicated logic remains in the three effect files or base-sound disconnect blocks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and clean up TODOs</name>
  <files>
    src/index.ts
    src/interfaces/playable.ts
    src/controllers/base-param-controller.ts
    src/beat-track.ts
  </files>
  <action>
1. In `src/index.ts`:
   - Line 641: Remove the commented-out `touchcancel` event listener in `useInteractionMethods()`. Either uncomment it (it should be included since touchcancel is a valid event) or delete it. Decision: UNCOMMENT it — touchcancel should trigger stop() just like touchend, preventing stuck playing state on interrupted touches.
   - Line 122 (TODO about unlockAudioContext): Replace TODO with a JSDoc comment explaining why unlockAudioContext is needed: "unlockAudioContext handles Safari/iOS where AudioContext starts suspended and requires a user gesture to resume. Without this, the first synth note may hang because the context never resumes."

2. In `src/interfaces/playable.ts`:
   - Line 13: Remove the commented-out `stopAfter` interface member.

3. In `src/controllers/base-param-controller.ts`:
   - Line 57 (TODO about handle all gainNode/pannerNode props): Replace with a JSDoc comment: "Currently exposes gain and pan. Additional AudioParam properties (e.g., for spatial audio) should be added via mapped types in v2."
   - Line 102 (TODO about renaming 'from'): Replace with: "Note: The .from() method name in the fluent API is a known DX concern — deferred to v2 as a breaking change."

4. In `src/beat-track.ts`:
   - Add a brief inline comment above the scheduler() method (line 305) explaining the lookahead scheduling pattern: "Lookahead scheduler: checks every 25ms and schedules beats 100ms ahead of current time. This prevents timing gaps from JS event loop jitter while keeping beat triggers close to real-time for UI sync."

5. Do NOT remove any TODOs that represent actual future work items — convert them to descriptive comments explaining the rationale.
  </action>
  <verify>
Run `pnpm test` — all tests pass. Run `pnpm typecheck` — no type errors. Grep for commented-out code: `grep -n "^[[:space:]]*//.*stopAfter\|^[[:space:]]*// key.addEventListener" src/interfaces/playable.ts src/index.ts` should return no results.
  </verify>
  <done>
No commented-out dead code remains in playable.ts or index.ts. TODOs converted to explanatory comments. beat-track scheduler has inline documentation.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (all 711+ tests)
- `pnpm typecheck` passes
- `pnpm build:lib` succeeds
- No commented-out code in playable.ts or index.ts useInteractionMethods
- Equal-power crossfade function exists at src/utils/equal-power-crossfade.ts
- FilterEffect and EffectWrapper both import from the shared utility
</verification>

<success_criteria>
- Code duplication measurably reduced (crossfade logic in 1 place instead of 2, disconnect in 1 helper instead of 4 inline blocks)
- Dead code removed (commented-out lines eliminated)
- TODOs converted to documented rationale
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-quality-implementation/13-01-SUMMARY.md`
</output>
