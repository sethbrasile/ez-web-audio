---
phase: 13-code-quality-implementation
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/base-sound.ts
  - src/interfaces/connectable.ts
  - src/effects/effect-wrapper.ts
  - src/controllers/base-param-controller.ts
autonomous: true

must_haves:
  truths:
    - "All public API methods have accurate JSDoc with @example tags"
    - "Deprecated APIs have @deprecated JSDoc tags"
    - "Duck-typing branches in EffectWrapper have explanatory JSDoc"
    - "AudioSource interface has JSDoc explaining its purpose"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/base-sound.ts"
      provides: "JSDoc @deprecated on connections, addConnection, removeConnection, startOffset"
    - path: "src/effects/effect-wrapper.ts"
      provides: "JSDoc examples for duck-typing branches"
    - path: "src/controllers/base-param-controller.ts"
      provides: "JSDoc on AudioSource interface"
  key_links:
    - from: "src/base-sound.ts"
      to: "@deprecated tags"
      via: "JSDoc annotations"
      pattern: "@deprecated"
---

<objective>
Review and fix JSDoc/TypeDoc across all public API surfaces for accuracy and completeness.

Purpose: Ensure generated API docs (TypeDoc) are accurate, complete, and helpful for library consumers (QUAL-04).
Output: Comprehensive JSDoc on all public APIs, deprecated tags on legacy methods, and explanatory comments on complex patterns.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-audit/AUDIT-developer-experience.md
@.planning/phases/13-code-quality-implementation/13-01-SUMMARY.md
@.planning/phases/13-code-quality-implementation/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @deprecated JSDoc tags and fix missing documentation</name>
  <files>
    src/base-sound.ts
    src/interfaces/connectable.ts
  </files>
  <action>
1. In `src/base-sound.ts`:
   - `addConnection()` method (line 576): Add `@deprecated Use addEffect() instead. The connections array and addConnection/removeConnection methods are from the legacy effect system.` as a JSDoc tag. Keep the existing method functional — deprecation is informational only.
   - `removeConnection()` method (line 592): Add same `@deprecated Use removeEffect() instead.` JSDoc tag.
   - `getConnection()` method (line 615): Add `@deprecated Use getEffects() instead.`
   - `getNodeFrom()` method (line 620): Add `@deprecated Use getEffects() instead.`
   - The `connections` property (line 129) already has @deprecated — verify it's correct.
   - `startOffset` property (line 142): Change from `public` to `protected`. This is a Web Audio internal that should not be part of the public API. The Track class accesses it internally via inheritance. If any test accesses startOffset directly, keep it public but add `@deprecated This property will become protected in v2. Use seek() on Track for position control.`
   - `gainNode` property (line 71): Verify it's already `public` — it needs to be accessible for custom effects. Add JSDoc: `/** The GainNode controlling this sound's volume. Exposed for advanced custom audio routing. For simple volume control, use changeGainTo() or update('gain'). */`

2. In `src/interfaces/connectable.ts`:
   - Add JSDoc to the `Connectable` interface: `/** Interface for audio sources that can be routed through connection chains. Provides gain/pan control and audio node management. @deprecated The connections-based routing is superseded by the Effect system (addEffect/removeEffect). */`
   - Add JSDoc to `Connection` interface: `/** A named audio node connection for legacy routing. @deprecated Use the Effect interface with addEffect() instead. */`

3. Verify that all public methods on BaseSound have JSDoc with @param and @returns tags. The following should already be documented (from Phase 7 JSDoc work), but verify and fill gaps:
   - play(), playAt(), playIn(), playFor(), playInAndStopAfter()
   - stop(), stopAt(), stopIn()
   - update(), onPlaySet(), onPlayRamp()
   - changePanTo(), changeGainTo()
   - addEffect(), removeEffect(), getEffects(), rewireEffects()
   - setDestination(), setAnalyzer(), getAnalyzer()
   - on(), once(), off()
  </action>
  <verify>
Run `pnpm test` — all tests pass. Run `pnpm typecheck` — no type errors. Verify deprecated tags: `grep -n "@deprecated" src/base-sound.ts` should show at least 4 @deprecated annotations (connections, addConnection, removeConnection, getConnection, getNodeFrom).
  </verify>
  <done>
All legacy methods (addConnection, removeConnection, getConnection, getNodeFrom) have @deprecated JSDoc. All public BaseSound methods have JSDoc. Connectable interface marked as deprecated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JSDoc to complex patterns and remaining gaps</name>
  <files>
    src/effects/effect-wrapper.ts
    src/controllers/base-param-controller.ts
    src/beat-track.ts
  </files>
  <action>
1. In `src/effects/effect-wrapper.ts`:
   - The constructor (lines 73-80) has duck-typing branches that are confusing. Add JSDoc comment blocks explaining each branch:
     ```
     // Branch 1: Effects with an `input` property (e.g., Tuna.js effects)
     // Tuna effects expose an `input` AudioNode for receiving signal.
     // We detect this via duck-typing: check for .input.connect method.
     ```
     ```
     // Branch 2: Effects that ARE AudioNodes (e.g., WaveShaperNode, ConvolverNode)
     // Native AudioNodes have both connect() and disconnect() methods.
     // We detect this by checking for both methods (duck-typing).
     ```
   - Add a class-level JSDoc section "## Supported Effect Types" with examples:
     ```
     * Supports three types of external effects:
     * 1. **Tuna.js effects** — Have an `input` AudioNode property
     * 2. **Native AudioNodes** — WaveShaperNode, ConvolverNode, etc.
     * 3. **Any object with connect()** — Minimum interface requirement
     ```

2. In `src/controllers/base-param-controller.ts`:
   - Add JSDoc to the `AudioSource` interface (lines 39-47):
     ```typescript
     /**
      * Duck-type interface for audio source nodes.
      *
      * Represents the minimum AudioParam surface needed by the controller system.
      * Both OscillatorNode and AudioBufferSourceNode satisfy this interface.
      * The `frequency` property is optional because only OscillatorNode has it.
      */
     ```
   - Add JSDoc to `ParamController` interface explaining it's the contract that SoundController and OscillatorController implement.
   - Add JSDoc to `BaseParamController` class explaining it's the shared base for parameter automation.

3. In `src/beat-track.ts`:
   - Add JSDoc to `noteType` parameter in `playBeats()` and `playActiveBeats()`: `@param noteType - Rhythmic subdivision as a fraction. Common values: 1/4 (quarter notes), 1/8 (eighth notes), 1/16 (sixteenth notes). The beat duration in seconds is calculated as: (240 * noteType) / bpm.`

4. Run `pnpm build` to verify TypeDoc generation succeeds with the new JSDoc.
  </action>
  <verify>
Run `pnpm test` — all tests pass. Run `pnpm typecheck` — no type errors. Run `pnpm build` — full build including TypeDoc succeeds.
  </verify>
  <done>
EffectWrapper duck-typing branches have clear explanatory comments. AudioSource, ParamController, BaseParamController all have JSDoc. noteType parameter documented with common values. TypeDoc builds without errors.
  </done>
</task>

</tasks>

<verification>
- `pnpm test` passes (all 711+ tests)
- `pnpm typecheck` passes
- `pnpm build` succeeds (including TypeDoc)
- At least 4 @deprecated tags in base-sound.ts
- EffectWrapper has inline comments for duck-typing
- AudioSource interface has JSDoc
- noteType has documented common values
</verification>

<success_criteria>
- All public API methods have JSDoc with @param, @returns, and @example
- All deprecated APIs have @deprecated tags with migration guidance
- Complex code patterns (duck-typing, mixin constructors) have explanatory comments
- Generated TypeDoc is accurate and complete
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-code-quality-implementation/13-03-SUMMARY.md`
</output>
