---
phase: 65-rebuild-validation-cleanup
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified: []
autonomous: false
requirements:
  - SC-6
  - SC-7

must_haves:
  truths:
    - "Phase 61 audio sprite demo plays full file with 6 distinct sounds"
    - "Phase 61 segment highlighting works correctly with glow effect"
    - "Phase 61 visual timeline has 6 proportionally-sized colored segments"
    - "Phase 64 visualization demo plays and animates canvases without errors"
    - "Phase 64 drum machine only plays active beats"
    - "Phase 64 audio sprite playhead sweeps and stop resets"
    - "Phase 64 lazy init shows brief loading then plays on first interaction"
  artifacts: []
  key_links: []
---

<objective>
Confirm human verification items for phases 61 and 64 by running the dev server and manually testing audio/visual behavior in the browser.

Purpose: Close SC-6 and SC-7 — the final tech debt items that require human eyes and ears to confirm audio playback, visual animations, and interactive behavior work correctly in a real browser.
Output: Confirmed human verification for phases 61 and 64
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-rebuild-validation-cleanup/65-RESEARCH.md
@.planning/phases/61-audio-sprites-redesign/61-VALIDATION.md
@.planning/phases/64-demo-example-ux-fixes/64-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for human verification</name>
  <files></files>
  <action>
Run `pnpm dev` to start the VitePress dev server. Note the local URL (typically http://localhost:5173). The server must be running for the human verification checkpoint that follows.
  </action>
  <verify>
    <automated>pnpm dev &amp; sleep 3 && curl -s -o /dev/null -w "%{http_code}" http://localhost:5173 | grep -q 200 && echo "PASS: Dev server running"</automated>
  </verify>
  <done>Dev server running and accessible at localhost</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 61 and Phase 64 demos</name>
  <action>
This is a human verification checkpoint. The user must manually test audio/visual behavior in the browser with the dev server running.

Phase 61 — Audio Sprites (navigate to Audio Sprites example page):

1. Full File Playback (SC-6a): Click "Play Full File" button. You should hear 6 distinct sounds playing sequentially (beep, cannon, whoosh, bling, punch, fanfare). Click "Stop" to halt playback.

2. Segment Highlighting (SC-6b): Click individual segment buttons or timeline segments. Each should play the correct sound. The timeline segment should highlight with a glow effect during playback and clear after the segment's duration ends.

3. Visual Timeline Layout (SC-6c): Verify 6 colored segments are proportionally sized in the timeline. Labels should be visible. Check responsive behavior by resizing the browser window.

Phase 64 — Demo UX Fixes:

4. Visualization Demo (SC-7a): Navigate to the Visualization example page. Click Play. Select different waveforms from the dropdown. Both frequency spectrum and waveform canvases should animate smoothly. Check browser console for errors — there should be none.

5. Drum Machine Beat Pattern (SC-7b): Navigate to both the Vue and Vanilla drum machine example pages. Toggle some beats on and off. Click Play. Only activated (highlighted) beats should produce sound. Deactivated beats should be silent.

6. Audio Sprite Playhead (SC-7c): Navigate to Audio Sprites page. Click "Play Full File". A white playhead line should smoothly sweep left-to-right across the timeline. Clicking "Stop" should halt playback and reset the playhead to the start.

7. Lazy Init on First Interaction (SC-7d): Open AudioSpriteDemo and LayeredSoundDemo pages in fresh tabs (hard refresh). Click any Play button. The first click should show a brief "Loading..." state, then audio plays. Subsequent clicks should play immediately without loading delay.
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>All 7 human verification checks confirmed by user</done>
</task>

</tasks>

<verification>
1. All 7 human verification checks confirmed by user
2. No console errors during visualization demo playback
3. Audio plays correctly in all tested demos
</verification>

<success_criteria>
- Phase 61 human checks confirmed: full file playback, segment highlighting, visual timeline (SC-6)
- Phase 64 human checks confirmed: visualization, drum machines, sprite playhead, lazy init (SC-7)
</success_criteria>

<output>
After completion, create `.planning/phases/65-rebuild-validation-cleanup/65-02-SUMMARY.md`
</output>
