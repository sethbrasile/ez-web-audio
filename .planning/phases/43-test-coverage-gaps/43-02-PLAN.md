---
phase: 43-test-coverage-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sound.test.ts
  - src/oscillator.test.ts
  - src/sampler.test.ts
autonomous: true
requirements:
  - TCOV-03
  - TCOV-04
  - TCOV-05
  - TCOV-11

must_haves:
  truths:
    - "Sound.loop property defaults to false and can be set/get"
    - "createOscillator({ note: 'A4' }) resolves to frequency 440"
    - "Invalid note name in createOscillator throws descriptive error"
    - "Sampler has no stop() method — documented in test"
    - "Oscillator frequency: 0 silently becomes 440 due to || operator"
  artifacts:
    - path: "src/sound.test.ts"
      provides: "Sound.loop property tests"
      contains: "loop"
    - path: "src/oscillator.test.ts"
      provides: "Note-based oscillator and frequency:0 verification tests"
      contains: "note.*A4"
    - path: "src/sampler.test.ts"
      provides: "Sampler stop behavior documentation test"
      contains: "stop"
  key_links:
    - from: "src/sound.test.ts"
      to: "src/sound.ts"
      via: "Sound.loop getter/setter"
      pattern: "\\.loop"
    - from: "src/oscillator.test.ts"
      to: "src/oscillator.ts"
      via: "Oscillator constructor note option"
      pattern: "note.*freq"
---

<objective>
Add test coverage for Sound.loop, note-based oscillators, Sampler.stop, and oscillator frequency:0.

Purpose: Close test gaps for Sound.loop property (M15), createOscillator note path (M16), Sampler.stop (M17), and frequency:0 behavior (L19).
Output: Enhanced tests in src/sound.test.ts, src/oscillator.test.ts, src/sampler.test.ts
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/sound.ts
@src/oscillator.ts
@src/sampler.ts
@src/sound.test.ts
@src/oscillator.test.ts
@src/sampler.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sound.loop and Sampler stop tests</name>
  <files>src/sound.test.ts, src/sampler.test.ts</files>
  <action>
**Add to `src/sound.test.ts`** — new `describe('loop property')` block:

1. `it('defaults to false')` — create Sound, assert `sound.loop === false`
2. `it('can be set to true')` — set `sound.loop = true`, assert `sound.loop === true`
3. `it('can be toggled back to false')` — set true then false, verify false
4. `it('persists through play/stop cycle')` — set loop=true, play(), stop(), verify loop is still true
5. `it('loop=true sets AudioBufferSourceNode.loop on play')` — set loop=true, play(), check that `sound.audioSourceNode.loop === true` (the mock may or may not support this; if not verifiable through mock, test that the property is persisted on Sound itself)

**Add to `src/sampler.test.ts`** — new `describe('stop behavior')` block:

1. `it('has no stop() method (one-shot sounds complete naturally)')` — assert `typeof sampler.stop === 'undefined'`. This documents the M17 finding: Sampler intentionally has no stop() because it delegates to one-shot Sound.play() calls that complete naturally.
2. `it('individual sounds can be stopped via getSounds()')` — get sounds array, verify each has a stop method (documenting the pattern for consumers who need stop behavior)
  </action>
  <verify>
    <automated>pnpm test src/sound.test.ts src/sampler.test.ts</automated>
  </verify>
  <done>Sound.loop has default, get/set, and lifecycle persistence tests. Sampler stop behavior is documented in tests — no stop() method exists, one-shot sounds complete naturally.</done>
</task>

<task type="auto">
  <name>Task 2: Add oscillator note path and frequency:0 verification tests</name>
  <files>src/oscillator.test.ts</files>
  <action>
**Add to `src/oscillator.test.ts`** — new `describe('note-based creation')` block:

1. `it('note "A4" resolves to frequency 440')` — create `new Oscillator(audioContext, { note: 'A4' })`, access the protected `freq` field. Since `freq` is protected, verify through the constructor not throwing AND compare against frequency map. Alternative: use `(osc as any).freq` to verify, or test via the factory in index.test.ts. Simplest approach: verify the oscillator creates successfully and use the existing `frequencyMap` import to confirm the mapping exists: `expect(frequencyMap.A4).toBe(440)` and then verify the oscillator was created with that note without throwing.
2. `it('note "C4" resolves to correct frequency')` — similar test for middle C
3. `it('invalid note throws descriptive error')` — `expect(() => new Oscillator(audioContext, { note: 'X9' })).toThrow(/Unknown note/)` — verify error message includes the bad note name
4. `it('note takes precedence over frequency when both provided')` — create with `{ note: 'A4', frequency: 220 }`, verify no error (A4=440 should be used, not 220). Since we can't easily read freq, verify through no-throw plus documentation in test comment.

**Enhance existing frequency:0 test** — the test at line ~234 already says "frequency 0 defaults to 440 because 0 is falsy with || operator". Add a follow-up test:

5. `it('frequency: 0 results in 440Hz (verified via setup)')` — create oscillator with `{ frequency: 0 }`, call `play()`, then verify that `osc.audioSourceNode.frequency.setValueAtTime` was called with 440 as the first argument (this is set in the `setup()` method at line 284 of oscillator.ts: `oscillator.frequency.setValueAtTime(this.freq || 440, ...)`). Use `vi.spyOn` on the created oscillator node's frequency.setValueAtTime if possible, or verify indirectly that play() succeeds without error.

Note: The existing test already covers `frequency: 0` not throwing. The key addition for TCOV-11 is verifying the *resulting* frequency is 440, not just that it doesn't throw. Since `this.freq` is set to `0 || 440 = 440` in the constructor, and `setup()` also uses `this.freq || 440`, the effective frequency is always 440 when 0 is passed.
  </action>
  <verify>
    <automated>pnpm test src/oscillator.test.ts</automated>
  </verify>
  <done>Oscillator note-to-frequency path tested (A4->440, C4->correct freq, invalid note throws). Frequency:0 behavior verified to result in 440Hz.</done>
</task>

</tasks>

<verification>
```bash
pnpm test src/sound.test.ts src/oscillator.test.ts src/sampler.test.ts
```
All tests pass. New test count increased by ~10-12 tests.
</verification>

<success_criteria>
- Sound.loop tested: default false, set/get, persistence through play/stop
- createOscillator note path tested: A4->440, invalid throws
- Sampler.stop documented: no stop() method, one-shot completion pattern
- Oscillator frequency:0 verified to produce 440Hz
</success_criteria>

<output>
After completion, create `.planning/phases/43-test-coverage-gaps/43-02-SUMMARY.md`
</output>
