---
phase: 43-test-coverage-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/preload.test.ts
  - src/audio-context.test.ts
  - src/envelope.test.ts
autonomous: true
requirements:
  - TCOV-01
  - TCOV-06
  - TCOV-12
  - TCOV-13

must_haves:
  truths:
    - "setPreloadCacheLimit sets the limit and evicts when exceeded"
    - "Cache accessor functions (getFromCache, setInCache, hasInCache, getCacheSize) work correctly"
    - "AudioContext singleton is created on first call and reused"
    - "AudioContext is recreated when in closed state"
    - "Envelope negative values behavior is documented in tests"
  artifacts:
    - path: "src/preload.test.ts"
      provides: "Tests for setPreloadCacheLimit, evictIfNeeded, and cache accessor functions"
      contains: "setPreloadCacheLimit"
    - path: "src/audio-context.test.ts"
      provides: "Dedicated test file for audio-context.ts singleton and recovery"
      contains: "getOrCreateAudioContext"
    - path: "src/envelope.test.ts"
      provides: "Enhanced envelope edge case tests for negative values"
      contains: "negative"
  key_links:
    - from: "src/preload.test.ts"
      to: "src/preload.ts"
      via: "import { setPreloadCacheLimit, evictIfNeeded, getFromCache, setInCache, hasInCache, getCacheSize }"
      pattern: "setPreloadCacheLimit|evictIfNeeded|getFromCache"
    - from: "src/audio-context.test.ts"
      to: "src/audio-context.ts"
      via: "import { getOrCreateAudioContext, _resetAudioContext }"
      pattern: "getOrCreateAudioContext"
---

<objective>
Add test coverage for cache management, AudioContext singleton, and envelope edge cases.

Purpose: Close test gaps for preload cache limit/eviction (M13), AudioContext singleton recovery (M18), envelope negative values (L20), and cache accessor functions (Phase 42 APIs).
Output: Tests in src/preload.test.ts, new src/audio-context.test.ts, enhanced src/envelope.test.ts
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/preload.ts
@src/audio-context.ts
@src/envelope.ts
@src/preload.test.ts
@src/envelope.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cache management and accessor function tests to preload.test.ts</name>
  <files>src/preload.test.ts</files>
  <action>
Add new describe blocks to the existing `src/preload.test.ts` test file:

1. **`describe('setPreloadCacheLimit')`** — test the following scenarios:
   - Setting a limit (e.g., 5) and verifying `getCacheSize()` respects it after adding more entries
   - Setting limit to 0 — all entries evicted immediately
   - Reducing limit below current cache size — oldest entries evicted to match new limit
   - Default limit (100) allows many entries without eviction
   - Limit does not affect `clearPreloadCache()` behavior

2. **`describe('evictIfNeeded')`** — test:
   - When cache size <= limit, no eviction occurs
   - When cache size > limit, oldest entries (first inserted) are evicted first (FIFO)
   - Multiple evictions reduce to exactly the limit count

3. **`describe('cache accessor functions')`** — test:
   - `setInCache(url, response)` stores a response retrievable by `getFromCache(url)`
   - `getFromCache(url)` returns `undefined` for missing keys
   - `hasInCache(url)` returns `true`/`false` correctly
   - `getCacheSize()` returns correct count after set/clear operations

Import the new functions: `import { setPreloadCacheLimit, evictIfNeeded, getFromCache, setInCache, hasInCache, getCacheSize } from './preload'`

Use `clearPreloadCache()` in `beforeEach` or `afterEach` to reset state between tests. Use `setPreloadCacheLimit(100)` in afterEach to restore the default limit.

For Response mocks, use `new Response('test')` or similar minimal Response objects.
  </action>
  <verify>
    <automated>pnpm test src/preload.test.ts</automated>
  </verify>
  <done>setPreloadCacheLimit tests cover limit setting, eviction when exceeded, limit of 0, and reducing below current size. Cache accessor functions (getFromCache, setInCache, hasInCache, getCacheSize) each have at least one test.</done>
</task>

<task type="auto">
  <name>Task 2: Create audio-context.test.ts and add envelope negative value tests</name>
  <files>src/audio-context.test.ts, src/envelope.test.ts</files>
  <action>
**Create `src/audio-context.test.ts`** — a dedicated test file for `src/audio-context.ts`:

Import: `import { getOrCreateAudioContext, _resetAudioContext, iosWorkaround, markIosWorkaroundPerformed } from './audio-context'`

Use `standardized-audio-context-mock` MockAudioContext and `vi.stubGlobal('AudioContext', MockAudioContext)` pattern (consistent with other test files).

Call `_resetAudioContext()` in `beforeEach` to ensure clean state.

Tests:
1. **`describe('getOrCreateAudioContext')`**:
   - `it('creates an AudioContext on first call')` — verify returns an AudioContext instance
   - `it('returns the same instance on subsequent calls')` — call twice, assert strict equality
   - `it('creates a new context when previous is closed')` — get context, mock its state as 'closed', call again, verify new instance (not equal to first)
   - `it('returns an AudioContext even without prior initAudio()')` — fresh module, call directly

2. **`describe('_resetAudioContext')`**:
   - `it('causes next getOrCreateAudioContext to create new instance')` — get context, reset, get again, verify different instances
   - `it('resets iosWorkaround.performed to false')` — set it true, reset, verify false

3. **`describe('markIosWorkaroundPerformed')`**:
   - `it('sets iosWorkaround.performed to true')` — verify before/after

**Enhance `src/envelope.test.ts`** — in the existing "edge cases - negative and extreme values" describe block, add one test that documents the behavior more explicitly:

- `it('negative attack/decay/release values are not validated — consumer responsibility')` — create envelope with all negative times, verify they are stored as-is. Add comment that this is documented behavior (TCOV-12: "documents acceptance").
  </action>
  <verify>
    <automated>pnpm test src/audio-context.test.ts src/envelope.test.ts</automated>
  </verify>
  <done>audio-context.test.ts exists with singleton creation and closed-state recovery tests. Envelope negative value acceptance is explicitly tested and documented.</done>
</task>

</tasks>

<verification>
```bash
pnpm test src/preload.test.ts src/audio-context.test.ts src/envelope.test.ts
```
All tests pass. New test count increased by ~15-20 tests.
</verification>

<success_criteria>
- setPreloadCacheLimit tested for: setting limit, eviction, limit=0, reduce below current size
- Cache accessors (getFromCache, setInCache, hasInCache, getCacheSize) each tested
- audio-context.test.ts exists with singleton and closed-state recreation tests
- Envelope negative value behavior documented in test
</success_criteria>

<output>
After completion, create `.planning/phases/43-test-coverage-gaps/43-01-SUMMARY.md`
</output>
