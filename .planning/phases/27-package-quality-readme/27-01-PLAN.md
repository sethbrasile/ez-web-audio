---
phase: 27-package-quality-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .github/workflows/publish.yml
  - .github/workflows/deploy-docs-site.yml
  - src/index.ts
autonomous: true
requirements: [SC-03, SC-04, SC-05, SC-06, SC-07]

must_haves:
  truths:
    - "package.json has homepage, bugs, and main fields"
    - "package.json keywords include all major feature terms"
    - "publish.yml runs lint before publish"
    - "deploy-docs-site.yml runs typecheck and lint before build"
    - "SoundController, OscillatorController, and Player are exported from src/index.ts"
  artifacts:
    - path: "package.json"
      provides: "Complete npm package metadata"
      contains: "homepage"
    - path: ".github/workflows/publish.yml"
      provides: "CI publish with lint gate"
      contains: "pnpm lint"
    - path: ".github/workflows/deploy-docs-site.yml"
      provides: "CI docs deploy with quality checks"
      contains: "pnpm typecheck"
    - path: "src/index.ts"
      provides: "All public exports including controllers and Player"
      contains: "SoundController"
  key_links:
    - from: "src/index.ts"
      to: "src/controllers/sound-controller.ts"
      via: "import and re-export"
      pattern: "SoundController"
    - from: "src/index.ts"
      to: "src/controllers/oscillator-controller.ts"
      via: "import and re-export"
      pattern: "OscillatorController"
---

<objective>
Fix package.json metadata, add CI quality gates, and export missing types.

Purpose: The npm package page needs homepage/bugs links and discoverable keywords. CI pipelines should catch lint errors before publish/deploy. Controllers and Player interface must be public for advanced consumers.
Output: Updated package.json, CI workflows, and src/index.ts with all missing exports.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REVIEW-FINDINGS.md
@package.json
@.github/workflows/publish.yml
@.github/workflows/deploy-docs-site.yml
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix package.json and CI workflows</name>
  <files>package.json, .github/workflows/publish.yml, .github/workflows/deploy-docs-site.yml</files>
  <action>
In package.json, add the following fields at the top level (after "repository"):
- `"homepage": "https://sethbrasile.github.io/ez-web-audio"`
- `"bugs": { "url": "https://github.com/sethbrasile/ez-web-audio/issues" }`
- `"main": "./dist/index.js"` (fallback for older bundlers that don't support "exports")

Replace the keywords array with: `["audio", "web", "audio-api", "web-audio", "web-audio-api", "audio-context", "audio-node", "synthesis", "oscillator", "drum-machine", "synthesizer", "soundfont", "audio-sprite", "beat", "rhythm", "filter", "sampler", "envelope", "effects", "typescript"]`

In .github/workflows/publish.yml, add a "Lint" step after "Type check" and before "Run unit tests":
```yaml
      - name: Lint
        run: pnpm lint
```

In .github/workflows/deploy-docs-site.yml, add two steps after "Install dependencies" and before "Build library":
```yaml
      - name: Type check
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint
```
  </action>
  <verify>
Verify package.json has homepage, bugs, main fields: `node -e "const p=require('./package.json'); console.log(!!p.homepage, !!p.bugs, !!p.main, p.keywords.length >= 20)"`

Verify publish.yml contains lint step: `grep -c 'pnpm lint' .github/workflows/publish.yml` should return 1.

Verify deploy-docs-site.yml contains typecheck and lint: `grep -c 'pnpm typecheck' .github/workflows/deploy-docs-site.yml` should return 1 and `grep -c 'pnpm lint' .github/workflows/deploy-docs-site.yml` should return 1.
  </verify>
  <done>package.json has homepage, bugs, main fields and 20 keywords. publish.yml runs lint. deploy-docs-site.yml runs typecheck and lint.</done>
</task>

<task type="auto">
  <name>Task 2: Export SoundController, OscillatorController, and Player</name>
  <files>src/index.ts</files>
  <action>
In src/index.ts, add imports for the controllers at the top with the other controller imports:
```typescript
import { SoundController } from '@controllers/sound-controller'
import { OscillatorController } from '@controllers/oscillator-controller'
```

Change the `Player` interface from `interface Player` to `export interface Player` so it becomes a named export.

Add `SoundController` and `OscillatorController` to the named export block (the `export { ... }` at line 678+), in alphabetical order among the existing entries.

Run `pnpm typecheck` to confirm no type errors. Run `pnpm lint` to confirm no lint issues. Run `pnpm test run` to confirm all tests pass.
  </action>
  <verify>
`pnpm typecheck` passes.
`pnpm lint` passes.
`pnpm test run` passes (all 913+ tests).
`grep -c 'SoundController' src/index.ts` returns at least 2 (import + export).
`grep -c 'OscillatorController' src/index.ts` returns at least 2.
`grep -c 'export interface Player' src/index.ts` returns 1.
  </verify>
  <done>SoundController, OscillatorController, and Player are exported from the library's public API. All tests and type checks pass.</done>
</task>

</tasks>

<verification>
- `node -e "const p=require('./package.json'); console.log('homepage:', p.homepage); console.log('bugs:', p.bugs.url); console.log('main:', p.main); console.log('keywords:', p.keywords.length)"` shows all fields present
- `grep 'pnpm lint' .github/workflows/publish.yml` shows lint in publish CI
- `grep 'pnpm typecheck' .github/workflows/deploy-docs-site.yml` shows typecheck in docs CI
- `pnpm typecheck && pnpm lint && pnpm test run` all pass
</verification>

<success_criteria>
- package.json has homepage, bugs.url, and main fields
- Keywords array has 20 entries covering all major features
- publish.yml runs lint before publish
- deploy-docs-site.yml runs typecheck and lint before build
- SoundController, OscillatorController, and Player are exported from src/index.ts
- All tests, typecheck, and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-package-quality-readme/27-01-SUMMARY.md`
</output>
