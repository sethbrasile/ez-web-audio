---
phase: 32-critical-fixes-api-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - CLAUDE.md
  - src/interfaces/playable.ts
  - src/sound.ts
  - src/index.ts
autonomous: true
requirements:
  - FIX-01
  - FIX-04
  - FIX-06
  - FIX-07
  - FIX-08

must_haves:
  truths:
    - "MIT LICENSE file exists at project root"
    - "CLAUDE.md code examples use .as('ratio') not .from('ratio')"
    - "Playable interface return types match async implementations"
    - "Sound constructor opts parameter has a proper type"
    - "createFont() handles fetch errors with descriptive messages"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: "src/interfaces/playable.ts"
      provides: "Playable interface with correct async return types"
      contains: "Promise<void>"
    - path: "src/sound.ts"
      provides: "Sound class with typed opts"
      contains: "BaseSoundOptions"
    - path: "src/index.ts"
      provides: "createFont with error handling"
      contains: "response.ok"
  key_links:
    - from: "src/sound.ts"
      to: "src/base-sound.ts"
      via: "opts type matches BaseSoundOptions"
      pattern: "opts\\?:\\s*BaseSoundOptions"
---

<objective>
Fix five independent small issues: add MIT LICENSE file, fix CLAUDE.md API examples, align Playable interface async return types, type Sound constructor opts, and add error handling to createFont().

Purpose: Close code review findings C-1, C-4, S-2, S-5, S-12
Output: Five corrected files with no test regressions
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/interfaces/playable.ts
@src/sound.ts
@src/index.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LICENSE file, fix CLAUDE.md, and type Sound opts</name>
  <files>LICENSE, CLAUDE.md, src/sound.ts</files>
  <action>
1. Create `LICENSE` file at project root with standard MIT License text. Use "Seth Brasile" as the copyright holder and "2024-present" as the year range (matching package.json author field).

2. In `CLAUDE.md`, fix the two `.from('ratio')` references to `.as('ratio')`:
   - Line 59: `sound.update('gain').to(0.5).from('ratio')` -> `sound.update('gain').to(0.5).as('ratio')`
   - Line 63: The `onPlayRamp` example uses `.from()` correctly (it means "from value 0") so leave it unchanged. Only the `.from('ratio')` usage on line 59 is wrong.

3. In `src/sound.ts` line 48, change `opts?: any` to `opts?: BaseSoundOptions`. Add the import for `BaseSoundOptions` from `./base-sound` (it is already exported from there). The `BaseSoundOptions` interface has `name?: string` and `setTimeout?` which is exactly what Sound passes to `super(audioContext, opts)`.
  </action>
  <verify>Run `pnpm typecheck` to confirm no type errors. Verify LICENSE file exists: `cat LICENSE | head -3`. Verify CLAUDE.md has no `.from('ratio')`: `grep -c "from('ratio')" CLAUDE.md` should be 0.</verify>
  <done>LICENSE exists with MIT text, CLAUDE.md uses .as('ratio'), Sound constructor opts is typed as BaseSoundOptions</done>
</task>

<task type="auto">
  <name>Task 2: Fix Playable interface return types and createFont error handling</name>
  <files>src/interfaces/playable.ts, src/index.ts</files>
  <action>
1. In `src/interfaces/playable.ts`, update return types to match the async implementations in BaseSound:
   - `playAt: (time: number) => void` -> `playAt: (time: number) => Promise<void>` (BaseSound.playAt is `public async playAt`)
   - `stopAt: (time: number) => void` -> `stopAt: (time: number) => Promise<void>` (BaseSound.stopAt is `public async stopAt`)
   - `play` and `stop` already return `Promise<void>` -- no change needed
   - `playIn`, `playFor`, `playInAndStopAfter`, `stopIn` are NOT async in BaseSound -- leave as `void`

2. In `src/index.ts`, update `createFont()` function (around line 410) to add error handling:
   - Wrap the entire function body in a try/catch
   - After `const response = await fetch(url)`, add: `if (!response.ok) { throw new Error(\`Failed to load soundfont from "\${url}": HTTP \${response.status} \${response.statusText}\`) }`
   - In the catch block, rethrow with a descriptive message: if the error is already an Error with a message containing "Failed to load soundfont", rethrow as-is. Otherwise wrap: `throw new Error(\`Failed to load soundfont from "\${url}": \${error instanceof Error ? error.message : String(error)}\`)`
  </action>
  <verify>Run `pnpm typecheck` to confirm Playable interface changes don't break implementations. Run `pnpm test` to confirm all tests pass.</verify>
  <done>Playable.playAt and Playable.stopAt return Promise&lt;void&gt; matching implementations. createFont() checks response.ok and wraps in try/catch with descriptive errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm test` passes
- `cat LICENSE` shows MIT license text
- `grep "from('ratio')" CLAUDE.md` returns no matches
- `grep "BaseSoundOptions" src/sound.ts` shows typed constructor
- `grep "response.ok" src/index.ts` shows fetch validation in createFont
</verification>

<success_criteria>
All five fixes applied. TypeScript compiles. All tests pass. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/32-critical-fixes-api-contracts/32-01-SUMMARY.md`
</output>
