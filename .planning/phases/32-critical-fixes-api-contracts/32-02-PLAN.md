---
phase: 32-critical-fixes-api-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/envelope.ts
  - src/envelope.test.ts
  - src/oscillator.ts
  - src/oscillator.test.ts
  - src/controllers/oscillator-controller.ts
  - src/controllers/oscillator-controller.test.ts
  - docs/guide/concepts.md
autonomous: true
requirements:
  - FIX-03
  - FIX-09

must_haves:
  truths:
    - "EnvelopeOptions uses short ADSR property names (attack, decay, sustain, release)"
    - "All JSDoc examples and docs use the same short property names"
    - "Oscillator.setup() JSDoc warns about GainNode replacement"
    - "All envelope tests pass with renamed properties"
  artifacts:
    - path: "src/envelope.ts"
      provides: "EnvelopeOptions with short names"
      contains: "attack?"
    - path: "src/oscillator.ts"
      provides: "Oscillator with GainNode replacement warning"
      contains: "GainNode"
  key_links:
    - from: "src/oscillator.ts"
      to: "src/envelope.ts"
      via: "import EnvelopeOptions"
      pattern: "EnvelopeOptions"
    - from: "src/controllers/oscillator-controller.ts"
      to: "src/envelope.ts"
      via: "Envelope instance methods"
      pattern: "envelope\\."
---

<objective>
Rename EnvelopeOptions from verbose property names (attackTime, decayTime, sustainLevel, releaseTime) to standard short ADSR names (attack, decay, sustain, release). Add JSDoc warning about GainNode replacement in Oscillator.setup().

Purpose: Match standard ADSR convention used by all JSDoc examples, docs, and every other audio library
Output: Consistent ADSR naming across interface, class, tests, and docs
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/envelope.ts
@src/envelope.test.ts
@src/oscillator.ts
@src/controllers/oscillator-controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename EnvelopeOptions properties and update Envelope class</name>
  <files>src/envelope.ts, src/envelope.test.ts, src/controllers/oscillator-controller.ts, src/controllers/oscillator-controller.test.ts</files>
  <action>
1. In `src/envelope.ts`:
   - Rename `EnvelopeOptions` interface properties:
     - `attackTime` -> `attack`
     - `decayTime` -> `decay`
     - `sustainLevel` -> `sustain`
     - `releaseTime` -> `release`
   - Update JSDoc `@property` tags to match
   - Rename `Envelope` class readonly properties:
     - `attackTime` -> `attack`
     - `decayTime` -> `decay`
     - `sustainLevel` -> `sustain`
     - `releaseTime` -> `release`
   - Update constructor: `this.attack = options.attack ?? 0.01` etc.
   - Update ALL internal references in `estimateCurrentValue()`, `applyTo()`, `triggerRelease()`:
     - `this.attackTime` -> `this.attack`
     - `this.decayTime` -> `this.decay`
     - `this.sustainLevel` -> `this.sustain`
     - `this.releaseTime` -> `this.release`
   - Update JSDoc examples in the class to use short names (the constructor example already has a mix -- make it consistent)

2. In `src/envelope.test.ts`:
   - Find all references to `attackTime`, `decayTime`, `sustainLevel`, `releaseTime` in test assertions and construction
   - Replace with `attack`, `decay`, `sustain`, `release`

3. In `src/controllers/oscillator-controller.ts`:
   - Find any references to `envelope.attackTime`, `envelope.decayTime`, `envelope.sustainLevel`, `envelope.releaseTime`
   - Replace with `envelope.attack`, `envelope.decay`, `envelope.sustain`, `envelope.release`

4. In `src/controllers/oscillator-controller.test.ts`:
   - Update any envelope property references to use short names
  </action>
  <verify>Run `pnpm typecheck` to verify all references updated. Run `pnpm test src/envelope.test.ts src/controllers/oscillator-controller.test.ts` to confirm tests pass.</verify>
  <done>EnvelopeOptions interface and Envelope class use short ADSR names. All tests referencing envelope properties updated and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Update Oscillator references and docs, add GainNode warning</name>
  <files>src/oscillator.ts, src/oscillator.test.ts, docs/guide/concepts.md</files>
  <action>
1. In `src/oscillator.ts`:
   - The JSDoc examples already use short names (`attack`, `decay`, `sustain`, `release`) so these are correct. No changes needed in the JSDoc examples.
   - In `stop()` method around line 387: `this.envelope.releaseTime` -> `this.envelope.release`
   - Add JSDoc warning to `setup()` method (around line 226):
     ```
     * @protected
     * @remarks
     * **Warning:** This method replaces the internal GainNode. If you have cached a
     * reference to the GainNode (e.g. via `getGainNode()`), the cached reference becomes
     * stale after `play()` is called. Always re-fetch the GainNode after play() if you
     * need a live reference.
     ```

2. In `src/oscillator.test.ts`:
   - Update any envelope property references from long names to short names (if any exist)

3. In `docs/guide/concepts.md`:
   - Around line 169-174: Change `attackTime` -> `attack`, `decayTime` -> `decay`, `sustainLevel` -> `sustain`, `releaseTime` -> `release` in the `new Envelope({...})` example
   - Around lines 185-190: Update the table: `attackTime` -> `attack`, `decayTime` -> `decay`, `sustainLevel` -> `sustain`, `releaseTime` -> `release`
  </action>
  <verify>Run `pnpm typecheck` and `pnpm test src/oscillator.test.ts` to confirm. Verify no stale long names remain: `grep -r "attackTime\|decayTime\|sustainLevel\|releaseTime" src/ docs/`</verify>
  <done>All envelope references use short ADSR names. Oscillator.setup() has GainNode replacement warning in JSDoc. No stale long property names remain in src/ or docs/.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm test` passes (all tests)
- `grep -r "attackTime\|decayTime\|sustainLevel\|releaseTime" src/ docs/` returns no matches
- `grep "attack\?" src/envelope.ts` shows short property name in interface
- `grep "GainNode" src/oscillator.ts` includes the warning JSDoc
</verification>

<success_criteria>
EnvelopeOptions uses standard short ADSR names matching all JSDoc examples and documentation. Oscillator.setup() warns about GainNode replacement. All tests pass with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/32-critical-fixes-api-contracts/32-02-SUMMARY.md`
</output>
