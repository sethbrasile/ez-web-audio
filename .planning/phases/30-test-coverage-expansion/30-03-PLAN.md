---
phase: 30-test-coverage-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/oscillator.test.ts
  - src/integration.test.ts
autonomous: true
requirements: [SC-06, SC-08, SC-09, SC-10]

must_haves:
  truths:
    - "Oscillator addFilter(), filter wiring, and anti-click fade-out are tested"
    - "Integration tests exist for Track+effects and BeatTrack+effects combinations"
    - "Factory function error propagation paths are tested"
    - "Cleanup/dispose pattern has at least one integration test"
  artifacts:
    - path: "src/oscillator.test.ts"
      provides: "Expanded tests for filter chain and anti-click"
      min_lines: 120
    - path: "src/integration.test.ts"
      provides: "New integration test blocks for Track+effects, BeatTrack+effects, factory errors, dispose"
      min_lines: 300
  key_links:
    - from: "src/oscillator.test.ts"
      to: "src/oscillator.ts"
      via: "import Oscillator"
      pattern: "addFilter|filters|wireConnections"
    - from: "src/integration.test.ts"
      to: "src/track.ts"
      via: "Track+effects integration"
      pattern: "Track.*addEffect|Track.*getEffects"
    - from: "src/integration.test.ts"
      to: "src/index.ts"
      via: "factory error propagation"
      pattern: "createSound|createTrack|createOscillator"
---

<objective>
Add Oscillator filter chain tests and expand integration tests with Track+effects, BeatTrack+effects, factory error propagation, and cleanup/dispose patterns.

Purpose: Closes review findings T-6, T-8, T-9, M-27 — filter chain testing and integration test expansion.
Output: Expanded oscillator.test.ts and integration.test.ts with comprehensive new test blocks.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/oscillator.ts
@src/oscillator.test.ts
@src/integration.test.ts
@src/track.ts
@src/beat-track.ts
@src/index.ts
@src/effects/effect-wrapper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand oscillator.test.ts with filter chain and anti-click tests</name>
  <files>src/oscillator.test.ts</files>
  <action>
Add new test blocks to `src/oscillator.test.ts`. Follow existing pattern: use `standardized-audio-context-mock`, create Oscillator with various options.

**describe('filter chain'):**
1. **addFilter() adds a BiquadFilterNode** — Create Oscillator, call `addFilter('lowpass', { frequency: 1000, q: 2 })`. Verify `getFilters()` returns array with one filter. Verify the filter's type is 'lowpass'.
2. **Multiple filters added in order** — Add lowpass then highpass. Verify `getFilters()` has 2 filters in order.
3. **Constructor filter options create filters** — Create `new Oscillator(ctx, { lowpass: { frequency: 500, q: 1 } })`. Verify `getFilters()` has one filter.
4. **Multiple constructor filter types** — Create with both `lowpass` and `highpass` options. Verify 2 filters created.
5. **Filters persist through play()** — Add filter, call `play()`, verify `getFilters()` still has the filter.

**describe('anti-click fade-out on stop'):**
1. **stop() completes without error when filters present** — Create Oscillator with lowpass filter, play, stop. No throw.
2. **Gain node disconnect on setup() prevents leak** — Play twice (each `play()` calls `setup()` which creates new gainNode). Verify no error — the old gainNode.disconnect() in setup() is called.

**describe('wireConnections with filters'):**
1. **Source connects through filters to gain** — Create Oscillator with a filter. Call `play()` (which calls setup/wireConnections). The test verifies the chain is established by checking the oscillator plays without error. (With mock AudioContext, we can't deeply inspect connections, but we verify no runtime errors.)

Note: The `standardized-audio-context-mock` has limited filter inspection. Focus on verifying: no throws, correct filter count via `getFilters()`, and correct behavior through play/stop cycles.
  </action>
  <verify>`pnpm test src/oscillator.test.ts` — all tests pass</verify>
  <done>Oscillator filter chain tests cover addFilter, constructor filters, filter persistence through play, anti-click fade-out, and wireConnections with filters</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for Track+effects, factory errors, and cleanup/dispose</name>
  <files>src/integration.test.ts</files>
  <action>
Add new `describe` blocks to `src/integration.test.ts`. Follow existing patterns (createMockContext, createSound helper, EffectWrapper with mock external effect, settle helper).

**describe('Track + effects integration'):**

Create Track instances using the Track class directly (import from `@/track`). Track constructor takes `(audioContext, audioBuffer)` same as Sound.

1. **Track can add effect and play** — Create Track, add EffectWrapper, play. Verify `track.getEffects()` has effect and `track.isPlaying` becomes true.
2. **Track with effect can stop and resume** — Create Track with effect, play, pause (stop), verify effect still attached after stop.
3. **Track with multiple effects maintains chain order** — Add 3 effects, verify `getEffects()` order matches add order.

**describe('BeatTrack + effects integration'):**

Create BeatTrack instances. BeatTrack constructor takes `(audioContext, sounds, opts)`.

1. **BeatTrack can add effect** — Create BeatTrack with mock sounds, add effect. Verify `getEffects()` has effect.
2. **BeatTrack effect persists through beat playback** — Add effect, play a beat, verify effect remains.

Note: BeatTrack is more complex to mock (needs Sound array). Create minimal Sound instances from AudioBuffers for the BeatTrack constructor. If BeatTrack constructor is too complex for unit-style integration tests, use the Sampler class instead (simpler constructor) and document why.

**describe('factory function error propagation'):**

Test factory functions from `src/index.ts`. These require `fetch` mocking since they load URLs.

1. **createSound with invalid URL rejects with descriptive error** — Mock `fetch` to reject. Verify `createSound('invalid.mp3')` rejects.
2. **createTrack with invalid URL rejects** — Same pattern for createTrack.
3. **createOscillator with negative frequency throws** — Verify `createOscillator({ frequency: -1 })` rejects with frequency validation error.
4. **createSampler with empty URL array** — Verify behavior (should resolve with empty sampler or throw — test actual behavior).
5. **createBeatTrack with empty URL array** — Similar verification.

For fetch mocking: use `vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new Error('Network error')))`. Restore in afterEach. Also mock `getOrCreateAudioContext` if needed.

**describe('cleanup/dispose pattern'):**

1. **Sound cleanup after stop releases references** — Create Sound, play, stop. Verify `isPlaying === false`. Call `removeEffect` if effects were added. Verify `getEffects()` is empty.
2. **Oscillator cleanup after stop** — Create Oscillator, play, stop. Verify `isPlaying === false`. Verify it can be played again (fresh setup).
3. **Effect removal cleans up chain** — Add 2 effects, remove both, verify `getEffects()` is empty and sound still plays.
4. **Repeated play/stop cycles don't leak** — Play and stop a Sound 5 times in a loop. Verify no errors and final state is stopped.
  </action>
  <verify>`pnpm test src/integration.test.ts` — all tests pass (existing + new)</verify>
  <done>Integration tests cover Track+effects, BeatTrack+effects (or Sampler+effects), factory error propagation for createSound/createTrack/createOscillator, and cleanup/dispose patterns including repeated play/stop cycles</done>
</task>

</tasks>

<verification>
```bash
pnpm test src/oscillator.test.ts src/integration.test.ts
```
All tests pass. Full test suite: `pnpm test` — no regressions.
</verification>

<success_criteria>
- Oscillator filter chain: addFilter, constructor filters, persistence, anti-click, wireConnections tested
- Track+effects and BeatTrack+effects integration tests exist
- Factory function error propagation tested for invalid URLs and invalid options
- Cleanup/dispose pattern tested with repeated play/stop and effect removal
</success_criteria>

<output>
After completion, create `.planning/phases/30-test-coverage-expansion/30-03-SUMMARY.md`
</output>
