---
phase: 28-documentation-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/guide/concepts.md
  - docs/examples/basic-playback.md
  - docs/examples/visualization.md
  - docs/examples/ambient-generator.md
  - docs/examples/effects.md
  - docs/examples/synth-keyboard.md
  - src/base-sound.ts
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04, SC-05, SC-06]

must_haves:
  truths:
    - "percentPlayed is documented as returning 0-100 (not 0-1) everywhere"
    - "createAnalyzer() examples show AudioContext as first parameter"
    - "FilterEffect property access uses setter pattern and lowercase q"
    - "addEffect() JSDoc uses context-free factory signature"
    - "Effect bypass is documented as auto-rewiring without manual rewireEffects()"
    - "Synth keyboard example playNote function is declared async"
  artifacts:
    - path: "docs/guide/concepts.md"
      provides: "Corrected percentPlayed values and no manual rewireEffects on bypass"
      contains: "percentPlayed"
    - path: "docs/examples/basic-playback.md"
      provides: "Corrected percentPlayed values and progress bar code"
      contains: "percentPlayed"
    - path: "docs/examples/visualization.md"
      provides: "Corrected createAnalyzer calls with AudioContext"
      contains: "createAnalyzer"
    - path: "docs/examples/ambient-generator.md"
      provides: "Corrected FilterEffect property access"
      contains: "lowpass.frequency"
    - path: "docs/examples/effects.md"
      provides: "Corrected bypass docs and wrapEffect calls"
      contains: "bypass"
    - path: "docs/examples/synth-keyboard.md"
      provides: "Corrected async playNote function"
      contains: "async"
    - path: "src/base-sound.ts"
      provides: "Corrected addEffect JSDoc example"
      contains: "createFilterEffect"
  key_links:
    - from: "docs/guide/concepts.md"
      to: "src/track.ts"
      via: "percentPlayed documentation matches implementation"
      pattern: "percentPlayed.*100|percentPlayed.*%"
    - from: "src/base-sound.ts"
      to: "src/effects/filter-effect.ts"
      via: "addEffect JSDoc references correct factory signature"
      pattern: "createFilterEffect\\("
---

<objective>
Fix all incorrect documentation identified in code review findings C-6, C-7, C-8, C-9, H-9, and H-17.

Purpose: Users following docs examples will get working code instead of runtime errors or incorrect behavior.
Output: 7 corrected files (6 docs + 1 source JSDoc)
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REVIEW-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix percentPlayed, createAnalyzer, and FilterEffect docs</name>
  <files>
    docs/guide/concepts.md
    docs/examples/basic-playback.md
    docs/examples/visualization.md
    docs/examples/ambient-generator.md
  </files>
  <action>
Fix C-6: percentPlayed returns 0-100 (not 0-1).

In docs/guide/concepts.md:
- Line 67: Change `console.log(song.percentPlayed) // 0.35 (35% complete)` to `console.log(song.percentPlayed) // 35 (35% complete)`
- Line 95: Change `console.log(track.percentPlayed) // 0.31 (31%)` to `console.log(track.percentPlayed) // 31 (31%)`

In docs/examples/basic-playback.md:
- Line 54: Change `console.log(song.percentPlayed) // 0.25 (25%)` to `console.log(song.percentPlayed) // 25 (25%)`
- Line 132: Change `console.log(track.percentPlayed) // 0.25` to `console.log(track.percentPlayed) // 25`
- Line 179: Change `progressBar.style.width = \`${track.percentPlayed * 100}%\`` to `progressBar.style.width = \`${track.percentPlayed}%\`` (percentPlayed already returns 0-100, no multiplication needed)

Fix C-7: createAnalyzer() requires AudioContext as first parameter.

In docs/examples/visualization.md:
- Line 57: Change `const analyzer = createAnalyzer({ fftSize: 1024 })` to `const analyzer = createAnalyzer(audioContext, { fftSize: 1024 })` and add `const audioContext = await getAudioContext()` before it, plus add `getAudioContext` to the import statement
- Line 147: Change `const analyzer = createAnalyzer({ fftSize: 2048 })` to `const analyzer = createAnalyzer(audioContext, { fftSize: 2048 })` and add `const audioContext = await getAudioContext()` with import if needed. Note: the section "Using Analyzer with Different Sound Sources" already has `const ctx = await getAudioContext()` nearby but uses a different variable name — check the effects.md version at line 310 which correctly uses `createAnalyzer(ctx, { ... })` as reference

Fix C-8: FilterEffect property access pattern.

In docs/examples/ambient-generator.md:
- Line 69: Change uppercase `Q: 1.0` to lowercase `q: 1.0` in the createFilterEffect options
- Line 93: Change `lowpass.frequency.value = 1200` to `lowpass.frequency = 1200` (FilterEffect has a setter, not AudioParam)
  </action>
  <verify>
    grep -n "percentPlayed.*0\." docs/guide/concepts.md docs/examples/basic-playback.md — should return zero matches
    grep -n "percentPlayed \* 100" docs/examples/basic-playback.md — should return zero matches
    grep -n "createAnalyzer({" docs/examples/visualization.md — should return zero matches (all calls should have audioContext first)
    grep -n "frequency\.value" docs/examples/ambient-generator.md — should return zero matches
    grep -n "Q:" docs/examples/ambient-generator.md — should return zero matches (only lowercase q)
  </verify>
  <done>
    percentPlayed documented as 0-100 everywhere. createAnalyzer() calls include AudioContext parameter. FilterEffect uses setter pattern with lowercase q.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix addEffect JSDoc, rewireEffects contradiction, synth keyboard async, and wrapEffect</name>
  <files>
    src/base-sound.ts
    docs/examples/effects.md
    docs/examples/synth-keyboard.md
  </files>
  <action>
Fix C-9: addEffect() JSDoc in base-sound.ts uses old factory signature with AudioContext.

In src/base-sound.ts, find the addEffect JSDoc (around line 341):
- Change `const filter = createFilterEffect(audioContext, 'lowpass', { frequency: 1000 })` to `const filter = createFilterEffect('lowpass', { frequency: 1000 })` (context-free signature)

Fix H-9: effects.md contradicts concepts.md on rewireEffects() and bypass.

In docs/examples/effects.md, around line 220-225, the "Toggling Effects On/Off" section shows:
```
function toggleFilter() {
  filter.bypass = !filter.bypass
  // When bypass changes, call rewireEffects if needed
  sound.rewireEffects()
}
```
Replace with:
```
function toggleFilter() {
  filter.bypass = !filter.bypass
  // Effect chain rewires automatically when bypass is toggled
}
```
This matches the actual behavior (base-sound.ts interceptBypass auto-rewires) and aligns with concepts.md line 277-279.

Also in docs/examples/effects.md, around line 244, the wrapEffect example uses old two-arg form:
```
const wrapped = wrapEffect(ctx, distortion)
```
Change to context-free form:
```
const wrapped = wrapEffect(distortion)
```
Do the same for the Tuna.js example around line 270:
```
const wrapped = wrapEffect(ctx, chorus)
```
Change to:
```
const wrapped = wrapEffect(chorus)
```
Note: Both forms work (overloaded), but docs should show the preferred context-free form consistent with createFilterEffect/createGainEffect.

Fix H-17: synth-keyboard.md playNote function missing async keyword.

In docs/examples/synth-keyboard.md, line 74:
- Change `function playNote(note: string) {` to `async function playNote(note: string) {` since the body uses `await createOscillator(...)`.
  </action>
  <verify>
    grep -n "createFilterEffect(audioContext" src/base-sound.ts — should return zero matches
    grep -n "rewireEffects" docs/examples/effects.md — should return zero matches
    grep -n "wrapEffect(ctx" docs/examples/effects.md — should return zero matches
    grep -n "function playNote" docs/examples/synth-keyboard.md — should show "async function playNote"
  </verify>
  <done>
    addEffect JSDoc uses context-free factory. Effects page no longer shows manual rewireEffects after bypass. wrapEffect examples use context-free form. playNote is declared async.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm build:lib` passes (base-sound.ts JSDoc change doesn't break build)
2. `pnpm typecheck` passes
3. No instances of old patterns remain:
   - `grep -rn "percentPlayed.*0\." docs/` — zero matches for 0-1 range
   - `grep -rn "createAnalyzer({" docs/` — zero matches without AudioContext
   - `grep -rn "createFilterEffect(audioContext" src/` — zero matches
   - `grep -rn "rewireEffects" docs/examples/effects.md` — zero matches
</verification>

<success_criteria>
- SC-01: percentPlayed documented as 0-100 in all guide and example pages
- SC-02: createAnalyzer() examples show AudioContext as first parameter
- SC-03: FilterEffect uses setter pattern (lowpass.frequency = 1200) and lowercase q
- SC-04: addEffect() JSDoc uses context-free factory signature
- SC-05: rewireEffects() documentation is consistent — auto-rewire for bypass
- SC-06: Synth keyboard example has async on function using await
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation-corrections/28-01-SUMMARY.md`
</output>
