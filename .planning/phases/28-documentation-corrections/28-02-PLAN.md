---
phase: 28-documentation-corrections
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - docs/guide/concepts.md
autonomous: true
requirements: [SC-07, SC-08, SC-09, SC-10]

must_haves:
  truths:
    - "AudioSprite/createSprite has narrative documentation with code example"
    - "crossfade utility has narrative documentation with code example"
    - "playTogether utility has narrative documentation with code example"
    - "useInteractionMethods and preventEventDefaults are documented"
    - "clearPreloadCache is documented with usage context"
    - "Debug utilities have expanded documentation with sample output"
    - "Envelope class has standalone documentation"
  artifacts:
    - path: "docs/guide/concepts.md"
      provides: "Expanded documentation for all missing features"
      contains: "AudioSprite"
    - path: "docs/guide/concepts.md"
      provides: "crossfade section"
      contains: "crossfade"
    - path: "docs/guide/concepts.md"
      provides: "playTogether section"
      contains: "playTogether"
    - path: "docs/guide/concepts.md"
      provides: "useInteractionMethods section"
      contains: "useInteractionMethods"
    - path: "docs/guide/concepts.md"
      provides: "clearPreloadCache section"
      contains: "clearPreloadCache"
    - path: "docs/guide/concepts.md"
      provides: "Envelope standalone section"
      contains: "Envelope"
  key_links:
    - from: "docs/guide/concepts.md"
      to: "src/index.ts"
      via: "documented APIs match actual exports"
      pattern: "createSprite|crossfade|playTogether|useInteractionMethods|preventEventDefaults|clearPreloadCache|Envelope"
---

<objective>
Add narrative documentation for all undocumented features identified in code review findings M-19 through M-25.

Purpose: Developers browsing the Core Concepts guide will discover all library features, not just the most common ones. No significant feature should be "hidden".
Output: Expanded concepts.md with 7 new documentation sections
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REVIEW-FINDINGS.md
@.planning/phases/28-documentation-corrections/28-01-SUMMARY.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AudioSprite, crossfade, and playTogether narrative docs</name>
  <files>docs/guide/concepts.md</files>
  <action>
Expand the existing "Other Sound Types" section and "Utility Functions" section in docs/guide/concepts.md with fuller narrative documentation for three features that currently have minimal or no coverage.

1. **AudioSprite section** (expand the existing brief mention around line 422-433):
Replace the current minimal AudioSprite subsection with a fuller explanation. Include:
- What audio sprites are and why they're useful (single HTTP request for multiple sounds, common in games)
- Full code example using `createSprite()` with manifest
- Show `sprite.play('name')` with options (gain, loop)
- Show `sprite.stop('name')` for looping sprites
- Note that boundaries are defined in seconds

2. **Crossfade section** (expand the existing brief mention around line 496-509):
Replace the current minimal crossfade subsection with fuller docs. Include:
- What crossfade does (equal-power curve transition between two sounds)
- Full code example with `crossfade(trackA, trackB, durationSeconds)`
- Explain that it returns a Promise that resolves when the transition completes
- Note the equal-power curve prevents volume dips at the midpoint
- Typical use cases: DJ transitions, ambient scene changes, background music swaps

3. **playTogether section** (expand the existing brief mention around line 466-479):
Replace the current minimal playTogether subsection with fuller docs. Include:
- What it does (synchronizes multiple sounds to exact same AudioContext timestamp)
- Full code example with different sound types
- Explain the difference from just calling play() on each — playTogether uses a shared start time
- Use cases: chord building, layered SFX, synchronized stems

Reference src/index.ts for actual function signatures. Use the context-free factory patterns established in the codebase. Do NOT add example pages (that's Phase 25) — just expand narrative docs in concepts.md.
  </action>
  <verify>
    grep -c "AudioSprite" docs/guide/concepts.md — should be >= 3 (heading + code + explanation)
    grep -c "crossfade" docs/guide/concepts.md — should be >= 3
    grep -c "playTogether" docs/guide/concepts.md — should be >= 3
    grep -n "createSprite" docs/guide/concepts.md — should show code example
  </verify>
  <done>
    AudioSprite, crossfade, and playTogether all have narrative documentation with code examples in concepts.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Add docs for interaction helpers, cache, debug, and Envelope</name>
  <files>docs/guide/concepts.md</files>
  <action>
Add new subsections to docs/guide/concepts.md for the remaining undocumented features (M-22 through M-25). Add these in logical locations within the existing document structure.

1. **Interaction Helpers section** — add under or near "Utility Functions":
Document `useInteractionMethods()` and `preventEventDefaults()`:
- `useInteractionMethods(element, player)` — binds touch/mouse events for play/stop on an HTML element. Returns a cleanup function to remove listeners. Show code example:
  ```typescript
  import { useInteractionMethods, createOscillator } from 'ez-web-audio'
  const synth = await createOscillator({ frequency: 440 })
  const key = document.getElementById('piano-key')!
  const cleanup = await useInteractionMethods(key, synth)
  // Later: cleanup() to remove all listeners
  ```
- `preventEventDefaults(element)` — prevents default behavior (text selection, context menu, drag) on interactive audio elements. Returns cleanup function. Show brief code example.
- Note: Both return cleanup functions for proper teardown.

2. **Cache Management section** — add near the existing "Preloading Audio" mention or in Utility Functions:
Document `clearPreloadCache()`:
- What it does: clears the internal Response cache used by `preload()`, `createSound()`, `createTrack()`, etc.
- When to use: freeing memory in long-running apps, after replacing audio files, testing
- Code example:
  ```typescript
  import { clearPreloadCache, preload } from 'ez-web-audio'
  await preload(['/audio/level1.mp3', '/audio/level2.mp3'])
  // ... later, when transitioning to a new level
  clearPreloadCache()
  ```

3. **Debug Utilities section** — expand the existing debug section (around line 513-525):
Add to the current debug section:
- Show sample debug output format (what DebugMessage looks like: `{ type, message, timestamp, context }`)
- Explain the two approaches: `setDebugMode(true)` for console logging, `setDebugHandler(fn)` for custom handling
- Show example of filtering debug messages by type
- Example:
  ```typescript
  setDebugHandler((msg) => {
    if (msg.type === 'connection') {
      console.log(`[${msg.type}] ${msg.message}`)
      // Example output: [connection] Effect added at position 0
    }
  })
  ```

4. **Envelope section** — add as a new subsection under or near Oscillator:
Document the `Envelope` class as a standalone concept:
- What it is: ADSR envelope that shapes volume over time
- That it's typically used via `createOscillator({ envelope: { ... } })` but can be created standalone: `new Envelope({ attack, decay, sustain, release })`
- The `EnvelopeOptions` interface: attack, decay, sustain, release (all in seconds except sustain which is 0-1 ratio)
- Mention it's exported from `ez-web-audio` for advanced use cases like custom envelope-driven automation

Keep each section concise — 10-20 lines of explanation + code example. These are "Core Concepts" docs, not API reference (that's TypeDoc).
  </action>
  <verify>
    grep -c "useInteractionMethods" docs/guide/concepts.md — should be >= 2
    grep -c "preventEventDefaults" docs/guide/concepts.md — should be >= 1
    grep -c "clearPreloadCache" docs/guide/concepts.md — should be >= 2
    grep -c "setDebugHandler" docs/guide/concepts.md — should be >= 2 (existing + expanded)
    grep -c "Envelope" docs/guide/concepts.md — should be >= 3 (heading + usage + options)
  </verify>
  <done>
    useInteractionMethods, preventEventDefaults, clearPreloadCache, debug utilities (expanded), and Envelope class all documented in concepts.md with code examples
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm dev` starts without errors (VitePress can parse the updated markdown)
2. All 7 previously undocumented features now have narrative docs with code examples
3. No broken markdown formatting (check headings, code fences, imports)
4. All code examples use current 1.0 API (context-free factories, .as() not .from(), lowercase q, etc.)
</verification>

<success_criteria>
- SC-07: AudioSprite/createSprite has narrative docs with code example in concepts.md
- SC-08: crossfade utility has narrative docs with code example in concepts.md
- SC-09: playTogether utility has narrative docs with code example in concepts.md
- SC-10: useInteractionMethods, preventEventDefaults, clearPreloadCache, debug utilities, and Envelope class all documented in concepts.md
</success_criteria>

<output>
After completion, create `.planning/phases/28-documentation-corrections/28-02-SUMMARY.md`
</output>
