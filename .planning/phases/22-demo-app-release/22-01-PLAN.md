---
phase: 22-demo-app-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/theme/components/TrackDemo.vue
  - docs/.vitepress/theme/components/VisualizationDemo.vue
  - docs/.vitepress/theme/components/XYPad.vue
  - docs/.vitepress/theme/components/AmbientGenerator.vue
  - docs/.vitepress/theme/components/FilterDemo.vue
  - docs/.vitepress/theme/components/SynthDrumKit.vue
  - docs/examples/effects.md
  - docs/examples/synth-drum-kit.md
  - docs/examples/synthesis.md
  - docs/examples/ambient-generator.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "All Vue demo components use .as() instead of .from() on update/seek chains"
    - "Vue demos use context-free createFilterEffect() where applicable"
    - "Vue demos use addEffects() batch where multiple effects are added sequentially"
    - "No Vue component references removed or renamed APIs"
    - "VitePress markdown examples use context-free createFilterEffect() and createGainEffect()"
  artifacts:
    - path: "docs/.vitepress/theme/components/TrackDemo.vue"
      provides: "Track seek demo with .as() API"
      contains: ".as('seconds')"
    - path: "docs/.vitepress/theme/components/FilterDemo.vue"
      provides: "Filter demo with .as() and context-free factory"
      contains: ".as('ratio')"
    - path: "docs/.vitepress/theme/components/SynthDrumKit.vue"
      provides: "Synth drum kit with addEffects batch"
      contains: "addEffects"
    - path: "docs/examples/effects.md"
      provides: "Effects examples with context-free factories"
      contains: "createFilterEffect("
  key_links:
    - from: "docs/.vitepress/theme/components/*.vue"
      to: "src/index.ts"
      via: "import from 'ez-web-audio'"
      pattern: "import.*ez-web-audio"
---

<objective>
Update all Vue demo components and VitePress examples to use the final 1.0 API exclusively.

Purpose: Ensure all demo code uses `.as()` instead of `.from()`, context-free effect factories, `addEffects()` batch API, and no references to removed/renamed APIs.
Output: All Vue components updated, demo site buildable with no API errors.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/.vitepress/theme/components/TrackDemo.vue
@docs/.vitepress/theme/components/VisualizationDemo.vue
@docs/.vitepress/theme/components/XYPad.vue
@docs/.vitepress/theme/components/AmbientGenerator.vue
@docs/.vitepress/theme/components/FilterDemo.vue
@docs/.vitepress/theme/components/SynthDrumKit.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .from() to .as() in all Vue components</name>
  <files>
    docs/.vitepress/theme/components/TrackDemo.vue
    docs/.vitepress/theme/components/VisualizationDemo.vue
    docs/.vitepress/theme/components/XYPad.vue
    docs/.vitepress/theme/components/AmbientGenerator.vue
    docs/.vitepress/theme/components/FilterDemo.vue
  </files>
  <action>
    Replace `.from('string')` with `.as('string')` on all `update().to()` and `seek()` chains in Vue components. Specifically:

    1. **TrackDemo.vue** line 109: `track.seek(seekPosition.value).from('seconds')` → `track.seek(seekPosition.value).as('seconds')`
    2. **VisualizationDemo.vue** line 225: `oscillator.update('frequency').to(frequency.value).from('number')` → `.as('number')`
    3. **XYPad.vue** line 146: `oscillator.update('frequency').to(frequency).from('ratio')` → `.as('ratio')`
    4. **XYPad.vue** line 147: `oscillator.update('gain').to(currentGain.value).from('ratio')` → `.as('ratio')`
    5. **AmbientGenerator.vue** line 168: `droneOscillator.update('frequency').to(droneFrequency.value).from('number')` → `.as('number')`
    6. **AmbientGenerator.vue** line 182: `shimmerOscillator.update('frequency').to(shimmerFrequency.value).from('number')` → `.as('number')`
    7. **FilterDemo.vue** line 58: `source.update('gain').to(0.3).from('ratio')` → `.as('ratio')`

    IMPORTANT: Do NOT change `.from()` on `onPlayRamp()` chains — those mean "from value X" and are correct as-is.
  </action>
  <verify>
    Run: `grep -rn "\.from\(['\"]" docs/.vitepress/theme/components/ | grep -v "Array.from" | grep -v "onPlayRamp"` — should return zero results.
    Run: `grep -rn "\.as\(['\"]" docs/.vitepress/theme/components/` — should show all the replacements.
  </verify>
  <done>All `.from('string')` calls on update/seek chains are replaced with `.as('string')`. No onPlayRamp().from() calls were touched.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt convenience APIs in Vue components</name>
  <files>
    docs/.vitepress/theme/components/SynthDrumKit.vue
    docs/.vitepress/theme/components/FilterDemo.vue
    docs/.vitepress/theme/components/AmbientGenerator.vue
  </files>
  <action>
    Adopt new convenience APIs where they simplify the demo code:

    1. **Context-free createFilterEffect()**: Remove the `audioContext`/`ctx` first argument from `createFilterEffect()` calls. The factory now gets the context internally. Update these files:
       - **FilterDemo.vue**: `lib.createFilterEffect(ctx, filterType.value, {...})` → `lib.createFilterEffect(filterType.value, {...})`. Remove the `const ctx = await lib.getAudioContext()` lines that are only used for createFilterEffect. If `ctx` is used elsewhere in the same function, keep it.
       - **AmbientGenerator.vue**: `createFilterEffect(audioContext, 'lowpass', {...})` → `createFilterEffect('lowpass', {...})`. Check if `audioContext` is still needed for other calls; keep import if so.
       - **SynthDrumKit.vue**: Multiple `lib.createFilterEffect(ctx, ...)` → `lib.createFilterEffect(...)`. Check if `ctx` is still needed for other calls in each function.

    2. **addEffects() batch**: In SynthDrumKit.vue, where two consecutive `addEffect()` calls exist for hi-hat oscillators (lines ~174-175), replace with `osc.addEffects([highpass, bandpass])`.

    3. Ensure the `addEffects` function is included in the import from 'ez-web-audio' if it's a named export, OR verify it's a method on the sound instance (which it is — it's on BaseSound). No import change needed since it's a method.
  </action>
  <verify>
    Run: `grep -n "createFilterEffect(ctx\|createFilterEffect(audioContext" docs/.vitepress/theme/components/` — should return zero results (all context-free now).
    Run: `grep -n "addEffects" docs/.vitepress/theme/components/SynthDrumKit.vue` — should show batch usage.
    Run: `pnpm typecheck` passes (no type errors from API changes).
  </verify>
  <done>Vue demo components use context-free createFilterEffect() and addEffects() batch API where applicable. No unnecessary audioContext fetches for effect creation.</done>
</task>

<task type="auto">
  <name>Task 3: Update VitePress markdown example files for context-free factories</name>
  <files>
    docs/examples/effects.md
    docs/examples/synth-drum-kit.md
    docs/examples/synthesis.md
    docs/examples/ambient-generator.md
  </files>
  <action>
    Replace `createFilterEffect(ctx, ...)` with `createFilterEffect(...)` and `createGainEffect(ctx, ...)` with `createGainEffect(...)` in all VitePress markdown example files. The context argument is no longer needed — factories resolve AudioContext internally.

    Affected files and counts:
    1. **docs/examples/effects.md** — 18 createFilterEffect(ctx, ...) calls + createGainEffect(ctx, ...) calls
    2. **docs/examples/synth-drum-kit.md** — 3 createFilterEffect(ctx, ...) calls
    3. **docs/examples/synthesis.md** — 1 createFilterEffect(ctx, ...) call
    4. **docs/examples/ambient-generator.md** — 1 createFilterEffect(ctx, ...) call

    For each file:
    - Remove the `ctx` or `audioContext` first argument from createFilterEffect/createGainEffect calls
    - If the `ctx`/`audioContext` variable was only obtained for these factory calls, remove those lines too
    - If `ctx`/`audioContext` is used for other purposes in the same code block, keep it

    IMPORTANT: Do NOT change `getAudioContext()` calls used for other purposes (like connecting nodes).
  </action>
  <verify>
    Run: `grep -rn "createFilterEffect(ctx\|createFilterEffect(audioContext\|createGainEffect(ctx\|createGainEffect(audioContext" docs/examples/` — should return zero results.
    Run: `grep -rn "createFilterEffect\|createGainEffect" docs/examples/` — should show calls without ctx argument.
  </verify>
  <done>All VitePress markdown example files use context-free effect factories. No old createFilterEffect(ctx, ...) or createGainEffect(ctx, ...) patterns remain.</done>
</task>

</tasks>

<verification>
1. `grep -rn "\.from\(['\"]" docs/.vitepress/theme/components/ | grep -v "Array.from" | grep -v "onPlayRamp"` returns no results
2. `grep -rn "createFilterEffect(ctx\|createFilterEffect(audioContext" docs/.vitepress/theme/components/ docs/examples/` returns no results
3. `grep -rn "createGainEffect(ctx\|createGainEffect(audioContext" docs/examples/` returns no results
4. `pnpm typecheck` passes
5. `pnpm build` succeeds (lib + docs build)
</verification>

<success_criteria>
All Vue demo components use the 1.0 API exclusively: .as() on update/seek chains, context-free effect factories, addEffects batch. No references to removed or deprecated APIs remain.
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-app-release/22-01-SUMMARY.md`
</output>
