---
phase: 22-demo-app-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - typedoc.json
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "TypeDoc shows protected members (gainNode, pannerNode, effectChainInput, startOffset) marked as protected"
    - "TypeDoc omits removed deprecated exports (connections API, OscillatorOpts aliases)"
    - "CHANGELOG.md documents all breaking changes with migration steps"
    - "CHANGELOG.md covers all features added since 0.1.0"
  artifacts:
    - path: "typedoc.json"
      provides: "TypeDoc configuration showing protected members"
      contains: "excludeProtected"
    - path: "CHANGELOG.md"
      provides: "Comprehensive 1.0.0 changelog"
      contains: "[1.0.0]"
  key_links:
    - from: "typedoc.json"
      to: "docs/api/"
      via: "typedoc generation"
      pattern: "out.*docs/api"
---

<objective>
Update TypeDoc configuration to show protected members and write a comprehensive 1.0.0 changelog.

Purpose: API reference must reflect the final protected visibility, and the changelog must document all changes from 0.1.0 to 1.0.0 for users upgrading or adopting the library.
Output: Updated typedoc.json, regenerated API docs, comprehensive CHANGELOG.md.
</objective>

<execution_context>
@/Users/seth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CHANGELOG.md
@typedoc.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeDoc config and regenerate API docs</name>
  <files>
    typedoc.json
  </files>
  <action>
    1. In `typedoc.json`, change `"excludeProtected": true` to `"excludeProtected": false` so protected members (gainNode, pannerNode, effectChainInput, startOffset) appear in the API reference clearly marked as protected.

    2. Run `npx typedoc` to regenerate the API docs into `docs/api/`.

    3. Verify the generated docs include protected members by checking the Sound or Track class docs for `gainNode` or `pannerNode`.

    4. Verify removed APIs (connections, OscillatorOpts, OscillatorOptsFilterValues) do NOT appear — they should already be absent since they were removed from source.
  </action>
  <verify>
    Run: `npx typedoc` completes without errors.
    Check: `grep -l "gainNode\|pannerNode" docs/api/classes/Sound.md docs/api/classes/Track.md` — should find matches (protected members now visible).
    Check: `grep -rl "addConnection\|OscillatorOpts[^F]" docs/api/` — should return no results (removed APIs absent).
  </verify>
  <done>TypeDoc config shows protected members. Generated API reference includes gainNode/pannerNode/effectChainInput/startOffset as protected. No removed APIs appear.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive 1.0.0 CHANGELOG.md</name>
  <files>
    CHANGELOG.md
  </files>
  <action>
    Rewrite CHANGELOG.md as a comprehensive 1.0.0 release changelog covering all changes since 0.1.0 (the initial MVP). Structure:

    ```
    # Changelog

    ## [1.0.0] - 2026-02-17

    ### Breaking Changes

    #### Method Renames
    - `.from()` renamed to `.as()` on `update().to()` and `seek()` chains
    - `ifActivePlayIn()` renamed to `playInIfActive()`
    (Include migration code examples for each)

    #### Visibility Changes
    - `gainNode`, `pannerNode`, `effectChainInput`, `startOffset` now protected
    - Use `getGainNode()` for advanced gain access

    #### Removed APIs
    - Connections API removed (addConnection, removeConnection, getConnection, getNodeFrom, connections array)
    - `OscillatorOpts` and `OscillatorOptsFilterValues` type aliases removed
    (Include migration examples: connections -> effects)

    ### Migration Guide
    (Inline numbered steps for upgrading from 0.x)

    ### Added
    - Effect bypass auto-rewire (toggling effect.bypass rewires chain automatically)
    - Context-free effect factories (createFilterEffect, createGainEffect work without AudioContext arg)
    - addEffects() batch method on all sounds
    - playTogether() utility for synchronized multi-sound playback
    - createSounds() batch loader with progress events
    - getFilters() on Oscillator, getSounds() on Sampler (readonly accessors)
    - Extensible ControlType via ControlTypeMap interface (module augmentation)
    - createEffect() generic factory for custom effects
    - Null guards and input validation throughout
    - Descriptive errors for invalid operations (negative effect position, empty sampler)
    - clearScheduledValues consume-once semantics (onPlaySet/onPlayRamp cleared after each play)
    - Integration tests (full effect chains, soundfont workflow)
    - Concurrent operation tests (play-while-playing, rapid seek, double-stop)
    - 937 unit tests + 20 E2E tests
    - Comprehensive JSDoc on all public methods
    - getGainNode() public accessor for controlled GainNode access

    ### Changed
    - Dependencies upgraded (Vite 7, Vitest 4, TypeScript 5.9, ESLint 10)
    - Test files split by concern for maintainability
    - Controller parameter arrays cleared between plays (memory optimization)

    ### Initial Features (from 0.1.0 MVP)
    - Sound, Track, Oscillator, SampledNote class hierarchy
    - ADSR Envelopes with visual preview
    - Effects system (FilterEffect, GainEffect, EffectWrapper)
    - Sampler with round-robin playback
    - BeatTrack/Beat drum machine pattern system
    - Audio sprites with named regions
    - Soundfont loading and playback
    - Musical note system (MusicallyAware mixin, frequency mapping)
    - Lazy AudioContext initialization with iOS workaround
    - Fluent parameter control API (update/onPlaySet/onPlayRamp)
    - Crossfade, playAll, pauseAll, stopAll utilities
    - Preload with caching
    - Debug mode with custom handler support
    - Analyzer for frequency/waveform data
    - VitePress documentation site with interactive examples
    ```

    Note: `onPlayRamp().from()` is NOT renamed — it means "from value X" and is a different semantic. Include this note in the changelog.
  </action>
  <verify>
    CHANGELOG.md exists with comprehensive 1.0.0 section.
    Contains breaking changes with migration examples.
    Contains all new features from phases 17-21.
    Contains initial features from 0.1.0 MVP.
    Note about onPlayRamp().from() being unchanged is present.
  </verify>
  <done>CHANGELOG.md is a comprehensive document covering all changes from 0.1.0 to 1.0.0 with breaking changes, migration guide, new features, and initial feature list.</done>
</task>

</tasks>

<verification>
1. `npx typedoc` generates without errors
2. Protected members visible in generated API docs
3. No removed APIs in generated API docs
4. CHANGELOG.md covers breaking changes, migration, new features, and initial features
</verification>

<success_criteria>
TypeDoc API reference shows protected members and omits removed APIs. CHANGELOG.md comprehensively documents all changes from 0.1.0 to 1.0.0 with clear migration guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/22-demo-app-release/22-02-SUMMARY.md`
</output>
