---
phase: between-phases
status: action_needed
last_updated: 2026-02-28T16:01:01.493Z
---

<current_state>
Deep review of phases 53 (Built-in Effects) and 54 (LFO) is complete. Findings report written. No phases created yet from the findings — that is the next action.
</current_state>

<completed_work>

- Deep review dispatched 4 parallel reviewers (Architecture, Security/Safety, Performance, API Correctness)
- All reviewer findings spot-checked and verified against source code
- Synthesized findings report written to `.planning/reviews/2026-02-28-deep-review.md`
- User approved acting on all 5 proposed work groupings
</completed_work>

<remaining_work>

Create 5 new GSD phases from the deep review findings. Use `/gsd:add-phase` for each:

### Phase 55: LFO Listener & Dispose Bugs (Medium effort)
- **C1**: Replace dispose monkey-patching (`lfo.ts:605-625`) with event-based cleanup
- **H1**: Make retrigger + syncLifecycle mutually exclusive (`lfo.ts:201-231`)
- **H2**: Replace empty catch blocks with targeted error handling (`lfo.ts:298,322,524,534`)
- **M1**: Address _disposePatchMap strong references (`lfo.ts:83`)
- Scope: `src/lfo.ts`, `src/lfo.test.ts`

### Phase 56: BaseEffect Completeness (Small effort)
- **H3**: Fix misaligned JSDoc blocks (`base-effect.ts:106-135`) — move rampTo docs above rampTo method
- **M2**: Add public `getAudioContext()` accessor to BaseEffect (LFO needs it, `lfo.ts:429`)
- **M3**: Export BaseEffect from `src/index.ts`
- **M4**: Add `dispose()` method to BaseEffect (disconnect nodes, enable cleanup for Delay/Reverb feedback loops)
- Scope: `src/effects/base-effect.ts`, `src/effects/base-effect.test.ts`, `src/index.ts`

### Phase 57: Input Validation (Small effort)
- **H4**: Validate syncToBPM inputs — throw on NaN/Infinity/bad format (`lfo.ts:364-374`)
- **M5**: DistortionEffect type='custom' without curve — throw or warn (`distortion-effect.ts:158-166`)
- **M8**: Clamp CompressorEffect params to spec ranges (`compressor-effect.ts:81-119`)
- **M9**: Clamp delay time to [0, maxTime] (`delay-effect.ts:81-83`)
- **L2**: Guard LFO frequency/depth against NaN (`lfo.ts:99-122`)
- **L3**: Guard rampTo against duration=0 (`base-effect.ts:139,151`)
- Scope: All effect files + `src/lfo.ts`

### Phase 58: Performance Polish (Small effort)
- **M6**: Reduce distortion curve to 1024 samples, reuse Float32Array buffer (`distortion-effect.ts:32-33`)
- **M7**: Use setTargetAtTime for reverb decay/damping setters (`reverb-effect.ts:227`)
- **L1**: Fix LFO type setter dead code — standard-to-standard should use .type not _restart() (`lfo.ts:134-141`)
- **L4**: Optimize S&H buffer fill — step by samplesPerStep (`lfo.ts:591-599`)
- Scope: `src/effects/distortion-effect.ts`, `src/effects/reverb-effect.ts`, `src/lfo.ts`

### Phase 59: Test Coverage (Small effort)
- **L8**: Add tests for: disconnect() with no args, getParam() directly, convolution URL path, zero/NaN/negative edge cases on all effects, retrigger+syncLifecycle mutual exclusion error
- Scope: All test files
- Dependencies: Phases 55-58 (test the fixes)

NOTE: The current roadmap has phases 55-58 already planned for Effects & Transport milestone. Check if those numbers conflict and adjust accordingly. Read `.planning/ROADMAP.md` to see current phase numbering before creating.
</remaining_work>

<decisions_made>

- All 5 work groupings approved by user — act on everything
- No tensions found between reviewers — all recommendations aligned
- Severity classifications confirmed via spot-checks
</decisions_made>

<blockers>
- None
</blockers>

<context>
The "Effects & Transport" milestone (phases 53-58) was already planned. Phases 53-54 are done. Check if phases 55-58 already have content in the roadmap — the deep review phases may need different numbering to avoid conflicts.

Pre-existing test failure in `src/utils/crossfade.test.ts` (5 tests) is unrelated to this work.
</context>

<next_action>
1. Read `.planning/ROADMAP.md` to check current phase 55-58 content
2. Run `/gsd:add-phase` five times (or adjust numbering to avoid conflicts) to create the 5 phases from the findings
3. Then `/gsd:plan-phase` and `/gsd:execute-phase` as normal
</next_action>
