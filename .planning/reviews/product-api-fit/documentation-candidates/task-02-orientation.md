# Task 02 Documentation Candidates

These are orientation-stage candidates, not audit findings and not authorization to change documentation. “Observed” means the source mismatch is directly present in the frozen tree or Task 1 deployment/registry record; “Hypothesis” means the teaching consequence still requires Task 3–7 evidence.

| ID | Source | Audience | Product layer | Observed friction or teaching opportunity | Recommended form | Supporting fixture/finding | Confidence | Priority | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DOC-T02-01 | Root/core/adapter manifests, npm, deployed docs | Human + agent researcher | acquisition | Observed version signals disagree: local packages `0.2.0`, public core `0.1.0`, adapters unavailable, deployed Schema.org `1.0.0` | Single-source versioning concept + release troubleshooting note | Task 1 baseline; package manifests | demonstrated mismatch; impact unknown | high | candidate |
| DOC-T02-02 | `Track` code and getting-started “long audio” guidance | Implementer | foundation / application playback | Observed `Track` decodes an `AudioBuffer`; the guide positions it for longer audio without a true streaming capability statement | Buffered-vs-streamed concept, capability matrix, long-form recipe/counterexample | LLM-03 and comparative background-media fixture | demonstrated implementation/doc wording; product severity pending | high | candidate |
| DOC-T02-03 | Global volume/mute public docs and code | Implementer + debugger | foundation | Observed managed master routing applies only to objects created after the first global-control call and exposes no public persisted state | Canonical global-routing recipe with ordering, current/future behavior, persistence, and diagnostic checks | LLM-04 and global-control graph fixture | demonstrated caveat; workflow impact pending | high | candidate |
| DOC-T02-04 | `preload()` implementation and guide | Implementer | foundation | Observed preload caches cloneable `Response` objects, while “faster creation” may be read as decoded-resource reuse | Resource lifecycle concept distinguishing fetch cache, decode, reusable media, and active playback | Decode instrumentation and LLM-02 | probable teaching gap; runtime cost pending | medium | candidate |
| DOC-T02-05 | `Sound.play()` overlap semantics | Implementer + debugger | application playback | Observed docs promise overlap but do not teach how reusable `Sound` state relates to each audible occurrence or how independently controllable occurrences are owned | Overlap recipe plus explicit resource-vs-playback concept and limitation/counterexample | LLM-02 concurrency journey | probable; exact controllability review pending | high | candidate |
| DOC-T02-06 | Getting-started activation section and adapter `useAudioContext()` APIs | Human + LLM implementer | foundation / framework | Core docs say explicit activation is optional, while adapters expose `ready`/`init`; retrieval may over-weight infrastructure readiness | Intent-bound activation concept, canonical recipes, exceptional explicit-activation recipe, counterexample, stable troubleshooting anchor | LLM-01/05/06/09 adversarial runs | hypothesis from first-party behavior; model/runtime validation pending | critical candidate | candidate |
| DOC-T02-07 | React/Vue factories and `useCleanup()` | Framework implementer | framework adaptation | Observed factory hooks/composables own one loaded object and `reset()` disposes it, but route cleanup for raw/ephemeral objects requires separate opt-in registration | Framework ownership recipe showing factory lifetime, ephemeral instances, cleanup ordering, Strict Mode/remount behavior | LLM-06 and React/Vue navigation fixtures | demonstrated API shape; omission frequency pending | high | candidate |
| DOC-T02-08 | `docs/examples/react-integration.md` vs `@ez-web-audio/react` | React implementer + agent | framework adaptation | Observed integration guide is hand-rolled over core rather than the shipped adapter's canonical hooks | Replace/split with adapter-first canonical recipe and clearly labeled core escape hatch | Framework/public-surface review | demonstrated mismatch | high | candidate |
| DOC-T02-09 | `docs/examples/visualization.md` vs context-free `createAnalyzer()` | Implementer | expressive audio | Observed examples import `getAudioContext()` and pass context explicitly although the public index offers a context-free analyzer factory | Canonical analyzer graduation recipe; label explicit-context overload as escape hatch | LLM-07 graduation run | demonstrated mismatch | medium | candidate |
| DOC-T02-10 | Effects/analyzer/transport examples | Ambitious implementer | expressive / musical systems | Existing feature-specific examples do not yet prove that advanced capabilities preserve the same resource, playback, routing, and disposal ownership used by the simple path | Progressive “graduate without replacement” recipes with before/after architecture diagram | LLM-07/08 and comparative journey | hypothesis; continuity scoring pending | high | candidate |
| DOC-T02-11 | Public error classes and generated reference | Debugger + agent | all runtime layers | Public errors are discoverable as classes, but recovery guidance may lack stable code, attempted operation, current state, and durable documentation anchor | Error/recovery reference with stable anchors and one-attempt repair examples | LLM-09 repair scoring and error-surface review | hypothesis; inventory pending | medium | candidate |
| DOC-T02-12 | Package/docs/`llms.txt` acquisition surface | LLM researcher | acquisition | No oriented canonical library-selection page was found that explains when to choose EZ, native Web Audio, Howler, or Tone by job and limitation | Comparison/selection page and agent-readable index entry | LLM-10 fit rubric and Task 3 corpus | probable; discoverability research pending | high | candidate |
| DOC-T02-13 | Generated TypeDoc HTML | Agent + human reference user | acquisition | Oriented generated module links contain `http://localhost:5173/ez-web-audio/docs/...`, which may create broken or noncanonical retrieval paths after deployment | Build-time reference-link validation and canonical deployment-base troubleshooting note | Agent/public-surface review | demonstrated in frozen generated artifact | medium | candidate |

No candidate above is scheduled for implementation in Workstream 0. Task 7 must deduplicate these against evidence-backed findings and may validate, refine, reject, or defer them.
